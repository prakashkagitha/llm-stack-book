{
  "chapter_id": "10-multimodal-and-arch/03-audio-speech-multimodal",
  "one_liner": "Convert audio to tokens/features and build ASR, TTS, and streaming speech LMs fused with text.",
  "key_terms": [
    {
      "term": "Mel spectrogram",
      "definition": "A 2-D time-frequency representation from an STFT mapped through Mel-scale filter banks; compresses a waveform (e.g. 160,000 samples) into an 80x1000 matrix for model input."
    },
    {
      "term": "Residual Vector Quantization (RVQ)",
      "definition": "Cascaded vector quantizers where each stage quantizes the residual left by the previous one, producing K integer codes per audio frame at increasing fidelity."
    },
    {
      "term": "Neural audio codec",
      "definition": "A convolutional encoder-RVQ-decoder system (e.g. EnCodec, SoundStream) trained end-to-end to reconstruct audio from discrete codec tokens."
    },
    {
      "term": "Delay pattern",
      "definition": "A codec-token interleaving scheme (used in AudioLM, VALL-E) that shifts each RVQ level right by k steps so a causal model conditions each level on coarser levels of the same and prior frames."
    },
    {
      "term": "Whisper",
      "definition": "An encoder-decoder ASR model that encodes a fixed 30-second window of log-Mel features into 1500 continuous hidden states and autoregressively decodes text transcripts."
    },
    {
      "term": "Q-Former",
      "definition": "A cross-attention module with N learnable query tokens that compresses variable-length encoder features (e.g. audio or vision) into a fixed-length token sequence for an LLM."
    },
    {
      "term": "Semantic-acoustic disentanglement",
      "definition": "Training a codec's first RVQ level (e.g. via SpeechTokenizer's distillation loss to HuBERT labels) to capture linguistic content separately from higher levels that capture acoustic style."
    },
    {
      "term": "HuBERT",
      "definition": "A BERT-style self-supervised speech model that predicts k-means cluster assignments (pseudo-labels) of masked audio frames, yielding discrete semantic units used as targets for speech LMs."
    },
    {
      "term": "Speech language model (speech LM)",
      "definition": "A transformer that natively ingests and emits audio tokens (e.g. AudioPaLM, Moshi), skipping separate ASR/TTS steps by treating audio and text tokens as one sequence."
    },
    {
      "term": "Full-duplex spoken dialogue",
      "definition": "A real-time conversational mode (as in Moshi) where the model continuously listens and speaks simultaneously via parallel audio token streams and a hierarchical temporal architecture."
    },
    {
      "term": "Multimodal token-stream view",
      "definition": "The abstraction that each modality (audio, vision, text) contributes tokens via its own embedding table/projection into one shared sequence processed by a single transformer backbone."
    },
    {
      "term": "AudioLM hierarchical generation",
      "definition": "A two-stage audio generation approach that first autoregressively models compact semantic tokens (long-range structure), then generates denser acoustic codec tokens conditioned on them."
    }
  ],
  "introduces": [
    "Mel spectrogram",
    "Residual Vector Quantization (RVQ)",
    "Neural audio codec",
    "Delay pattern",
    "Whisper",
    "Q-Former",
    "Semantic-acoustic disentanglement",
    "HuBERT",
    "Speech language model (speech LM)",
    "Full-duplex spoken dialogue",
    "Multimodal token-stream view",
    "AudioLM hierarchical generation"
  ],
  "prerequisites": [
    "the attention mechanism / transformer architecture",
    "autoregressive next-token prediction and text tokenization (BPE)",
    "cross-attention and encoder-decoder architectures",
    "vector quantization / embedding lookup basics",
    "KV cache and inference latency (prefill/decode, TTFT)",
    "basic signal processing (sampling rate, Fourier transform)"
  ],
  "difficulty": "core",
  "tags": ["multimodal", "architecture", "inference", "serving", "data"]
}
