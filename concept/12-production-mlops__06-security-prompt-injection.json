{
  "chapter_id": "12-production-mlops/06-security-prompt-injection",
  "one_liner": "Design layered defenses (sandboxing, dual-LLM, structured output) against prompt injection and jailbreaks.",
  "key_terms": [
    {
      "term": "prompt injection",
      "definition": "An attack where adversarial text hijacks an LLM's instruction-following, exploiting the fact that instructions and data share the same context window."
    },
    {
      "term": "indirect injection",
      "definition": "Injection where the attacker plants malicious instructions in data the model later reads (web pages, emails, docs), not in the user's own message."
    },
    {
      "term": "lethal trifecta",
      "definition": "The co-occurrence of private data in context, untrusted content in context, and an exfiltration channel; all three enable a complete data-exfiltration attack."
    },
    {
      "term": "jailbreak",
      "definition": "An attack targeting the model's trained values/RLHF alignment rather than context framing, eliciting policy-violating outputs via personas, fiction, or encoding tricks."
    },
    {
      "term": "Greedy Coordinate Gradient (GCG)",
      "definition": "A gradient-based algorithm that searches for adversarial token suffixes minimizing the loss toward a compliant response, transferring across models."
    },
    {
      "term": "many-shot jailbreaking",
      "definition": "A jailbreak that fills a long context with fabricated examples of the model complying with harmful requests, using in-context learning to override RLHF priors."
    },
    {
      "term": "dual-LLM pattern",
      "definition": "An architecture splitting a privileged orchestrator from an unprivileged reader model that processes untrusted content and returns only structured, schema-constrained output."
    },
    {
      "term": "least privilege",
      "definition": "The architectural principle of granting an agent's tools only the minimum scope and permissions needed for the current task, limiting exfiltration channels."
    },
    {
      "term": "canary token",
      "definition": "A secret string embedded in a system prompt that the model is told never to reveal; its appearance in output signals an injection/exfiltration attempt."
    },
    {
      "term": "schema-constrained generation",
      "definition": "Forcing model output into a predefined JSON schema so injected instructions cannot cause arbitrary actions, since only structured fields are parsed."
    },
    {
      "term": "defense-in-depth",
      "definition": "A layered security architecture (alignment, input filtering, architectural isolation, output filtering, monitoring) where each imperfect layer multiplicatively reduces attack success."
    }
  ],
  "introduces": [
    "prompt injection",
    "indirect injection",
    "lethal trifecta",
    "jailbreak",
    "Greedy Coordinate Gradient (GCG)",
    "many-shot jailbreaking",
    "dual-LLM pattern",
    "canary token",
    "schema-constrained generation"
  ],
  "prerequisites": [
    "tool use and function calling in agents",
    "the agentic loop (ReAct-style tool execution)",
    "RLHF and model alignment",
    "context window / in-context learning",
    "structured/constrained generation basics"
  ],
  "difficulty": "core",
  "tags": ["safety", "agents", "production", "security"]
}
