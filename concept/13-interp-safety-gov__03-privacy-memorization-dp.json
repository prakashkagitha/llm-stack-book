{
  "chapter_id": "13-interp-safety-gov/03-privacy-memorization-dp",
  "one_liner": "Measure and defend against LLM training-data leakage using canaries, MIA audits, and DP-SGD.",
  "key_terms": [
    {
      "term": "Memorization (eidetic/verbatim memorization)",
      "definition": "A training string is k-eidetic memorized if a short prompt makes greedy decoding reproduce it exactly and it appeared in at most k training documents."
    },
    {
      "term": "Counterfactual memorization",
      "definition": "The gap in a model's performance on an example when trained with versus without that example in the training set; the quantity DP bounds."
    },
    {
      "term": "Training-data extraction attack",
      "definition": "Attack that generates candidate completions, ranks them by a membership signal (e.g. zlib or reference-model ratio), then verifies against the training corpus to recover verbatim data."
    },
    {
      "term": "Membership inference attack (MIA)",
      "definition": "Attack deciding whether a specific record was in a model's training set, typically by comparing its loss (calibrated against a reference) to a threshold."
    },
    {
      "term": "TPR at low FPR (true-positive rate at low false-positive rate)",
      "definition": "The correct way to score an MIA: measure how many members are caught while flagging very few non-members, since average accuracy hides worst-case leakage in the tail."
    },
    {
      "term": "Canary and exposure metric",
      "definition": "A canary is a synthetic secret inserted into training data; exposure = log2|R| − log2(rank of the true secret among all candidates) quantifies how memorized it became."
    },
    {
      "term": "Differential privacy (DP)",
      "definition": "A training algorithm is (ε,δ)-differentially private if its output distribution changes by at most a factor e^ε (plus δ) when one training record is added or removed, bounding every membership-inference adversary."
    },
    {
      "term": "DP-SGD (differentially private stochastic gradient descent)",
      "definition": "SGD variant that clips each per-example gradient to norm C and adds calibrated Gaussian noise before averaging, giving a formal (ε,δ) privacy guarantee tracked by a privacy accountant."
    },
    {
      "term": "Per-example gradient clipping",
      "definition": "Computing each training example's gradient separately and rescaling its L2 norm to a bound C, capping how much any single example can influence a DP-SGD update."
    },
    {
      "term": "Likelihood Ratio Attack (LiRA)",
      "definition": "State-of-the-art MIA that trains many shadow models with/without the target record, fits Gaussians to per-example loss distributions, and computes a likelihood-ratio test for membership."
    },
    {
      "term": "Deduplication",
      "definition": "Removing (near-)duplicate documents from training data; since memorization scales with duplication count, dedup is the cheapest, highest-ROI privacy defense."
    },
    {
      "term": "PII scrubbing",
      "definition": "Detect-and-redact pipeline combining regexes and NER to remove personally identifiable information from training or output text before it can be memorized or leaked."
    }
  ],
  "introduces": [
    "Memorization (eidetic/verbatim memorization)",
    "Counterfactual memorization",
    "Training-data extraction attack",
    "Membership inference attack (MIA)",
    "TPR at low FPR (true-positive rate at low false-positive rate)",
    "Canary and exposure metric",
    "Differential privacy (DP)",
    "DP-SGD (differentially private stochastic gradient descent)",
    "Per-example gradient clipping",
    "Likelihood Ratio Attack (LiRA)"
  ],
  "prerequisites": [
    "cross-entropy loss and maximum-likelihood training objective",
    "gradient descent / SGD",
    "basic probability (log-likelihood, Gaussian distributions, ROC curves)",
    "overfitting vs. generalization",
    "next-token prediction / autoregressive language modeling"
  ],
  "difficulty": "advanced",
  "tags": ["safety", "training", "evaluation", "data"]
}
