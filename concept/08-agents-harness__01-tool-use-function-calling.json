{
  "chapter_id": "08-agents-harness/01-tool-use-function-calling",
  "one_liner": "Build a robust tool-calling loop: define schemas, parse/validate/dispatch calls, handle errors, run in parallel.",
  "key_terms": [
    {
      "term": "tool schema (function schema)",
      "definition": "A JSON Schema description of a tool's name, description, and parameters injected into the model's context so it knows what it can call."
    },
    {
      "term": "function-calling fine-tune",
      "definition": "SFT (optionally plus RL) on multi-turn conversations containing tool calls and results, teaching a model when and how to invoke tools."
    },
    {
      "term": "parse-validate-dispatch pipeline",
      "definition": "The harness sequence that extracts a tool call from model output text, checks it against the tool's JSON schema, then executes the matching function."
    },
    {
      "term": "tool-call loop (agentic loop)",
      "definition": "The orchestration cycle that repeatedly calls the model, executes any requested tool calls, appends results, and repeats until a final text answer emerges."
    },
    {
      "term": "parallel tool calling",
      "definition": "Emitting multiple independent tool calls in a single model response so they can be executed concurrently, reducing latency versus sequential calls."
    },
    {
      "term": "structured outputs (JSON mode)",
      "definition": "Constrained decoding that forces model output to conform to a JSON schema, used for extraction/classification tasks independent of tool invocation."
    },
    {
      "term": "constrained decoding",
      "definition": "Masking token sampling so only tokens consistent with a schema-derived grammar (e.g., a CFG) can be generated, guaranteeing valid structured output."
    },
    {
      "term": "error-as-tool-result pattern",
      "definition": "Feeding parse, validation, or execution errors back into the conversation as if they were normal tool results, so the model can self-correct on the next turn."
    },
    {
      "term": "tool_call_id",
      "definition": "A unique identifier attached to each model-issued tool call that must be echoed back in the corresponding tool result message to correlate calls with results."
    },
    {
      "term": "Model Context Protocol (MCP)",
      "definition": "A standardized JSON-RPC protocol, introduced by Anthropic, that lets any server expose tools, resources, and prompts to any compliant client uniformly."
    }
  ],
  "introduces": [
    "tool schema (function schema)",
    "function-calling fine-tune",
    "parse-validate-dispatch pipeline",
    "tool-call loop (agentic loop)",
    "parallel tool calling",
    "structured outputs (JSON mode)",
    "error-as-tool-result pattern",
    "Model Context Protocol (MCP)"
  ],
  "prerequisites": [
    "JSON and JSON Schema basics",
    "chat templates and system prompts",
    "supervised fine-tuning (SFT)",
    "context window limits",
    "basic Python and REST APIs"
  ],
  "difficulty": "core",
  "tags": ["agents", "inference", "systems", "training"]
}
