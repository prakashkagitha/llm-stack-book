{
  "chapter_id": "06-rl-infra/08-reward-verifiers-sandboxes",
  "one_liner": "Design rule-based verifiers, LLM judges, and sandboxes to compute reliable RL training rewards.",
  "key_terms": [
    {
      "term": "RL with Verifiable Rewards (RLVR)",
      "definition": "Post-training paradigm that scores completions with deterministic rule-based verifiers (math/code checkers) instead of a learned reward model."
    },
    {
      "term": "Rule-based verifier",
      "definition": "A deterministic function that checks a completion against ground truth (math equivalence, unit tests) and returns a reward at zero training-time cost."
    },
    {
      "term": "LLM-as-a-judge",
      "definition": "A second LLM prompted to score completions against rubric criteria for open-ended tasks that lack a crisp ground truth."
    },
    {
      "term": "Learned reward model",
      "definition": "A model, e.g. Bradley-Terry, trained on preference data to output a smooth reward signal; general-purpose but prone to reward hacking."
    },
    {
      "term": "Sandboxed execution",
      "definition": "Running untrusted model-generated code inside an isolated container or microVM with no network access, capped CPU/memory, and restricted syscalls."
    },
    {
      "term": "Reward hacking",
      "definition": "When a policy discovers a high-reward behavior, such as exploiting a verifier bug, that does not correspond to genuinely correct task performance."
    },
    {
      "term": "Format reward",
      "definition": "A small auxiliary reward for producing the expected output structure (e.g., a boxed answer), added to fix sparse-reward cold-start problems."
    },
    {
      "term": "Process Reward Model (PRM)",
      "definition": "A model that assigns a reward to each step of a chain-of-thought rather than only to the final answer."
    },
    {
      "term": "Reward server",
      "definition": "An HTTP service that decouples reward computation (verifiers, judges, sandboxes) from the training loop, enabling independent scaling and result caching."
    },
    {
      "term": "Positional bias",
      "definition": "An LLM judge's tendency to favor whichever response is presented first in a comparison, independent of actual quality."
    },
    {
      "term": "Per-group reward normalization",
      "definition": "Z-scoring rewards within a prompt's group of sampled completions so resulting advantages are zero-mean regardless of the raw reward scale."
    }
  ],
  "introduces": [
    "Rule-based verifier",
    "LLM-as-a-judge",
    "Sandboxed execution",
    "Format reward",
    "Reward server",
    "Positional bias"
  ],
  "prerequisites": [
    "RLHF/RLVR post-training pipeline",
    "the generation-training rollout loop",
    "policy-gradient/GRPO advantage estimation",
    "KL penalty against a reference policy",
    "basic containerization (Docker)",
    "chain-of-thought reasoning format"
  ],
  "difficulty": "advanced",
  "tags": ["rl", "training", "systems", "safety", "evaluation"]
}
