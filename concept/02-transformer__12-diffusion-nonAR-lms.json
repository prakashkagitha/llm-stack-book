{
  "chapter_id": "02-transformer/12-diffusion-nonAR-lms",
  "one_liner": "Build and reason about masked-diffusion LLMs: derive the objective, implement a sampler, and design block diffusion.",
  "key_terms": [
    {
      "term": "absorbing-state (masked) diffusion",
      "definition": "A discrete diffusion process where tokens are independently replaced by [MASK] with growing probability over time, and never transition to any other token."
    },
    {
      "term": "masking schedule (alpha_t)",
      "definition": "A monotone function from alpha_0=1 to alpha_1=0 giving the probability a token survives unmasked at diffusion time t; drives both corruption and the loss weight."
    },
    {
      "term": "remasking schedule",
      "definition": "The decoding-time rule for how many and which denoised positions to commit (unmask) each sampling step versus return to [MASK] for later steps."
    },
    {
      "term": "confidence-based remasking",
      "definition": "A remasking strategy that commits the highest-confidence predicted tokens each step and re-masks the rest, used by LLaDA and the chapter's sampler."
    },
    {
      "term": "serial depth",
      "definition": "The number of sequentially dependent forward passes needed to produce an output; the quantity diffusion reduces (N steps) relative to AR's L tokens."
    },
    {
      "term": "block diffusion",
      "definition": "A semi-autoregressive hybrid that generates fixed-size blocks left-to-right autoregressively while denoising tokens within each block in parallel via diffusion."
    },
    {
      "term": "block-causal attention mask",
      "definition": "An attention mask that is bidirectional within a block but causal across blocks, letting earlier finalized blocks' keys/values be cached for reuse."
    },
    {
      "term": "conditional-independence trap",
      "definition": "The failure mode where a single denoising step predicts all masked positions independently, producing locally plausible but jointly incoherent text if committed at once."
    },
    {
      "term": "reversal curse",
      "definition": "The tendency of autoregressive models trained on 'A is B' to fail at answering 'what is B?', arising from the strictly left-to-right training gradient."
    },
    {
      "term": "Evidence Lower Bound (ELBO) for masked diffusion",
      "definition": "The mask-rate-weighted (by 1/(1-alpha_t)) cross-entropy loss on masked positions, shown to be a valid variational bound on the true data log-likelihood."
    },
    {
      "term": "non-autoregressive (NAR) language model",
      "definition": "A language model that generates a sequence without a strict left-to-right token-by-token dependency, instead producing multiple positions in parallel each step."
    }
  ],
  "introduces": [
    "absorbing-state (masked) diffusion",
    "masking schedule (alpha_t)",
    "remasking schedule",
    "confidence-based remasking",
    "serial depth",
    "block diffusion",
    "block-causal attention mask",
    "conditional-independence trap",
    "non-autoregressive (NAR) language model"
  ],
  "prerequisites": [
    "autoregressive next-token prediction and the chain-rule factorization",
    "the attention mechanism and causal masking",
    "KV cache and memory-bandwidth-bound decode",
    "masked language modeling (BERT-style pretraining)",
    "softmax and cross-entropy loss",
    "basic diffusion models (forward/reverse noising process)"
  ],
  "difficulty": "advanced",
  "tags": ["architecture", "inference", "training", "systems"]
}
