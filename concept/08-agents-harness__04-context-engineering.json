{
  "chapter_id": "08-agents-harness/04-context-engineering",
  "one_liner": "Budget, retrieve, compact, and cache an LLM agent's context window to keep it accurate and cheap.",
  "key_terms": [
    {
      "term": "context window",
      "definition": "The single finite buffer of tokens an LLM sees at each decode step; the model's only working memory for an agentic task."
    },
    {
      "term": "context engineering",
      "definition": "The discipline of deciding what content enters the context window, in what form, order, and for how long, across an agent's turns."
    },
    {
      "term": "context rot",
      "definition": "The degradation of model accuracy on a fixed task as irrelevant or merely long context is added, even when the needed information is present."
    },
    {
      "term": "lost-in-the-middle",
      "definition": "The empirical U-shaped position effect where information placed in the middle of a long context is retrieved less accurately than information at the start or end."
    },
    {
      "term": "effective window",
      "definition": "The context length at which a model's accuracy on a task is still acceptable, measured empirically; typically much smaller than the advertised maximum window."
    },
    {
      "term": "token budgeting",
      "definition": "Partitioning the effective context window into per-segment token allocations (system, tools, retrieved, history, scratchpad) enforced with a caps-and-eviction policy."
    },
    {
      "term": "compaction",
      "definition": "Replacing a long, low-density span of an agent's conversation history with a short, model-generated structured summary that preserves durable facts and decisions."
    },
    {
      "term": "scratchpad (plan file)",
      "definition": "A persistent external file (e.g. PLAN.md) the agent reads and writes each turn to externalize working memory so it survives context compaction."
    },
    {
      "term": "prompt caching (prefix caching)",
      "definition": "Reusing a previously computed KV cache for an identical prompt prefix across turns instead of re-prefilling it, cutting cost and time-to-first-token."
    },
    {
      "term": "just-in-time retrieval (agentic retrieval)",
      "definition": "Exposing search/read as tools so the model itself decides what context to fetch on demand, instead of pre-stuffing the whole corpus into the prompt."
    },
    {
      "term": "sub-agent context isolation",
      "definition": "Delegating a bounded subtask to a sub-agent with its own fresh context window, returning only its conclusion so verbose exploration never enters the parent's window."
    },
    {
      "term": "memory hierarchy (RAM-vs-disk framing)",
      "definition": "Treating the context window as fast, small, volatile RAM and external storage (files, databases) as slow, large, durable disk that the agent pages state through."
    }
  ],
  "introduces": [
    "context engineering",
    "context rot",
    "lost-in-the-middle",
    "effective window",
    "token budgeting",
    "compaction",
    "scratchpad (plan file)",
    "just-in-time retrieval (agentic retrieval)",
    "sub-agent context isolation",
    "memory hierarchy (RAM-vs-disk framing)"
  ],
  "prerequisites": [
    "causal attention and softmax",
    "KV cache (key-value cache)",
    "tokenization (BPE/tokenizer basics)",
    "the agentic loop (ReAct / plan-execute)",
    "tool use and function calling for LLM agents",
    "prefill and decode phases of LLM inference"
  ],
  "difficulty": "core",
  "tags": ["agents", "inference", "serving", "systems", "optimization"]
}
