{
  "chapter_id": "01-foundations/06-neural-nets-from-scratch",
  "one_liner": "Build and hand-verify backprop for an MLP, from scalar autograd to a vectorized NumPy classifier.",
  "key_terms": [
    {
      "term": "Multi-layer perceptron (MLP)",
      "definition": "A chain of affine layers (matrix multiply plus bias) each followed by an elementwise nonlinearity; the basic feed-forward neural network."
    },
    {
      "term": "Backpropagation",
      "definition": "Reverse-mode algorithm computing gradients of a scalar loss with respect to all parameters in one backward sweep via the chain rule."
    },
    {
      "term": "Vector-Jacobian product (VJP)",
      "definition": "The rule at each computation-graph node converting an output's upstream gradient into gradients for its inputs, without ever forming the full Jacobian."
    },
    {
      "term": "Upstream gradient (adjoint)",
      "definition": "The running quantity u-bar = dL/du carried backward through the computation graph, representing how much the loss changes with tensor u."
    },
    {
      "term": "Xavier/Glorot initialization",
      "definition": "Weight initialization setting Var(W) = 2/(d_in+d_out) to balance signal variance across forward and backward passes, for symmetric activations like tanh."
    },
    {
      "term": "Kaiming/He initialization",
      "definition": "Weight initialization setting Var(W) = 2/d_in, correcting Xavier's factor for ReLU's variance-halving effect, keeping deep ReLU networks trainable."
    },
    {
      "term": "Vanishing/exploding gradients",
      "definition": "Geometric shrinkage or growth of gradients across many layers when each layer's typical Jacobian singular value is below or above 1."
    },
    {
      "term": "Dead ReLU",
      "definition": "A neuron whose pre-activation is negative on every training example, so its ReLU gradient is permanently zero and it never updates again."
    },
    {
      "term": "Batch normalization",
      "definition": "Normalizes each pre-activation to zero mean and unit variance across the mini-batch, then applies a learnable affine (gamma, beta), stabilizing deep-network training."
    },
    {
      "term": "Gradient checking",
      "definition": "Verifying a hand-written analytic gradient by comparing it to the central-difference numerical gradient; near-zero relative error confirms correctness."
    },
    {
      "term": "Universal approximation theorem",
      "definition": "Result (Cybenko, Hornik) that a single sufficiently wide hidden layer can approximate any continuous function on a compact set to arbitrary accuracy."
    },
    {
      "term": "Softmax-cross-entropy gradient",
      "definition": "The fused gradient of softmax followed by cross-entropy loss with respect to logits, which simplifies exactly to predicted-minus-true probabilities (P - Y)."
    }
  ],
  "introduces": [
    "Multi-layer perceptron (MLP)",
    "Backpropagation",
    "Vector-Jacobian product (VJP)",
    "Upstream gradient (adjoint)",
    "Xavier/Glorot initialization",
    "Kaiming/He initialization",
    "Vanishing/exploding gradients",
    "Dead ReLU",
    "Batch normalization",
    "Gradient checking",
    "Universal approximation theorem"
  ],
  "prerequisites": [
    "matrix multiplication and transposes",
    "partial derivatives and the chain rule",
    "gradient descent and convex/non-convex optimization basics",
    "vectors and linear maps (linear algebra)",
    "softmax and probability basics"
  ],
  "difficulty": "core",
  "tags": ["math", "training", "optimization", "architecture"]
}
