{
  "chapter_id": "08-agents-harness/05-agent-memory",
  "one_liner": "Architect and implement episodic, semantic, and vector agent memory with compaction and session persistence.",
  "key_terms": [
    {
      "term": "episodic memory",
      "definition": "Timestamped, situated record of a specific past event or interaction; append-only, grows monotonically, queried with temporal or situational cues."
    },
    {
      "term": "semantic memory",
      "definition": "Stable, general facts (preferences, configuration) that hold across sessions; upserted/overwritten on new evidence and queried by content."
    },
    {
      "term": "memory compaction",
      "definition": "Replacing a long in-context conversation prefix with a shorter, lossier summary (free-text or structured JSON) to stay under the context-window limit."
    },
    {
      "term": "reflection (reflect operation)",
      "definition": "A scheduled LLM call that reads a batch of recent episodic entries and distils them into compact, stable semantic facts."
    },
    {
      "term": "file-as-memory pattern",
      "definition": "Using a structured file-system directory (markdown/JSON files, indexed by grep or a vector index) as the agent's persistent, human-auditable memory store."
    },
    {
      "term": "bootstrap read",
      "definition": "The harness's first action at session start: querying external memory stores and prepending relevant results to the system prompt for continuity."
    },
    {
      "term": "session persistence",
      "definition": "The architecture (session ID, bootstrap read, graceful shutdown write, cross-session reflection) that lets an agent carry state across restarts."
    },
    {
      "term": "vector store",
      "definition": "A memory substrate that embeds each entry as a dense vector and retrieves by cosine similarity; strong at fuzzy matching, weak at exact-match and structured queries."
    },
    {
      "term": "property graph",
      "definition": "A knowledge store of nodes and labelled edges (e.g. Neo4j, Kuzu) used for agent memory when multi-hop relational queries matter more than similarity search."
    },
    {
      "term": "write / read / reflect triad",
      "definition": "The three memory operations every memory-capable agent implements: committing information to storage, retrieving it into context, and distilling episodes into facts."
    }
  ],
  "introduces": [
    "episodic memory",
    "semantic memory",
    "memory compaction",
    "reflection (reflect operation)",
    "file-as-memory pattern",
    "bootstrap read",
    "session persistence",
    "write / read / reflect triad"
  ],
  "prerequisites": [
    "the context window and token budgets",
    "embeddings and cosine similarity",
    "the agentic loop (ReAct / plan-execute)",
    "vector databases and ANN search basics",
    "JSON / key-value data structures"
  ],
  "difficulty": "core",
  "tags": ["agents", "systems", "rag", "production"]
}
