{
  "chapter_id": "05-posttraining-alignment/04-peft-prompt-merging",
  "one_liner": "Adapt LLMs via soft prompts, prefix tuning, IA3, or by merging fine-tuned checkpoints without retraining.",
  "key_terms": [
    {
      "term": "soft prompting",
      "definition": "Replacing a discrete text prompt with continuous, trainable embedding vectors learned end-to-end while the base model stays frozen."
    },
    {
      "term": "prompt tuning",
      "definition": "Prepends k learnable embedding vectors to the input layer only; trains under 0.01% of parameters, closing the full-fine-tuning gap at large scale."
    },
    {
      "term": "prefix tuning",
      "definition": "Injects learned key/value pairs at every transformer attention layer (via an MLP reparameterization), giving per-layer task steering instead of input-only."
    },
    {
      "term": "IA3 (Infused Adapter by Inhibiting and Amplifying Inner Activations)",
      "definition": "Rescales key, value, and FFN activations with learned per-channel vectors; can be folded into weights for zero inference overhead."
    },
    {
      "term": "model merging",
      "definition": "Combining two or more independently trained checkpoints by arithmetic on their weight tensors, with no gradient-based training or data access needed."
    },
    {
      "term": "task vector",
      "definition": "The weight-space delta between a fine-tuned checkpoint and its shared pre-trained base, used as an additive/subtractive unit for editing model skills."
    },
    {
      "term": "task arithmetic",
      "definition": "Adding, subtracting, or summing scaled task vectors onto a base model to add, remove, or combine learned behaviors algebraically."
    },
    {
      "term": "TIES-Merging (Trim, Elect Sign, Disjoint Merge)",
      "definition": "Merge algorithm that trims small-magnitude task-vector entries, resolves sign conflicts by majority vote, and averages only agreeing values to reduce interference."
    },
    {
      "term": "DARE (Drop And REscale)",
      "definition": "Stochastically zeros a fraction of task-vector entries and rescales survivors by 1/(1-p) to reduce merge interference, like dropout on delta weights."
    },
    {
      "term": "model soups",
      "definition": "Averaging the weights of several fine-tuned checkpoints of the same base model; the average often generalizes better than any single checkpoint."
    },
    {
      "term": "SLERP (spherical linear interpolation)",
      "definition": "Interpolates two weight tensors along the great circle of the unit sphere instead of linearly, preserving vector norm during merging."
    }
  ],
  "introduces": [
    "soft prompting",
    "prompt tuning",
    "prefix tuning",
    "IA3 (Infused Adapter by Inhibiting and Amplifying Inner Activations)",
    "model merging",
    "task vector",
    "task arithmetic",
    "TIES-Merging (Trim, Elect Sign, Disjoint Merge)",
    "DARE (Drop And REscale)",
    "model soups",
    "SLERP (spherical linear interpolation)"
  ],
  "prerequisites": [
    "the attention mechanism (query/key/value)",
    "full fine-tuning of neural networks",
    "gradient descent and backpropagation",
    "token embeddings",
    "low-rank adaptation (LoRA) and PEFT basics"
  ],
  "difficulty": "core",
  "tags": ["training", "optimization", "architecture", "production"]
}
