# Dependencies for the CI-tested code extracts under tests/.
# Only offline-safe, deterministic packages — tests must be hermetic (no network).
# torch is installed CPU-only in CI (see .github/workflows). Any package NOT listed
# here must be try/except-guarded in a test so the module still loads without it.
numpy
torch
einops
scikit-learn
pytest
