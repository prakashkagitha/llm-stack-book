{
  "chapter": "content/04-kernels-efficiency/03-flash-attention-2-3.md",
  "core_intuitions": [
    "FA1 was memory-optimal but compute-under-utilized: it already minimized HBM traffic, so the FA2/FA3 story is entirely about OCCUPANCY and INSTRUCTION MIX (keeping the tensor cores fed), not about bandwidth. This reframing is the pivot of the whole chapter.",
    "FLOPs are NOT fungible. Matmul runs on fast tensor cores; the softmax's exp/max/rescale run on slow special-function units at a fraction of the rate. So a tiny FRACTION of the FLOP count (the ~1% that is softmax) can dominate a large FRACTION of the RUNTIME because it sits on the critical path -- which is WHY FA2 minimizes it and FA3 overlaps it.",
    "FA2's three edits are all 'who computes what, in what order' changes that leave the math identical: (1) defer the normalization by l to a single final divide (keep an unnormalized accumulator), (2) make the query-block / sequence axis the parallel grid dimension so enough thread blocks exist to fill every SM at small batch x heads, and (3) partition warps over QUERY ROWS (split-Q) instead of key columns (split-K).",
    "Split-K vs split-Q: in FA1 each warp owned a slice of KEY columns and held all query rows, so after PV the warps had to reduce partial sums through shared memory with a __syncthreads every iteration; FA2's split-Q gives each warp disjoint QUERY rows (full output for those rows), K/V shared read-only, so NO inter-warp reduction is needed -- removing the per-iteration barrier is a big chunk of the speedup.",
    "FA3 turns the thread block into a software pipeline via WARP SPECIALIZATION: a few 'producer' warps do almost no math and just fire asynchronous TMA copies to prefetch the next K/V tiles into a shared-memory ring buffer, staying a stage or two ahead, while 'consumer' warpgroups run the wgmma matmuls and softmax -- data movement for tile j+1 overlaps compute on tile j.",
    "FA3 hides the slow softmax behind matmul by OVERLAPPING them, which is only possible because Hopper's wgmma is asynchronous (issue-and-continue): inter-warpgroup 'ping-pong' runs one warpgroup's softmax on the SFUs while the other's GEMM runs on the tensor cores, so both unit types stay busy simultaneously.",
    "FP8 attention roughly doubles matmul throughput but outliers wreck a single per-tensor scale; INCOHERENT PROCESSING multiplies Q and K by a random-sign Hadamard rotation M (M^T M = I, so QK^T is exactly unchanged) that SPREADS each outlier across all coordinates, letting the FP8 scale stay tight. Inputs are FP8 but accumulation and the softmax stats (m, l) stay in FP32."
  ],
  "existing": [],
  "new_figures": [
    {
      "name": "fa2-flops-not-fungible",
      "concept": "A tiny slice of the FLOP count (softmax exp/max/rescale) balloons into a large slice of runtime because it runs on slow special-function units while matmul runs on fast tensor cores.",
      "rationale": "This is the single conceptual pivot of the entire chapter: everything FA2 minimizes and FA3 overlaps is justified by 'FLOPs are not fungible.' The worked example carries the numbers but a reader only truly internalizes it by SEEING the softmax sliver -- invisible in the FLOP-count bar -- swell to a fat chunk of the time bar. Nothing else in the chapter conveys why non-matmul work is the enemy, and it is the through-line from Edit 1 to FA3's ping-pong.",
      "kind": "static",
      "placement": "Section 'FlashAttention-2: three edits to the inner loop' > worked-example admonition 'Worked example: how much does the non-matmul work matter?' (place immediately after it, ~line 183)",
      "brief": "A two-bar 'FLOPs vs time' infographic that visually inverts the proportions. Draw two horizontal 100%-width bars stacked vertically, each split into two segments: a large 'matmul (QK^T and PV)' segment and a small 'softmax (exp / rowmax / rescale)' segment. TOP bar titled 'Share of FLOP COUNT': matmul fills almost the whole bar, softmax is a thin sliver at the end (annotate the sliver 'approx 1% of FLOPs'). BOTTOM bar titled 'Share of RUNTIME': the SAME two categories but now the softmax segment is a fat chunk (annotate 'approx one-seventh of the time' or 'a large slice' -- use the chapter's qualitative framing, do NOT stamp a specific TFLOP number as fact) and the matmul segment correspondingly shorter. Between the two bars draw two labeled 'execution unit' lanes explaining the cause: 'TENSOR CORES -- fast (matmul)' and 'SPECIAL-FUNCTION UNIT -- much slower (exp, transcendental)', with a small illustrative rate badge like 'approx 16x slower per FLOP' (this ratio is the chapter's own 1/16, mark it illustrative). Add curved connector arrows from the thin softmax sliver in the top bar to the fat softmax chunk in the bottom bar, captioned 'same work, but on the slow pipe -> disproportionate time'. Bottom takeaway strip: 'This is why FA2 fights to do softmax rarely, and FA3 fights to overlap it behind matmul.' Theme-safe: distinguish matmul vs softmax by shade family + label + hatch, never color alone; keep both bars equal length so the proportion flip is unmistakable. No invented device benchmark numbers -- only the two illustrative small integers (1% share, ~16x rate) the chapter itself uses, labeled illustrative. Everything inside the viewBox; must read fully as a static frame."
    },
    {
      "name": "fa2-split-k-vs-split-q",
      "concept": "FA1's split-K warp layout forces a per-iteration shared-memory reduction across warps; FA2's split-Q layout gives each warp disjoint query rows so no inter-warp reduction is needed.",
      "rationale": "This is FA2's signature and most hardware-specific win, and the removed per-iteration __syncthreads is 'worth a large fraction of FA2's speedup.' The chapter already reaches for an ASCII diagram, which is a tell that a clean 2D figure is wanted. Seeing WHO owns which slice of the Br x Bc tile -- columns (split-K, must reduce) vs rows (split-Q, independent) -- is the fastest path to the intuition that split-Q eliminates the barrier.",
      "kind": "static",
      "placement": "Section 'Edit 3 -- partition warps over queries, not keys' (place immediately after the ASCII split-K/split-Q block, ~line 148)",
      "brief": "A side-by-side comparison of the same Br x Bc score tile partitioned two ways, with 4 warps (W0..W3) in each. LEFT panel titled 'SPLIT-K (FA1)': draw the Br x Bc tile as a grid; shade it in VERTICAL stripes so each warp W0..W3 owns a slice of KEY COLUMNS spanning ALL query rows (label the top axis 'key columns Bc', side axis 'query rows Br'). Below the tile show each warp producing a PARTIAL PV result (four little partial-output boxes), then a bold 'REDUCE across warps via shared memory' box with a red-flag icon and a recurring '__syncthreads() every inner iteration' tag; caption 'each warp has only a column-slice -> outputs must be summed -> shared-mem round trip + barrier in the hot path'. RIGHT panel titled 'SPLIT-Q (FA2)': same tile but shaded in HORIZONTAL stripes so each warp owns a slice of QUERY ROWS spanning ALL key columns; to the right of the tile show four SEPARATE, already-final output rows ('W0 -> rows 0-15 final', 'W1 -> rows 16-31 final', ...), and depict K/V as a single shared block beneath with 'K_j, V_j: SHARED in shared memory, read-only broadcast' and a checkmark 'no inter-warp reduction, no extra __syncthreads'. Make the visual contrast the axis of the stripes (vertical=columns=must-reduce vs horizontal=rows=independent) so the reader groks it pre-verbally. Bottom takeaway strip spanning both: 'Same math, different owner: split-Q makes each warp's rows independent, deleting the per-iteration barrier.' Theme-safe: use shade/pattern to distinguish the four warps consistently across both panels; mark the 'reduce/barrier' cost with an icon + label, not color alone. Keep both panels balanced, all warp labels legible, nothing clipped. Fully understandable as a static frame."
    },
    {
      "name": "fa3-producer-consumer-pingpong",
      "concept": "FA3's software pipeline: TMA producer warps prefetch K/V tiles into a shared-memory ring buffer a stage ahead, while consumer warpgroups ping-pong so that one does softmax on the SFU while the other does wgmma on the tensor cores -- keeping both unit types busy.",
      "rationale": "The FA3 payoff is fundamentally TEMPORAL: two independent overlaps happening over time (producer-ahead-of-consumer, and softmax-hidden-behind-matmul). A static frame can show the layout but cannot convey the essential thing -- that at every instant BOTH the tensor cores and the SFUs are busy because the warpgroups are staggered, and the ring never starves because the producer runs ahead. This is the mechanism that lifts FA3 from FA2's ~40% to ~75% peak, and motion is genuinely needed to see why nothing idles. This one animation subsumes both ASCII timelines the chapter draws.",
      "kind": "long-animation",
      "placement": "Section 'FlashAttention-3: exploiting Hopper' > 'Warp specialization: producer/consumer with TMA' (place after the ring-buffer ASCII block, ~line 215; the animation then also delivers the ping-pong from the next subsection)",
      "brief": "A two-tier timeline animation. TIER 1 (top): the shared-memory RING BUFFER as 3 slots [stage 0 | 1 | 2]. A 'PRODUCER warps (TMA)' lane on the left issues async copies that fill slots ahead of consumption; animate tiles K0V0, K1V1, K2V2... streaming in, with the producer always keeping 1-2 slots full ahead of the consumer's read cursor, and an 'mbarrier: tile ready / slot free' handshake pulsing between producer and consumer. Show the producer doing 'almost no compute -- just address-free TMA copies'. TIER 2 (bottom): two CONSUMER warpgroup lanes, WG1 and WG2, each a sequence of colored blocks: GEMM(QK) [tensor core], SOFTMAX [SFU], GEMM(PV) [tensor core]. Stagger them so that WHEN WG1's block is SOFTMAX, WG2's block is a GEMM, and vice versa -- the ping-pong. Alongside, draw two RESOURCE UTILIZATION meters: 'Tensor cores' and 'SFUs', and animate them BOTH staying near-full because at every timestep at least one warpgroup is feeding each. Contrast callout: a small ghosted 'FA2 (synchronous mma)' mini-timeline where a single lane goes GEMM -> [tensor cores IDLE while] SOFTMAX -> GEMM, with the tensor-core meter dropping to near-zero during softmax, captioned 'FA2 cannot overlap: exp stalls the tensor cores'. A moving 'now' playhead sweeps left to right. Per-phase captions: 'producer stays ahead -> consumer never starved'; 'wgmma is ASYNC: issue and continue -> softmax of tile j overlaps QK matmul of tile j+1'; 'ping-pong: SFU-bound softmax hides behind tensor-core GEMM'. The FINAL STATIC frame (non-animated viewers) must show: the ring buffer with the producer visibly one stage ahead, the two staggered warpgroup lanes at a moment where one is in softmax and the other in GEMM, and BOTH utilization meters full, plus the ghosted idle-FA2 comparison -- so the whole intuition stands without motion. Theme-safe: give GEMM vs SOFTMAX blocks distinct shade+label+pattern (not color alone); label the two execution-unit meters explicitly. Keep both tiers, both meters, and the FA2 ghost inside the viewBox with no overlapping labels; readable in light and dark."
    },
    {
      "name": "fa3-incoherent-processing",
      "concept": "A random-sign Hadamard rotation leaves QK^T exactly unchanged but spreads a single outlier across all coordinates, so the FP8 scale no longer stretches and quantization becomes uniform.",
      "rationale": "This is the FP8 accuracy fix -- the reason FP8 attention is usable at all -- and it is deeply non-obvious that you can rotate the inputs, change every value, and yet leave the scores identical while dramatically cutting quantization error. The picture (one tall outlier crushing everything into a few FP8 levels, then energy smeared flat across coordinates) makes the whole 'incoherent processing' idea click in one glance, and it recurs across the quantization chapters (QuaRot/SpinQuant), so the intuition pays off repeatedly.",
      "kind": "static",
      "placement": "Section 'FP8 attention and incoherent processing' > 'Incoherent processing: rotate away the outliers' (place after the QuIP/Hadamard paragraph, ~line 326)",
      "brief": "A three-stage left-to-right infographic over a single row vector of d coordinates (use ~12 coordinate bars). STAGE 1 'BEFORE (raw Q row)': eleven small similar-height bars and ONE towering outlier bar (label 'outlier feature'). Beneath it draw the FP8 quantization grid as a set of evenly spaced horizontal levels: because the scale s must cover the tall outlier, ALL the small bars collapse onto just 1-2 of the lowest levels -- show them squashed with a red-flag caption 'scale stretches to the outlier -> typical values crushed into a few FP8 levels -> big error'. STAGE 2 'ROTATE by M = (1/sqrt d) H D (Hadamard x random signs)': a labeled operator block; annotate 'orthogonal: M^T M = I' and the key invariance equation '(QM)(KM)^T = Q M M^T K^T = QK^T -- scores UNCHANGED'. Draw a small arrow indicating O(d log d) fast Hadamard cost. STAGE 3 'AFTER (rotated Q row)': the SAME total energy now SPREAD across all d coordinates -- twelve moderate, roughly-even bars, no single one dominating; the FP8 level grid now has the values distributed across MANY levels, with a checkmark caption 'no coordinate dominates -> tight scale -> uniform quantization -> small error'. Add a compact side note strip: 'The outlier is smeared over all coordinates; the matmul result is identical, the quantization is far better.' And a small precision reminder: 'inputs -> FP8, but accumulation and softmax stats (m, l) stay FP32.' Theme-safe: use one accent to mark the outlier in Stage 1 and the same-energy uniform bars in Stage 3; pair the 'bad/good' judgement with icons + captions, not color alone. The FP8 level grid must stay legible and the towering outlier bar must not clip the top of the viewBox (leave headroom or add a break mark). No invented numbers -- the only concrete values are the chapter's own d, the 448 e4m3 max magnitude may be referenced in a caption. Fully readable as a static frame in both themes."
    }
  ],
  "summary": "FlashAttention-2 & -3 is a long, deeply concept-rich chapter with ZERO figures, and its intuitions rest on four ideas that each reward one strong picture. I propose 4 new figures, no refits. (1) 'FLOPs are not fungible' (P1 static) is the conceptual pivot of the whole chapter -- softmax is ~1% of FLOPs but a large slice of runtime because it runs on the slow SFU pipe -- and it is the through-line justifying every FA2/FA3 move; a two-bar FLOP-vs-time inversion makes it visceral. (2) split-K vs split-Q warp partitioning (P1 static) is FA2's signature win (deletes the per-iteration __syncthreads); the chapter already gropes for an ASCII diagram, so a clean 2D stripe-direction contrast is clearly wanted. (3) The FA3 producer/consumer + ping-pong pipeline (P1 long-animation) is the one place motion is genuinely essential: two simultaneous overlaps over time (producer-ahead ring buffer, and softmax-hidden-behind-matmul across staggered warpgroups) with both execution-unit meters staying full -- a static frame cannot convey 'nothing idles', and this one animation subsumes both ASCII timelines. (4) Incoherent processing (P2 static) makes the FP8 accuracy fix click: a rotation that changes every value yet leaves QK^T identical while smearing outliers flat. I deliberately skip separate figures for Edit-1 deferred normalization (the reference code carries the bookkeeping cleanly) and Edit-2 seqlen/SM occupancy (a standard roofline-chapter idea, adequately served by prose) to stay disciplined and keep each figure foundational rather than decorative."
}
