{
  "chapter": "04-kernels-efficiency/02-flash-attention-1",
  "core_intuitions": [
    "Naive attention is memory-bound, not compute-bound: the N×N score S and probability P matrices are written to and read back from slow HBM, so Θ(N²) BYTE traffic (not FLOPs) dominates wall-clock time — on the roofline it sits on the bandwidth-limited side with tensor cores starved.",
    "The online-softmax correction factor is the heart of the chapter: keep a running max m, running denominator ℓ, and running unnormalized output o; when a block reveals a larger logit, multiply the ALREADY-accumulated ℓ and o by α = e^(m_old − m_new) ∈ (0,1] to retroactively re-base them onto the new max. One multiply replaces a second pass, and every exponent stays ≤ 0 so it never overflows.",
    "FlashAttention fuses QKᵀ, softmax, and ·V into a tiled double loop (outer over query blocks, inner over key/value blocks); each Br×Bc score tile is born and dies in fast on-chip SRAM and is NEVER written to HBM — only Q,K,V stream in and O,L stream out.",
    "It is EXACT, bit-for-bit equal to naive attention up to round-off; the speedup is pure data-movement reduction, not any approximation, sparsity, or low-rank trick.",
    "The backward pass uses recomputation: it does not store P but reconstructs each probability tile as exp(S_ij − L_i) from Q, K, and the saved per-row logsumexp L — trading idle tensor-core FLOPs for avoiding the O(N²) HBM read of a stored P.",
    "IO complexity drops from Θ(N²) to Θ(N² d² / M) where M is SRAM size, and peak extra memory from O(N²) to O(N); bigger SRAM means fewer passes over K,V and less traffic."
  ],
  "existing": [
    {
      "name": "flash-tiling",
      "verdict": "refine",
      "issue": "Two real problems. (1) Correctness: the figcaption ends '...turning attention from memory-bound to compute-bound', which directly contradicts the chapter — the 'FlashAttention is memory-bound, not compute-bound' warning box and the Interview Corner both stress the kernel stays memory-bound; the win is fewer bytes, not becoming compute-bound. (2) Missing glyphs render as tofu boxes in the preview (both themes): the arrows in the running-state lines 'mᵢ □ max(mᵢ, rowmax)', 'ℓᵢ □ ...', 'Oᵢ □ ...', the 'Kⱼ,Vⱼ □' stream arrow, and the '✓ / ✗' marks on 'write Oᵢ once O(L·d)' and 'materialize S = O(L²)'.",
      "improvement": "Rewrite the caption's final clause to say it eliminates the Θ(L²) HBM traffic / keeps the score matrix off HBM — do NOT claim it becomes compute-bound. Replace every Unicode arrow and check/cross (←, →, ✓, ✗) with font-safe ASCII (<-, ->, OK, x) or small inline SVG marks so they render on all systems; verify no tofu remains in both light and dark renders."
    },
    {
      "name": "flash1-forward-dataflow",
      "verdict": "refine",
      "issue": "Content is faithful and the HBM|SRAM|HBM three-column dataflow reads clearly in light theme. But in DARK theme the bottom recap block (the three 'Score tile S_ij lives and dies...' lines) and the INPUT / COMPUTE (on-chip SRAM) / OUTPUT section labels are dark-gray on the near-black outer margin — low contrast and hard to read. Minor secondary crowding: the left-edge 'load' label and the inner-j-loop arrows sit right against the K_j/V_j input boxes.",
      "improvement": "Promote the bottom-caption and section-label text from the muted token to the higher-contrast ink-soft token so it flips legibly in dark mode; nudge the left load arrows a few px so they clear the K_j/V_j box labels. No structural redraw — the layout and content stay."
    }
  ],
  "new_figures": [
    {
      "name": "online-softmax-rescale",
      "concept": "The streaming online softmax: running max/denominator/output and the retroactive correction factor α that re-bases prior accumulators when a larger logit appears.",
      "rationale": "This is the single most important and most elegant idea in the chapter — 'the heart of this chapter' by the author's own words — yet NEITHER existing figure visualizes it; they only list the update lines as text. The correction factor is inherently temporal ('change our mind about the normalizer after the fact'): a reader groks it only by SEEING previously-accumulated state get shrunk down when a bigger logit is discovered. The chapter even ships the exact worked example to animate. Nothing else in the chapter builds this intuition visually.",
      "kind": "long-animation",
      "placement": "Under the section heading '### The key idea: a running max and a correction factor' (right where the recurrence is introduced; it also directly serves the 'Watch the correction factor work' example box below).",
      "brief": "Visualize the online softmax as a left-to-right streaming pass over the chapter's own toy logits x = [1, 2, 8, 3] with block size 2 (block A = [1,2], block B = [8,3]) and small value vectors v. These are the chapter's illustrative integers, not invented benchmarks — keep them. Lay the figure out as a 4-panel comic strip so a STILL final frame conveys the whole recurrence; the animation just reveals the panels in order and animates the one key beat (the shrink). Persistent 'running state' registers shown as three labeled elements: m (running max, a single number), ℓ (running denominator, drawn as a small horizontal bar whose length encodes its value), and o (running unnormalized output, a tiny vector swatch). PANEL 1 — Block A enters: local max = 2, so m goes -inf -> 2; show the two weights e^(1-2)=0.368 and e^(2-2)=1 as small bars; ℓ = 1.368; annotate 'accumulated against local max = 2'. PANEL 2 — Block B enters and CONTAINS a bigger logit: highlight '8 > current max 2 !'; m jumps 2 -> 8; compute the correction α = e^(m_old - m_new) = e^(2-8) = e^-6 ≈ 0.00248. THE KEY VISUAL BEAT: the previously-accumulated ℓ and o bars visibly COLLAPSE/shrink by α (ℓ: 1.368 -> 0.0034), with a labeled arrow '× α = e^(m_old - m_new)' pointing at the shrinking bars — this is 'retroactively re-base the old state onto the new, larger max'. PANEL 3 — Add block B's fresh terms on the new scale: e^(8-8)=1 and e^(3-8)=0.0067; ℓ = 0.0034 + 1.0067 = 1.0101; show a small badge 'equals the two-pass reference ℓ* = 1.0101' to hammer EXACTNESS. PANEL 4 — Finalize: one division o/ℓ at the very end yields the output; label 'normalize once, at the end'. Running invariant banner across the strip: 'every exponent stays ≤ 0 (subtract the current running max) -> never overflows'. Theme-safe: use the accent token for the current/new-scale quantities and a muted or warn token ONLY illustratively for the old, about-to-shrink bars; no hardcoded hex. Everything must fit the viewBox with clear left-to-right reading order; the shrink animation is enhancement — the static final frame with all four panels and the labeled α arrow must stand alone."
    },
    {
      "name": "naive-vs-flash-hbm",
      "concept": "Why naive attention is memory-bound: a side-by-side of HBM byte-traffic — naive drags the N×N S and P matrices across the slow bus twice each (Θ(N²)), FlashAttention keeps the score tile in SRAM and moves only Q,K,V,O.",
      "rationale": "The chapter's opening intuition — 'slow because of WHERE the numbers live' — has no figure, and both existing figures show only the Flash side, so the reader never sees the 'before' that makes 'memory-bound' click. A direct naive-vs-flash traffic contrast grounds core intuitions #1 (memory-bound / roofline) and #6 (IO complexity Θ(N²) -> Θ(N² d²/M)) in one picture, and motivates the entire algorithm before the machinery arrives.",
      "kind": "static",
      "placement": "In the section 'The memory bottleneck: why naive attention is IO-bound', anchored after the 'operation / reads / writes / bytes' HBM-traffic table and the roofline paragraph.",
      "brief": "A clean static infographic contrasting two lanes that share a single visual motif: a WIDE, SLUGGISH 'HBM bus' channel (slow) running across the figure, and a small 'SRAM (on-chip, fast)' box. LEFT LANE 'Naive attention' shows three sequential steps each crossing the slow bus: (1) S = QKᵀ — read Q, read K, WRITE S (an N×N blob) to HBM; (2) P = softmax(S) — READ S, WRITE P (a second N×N blob) to HBM; (3) O = PV — read P, read V, write O. Draw the two big N×N blobs (S and P) as heavy shapes being dragged across the slow bus, each making a round trip; label total HBM traffic 'Θ(N²)' and add a small annotation 'arithmetic intensity low -> memory-bound: tensor cores idle, waiting on HBM' (optionally a tiny roofline tick showing the point sitting on the bandwidth-limited ramp). RIGHT LANE 'FlashAttention' shows Q,K,V streaming in as small tiles, the Br×Bc score tile drawn INSIDE the SRAM box (with a 'never crosses the bus' marker), and only O (plus the length-N L) written out; label HBM traffic 'Θ(N² d² / M) ≪ Θ(N²)' and 'peak memory O(N), not O(N²)'. A slim bottom strip carries the punchline chip: 'same FLOPs, same answer, ~M/d² fewer bytes'. Use only small illustrative size LABELS (S and P as 'N×N', O as 'N×d') — invent NO benchmark numbers. Theme-safe tokens throughout (accent for the Flash lane's kept-in-SRAM tile, a muted/warn tone for the naive lane's spilled N×N blobs); no hardcoded primary hex. Fully legible as a single static frame with clear left(naive)-to-right(flash) reading order; no animation."
    }
  ],
  "summary": "Concept-rich marquee chapter already served by two forward-pass dataflow figures (flash-tiling overview + flash1-forward-dataflow detailed HBM|SRAM|HBM flow). Both stay (refine, not replace): flash-tiling has a real correctness bug — its caption says FlashAttention becomes 'compute-bound', which contradicts the chapter's own warning box and Interview Corner — plus tofu/missing-glyph arrows and check marks; flash1-forward-dataflow just needs a dark-mode contrast bump on its bottom recap. The two big gaps are the chapter's most foundational ideas that NO figure visualizes: (1) the online-softmax correction factor — proposed as a long-animation because the retroactive rescale ('shrink prior accumulators by α when a bigger logit appears') is inherently temporal and is the declared heart of the chapter; and (2) a naive-vs-flash HBM-traffic contrast (static) that supplies the missing 'before' picture and makes 'memory-bound' + the Θ(N²)->Θ(N² d²/M) IO argument click. Disciplined total: keep-and-refine both existing, add exactly two new figures (priority 1 animation, priority 2 static)."
}
