export const meta = {
  name: 'figdraw-p23c',
  description: 'Draw + revise foundational figures for 12 chapters',
  phases: [{ title: 'Draw' }, { title: 'Revise' }],
}

const CH = [
  {
    "chapter": "02-transformer/01-tokenization",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/01-tokenization.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=digit-place-value-scramble\n       concept: Inconsistent number chunking hides place value and breaks arithmetic; per-digit / fixed-size grouping keeps it aligned.\n       placement: after 'The digit and arithmetic problem'\n       brief: A two-panel contrast built around a column-addition layout (the load-bearing visual is the vertical place-value grid). Use the same two illustrative numbers in both panels: 12345 and 6789, stacked for addition. LEFT panel 'Frequency-trained BPE (inconsistent)': tokenize 12345 as [123][45] and 6789 as [67][89] \u2014 deliberately different boundary positions. Draw faint vertical column guides for units / tens / hundreds / thousands / ten-thousands, and show that the token-boundary brackets do NOT align to these columns (a boundary falls mid-column). Annotation in a caution tone: 'token cuts don't match place value -> the model can't line up units-with-units -> carrying is hard to learn'. Add a small note: 'equal-length numbers even chunk differently run to run, depending on which digit strings were frequent'. RIGHT panel 'Per-digit / 1-3-digit grouping (consistent)': tokenize 12345 as [1][2][3][4][5] and 6789 as [6][7][8][9]. Now every token is exactly one column wide, so units sit under units and tens under tens \u2014 the brackets line up perfectly with the column guides. Annotation in the accent/positive tone: 'every digit is its own stable token -> place value is positional and consistent -> carrying is learnable (Llama splits digits; GPT-4 groups 1-3).' At the bottom, a one-line tie-in chip to the same disease: 'strawberry -> [str][aw][berry]: individual letters are never exposed, so the model cannot count its r's.' Use only the small integers shown; invent no benchmarks. Both panels must be legible side by side and understandable as a single static frame in both themes.\n\n  NEW  [static]  name=unigram-segmentation-lattice\n       concept: Unigram scores ALL segmentations of a string under a unigram LM and picks the Viterbi-best path \u2014 top-down probabilistic, unlike BPE's single fixed merge order.\n       placement: after 'Unigram / SentencePiece (T5, Llama, Gemma)'\n       brief: Draw the segmentation lattice for a short word, e.g. 'hello', as a left-to-right DAG. Nodes are the 6 character-boundary positions 0|1|2|3|4|5 laid out along a horizontal axis (between the letters h e l l o). Edges are candidate subword pieces that span from one boundary to a later one, each drawn as an arc labeled with its piece text and a small illustrative log-probability weight \u2014 include overlapping options so the branching is visible: single chars 'h','e','l','l','o'; pieces 'he','ll','llo','lo'; and the whole 'hello'. Highlight the Viterbi best path (the route maximizing the sum of log-probs) in the theme-safe accent color \u2014 for the toy weights, make 'hello' as one high-probability piece the winning path, with 'he'+'llo' as a close runner-up shown faintly. Add two contrast captions: LEFT 'BPE / WordPiece: build up by a fixed merge order -> one deterministic segmentation'; RIGHT (on the lattice) 'Unigram: score every segmentation under p(x1)...p(xk), take the best path (Viterbi) \u2014 and optionally SAMPLE an alternate path for subword regularization', with one non-best path drawn as a dashed 'sampled alternative'. Keep the lattice small (a handful of nodes and edges) so labels never overlap. Use only small illustrative probabilities; invent no real numbers. Must be fully readable as a static frame in both themes.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/01-tokenization.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/01-tokenization.md\nThe chapter's core intuitions: The granularity tradeoff: words explode the vocabulary and hit OOV, characters/bytes explode sequence length (and O(n^2) attention), so subwords are the Goldilocks middle \u2014 frequent strings whole, rare strings split, nothing OOV.; BPE is a greedy loop: repeatedly merge the most frequent adjacent pair into a new symbol, yielding an ORDERED merge list; encoding re-applies those merges in learned (rank) order, not left-to-right.; Byte-level BPE starts from the 256 byte values, a complete and finite base alphabet, so any string in any language is representable and there is provably no <unk> \u2014 worst case a novel glyph falls back to its individual UTF-8 bytes.; Three algorithms, different objectives: BPE (most frequent pair) and WordPiece (max likelihood gain) build UP by merging; Unigram prunes DOWN a big vocabulary via EM and decodes by Viterbi over a segmentation lattice.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  digit-place-value-scramble, unigram-segmentation-lattice\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "02-transformer/03-attention-from-scratch",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/03-attention-from-scratch.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=scale-sqrt-dk-saturation\n       concept: Why 1/\u221ad_k matters: unscaled scores widen with d_k, saturate softmax into a one-hot, and kill the gradient; scaling keeps softmax soft and gradients alive.\n       placement: after 'Why Divide by \u221ad_k? The Variance Argument'\n       brief: A left-to-right, three-stage causal-chain infographic telling one story: score spread \u2192 softmax shape \u2192 gradient fate. STAGE 1 'Raw score std grows as \u221ad_k': three symmetric bell-shaped spread indicators for d_k = 8, 64, 128 (adjacent or overlaid), each labeled with its std \u2248 2.8, 8, 11 (these are just \u221a8, \u221a64, \u221a128 \u2014 derived, not benchmark numbers), the d_k=128 curve visibly the widest. Caption: 'std(q\u00b7k) = \u221ad_k'. STAGE 2 'Feed one row of scores to softmax', shown as TWO small bar charts over ~4 key positions using the chapter's own worked-example logits: (a) UNSCALED logits [20, 4, \u22123, 1] \u2192 softmax bars = one tall bar \u2248 1.0, the rest flat \u2248 0, labeled 'saturated \u2248 hard arg-max'; (b) SCALED (\u00f7\u221a64) logits [2.5, 0.5, \u22120.375, 0.125] \u2192 softmax bars \u2248 [0.77, 0.10, 0.04, 0.07], one dominant but several visibly nonzero, labeled 'soft, still peaked'. STAGE 3 'Gradient consequence': for the unscaled/one-hot case, a FADED or broken arrow to a chip 'Jacobian diag(a)\u2212aa\u1d40 \u2192 0  \u21d2  no gradient to Q,K \u2014 training stalls'; for the scaled/soft case, a solid ACCENTED arrow to a chip 'Jacobian healthy  \u21d2  gradient flows to Q,K'. Use theme-safe classes only (accent for the alive path, muted/grid for the dead path); the dead path should read as visually de-emphasized. All small illustrative integers already appear in the chapter. Clear reading order spread\u2192softmax\u2192gradient; everything inside the viewBox, no label overlaps, legible in both themes.\n\n  NEW  [static]  name=attn-output-convex-hull\n       concept: The attention output is a convex combination of value vectors \u2014 a point inside their convex hull whose position is set by the attention weights; sharpening the weights slides it toward a vertex (hard retrieval).\n       placement: after 'Scaled Dot-Product Attention, One Query at a Time'\n       brief: A 2D geometric panel, ideally shown as two states side by side to make the point. Plot 5 value vectors as labeled points v1\u2026v5 scattered in the plane; draw the convex-hull polygon around the outer points, lightly shaded (v-fill). The attention output is a distinct accented marker (v-accent) sitting STRICTLY inside the hull. Draw thin spokes from the output marker to each value point, with stroke weight/opacity encoding that value's attention weight a_j (i.e. barycentric coordinates). LEFT state 'soft weights' (e.g. a \u2248 [0.30, 0.25, 0.20, 0.15, 0.10]): output sits near the weighted centroid, several spokes of comparable weight. RIGHT state 'sharp weights (large scores)' (e.g. a \u2248 [0.86, 0.06, 0.04, 0.03, 0.01]): output pulled almost onto vertex v1, one bold dominant spoke (v-accent-s), the rest faint \u2014 annotate 'sharpen \u2192 hard retrieval (a single value)'. Shared caption: 'output = \u03a3_j a_j v_j,  a_j \u2265 0,  \u03a3 a_j = 1  \u21d2  always inside the hull: interpolate, never extrapolate.' Small illustrative weights only; theme-safe classes (hull fill v-fill, output v-accent, spokes v-grid/v-muted with the dominant one v-accent-s). Keep labels legible and non-overlapping, whole scene inside the viewBox, works in both themes.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/03-attention-from-scratch.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/03-attention-from-scratch.md\nThe chapter's core intuitions: Attention is differentiable soft retrieval: a dict lookup where exact key-match becomes dot-product similarity and hard selection becomes a softmax-weighted blend of values.; The single-query pipeline: score every key (q\u00b7k) \u2192 scale by 1/\u221ad_k \u2192 softmax into a probability distribution over positions \u2192 return the weighted average of value vectors.; The output is a convex combination of the value vectors \u2014 it lives strictly inside their convex hull, can interpolate but never extrapolate, and approaches a single value (hard retrieval) as the weights sharpen.; Why \u221ad_k: Var(q\u00b7k)=d_k so score std grows like \u221ad_k; large scores saturate softmax into a near one-hot, its Jacobian diag(a)\u2212aa\u1d40 collapses to 0, and query/key gradients die. Dividing by \u221ad_k fixes score variance at 1 so the distribution stays soft and gradients flow.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  scale-sqrt-dk-saturation, attn-output-convex-hull\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "02-transformer/06-transformer-block",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/06-transformer-block.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=tblock-layernorm-vs-rmsnorm\n       concept: Side-by-side of how LayerNorm and RMSNorm transform the SAME small vector, making the 'RMSNorm only rescales magnitude, LayerNorm also recenters' intuition visible.\n       placement: after 'After the '### RMSNorm' worked example block (the 'Worked example: LayerNorm vs RMSNorm on a small vector' admonition), before the 'Pre-Norm vs Post-Norm' section.'\n       brief: A three-panel static infographic driven entirely by the chapter's worked example: input vector x = [1, 3, -1, 5], d=4, gamma=1, beta=0. Draw each vector as four labeled vertical bars sharing a common horizontal zero-baseline (bars go up for positive, down for negative). PANEL 1 titled 'Input x': bars at 1, 3, -1, 5; draw a dashed horizontal 'mean = 2.0' line across them and annotate 'mean drifts, magnitude arbitrary'. PANEL 2 titled 'LayerNorm: center, then scale' shown as a two-step transform \u2014 a small step-a caption 'subtract mean (2.0)' producing intermediate bars [-1, 1, -3, 3] centered on zero, then a step-b caption 'divide by std (sqrt5 approx 2.236)' producing the OUTPUT bars [-0.447, 0.447, -1.342, 1.342]; annotate the output 'zero mean, unit variance' and show the mean line now coinciding with the zero baseline. PANEL 3 titled 'RMSNorm: rescale only' \u2014 one step, caption 'divide by RMS = 3.0', output bars [0.333, 1.0, -0.333, 1.667]; annotate 'offset preserved, only overall magnitude rescaled \u2014 NOT zero-mean' and draw the (nonzero) mean line to show it did NOT move to zero. The load-bearing visual contrast: in Panel 3 the bar SHAPE/pattern (the relative ups and downs) is identical to the input just shrunk, whereas in Panel 2 the whole profile is shifted down so its mean sits on zero. Include the two compact formulas near their panels: LN = (x - mu)/sqrt(sigma^2+eps) * gamma + beta and RMSNorm = x / sqrt(mean(x^2)+eps) * gamma, and note beneath that 'when mu approx 0 the two outputs coincide'. Use one accent color for positive bars and a muted/negative color for the below-zero bar so the sign is legible; keep the zero baseline in a neutral/muted stroke. Small illustrative integers only (all numbers taken from the chapter). Everything must sit inside the viewBox with a clear left-to-right reading order (input then the two norms). Fully understandable as a single static frame; theme-safe (no hardcoded primary fills, style both light and dark).\n\n  NEW  [static]  name=tblock-parameter-budget\n       concept: Where one transformer block's parameters live: attention ~1/3 vs FFN ~2/3, and the FFN's three gated matrices as three equal slabs.\n       placement: after 'In 'The Feed-Forward Network (FFN) Sublayer' section, right after the 'Parameter count worked example' list.'\n       brief: A compact static budget infographic for ONE pre-norm block. Top element: a single horizontal 100%-width stacked bar representing the block's parameters, split into two labeled segments sized by the true architectural ratio \u2014 'Attention ~ 1/3' (subtitle: 'fused QKV + output projection, 4d^2') taking about one third of the width, and 'FFN ~ 2/3' (subtitle: 'gated MLP, 3 x d x d_ff') taking about two thirds; render the two RMSNorms as a hairline sliver at the join labeled 'norms: negligible (2d)'. Use two distinct theme-safe hues (e.g. accent for attention, the 'good' hue for FFN). Below the bar, expand the FFN segment into a small breakdown: three equal rectangles side by side labeled W_gate, W_up, W_down, each annotated 'approx d x d_ff' with a bracket 'three matrices, not two (gated)'. Keep all quantities SYMBOLIC (4d^2, 3 x d x d_ff, d x d_ff) and the ratio (1/3, 2/3) \u2014 do NOT hardcode specific million-parameter counts; the fractions are the intuition. Add a one-line takeaway strip: 'Attention routes between tokens; the FFN computes per token and is the bulk of the weights \u2014 the part Mixture-of-Experts later swaps out.' Reading order top (whole-block split) to bottom (FFN internals). Must fit inside the viewBox, no overlapping labels; style for both light and dark themes with no hardcoded primary colors.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/06-transformer-block.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/06-transformer-block.md\nThe chapter's core intuitions: A block ADDS a small correction to the residual stream, never replaces it; the identity skip keeps an unbroken gradient highway so deep stacks train.; The residual gradient is a product of (I + Jacobian) factors, so it never vanishes even when sublayers are near-zero at init \u2014 unlike a plain chain of Jacobians.; LayerNorm centers THEN scales; RMSNorm only rescales magnitude (drops mean-centering), preserving the vector's offset structure \u2014 the two coincide when the mean is ~0.; Pre-norm normalizes the sublayer INPUT so the skip carries raw x (a clean unit-variance gradient path); post-norm normalizes after the add and needs LR warmup.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  tblock-layernorm-vs-rmsnorm, tblock-parameter-budget\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/01-pretraining-data",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/01-pretraining-data.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=warc-vs-wet-extraction\n       concept: Why pipelines re-extract main content from raw WARC HTML instead of using the convenient WET text: WET keeps boilerplate, trafilatura returns just the article.\n       placement: after '### Extracting Text from WARC (anchor after the code block, near the 'Why bother' paragraph)'\n       brief: Three-panel layout. CENTER: a stylized rendered web page inside a minimal browser-chrome frame, decomposed into labelled regions: top nav bar, a cookie-consent banner strip, a left sidebar, a large central ARTICLE BODY column, a 'related stories' rail on the right, and a footer. Tint the article body with var(--accent) (the signal) and every other region with a muted/hatched var(--muted) fill (boilerplate); include a tiny legend 'signal = main content / hatched = boilerplate'. From the page, TWO branches: LEFT branch labelled 'CC WET (built-in extractor)' -> a text-block glyph that stacks nav + cookie + article + related + footer lines (article lines accent, rest muted) with a badge 'High boilerplate'. RIGHT branch labelled 'trafilatura on raw WARC HTML' -> a text-block glyph with ONLY clean article lines (all accent) and a badge 'Low boilerplate \u2014 what you train on'. Bottom caption strip: 'RefinedWeb, FineWeb and Dolma re-extract from WARC and never train on WET.' Keep both output blocks the same frame size so the difference is purely fill composition. Theme-safe vars, no clipping, left-to-right reading with the page as the hub.\n\n  NEW  [static]  name=token-packing-shard-rows\n       concept: Documents are concatenated with EOS and chopped into fixed ctx_len+1 rows; the +1 overlap yields the input/target label shift.\n       placement: after '### Shard Layout for Streaming Training (anchor after the ShardedTokenDataset code / practitioner tip)'\n       brief: A horizontal token-stream ribbon at the top: colored token cells for Doc A, then a distinct EOS cell, then Doc B, EOS, Doc C... (use a small illustrative token count, e.g. cells numbered 1..N). Below it, show the same stream sliced into fixed rows of length ctx_len+1 (use a tiny ctx_len like 4 so a row is 5 cells) \u2014 draw 2-3 stacked rows, each a contiguous 5-cell slice, and clearly show the last partial slice being DROPPED (greyed/hatched with a 'partial row dropped' tag). Then EXPAND one row into two aligned copies to show the label shift: top copy highlights cells [0:ctx_len] as 'input = row[:-1]', bottom copy highlights cells [1:ctx_len+1] as 'target = row[1:]', with faint diagonal ties showing target is input shifted by one and the shared overlap. Small caption: 'the +1 overlap gives the input/label pair with no off-by-one at read time'. Note EOS cells belong to the stream and can land anywhere in a row. Theme-safe: input highlight var(--accent), target a second theme var or outline, EOS a muted distinct fill; everything inside the viewBox, reading top-to-bottom.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/01-pretraining-data.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/01-pretraining-data.md\nThe chapter's core intuitions: Pretraining data is a funnel: hundreds of terabytes of raw crawl are progressively cut down \u2014 URL filter, WARC text extraction, language ID, quality heuristics, dedup, tokenize+pack \u2014 into a much smaller set of training-ready shards; volume shrinks monotonically and dedup is one of the largest single cuts.; WET files are convenient but keep boilerplate (nav, footer, cookie banner, 'related'); re-extracting main content from the raw WARC HTML with trafilatura yields cleaner text, which is why RefinedWeb/FineWeb/Dolma never train on WET.; Domain mixture weights combined with corpus size fix how many epochs each domain is seen: epochs_i = N*w_i/|D_i|. A tiny high-quality corpus (Wikipedia) up-weighted heavily is seen ~20x while the huge web corpus stays under 1 epoch; repeating past ~4 epochs degrades generalization.; Documents are concatenated with an EOS separator and packed into fixed rows of ctx_len+1 tokens; the +1 overlap gives the input/label shift (input = row[:-1], target = row[1:]) with no off-by-one at read time.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  warc-vs-wet-extraction, token-packing-shard-rows\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/04-scaling-laws",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/04-scaling-laws.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=emergent-mirage-metric\n       concept: The same smoothly-improving model looks 'emergent' or not depending only on the metric: smooth per-token accuracy p vs scale becomes a sharp cliff under an all-or-nothing exact-match metric p^k.\n       placement: after 'The Emergent Abilities Debate'\n       brief: A static two-panel figure driven by ONE shared underlying quantity. Center or top: a single labeled source - 'per-token correctness p improves SMOOTHLY with scale (a gentle power law)'. LEFT PANEL: x-axis 'model scale N (log)', y-axis 'per-token accuracy p (0..1)'; draw a smooth, gently rising curve from low to high, no kinks - label it 'smooth metric: per-token accuracy'. RIGHT PANEL: same x-axis 'model scale N (log)', y-axis 'exact-match accuracy (all k tokens right)'; draw the curve p^k for a multi-token task - it hugs zero across most of the range then rises STEEPLY (a cliff) near the high end - label it 'all-or-nothing metric: exact match = p^k'. Annotate the cliff region 'looks like a sudden emergent ability'. Add a connective annotation between panels: 'same model, same smooth p - only the ruler changed', and a small inset formula 'exact-match ~ (1 - p_err)^k' with 'k = number of tokens that must ALL be correct'. Use small illustrative integers only for k (e.g. k=1 vs k=30) to contrast the two panels if helpful - a faint k=1 curve overlaid on the right panel that tracks the smooth left curve, versus the k=30 cliff, drives it home; invent NO benchmark accuracy numbers, axes are schematic. Theme-safe accents: one accent for the smooth curve, a second for the cliff; ensure both curves, the cliff annotation, and the formula read clearly in light and dark. Left-to-right reading order, no overlaps, all within viewBox.\n\n  NEW  [static]  name=inference-aware-overtraining\n       concept: When a model will be served heavily, minimizing lifetime compute (train once + serve forever) shifts the optimum away from Chinchilla toward a SMALLER model trained on MORE tokens.\n       placement: after 'Inference-aware scaling: why you deliberately over-train'\n       brief: A static figure showing the optimum MOVING as a function of serving load. Core: a horizontal axis 'tokens per parameter (D_train / N, log)' running left (Chinchilla ~20x) to right (over-trained, 100-2000x). Place three regime markers along it: a Chinchilla-optimal point (~20 tok/param, labeled 'minimizes TRAINING compute'), and two over-trained points further right (labeled e.g. 'Llama-2-7B ~280 tok/param' and 'Llama-3-8B ~1900 tok/param' - these token/param figures are stated in the chapter, so they are allowed; do not invent others). Above the axis, show the lifetime-compute decomposition as a stacked-bar or two-term motif for three serving scenarios (light / medium / heavy inference demand D_inf): each bar = train term 6*N*D_train + serve term 2*N*D_inf, subject to hitting the SAME target loss L*. As D_inf grows across the three scenarios, annotate that the optimum shifts: N SHRINKS and D_train/N GROWS (arrow pointing right along the tok/param axis, labeled 'more serving -> smaller model, more training tokens'). Include the governing objective as a small caption: 'minimize 6 N D_train + 2 N D_inf s.t. loss = L*'. Use small illustrative integers only for any bar proportions; the point is the DIRECTION of the shift, not exact magnitudes. Theme-safe: distinct accents for the train vs serve terms in the stacked bars, theme ink for axis/labels; legible in both themes, no overlapping labels, all within viewBox. A static frame fully conveys it (three scenarios side by side); no animation needed.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/04-scaling-laws.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/04-scaling-laws.md\nThe chapter's core intuitions: Loss falls as a power law toward an irreducible floor E, and the exponents are SMALL - so returns diminish but never stop, which is why halving loss-above-floor costs orders of magnitude more compute.; The joint law L = E + A/N^-alpha + B/D^-beta is two INDEPENDENT, ADDITIVE penalties (finite-model + finite-data) on top of a floor - additivity is what makes the N-vs-D trade-off tractable.; Training compute is C ~= 6ND: 2 FLOPs/param/token forward + 4 backward; this is the shared currency that lets you trade N against D.; Compute-optimal allocation is found on an ISOFLOP SLICE: fix C, sweep N (with D=C/6N), loss traces a shallow VALLEY whose minimum is N_opt(C); connect valleys across budgets to read the allocation exponent. This is the operational heart of Chinchilla.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  emergent-mirage-metric, inference-aware-overtraining\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/08-mixed-precision-fp8",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/08-mixed-precision-fp8.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=fp8mp-format-anatomy-range-vs-precision\n       concept: Bit-field anatomy of fp32/fp16/bf16/fp8 side by side, showing how the sign|exponent|mantissa split sets range vs precision.\n       placement: after 'Section 'The floating-point formats: fp32, fp16, bf16, fp8', immediately after the format table and before the '### Tensor cores accumulate in fp32' subsection.'\n       brief: A single static infographic. Draw five horizontal bit-field bars, one per format, left-aligned and stacked top to bottom in this order: FP32, FP16, BF16, FP8 E4M3, FP8 E5M2. Each bar is segmented left-to-right into three regions: a thin SIGN cell (1 bit), an EXPONENT region, and a MANTISSA region, with segment WIDTHS drawn proportional to the bit counts so the eye reads the trade geometrically. Label each region with its bit count: FP32 = 1/8/23, FP16 = 1/5/10, BF16 = 1/8/7, E4M3 = 1/4/3, E5M2 = 1/5/2. Use ONE consistent visual treatment for all exponent regions and a DIFFERENT consistent one for all mantissa regions (theme-safe shading/hatching, not hardcoded brand colors), and add a legend: 'exponent bits -> dynamic RANGE (how big / how small)' and 'mantissa bits -> PRECISION (how finely you can tell nearby numbers apart)'. Critical alignment device: draw the FP32 bar as the reference at the top; place the BF16 bar directly beneath it and align their exponent regions edge-to-edge, then draw a dashed 'chopped here' cut line showing bf16 = fp32 with the bottom 16 mantissa bits removed (label: 'bf16 = fp32 with the mantissa tail cut off -> same range, coarser precision'). Put a subtle bracket connecting FP16 and BF16 with the caption 'same 16 bits, opposite trade: fp16 buys precision, bf16 buys range -> for training, range wins'. To the RIGHT of each bar, show two tiny horizontal meter bars, a RANGE meter and a PRECISION meter, filled proportionally so the pattern is visible at a glance: fp32 = long range + long precision; fp16 = short range + medium precision; bf16 = long range + short precision; e4m3 = very short range + tiny precision; e5m2 = slightly longer range + tinier precision. Optionally annotate each exponent region with its max finite value drawn from the chapter table only (fp16 -> 65504, e4m3 -> 448, e5m2 -> 57344) as small tags on the range meter; do NOT invent any other numbers. Everything must fit the viewBox with no overlaps and a clear top-to-bottom reading order; both themes must keep all bit-count labels and legends legible.\n\n  NEW  [static]  name=fp8mp-master-weights-swamping\n       concept: Update swamping (stagnation): a tiny weight update lands below half an fp16 ulp and rounds back to the old weight, so it is silently lost \u2014 fixed by applying the update to an fp32 master copy.\n       placement: after 'Section 'Why naive fp16 breaks: range, underflow, and the update problem', after the worked w = 1.0 / eta*g = 2e-4 example paragraph and before the numbered '1. Master weights in fp32 / 2. Loss scaling' list.'\n       brief: A static two-panel comparison built around number lines, using the chapter's own worked example: weight w = 1.0, desired update eta*g = 2e-4, target 0.9998. TOP panel titled 'fp16 update: swamped': draw a horizontal number line zoomed around 1.0 with fp16 grid ticks spaced one ulp apart (label the spacing '1 ulp ~ 2^-10 ~ 9.77e-4'); mark w = 1.0 sitting exactly on a tick. Draw a short leftward arrow of length 2e-4 from 1.0 (label 'w - eta*g = 0.9998 wanted'); show that its landing point falls INSIDE the first grid cell, less than half a tick from 1.0, and a 'round-to-nearest' snap arrow bends it back onto the 1.0 tick. Big verdict tag: 'update < half an ulp -> rounds back to 1.0 -> LOST'. Add a small strip beside it showing 'after many steps' with the value stuck flat at 1.0 (stagnation). BOTTOM panel titled 'fp32 master: sticks': same zoom, but a FINE fp32 grid (many closely spaced ticks) where 0.9998 lands on a real representable tick; the update arrow ends cleanly on it. Verdict tag: 'representable -> update accumulates -> progress'. Connect the two panels with a small loop diagram of the standard recipe: 'compute in fp16 (fast)' -> 'tiny gradient' -> 'apply update to fp32 MASTER weight (precise)' -> 'cast fresh fp16 copy for next forward' -> back to compute; annotate that the optimizer touches only the fp32 master. Keep a one-line footer distinguishing this from loss scaling: 'loss scaling fixes grads that are too small to REPRESENT; master weights fix updates too small to STICK.' Use only the small illustrative numbers already in the chapter (1.0, 2e-4, 0.9998, 2^-10 spacing); invent nothing else. Number lines, ticks, arrows, and verdict tags must stay legible and non-overlapping in both light and dark themes (theme-aware strokes/text, no hardcoded brand colors).\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/08-mixed-precision-fp8.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/08-mixed-precision-fp8.md\nThe chapter's core intuitions: Exponent bits buy dynamic range; mantissa bits buy precision. The bit-budget split IS the whole trade, and for training range beats precision \u2014 which is why bf16 (fp32-sized exponent, 7-bit mantissa) beats fp16 (5-bit exponent, 10-bit mantissa) despite equal size.; Tensor cores take narrow inputs but accumulate the long dot-product reduction in fp32 \u2014 'narrow inputs, wide accumulator' \u2014 which is the single trick that makes any low-precision matmul numerically survivable.; fp16 has TWO distinct failures with TWO distinct fixes: gradient underflow (small grads round to 0), fixed by loss scaling; and update swamping/stagnation (a tiny update added to a large weight is lost below half an ulp), fixed by keeping fp32 master weights.; Loss scaling multiplies the loss by S so the chain rule lifts the entire gradient distribution up out of the underflow zone; it is a mathematical no-op on the final update. bf16 needs none because its exponent already matches fp32.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  fp8mp-format-anatomy-range-vs-precision, fp8mp-master-weights-swamping\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/10-lr-schedules-hparams",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/10-lr-schedules-hparams.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=lrsched-why-warmup\n       concept: Warmup prevents the cold-start blowup: with full LR from step 0, cold Adam moments plus large erratic gradients cause an early loss spike/divergence; ramping the LR keeps training smooth.\n       placement: after 'Why Schedules and Warmup Exist'\n       brief: A main loss-vs-step panel with a slim LR strip aligned above it (shared x-axis = training step, early portion only \u2014 the first ~T_w plus a bit more). Shade the first T_w steps of BOTH strips lightly and label the band 'cold-start danger zone: Adam v_t still ~0, gradients large & erratic'. TOP LR strip: two lines \u2014 a 'no warmup' line flat at eta_max from step 0, and a 'with warmup' line that ramps linearly from near-zero to eta_max over T_w then holds. MAIN loss panel, two trajectories from the same starting loss at step 0: (1) 'no warmup' \u2014 rises into a sharp spike within the danger zone and breaks off with an X marker labeled 'diverged / NaN' (or stays pinned high); draw it in a muted/warning treatment. (2) 'with warmup' \u2014 descends smoothly and monotonically, clearly the healthy curve. Add one concise callout arrow to the no-warmup spike: 'full LR x random weights -> update overshoots, logits saturate'. Y-axes carry no benchmark numbers (qualitative 'loss' and 'LR' with eta_max / eta_min ticks on the LR strip only). Keep the two curves clearly distinguishable by style, not by relying on color alone, so it survives both themes. Everything inside the viewBox, no text crossing plotted lines. Static frame is sufficient; do not animate.\n\n  NEW  [static]  name=lrsched-coordinate-check\n       concept: The coordinate check: per-layer activation/update scale plotted against width is flat under correct muP and drifts under standard parameterization.\n       placement: after 'Verifying muP: The Coordinate Check'\n       brief: A single plot. X-axis: model width on a LOG scale with ticks 256, 512, 1024, 2048. Y-axis: 'per-layer activation scale  mean|h|' on a LOG scale (no benchmark numbers needed; qualitative). Two connected-dot series: (1) muP \u2014 a FLAT horizontal line across all widths, dots on it, labeled 'muP: flat = feature updates stay O(1) at every width  [check]'. (2) SP \u2014 a line that DRIFTS monotonically across widths (show it shrinking several-fold from left to right, e.g. steadily downward, consistent with the chapter's note that SP activation scale collapses as width grows), labeled 'SP: drifts -> optimal HPs will NOT transfer  [x]'. Distinguish the two series by line style AND label, not color alone. Add a short caption strip beneath: 'Flat curves = correct muP. Any drift means a bug in init std, per-layer LR multiplier, or attention/readout scaling.' Keep labels off the plotted lines; everything inside the viewBox. Theme-safe via theme variables; the only numbers are the illustrative width ticks. Static single frame.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/10-lr-schedules-hparams.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/10-lr-schedules-hparams.md\nThe chapter's core intuitions: Warmup is mandatory at scale because Adam's second-moment v_t is cold-started at zero while step-0 gradients are large and erratic; applying full LR immediately produces oversized updates that saturate softmax logits and diverge. Ramping LR gives the moments time to calibrate.; The schedule shape trades exploration against convergence: cosine spends most of the budget near peak LR (aggressive steps for exploration) then softly anneals; WSD decouples training length from schedule shape by holding a stable plateau you can extend, then decaying only to 'polish'.; Batch size and LR are coupled: below the critical batch size B* larger batches cut steps proportionally (linear scaling rule); above B* gradient noise is already averaged out and extra data-per-step is wasted (sqrt / saturated regime).; muP's payoff: under standard parameterization the optimal LR shifts as width grows so you must re-tune at every scale; muP reparameterizes the net so the optimal LR is width-invariant \u2014 sweep once on a tiny proxy, transfer to the full model.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  lrsched-why-warmup, lrsched-coordinate-check\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/11-training-stability",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/11-training-stability.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=attention-logit-overflow-qknorm\n       concept: How an unbounded QK logit drives a peaked softmax that underflows to NaN, and how QK-Norm bounds the logit so the softmax stays stable.\n       placement: after 'QK-Norm'\n       brief: Two parallel horizontal pathways sharing the same start. TOP pathway 'Without QK-Norm' (ends in --bad): stage 1 - Q and K vectors both with large L2 norm (~100) drawn as long vectors; stage 2 - dot product scaled by 1/sqrt(d_k) with d_k=128, annotate 1/sqrt(128) approx 0.088; stage 3 - resulting logit approx 100*100*0.088 approx 880, annotate 'within fp16 max 65504 - but far too peaked'; stage 4 - a softmax bar chart that COLLAPSES to a single tall spike on one token with all others approx 0; stage 5 - endpoint marked --bad: 'exp-sum underflows -> NaN gradient cascade'. Draw the fp16 ceiling 65504 as a faint --muted reference line and make explicit that the real danger is the peaked softmax + underflow, not raw overflow of 880. BOTTOM pathway 'With QK-Norm' (ends in --good): same Q,K -> RMSNorm(Q), RMSNorm(K) shown shrinking/normalizing the vectors so their norm is bounded -> logits now O(sqrt(d_k)), a modest range -> softmax drawn as a smooth, spread distribution -> endpoint marked --good: 'well-conditioned softmax, stable gradients'. A short caption anchors it to the chapter's Story 3 (fp16, head 7, logits near 60000). Theme-safe: --bad for the failure endpoint and collapsed bar, --good for the stable path, --muted for the reference line, currentColor for text/vectors. Use ONLY the chapter's illustrative integers (128, 0.088, 100, 880, 65504); no invented benchmark numbers. Everything inside the viewBox, the two pathways clearly separated top/bottom.\n\n  NEW  [static]  name=spike-diagnostic-decision-tree\n       concept: Reading grad_norm (plus LR timing) to narrow a loss spike to its root-cause category.\n       placement: after 'Debugging Playbook: A Step-by-Step Checklist'\n       brief: A clean top-down decision tree rooted at 'Loss spike detected'. The FIRST and visually-emphasized fork is on the key signal grad_norm (emphasize it as the primary discriminator with a bold node), with four labelled outcomes, each a rounded leaf card: (a) 'grad_norm normal (<2x clip) + loss spiked' -> category DATA: 'anomalous batch (repetition / encoding / dedup bug)' -> fix: 'skip-batch, audit the source shard'; (b) 'grad_norm large (>5x clip)' -> category OPTIMIZER: 'Adam amplification, stale sqrt(v_hat)' -> fix: 'clip, reduce LR ~20-30%, resume from pre-spike ckpt'; (c) 'grad_norm NaN / Inf' -> category FLOATING-POINT: 'activation/attention overflow' -> fix: 'QK-Norm, switch to bf16, add z-loss'; (d) 'grad_norm approx 0' -> category VANISHING / BAD INIT -> fix: 'check depth-scaling & init'. Add a separate cross-cutting condition node off to the side: 'Spike coincides with LR peak / end of warmup?' -> leaf 'WARMUP too short: extend to >=1% of steps, use sqrt-warmup'. Color-code the four category leaves with theme-safe surface tints and accent/good/bad borders (DATA neutral, OPTIMIZER --accent, FLOATING-POINT --bad, VANISHING --muted). Keep the tree shallow and scannable; the root grad_norm node should read as 'this one number tells you which branch'. Theme-safe vars only; use the chapter's own multiples (2x, 5x clip, >=1%), invent no numbers. All cards legible and inside the viewBox.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/11-training-stability.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/11-training-stability.md\nThe chapter's core intuitions: Why a loss spike amplifies: on the spike step Adam's second moment sqrt(v_hat) is still STALE (beta2 near 1, updates slowly), so the update scales with the raw gradient ratio (~100x normal); over the next few hundred steps sqrt(v_hat) absorbs the spike, the denominator grows, updates damp, and the spike is usually transient.; The spike causal chain: bad batch OR bad state -> large gradient -> large parameter update -> bad model state (wrong basin) -> elevated loss; every mitigation is just a valve that severs one specific link of this chain.; Gradient clipping caps amplification by roughly one order of magnitude (100x -> 10x) by rescaling on the GLOBAL norm while preserving gradient direction.; Attention-logit overflow: an unbounded QK^T/sqrt(d_k) drives a peaked softmax whose exp-sum underflows into NaN gradients; QK-Norm (and z-loss) bound the logits at the source so the softmax stays well-conditioned.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  attention-logit-overflow-qknorm, spike-diagnostic-decision-tree\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/14-data-mixing-curriculum",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/14-data-mixing-curriculum.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=mixing-level-vs-velocity\n       concept: DoReMi's excess loss is a LEVEL (vertical gap to the achievable floor); online bandit mixing uses VELOCITY (the current slope of the loss curve) - same reweighting machinery, different signal, and they can point opposite ways.\n       placement: after 'Group-DRO vs. online bandit mixing - same family, different reward'\n       brief: Two small per-domain loss-vs-tokens curves side by side, sharing an axis style, each with a dashed horizontal reference-floor line and a vertical guide marking 'now' (current tokens seen). LEFT, 'Domain X': a decreasing curve that has FLATTENED but still sits well ABOVE its floor. Annotate the vertical distance from the curve down to the floor with a bracket labeled 'excess (LEVEL) = large', and draw a short tangent segment at 'now' that is nearly flat, labeled 'velocity (slope) ~ 0 (stuck)'. RIGHT, 'Domain Y': a still-steeply-decreasing curve that is currently CLOSE to its floor. Bracket its small vertical gap 'excess = small' and draw a steep tangent with a little slope triangle 'velocity = large (improving fast)'. Under each curve place two verdict chips: for X -> 'DoReMi (excess): UPWEIGHT' and 'Bandit (velocity): skip'; for Y -> 'DoReMi: leave it' and 'Bandit: UPWEIGHT'. Bottom strap: 'Excess = fix what is broken (a gap). Velocity = ride what is improving (a slope). They can point opposite ways.' Schematic axes only, no numbers. Use bracket + tangent-triangle geometry (not color) to carry the level-vs-slope contrast so it survives both themes.\n\n  NEW  [static]  name=mixing-dedup-upsample-multiply\n       concept: Deduplication and upsampling act on the SAME axis (effective times a token is seen); done in the wrong order they multiply - internal-duplication factor times upsample factor - and silently overshoot the repetition danger zone.\n       placement: after 'Upsampling vs. deduplication: the trade-off'\n       brief: A two-lane before/after for one small high-quality domain, with a shared vertical gauge on the right labeled 'effective views of a passage' that has a danger band near the top labeled '~4-6 epochs -> memorization'. TOP lane 'upsample BEFORE dedup (trap)': draw a document as a row of tiles where 4 tiles are near-duplicates of one passage (same shading/hatch + label 'dup') plus a couple of distinct 'unique' tiles; apply an operation box 'x3 upsample'; result: the duplicated passage now has 4 x 3 = 12 effective views, and its marker on the gauge lands INSIDE the danger band. Show the arithmetic '4 x 3 = 12' prominently. BOTTOM lane 'dedup THEN upsample (correct)': same starting document, a 'dedup' box collapses the 4 duplicate tiles into 1 unique tile (annotate 'now one epoch = one genuine pass'), then 'x3 upsample' -> 1 x 3 = 3 effective views, whose gauge marker sits in a safe band labeled '2-4 epochs, net-positive'. Punchline strap: 'Dedup makes the epoch count honest; upsampling then spends that honest budget.' Use only small illustrative integers; the danger/safe bands are schematic (invent no benchmark numbers). Distinguish duplicate vs unique tiles by hatch/pattern and text label, not hue alone, and keep both lanes and the gauge inside the viewBox with clear top-to-bottom reading order.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/14-data-mixing-curriculum.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/14-data-mixing-curriculum.md\nThe chapter's core intuitions: A mixture is a sampling distribution w over domains, a separate object from how much data you have and from the target distribution p* you care about; the natural proportional mixture is almost never optimal.; Mixing is fundamentally an epoch-budget decision: e_i = w_i D / n_i, so upweighting a small pool forces repetition, and repetition has a decaying, eventually negative payoff past ~4-6 epochs.; Deduplication and upsampling act on the SAME axis (effective times a token is seen); dedup makes 'one epoch' honest, upsampling spends that honest budget, and doing both blindly MULTIPLIES duplication.; DoReMi reweights by EXCESS loss (proxy loss minus a fixed reference floor), not raw loss, so intrinsically hard-but-irreducible domains stop hogging weight and closeable-headroom domains get upweighted.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  mixing-level-vs-velocity, mixing-dedup-upsample-multiply\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/15-synthetic-data",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/15-synthetic-data.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=syndata-collapse-replace-vs-accumulate\n       concept: Two side-by-side generational chains of a fitted Gaussian: REPLACE narrows toward a spike as variance decays like (1-1/n)^t; ACCUMULATE holds its width because retained real data re-anchors it.\n       placement: after 'A toy model of collapse: the shrinking Gaussian'\n       brief: A two-panel figure sharing one horizontal value axis per panel. LEFT panel titled 'REPLACE \u2014 fit only on last generation's samples'. RIGHT panel titled 'ACCUMULATE \u2014 fit on original real data + all synthetic so far'. In EACH panel draw a family of ~5 bell curves representing generations t = 0, 5, 10, 15, 20, all centered on the same mean, distinguished by opacity/line-weight (gen 0 the boldest/darkest, later generations progressively lighter) with tiny 't=0','t=20' labels so the direction of time is unmistakable. LEFT: the curves get progressively TALLER and NARROWER (variance shrinking) \u2014 the latest curve is a thin tall spike collapsing onto the mean; annotate with a small callout 'tails vanish; variance -> 0'. RIGHT: all curves stay essentially the SAME width, overlapping nearly on top of each other; annotate 'real data re-anchors width each generation'. Between or above the two panels place the governing relation as a clean label: replace-regime expected variance ~ sigma^2 (1 - 1/n)^t decays; accumulate halts the decay. Add a small anchor motif on the RIGHT panel only \u2014 e.g. a small anchor icon or a bold 'real data' baseline curve that stays fixed \u2014 to visually explain WHY it holds. A one-line bottom caption: 'Collapse is recursive REPLACEMENT, not synthetic data itself.' Theme-safe: use one hue for the replace family and a second for the accumulate family (no hardcoded reds/greens that break in dark mode; rely on the palette variables). Only illustrative labels, invent no benchmark numbers; the (1-1/n)^t form and small integer generation labels are the only quantities. Everything must fit the viewBox with clear reading order left-to-right, no curve labels overlapping.\n\n  NEW  [static]  name=syndata-overgenerate-filter-hard\n       concept: Rejection-sampling fine-tuning: sample K chain-of-thought traces per problem, verify each, discard the wrong ones, keep verified-correct traces \u2014 with the coverage 1-(1-p)^K rising steeply in K.\n       placement: after 'Rejection-sampling fine-tuning, end to end'\n       brief: A composite figure in two linked halves. LEFT half \u2014 the sample-and-filter mechanic for ONE problem: at top a single 'Problem (known gold answer)' node; from it fan out K (~8) parallel 'trace' cards labeled 'CoT trace'. Each trace ends in an extracted final answer that is checked against the gold answer by a small 'verify' gate: mark the majority as WRONG (crossed out / dimmed, e.g. 5 of 8) and a minority as CORRECT (checkmark, highlighted, e.g. 3 of 8). Funnel the CORRECT traces down into a 'kept: verified (problem -> correct trace)' SFT bucket; show the WRONG traces dropping away. Add a tiny note 'cap per problem + keep DIVERSE correct traces' near the bucket. RIGHT half \u2014 the payoff curve: a small plot of coverage = 1-(1-p)^K on the y-axis (0 to 1) versus K on the x-axis (1..8+), drawn as a steeply rising, saturating curve. Mark the illustrative point at K=8 with a dashed guide showing coverage near the top for a modest per-attempt success p (label it 'p~0.4 teacher, K=8 -> ~0.98 covered' as an ILLUSTRATIVE example, using only these small round numbers that appear in the chapter text, inventing nothing new). Caption tying them together at the bottom: 'Over-generate, filter hard: verification converts an unreliable generator into a high-precision dataset.' Theme-safe palette variables only; correct = success hue, wrong = muted/neutral, no hardcoded colors. Keep all trace cards legible and non-overlapping; clear top-to-bottom then left-to-right reading order; everything inside the viewBox.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/15-synthetic-data.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/15-synthetic-data.md\nThe chapter's core intuitions: The verification/transformation asymmetry: restyling text is easier than writing it, and verifying an answer is easier than generating it \u2014 this asymmetry is the engine that lets cheap compute be converted into scarce high-quality tokens.; The grounding spectrum: the further synthetic text departs from a human-written source (rephrase -> knowledge-augment -> prompt-seed -> free-gen), the more value it can add AND the more hallucination risk it carries.; Model collapse is caused by recursive REPLACEMENT, not by synthetic data per se: in 'replace' the fitted variance decays like (1-1/n)^t and the tails vanish; in 'accumulate' the retained real data re-anchors the distribution each generation and the decay halts.; Over-generate, filter hard: because correctness can be verified objectively (math/code), sampling K traces and keeping only verified-correct ones gives high coverage even from an unreliable teacher (1-(1-p)^K), which is why post-training data can be almost fully synthetic while pretraining mixes cannot.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  syndata-collapse-replace-vs-accumulate, syndata-overgenerate-filter-hard\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "04-kernels-efficiency/04-triton-kernels",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/04-triton-kernels.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=matmul-tile-reuse-kloop\n       concept: One program computes one BLOCK_M x BLOCK_N tile of C by walking K in BLOCK_K steps, accumulating tl.dot into an fp32 tile; plus the GROUP_M swizzle for L2 reuse.\n       placement: after '## Kernel 3: Matrix Multiplication \u2014 Tiling, Accumulation, and Swizzling'\n       brief: Static GEMM tiling diagram, C = A\u00b7B. Lay out A on the left, B on top, C at the intersection (bottom-right), the classic mm layout. Highlight ONE output tile C[pid_m,pid_n] of size BLOCK_M x BLOCK_N. Draw the matching horizontal strip of A (BLOCK_M rows, full K width) and vertical strip of B (full K height, BLOCK_N cols); shade only these strips. Overlay the K-loop: divide the strips into BLOCK_K chunks (show 3 chunks) and annotate one step: 'load A tile (BLOCK_M x BLOCK_K) + B tile (BLOCK_K x BLOCK_N) -> tl.dot -> acc += (fp32 register tile)'. A left-to-right arrow over the chunks: 'walk K in steps of BLOCK_K, accumulate in fp32; write C tile ONCE after the loop.' Add a short reuse note: 'each loaded A row-tile is reused across BLOCK_N output cols; each B col-tile across BLOCK_M rows \u2014 that reuse is the whole point.' Include a small INSET illustrating the GROUP_M swizzle: two mini 4x4 grids of output tiles with numbered visitation order \u2014 'row-major (poor L2 reuse)' where consecutive programs span far-apart B columns, vs 'GROUP_M grouped (nearby programs share A rows / B cols -> better L2 hit rate)'. Use small integers only (e.g. BLOCK sizes as labels, GROUP_M=2 in the inset); no benchmark numbers. Theme-safe: distinct-but-neutral accents for the active C tile, the A/B strips, and the fp32 accumulator; keep the inset from crowding the main diagram; everything inside the viewBox.\n\n  NEW  [animated]  name=online-softmax-rescale-both\n       concept: As K/V blocks stream in, the running max jumps and \u03b1=exp(m_old\u2212m_new) must rescale BOTH the running sum \u2113 AND the output accumulator O before the new block is added.\n       placement: after '### The Online Softmax Recurrence'\n       brief: Long-animation of the online-softmax update for ONE query row as key/value blocks j=1,2,3 stream in, focused on the 'rescale BOTH' mechanic. Persistent RUNNING-STATE panel with three registers: m (running max, a number), \u2113 (running sum, a number), O (running output accumulator, drawn as a short horizontal bar of d entries). A queue of incoming score blocks S^(1), S^(2), S^(3) feeds in from the left one at a time. Per block, animate the steps in order: (1) block S^(j) arrives; compute its block max m_j. (2) m_new = max(m, m_j): when m_j exceeds the old m, animate m ticking UP and flash this jump as the trigger. (3) \u03b1 = exp(m_old \u2212 m_new) appears as a correction factor \u2264 1. (4) THE FOCAL MOMENT: BOTH \u2113 and O visibly shrink by \u03b1 with a single synchronized pulse; overlay 'rescale BOTH \u2014 the #1 bug is rescaling \u2113 but forgetting O (or vice versa)'. (5) add new mass: \u2113 += \u03a3 exp(S^(j) \u2212 m_new) and O += exp(S^(j) \u2212 m_new)\u00b7V^(j), shown as the bar growing. After the final block, a distinct closing frame: 'O \u00f7 \u2113 once' normalizes the accumulator to the final output. Every frame must be legible as a static snapshot: show current j and current m/\u2113 values as small illustrative integers/decimals (e.g. m: 2 -> 5, \u03b1 \u2248 0.05) \u2014 invent NO benchmark numbers. Keep a persistent caption: 'both \u2113 and O were exponentiated against the OLD max, so both must be multiplied by \u03b1 before adding the new block.' The animated focal points are the running-max jump and the synchronized \u03b1 pulse on \u2113 and O. Theme-safe; the first frame (empty running state) and last frame (normalized O) each stand alone as clear stills.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/04-kernels-efficiency/04-triton-kernels.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/04-kernels-efficiency/04-triton-kernels.md\nThe chapter's core intuitions: The block/tile abstraction: a Triton program owns one TILE of the output; the compiler owns the threads, coalescing, shared memory, and Tensor Core fragment layout.; Pointer-block arithmetic is the load-bearing skill: turn 'the elements I want' into a tile of addresses via base + row[:,None]*stride_m + col[None,:]*stride_n, and use masks (other=-inf/0) for ragged edges.; Fusion is the win for memory-bound ops: load a row once, do max+exp+sum on-chip, write once (~2 HBM passes vs ~5) \u2014 memory-bound, so the ~2.5x ceiling is set by bandwidth, not compute.; Matmul is about reuse: tile M/N/K, walk K in BLOCK_K steps accumulating tl.dot in an fp32 register tile, reuse each loaded A/B tile from SRAM, and swizzle (GROUP_M) tile visitation for L2 locality.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  matmul-tile-reuse-kloop, online-softmax-rescale-both\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "04-kernels-efficiency/09-compilers-fusion",
    "n_new": 2,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/09-compilers-fusion.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=cuda-graph-cpu-launch-timeline\n       concept: Why CUDA graphs help: eager mode leaves the GPU idle between many CPU kernel launches; a captured graph replays with one CPU call and packs kernels back-to-back.\n       placement: after 'CUDA Graphs: Eliminating CPU Launch Overhead (after the 'How CUDA Graphs Work' subsection heading)'\n       brief: A two-scenario timeline infographic, time flowing left to right, x-axis labeled 'time' with a small arrowhead. TOP scenario titled 'Eager launch (CPU-bound, small kernels)'. Draw two horizontal lanes stacked: an upper lane labeled 'CPU' and a lower lane labeled 'GPU'. In the CPU lane place a row of ~5 small identical blocks each labeled 'launch' (representing cudaLaunchKernel), separated by visible gaps. In the GPU lane, directly BELOW and slightly to the right of each CPU launch block, place a short compute block labeled with a tiny op name (k1, k2, k3, k4, k5); between consecutive GPU compute blocks draw clearly-marked IDLE gaps (hatched or dashed, labeled 'idle \u2014 waiting for next launch'). The staggering must show each GPU kernel starting only after its CPU launch, so the GPU lane has bubbles. Add a caption under this scenario: 'CPU launch latency (~microseconds each) dominates; GPU starves'. BOTTOM scenario titled 'CUDA graph replay'. First show a small one-time, visually-set-apart 'capture (once)' marker at far left \u2014 a dashed box labeled 'capture: record launch sequence' \u2014 with a subtle note '(warmup, one time)'. Then in the CPU lane a SINGLE small block labeled 'cudaGraphLaunch (1 call)'. In the GPU lane, a contiguous run of the SAME kernels k1 k2 k3 k4 k5 packed tightly back-to-back with NO idle gaps between them. Caption under this scenario: 'one CPU call submits the whole recorded DAG; GPU runs kernels back-to-back'. On the right edge, align the two GPU lanes vertically and add a bracket/annotation showing the bottom scenario finishes sooner: label 'wall-clock saved'. Use only a small handful of kernels (5) and NO numeric microsecond values on the blocks \u2014 the gaps vs no-gaps contrast carries the meaning. Theme-safe: idle regions distinguished by hatching/dashes and a text label, not by a hardcoded warning color; use theme-aware fills for CPU vs GPU lanes (e.g. two tints of the neutral/accent palette) with clear lane labels so it reads in both light and dark. Everything inside the viewBox, no overlapping labels, clear top-to-bottom then left-to-right reading order. A single static frame must fully convey it (the two stacked scenarios ARE the before/after).\n\n  NEW  [static]  name=dynamo-graph-breaks-subgraphs\n       concept: How TorchDynamo partitions a Python function into separately-compiled subgraphs at graph breaks, falling back to eager for untraceable ops, with guards that trigger recompiles.\n       placement: after 'TorchDynamo: Safe Graph Capture via Bytecode Rewriting (right after the dynamo.explain code block, near the guard-system paragraph)'\n       brief: A horizontal 'ribbon' showing one Python function's execution as a left-to-right sequence of operation tokens, then how Dynamo carves it into compiled subgraphs. Top row: a strip of ~7 small op tokens in execution order, e.g. 'y = sin(x)', 'z = y + 1', 'if x.shape[0] > 10:', 'y = y * 2', 'w = matmul(...)', 'print(w)', 'return cos(w)'. Mark the untraceable ones distinctly: put a break glyph (a jagged/lightning break symbol plus a small label 'GRAPH BREAK') at the shape-dependent 'if' token and at the 'print' token, each with a tiny reason tag ('data-dependent control flow', 'side effect / host sync'). Below the strip, draw grouping brackets that partition the traceable runs into boxes: 'Subgraph 1 (compiled -> fused Triton)' covering the first traceable run before the if; a small eager-fallback pill labeled 'eager' under the if; 'Subgraph 2 (compiled)' covering the traceable ops after the if up to print; another 'eager' pill under print; 'Subgraph 3 (compiled)' covering the final return-cos run. Make clear visually that compiled subgraphs are fused/optimized (solid accent boxes) while the eager segments are plain/unoptimized (outline pills). Off to the side or beneath Subgraph 1, attach a small 'guards' note listing example recorded assumptions ('x.shape == (5,)', 'x.dtype == float32') with a caption 'if a guard fails at runtime -> recompile'. Add a one-line takeaway at the bottom: 'more breaks = smaller fusion regions + eager gaps = less speedup'. Constraints: use only the small op set shown, invent no numbers beyond the illustrative shape/dtype; theme-safe (compiled vs eager distinguished by fill style and labels, not by a fixed color that fails in one theme); keep all labels legible and non-overlapping, reading strictly left to right; fully understandable as a static frame.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/04-kernels-efficiency/09-compilers-fusion.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/04-kernels-efficiency/09-compilers-fusion.md\nThe chapter's core intuitions: Eager PyTorch pays a hidden tax on every op: Python/dispatch overhead, a CPU kernel-launch call, and a DRAM round-trip for each intermediate \u2014 even when the arithmetic is trivial.; Kernel fusion keeps intermediates on-chip (registers/shared memory) so a chain of elementwise ops reads inputs once and writes the output once, cutting DRAM traffic ~N->1.; CUDA graphs attack a DIFFERENT bottleneck than fusion: for many small kernels the CPU cannot issue cudaLaunchKernel fast enough, so the GPU sits idle; recording the launch sequence once and replaying it with a single CPU call fills the GPU back-to-back.; torch.compile is a three-stage pipeline: TorchDynamo captures an FX graph from Python bytecode, AOTAutograd traces the joint forward+backward graph, TorchInductor lowers to fused Triton/C++.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  cuda-graph-cpu-launch-timeline, dynamo-graph-breaks-subgraphs\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  }
]
const DRAW_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "figures_done", "figures_skipped"], "properties": {"chapter": {"type": "string"}, "figures_done": {"type": "array", "items": {"type": "string"}}, "figures_skipped": {"type": "array", "items": {"type": "string"}, "description": "names you could NOT complete, with why appended"}, "notes": {"type": "string"}}}
const REVISE_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "figures", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "true iff every planned figure is present, correct, theme-safe, unclipped and builds intuition"}, "figures": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["name", "verdict"], "properties": {"name": {"type": "string"}, "verdict": {"type": "string", "enum": ["good", "fixed", "still-broken", "missing"]}, "issue": {"type": "string"}}}}, "notes": {"type": "string"}}}

// Draw on Sonnet 5, revise on Opus (user routing 2026-07-20).
const results = await pipeline(
  CH,
  c => agent(c.draw, { label: `draw:${c.chapter}`, phase: 'Draw', schema: DRAW_SCHEMA,
                       model: 'claude-sonnet-5' }).then(d => ({ c, d })),
  ({ c }) => agent(c.revise, { label: `revise:${c.chapter}`, phase: 'Revise', schema: REVISE_SCHEMA,
                                 model: 'opus', effort: 'high' })
                .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed)
                   .map(r => ({ chapter: r.chapter, figs: r.verdict?.figures?.filter(f => f.verdict === 'still-broken' || f.verdict === 'missing') }))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`figures: drawn+revised ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | died ${died.length}`)
log(`PASSED (append to figplan_done.txt): ${passed.join(' ')}`)
if (failed.length) log(`FAILED: ${JSON.stringify(failed)}`)
if (died.length) log(`DIED (re-run to resume): ${died.join(' ')}`)
return { passed, failed, died }
