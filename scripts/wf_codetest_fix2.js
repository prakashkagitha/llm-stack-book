export const meta = {
  name: 'codetest-fix2',
  description: 'Assemble + run + verify CI-tested code for 8 chapters',
  phases: [{ title: 'Assemble+Run' }, { title: 'Verify' }],
}

const CH = [
  {
    "chapter": "09-rag-retrieval/04-chunking-reranking-hybrid",
    "slug": "09-rag-retrieval__04-chunking-reranking-hybrid",
    "n_ci": 12,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/09-rag-retrieval/04-chunking-reranking-hybrid.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__04-chunking-reranking-hybrid.py\n\nThis chapter has 12 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~32, 57 lines)\n    - block #1 (line ~103, 76 lines)\n    - block #2 (line ~187, 65 lines)\n    - block #4 (line ~384, 128 lines)\n    - block #5 (line ~545, 54 lines)\n    - block #6 (line ~605, 25 lines)\n    - block #7 (line ~642, 46 lines)\n    - block #8 (line ~698, 63 lines)\n    - block #9 (line ~786, 63 lines)\n    - block #10 (line ~855, 50 lines)\n    - block #11 (line ~913, 106 lines)\n    - block #12 (line ~1040, 53 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #3=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__04-chunking-reranking-hybrid.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/09-rag-retrieval__04-chunking-reranking-hybrid.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/09-rag-retrieval/04-chunking-reranking-hybrid.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/09-rag-retrieval/04-chunking-reranking-hybrid.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__04-chunking-reranking-hybrid.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/09-rag-retrieval__04-chunking-reranking-hybrid.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/09-rag-retrieval/04-chunking-reranking-hybrid.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "03-pretraining/04-scaling-laws",
    "slug": "03-pretraining__04-scaling-laws",
    "n_ci": 8,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/03-pretraining/04-scaling-laws.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/03-pretraining__04-scaling-laws.py\n\nThis chapter has 8 heuristically CPU-runnable Python blocks to test:\n    - block #1 (line ~201, 26 lines)\n    - block #3 (line ~246, 89 lines)\n    - block #4 (line ~341, 33 lines)\n    - block #5 (line ~390, 26 lines)\n    - block #6 (line ~428, 17 lines)\n    - block #7 (line ~463, 9 lines)\n    - block #8 (line ~548, 28 lines)\n    - block #9 (line ~601, 21 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=fragment, #2=non-python\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/03-pretraining__04-scaling-laws.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/03-pretraining__04-scaling-laws.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/03-pretraining/04-scaling-laws.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/03-pretraining/04-scaling-laws.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/03-pretraining__04-scaling-laws.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/03-pretraining__04-scaling-laws.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/03-pretraining/04-scaling-laws.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "11-evaluation/05-redteaming-safety-eval",
    "slug": "11-evaluation__05-redteaming-safety-eval",
    "n_ci": 6,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/11-evaluation/05-redteaming-safety-eval.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/11-evaluation__05-redteaming-safety-eval.py\n\nThis chapter has 6 heuristically CPU-runnable Python blocks to test:\n    - block #1 (line ~182, 54 lines)\n    - block #2 (line ~248, 71 lines)\n    - block #3 (line ~355, 59 lines)\n    - block #4 (line ~449, 48 lines)\n    - block #5 (line ~541, 86 lines)\n    - block #7 (line ~678, 69 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=needs-net, #6=non-python, #8=non-python\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/11-evaluation__05-redteaming-safety-eval.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/11-evaluation__05-redteaming-safety-eval.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/11-evaluation/05-redteaming-safety-eval.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/11-evaluation/05-redteaming-safety-eval.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/11-evaluation__05-redteaming-safety-eval.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/11-evaluation__05-redteaming-safety-eval.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/11-evaluation/05-redteaming-safety-eval.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "08-agents-harness/07-multi-agent-systems",
    "slug": "08-agents-harness__07-multi-agent-systems",
    "n_ci": 5,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/08-agents-harness/07-multi-agent-systems.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/08-agents-harness__07-multi-agent-systems.py\n\nThis chapter has 5 heuristically CPU-runnable Python blocks to test:\n    - block #2 (line ~267, 56 lines)\n    - block #3 (line ~327, 23 lines)\n    - block #5 (line ~483, 57 lines)\n    - block #6 (line ~546, 78 lines)\n    - block #8 (line ~713, 15 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #1=needs-net, #4=needs-net, #7=needs-net, #9=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/08-agents-harness__07-multi-agent-systems.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/08-agents-harness__07-multi-agent-systems.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/08-agents-harness/07-multi-agent-systems.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/08-agents-harness/07-multi-agent-systems.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/08-agents-harness__07-multi-agent-systems.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/08-agents-harness__07-multi-agent-systems.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/08-agents-harness/07-multi-agent-systems.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "09-rag-retrieval/01-embeddings-representation",
    "slug": "09-rag-retrieval__01-embeddings-representation",
    "n_ci": 5,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/09-rag-retrieval/01-embeddings-representation.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__01-embeddings-representation.py\n\nThis chapter has 5 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~49, 32 lines)\n    - block #1 (line ~107, 58 lines)\n    - block #2 (line ~261, 23 lines)\n    - block #5 (line ~355, 18 lines)\n    - block #7 (line ~400, 14 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #3=fragment, #4=non-python, #6=shell, #8=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__01-embeddings-representation.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/09-rag-retrieval__01-embeddings-representation.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/09-rag-retrieval/01-embeddings-representation.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/09-rag-retrieval/01-embeddings-representation.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__01-embeddings-representation.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/09-rag-retrieval__01-embeddings-representation.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/09-rag-retrieval/01-embeddings-representation.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "99-appendix/04-tooling-setup",
    "slug": "99-appendix__04-tooling-setup",
    "n_ci": 4,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/99-appendix/04-tooling-setup.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/99-appendix__04-tooling-setup.py\n\nThis chapter has 4 heuristically CPU-runnable Python blocks to test:\n    - block #5 (line ~152, 24 lines)\n    - block #21 (line ~602, 22 lines)\n    - block #23 (line ~688, 26 lines)\n    - block #24 (line ~724, 40 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=shell, #1=shell, #2=shell, #3=shell, #4=shell, #6=needs-gpu, #7=needs-gpu, #8=needs-gpu, #9=needs-net, #10=needs-gpu, #11=shell, #12=shell, #13=non-python, #14=non-python, #15=shell, #16=needs-gpu, #17=needs-gpu, #18=shell, #19=needs-gpu, #20=shell, #22=needs-gpu, #25=shell, #26=needs-net, #27=shell, #28=needs-gpu, #29=non-python, #30=shell\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/99-appendix__04-tooling-setup.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/99-appendix__04-tooling-setup.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/99-appendix/04-tooling-setup.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/99-appendix/04-tooling-setup.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/99-appendix__04-tooling-setup.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/99-appendix__04-tooling-setup.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/99-appendix/04-tooling-setup.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "07-inference-serving/05-trtllm-tgi-stacks",
    "slug": "07-inference-serving__05-trtllm-tgi-stacks",
    "n_ci": 3,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/07-inference-serving/05-trtllm-tgi-stacks.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/07-inference-serving__05-trtllm-tgi-stacks.py\n\nThis chapter has 3 heuristically CPU-runnable Python blocks to test:\n    - block #1 (line ~79, 41 lines)\n    - block #4 (line ~204, 24 lines)\n    - block #5 (line ~265, 21 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=shell, #2=shell, #3=shell, #6=shell, #7=shell, #8=needs-gpu, #9=non-python\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/07-inference-serving__05-trtllm-tgi-stacks.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/07-inference-serving__05-trtllm-tgi-stacks.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/07-inference-serving/05-trtllm-tgi-stacks.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/07-inference-serving/05-trtllm-tgi-stacks.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/07-inference-serving__05-trtllm-tgi-stacks.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/07-inference-serving__05-trtllm-tgi-stacks.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/07-inference-serving/05-trtllm-tgi-stacks.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "09-rag-retrieval/03-rag-architectures",
    "slug": "09-rag-retrieval__03-rag-architectures",
    "n_ci": 1,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/09-rag-retrieval/03-rag-architectures.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__03-rag-architectures.py\n\nThis chapter has 1 heuristically CPU-runnable Python blocks to test:\n    - block #3 (line ~467, 202 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #1=needs-gpu, #2=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__03-rag-architectures.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/09-rag-retrieval__03-rag-architectures.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/09-rag-retrieval/03-rag-architectures.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/09-rag-retrieval/03-rag-architectures.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/09-rag-retrieval__03-rag-architectures.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/09-rag-retrieval__03-rag-architectures.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/09-rag-retrieval/03-rag-architectures.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  }
]
const VERIFY_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "blocks_tested", "blocks_skipped", "real_bugs_fixed", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "test runs green AND is faithful (no cheating) AND skips are legit"}, "blocks_tested": {"type": "integer"}, "blocks_skipped": {"type": "integer"}, "real_bugs_fixed": {"type": "array", "items": {"type": "string"}, "description": "real bugs in the book's code found and fixed"}, "cheating_found": {"type": "array", "items": {"type": "string"}, "description": "any blanket try/except, fake stub, or removed assert you had to fix"}, "notes": {"type": "string"}}}

const results = await pipeline(
  CH,
  c => agent(c.assemble, { label: `run:${c.chapter}`, phase: 'Assemble+Run', model: 'claude-sonnet-5' })
        .then(() => c),
  c => agent(c.verify, { label: `verify:${c.chapter}`, phase: 'Verify', schema: VERIFY_SCHEMA,
                         model: 'opus', effort: 'high' })
        .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed).map(r => r.chapter)
const bugs = done.flatMap(r => (r.verdict?.real_bugs_fixed || []).map(b => `${r.chapter}: ${b}`))
const cheats = done.flatMap(r => (r.verdict?.cheating_found || []).map(b => `${r.chapter}: ${b}`))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`codetest: ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | real-bugs ${bugs.length}`)
log(`PASSED (append to code_done.txt): ${passed.join(' ')}`)
if (bugs.length) log(`REAL BUGS FIXED: ${JSON.stringify(bugs, null, 1)}`)
if (cheats.length) log(`CHEATING CORRECTED: ${JSON.stringify(cheats)}`)
if (failed.length) log(`FAILED: ${failed.join(' ')}`)
if (died.length) log(`DIED (re-run): ${died.join(' ')}`)
return { passed, failed, died, bugs, cheats }
