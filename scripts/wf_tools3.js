export const meta = {
  name: 'interactive-tools',
  description: 'Build 16 interactive visualizer tools (Sonnet build -> Opus verify)',
  phases: [{ title: 'Build' }, { title: 'Verify' }],
}
const JOBS = [{"slug": "ssm-selective-scan", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: State-space models: the selective scan (Mamba)  (slug: ssm-selective-scan)\nIt teaches / does:\nShow how a state-space model processes a sequence as a linear RECURRENCE instead of quadratic attention. Draw the recurrence h_t = A_bar*h_{t-1} + B_bar*x_t, y_t = C*h_t stepping left-to-right along a token sequence, with the hidden state h (a small vector, ~4 dims) shown as bars that update each step and the output y_t emitted per step. A 'selective' toggle makes B_bar/C/step-size input-DEPENDENT (Mamba) vs constant (S4): show that a selective model can 'gate' \u2014 reset/ignore the state for some tokens (visualize the gate opening/closing). Contrast panel: attention is O(T^2) (an S x S grid lights up) vs SSM is O(T) with an O(1) state at inference (the state size stays fixed as T grows) \u2014 animate both as T increases. Show the parallel-scan equivalence (the recurrence can be computed as an associative scan). Real recurrence math on tiny seeded A,B,C; make the constant-size state vs growing KV-cache the punchline.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/ssm-selective-scan.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-ssm-selective-scan\">` containing: a `<div class=\"vt-title\">State-space models: the selective scan (Mamba)</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-ssm-selective-scan`) and an inline `<script>` (an IIFE; all element ids prefixed `ssm-selective-scan-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:ssm-selective-scan}}` on its own line into content/02-transformer/11-ssm-and-alternatives.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/ssm-selective-scan.html   (slug ssm-selective-scan)\nChapter with the marker: content/02-transformer/11-ssm-and-alternatives.md\n\nRead the tool file (and the chapter section around the {{tool:ssm-selective-scan}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/ssm-selective-scan.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-ssm-selective-scan / ssm-selective-scan- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:ssm-selective-scan}}` marker exists in content/02-transformer/11-ssm-and-alternatives.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "diffusion-lm-denoising", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Diffusion language models: iterative denoising  (slug: diffusion-lm-denoising)\nIt teaches / does:\nContrast autoregressive left-to-right generation with a masked/discrete diffusion LM that generates in parallel by iterative denoising. Show a sequence of [MASK] tokens; on each 'denoise step' the model predicts a distribution for the masked positions and UNMASKS the most-confident ones (confidence-based remasking), so the sentence fills in non-left-to-right over T steps (a slider for number of steps). Side-by-side: AR fills one token per step strictly L->R (T steps = T tokens); diffusion fills many positions per step in a few steps (quality-vs-steps tradeoff). Show the number-of-function-evaluations vs quality knob and the any-order infilling ability (mask a middle span, watch it fill). Deterministic scripted denoising over a toy sentence with seeded per-position confidences. Teaches: non-AR parallel generation, iterative refinement, the steps/quality tradeoff, and infilling.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/diffusion-lm-denoising.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-diffusion-lm-denoising\">` containing: a `<div class=\"vt-title\">Diffusion language models: iterative denoising</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-diffusion-lm-denoising`) and an inline `<script>` (an IIFE; all element ids prefixed `diffusion-lm-denoising-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:diffusion-lm-denoising}}` on its own line into content/02-transformer/12-diffusion-nonAR-lms.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/diffusion-lm-denoising.html   (slug diffusion-lm-denoising)\nChapter with the marker: content/02-transformer/12-diffusion-nonAR-lms.md\n\nRead the tool file (and the chapter section around the {{tool:diffusion-lm-denoising}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/diffusion-lm-denoising.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-diffusion-lm-denoising / diffusion-lm-denoising- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:diffusion-lm-denoising}}` marker exists in content/02-transformer/12-diffusion-nonAR-lms.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "hnsw-ann-search", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Approximate nearest neighbors: HNSW graph search  (slug: hnsw-ann-search)\nIt teaches / does:\nAnimate greedy search over an HNSW (hierarchical navigable small world) graph \u2014 how a vector DB finds nearest neighbors in log time instead of scanning all vectors. Draw ~30 points in 2D with a multi-layer graph (a sparse top layer for long hops, denser bottom layer). A query point appears; animate the search: start at the top-layer entry point, greedily hop to the neighbor closest to the query, descend a layer, repeat, until it converges to the local nearest neighbors at the bottom layer (highlight visited nodes and the current best). Contrast with brute-force (all N distance computations light up) \u2014 show the distance-computation COUNT for each (HNSW ~log N vs N). Sliders: number of points, ef (search beam width) showing the recall-vs-speed tradeoff (small ef = fast but may miss the true NN). Real 2D Euclidean distances + greedy graph traversal; deterministic graph.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/hnsw-ann-search.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-hnsw-ann-search\">` containing: a `<div class=\"vt-title\">Approximate nearest neighbors: HNSW graph search</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-hnsw-ann-search`) and an inline `<script>` (an IIFE; all element ids prefixed `hnsw-ann-search-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:hnsw-ann-search}}` on its own line into content/09-rag-retrieval/02-vector-databases-ann.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/hnsw-ann-search.html   (slug hnsw-ann-search)\nChapter with the marker: content/09-rag-retrieval/02-vector-databases-ann.md\n\nRead the tool file (and the chapter section around the {{tool:hnsw-ann-search}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/hnsw-ann-search.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-hnsw-ann-search / hnsw-ann-search- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:hnsw-ann-search}}` marker exists in content/09-rag-retrieval/02-vector-databases-ann.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "moe-expert-parallel", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Serving MoE: expert parallelism & all-to-all  (slug: moe-expert-parallel)\nIt teaches / does:\nShow how a Mixture-of-Experts layer is served across GPUs with expert parallelism. Draw G GPUs, each holding a subset of the E experts. A batch of tokens on each GPU is routed (top-k gate) to experts that may live on OTHER GPUs \u2014 animate the ALL-TO-ALL dispatch (tokens fly to the GPU holding their chosen expert), the expert FFN compute, then the all-to-all COMBINE (results fly back). Show per-GPU expert-load imbalance and the resulting straggler (one overloaded GPU stalls the collective), and the capacity-factor drop. Contrast active vs total params (only k/E experts compute per token) to explain why MoE decodes fast for its size but needs lots of memory for all experts. Sliders: experts, GPUs, top-k, capacity factor. Real routing + load accounting; the all-to-all is the star.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/moe-expert-parallel.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-moe-expert-parallel\">` containing: a `<div class=\"vt-title\">Serving MoE: expert parallelism & all-to-all</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-moe-expert-parallel`) and an inline `<script>` (an IIFE; all element ids prefixed `moe-expert-parallel-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:moe-expert-parallel}}` on its own line into content/07-inference-serving/13-serving-moe.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/moe-expert-parallel.html   (slug moe-expert-parallel)\nChapter with the marker: content/07-inference-serving/13-serving-moe.md\n\nRead the tool file (and the chapter section around the {{tool:moe-expert-parallel}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/moe-expert-parallel.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-moe-expert-parallel / moe-expert-parallel- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:moe-expert-parallel}}` marker exists in content/07-inference-serving/13-serving-moe.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "constitutional-ai-loop", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Constitutional AI / RLAIF: critique and revise  (slug: constitutional-ai-loop)\nIt teaches / does:\nAnimate the constitutional-AI self-improvement loop that replaces human feedback with AI feedback against a written 'constitution'. Step-through transcript on 'Next step': a red-team prompt -> the model's initial (possibly harmful) response -> a CRITIQUE step where the model checks the response against a sampled constitutional principle (shown) -> a REVISION producing a safer response -> repeat for a couple of rounds. Then the SL-CAI / RLAIF stage: an AI feedback model picks the better of two responses (a preference label generated WITHOUT humans) that trains the reward model / policy. Draw the two phases (supervised critique-revise, then RL from AI feedback) as a pipeline. Deterministic scripted example with a small set of principles the user can switch between. Teaches: principle-guided self-critique, AI feedback replacing human labels, and the two-stage CAI recipe.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/constitutional-ai-loop.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-constitutional-ai-loop\">` containing: a `<div class=\"vt-title\">Constitutional AI / RLAIF: critique and revise</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-constitutional-ai-loop`) and an inline `<script>` (an IIFE; all element ids prefixed `constitutional-ai-loop-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:constitutional-ai-loop}}` on its own line into content/05-posttraining-alignment/11-constitutional-rlaif.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/constitutional-ai-loop.html   (slug constitutional-ai-loop)\nChapter with the marker: content/05-posttraining-alignment/11-constitutional-rlaif.md\n\nRead the tool file (and the chapter section around the {{tool:constitutional-ai-loop}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/constitutional-ai-loop.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-constitutional-ai-loop / constitutional-ai-loop- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:constitutional-ai-loop}}` marker exists in content/05-posttraining-alignment/11-constitutional-rlaif.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "test-time-compute", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Test-time compute: best-of-N, majority vote, search  (slug: test-time-compute)\nIt teaches / does:\nShow how spending more compute at inference raises accuracy. For a toy problem, sample N candidate solutions (N slider, each a seeded chain with a correctness label and a verifier/reward score). Selection method: best-of-N (pick the highest verifier score), majority vote / self-consistency (pick the most common final answer), or weighted vote. Plot accuracy vs N (a rising, saturating curve) for each method, and show a 'verifier quality' slider (a perfect verifier makes best-of-N strong; a noisy one hurts). Add a simple tree-search / beam-over-steps toggle showing budget allocated across depth vs width. Teaches: the inference-compute scaling law, why a verifier/reward model unlocks best-of-N, self-consistency, and generator-vs-verifier. Real sampling + vote/argmax + accuracy math on seeded candidates.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/test-time-compute.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-test-time-compute\">` containing: a `<div class=\"vt-title\">Test-time compute: best-of-N, majority vote, search</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-test-time-compute`) and an inline `<script>` (an IIFE; all element ids prefixed `test-time-compute-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:test-time-compute}}` on its own line into content/05-posttraining-alignment/10-reasoning-test-time-compute.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/test-time-compute.html   (slug test-time-compute)\nChapter with the marker: content/05-posttraining-alignment/10-reasoning-test-time-compute.md\n\nRead the tool file (and the chapter section around the {{tool:test-time-compute}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/test-time-compute.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-test-time-compute / test-time-compute- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:test-time-compute}}` marker exists in content/05-posttraining-alignment/10-reasoning-test-time-compute.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "constrained-decoding-fsm", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Constrained decoding: FSM-guided JSON generation  (slug: constrained-decoding-fsm)\nIt teaches / does:\nShow how grammar/FSM-constrained decoding guarantees valid structured output (e.g. JSON matching a schema). Draw a small finite-state machine for a JSON object (states: expect-key, in-string, expect-colon, expect-value, ...). Step token by token: at each step show the model's raw next-token distribution, then the FSM MASK that zeroes out every token that would be illegal in the current state (highlight allowed vs forbidden tokens), then sampling only from allowed tokens and the FSM state transition. Build up a valid JSON string that always parses. Contrast an 'unconstrained' toggle that can emit invalid JSON. Teaches: logit masking, the FSM/regex/grammar as a constraint, and why structured generation is guaranteed-valid (Outlines/XGrammar-style). Real per-state allowed-token sets + masked renormalized softmax; deterministic.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/constrained-decoding-fsm.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-constrained-decoding-fsm\">` containing: a `<div class=\"vt-title\">Constrained decoding: FSM-guided JSON generation</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-constrained-decoding-fsm`) and an inline `<script>` (an IIFE; all element ids prefixed `constrained-decoding-fsm-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:constrained-decoding-fsm}}` on its own line into content/07-inference-serving/10-structured-generation.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/constrained-decoding-fsm.html   (slug constrained-decoding-fsm)\nChapter with the marker: content/07-inference-serving/10-structured-generation.md\n\nRead the tool file (and the chapter section around the {{tool:constrained-decoding-fsm}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/constrained-decoding-fsm.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-constrained-decoding-fsm / constrained-decoding-fsm- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:constrained-decoding-fsm}}` marker exists in content/07-inference-serving/10-structured-generation.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "multi-agent-orchestration", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Multi-agent orchestration: supervisor and workers  (slug: multi-agent-orchestration)\nIt teaches / does:\nAnimate a supervisor/worker multi-agent system solving a task. A SUPERVISOR agent decomposes a task into subtasks and dispatches them to specialized WORKER agents (e.g. researcher, coder, critic), shown as nodes with message arrows. Step-through: supervisor plans -> sends subtask messages to workers (parallel or sequential toggle) -> workers return results -> supervisor aggregates / re-dispatches if a critic rejects -> final answer. Show the message log growing and a token/context-cost meter (more agents = more total tokens = the coordination cost). Include a topology selector (single agent vs supervisor-workers vs sequential pipeline vs debate) and show the tradeoff: parallelism/specialization vs coordination overhead and error propagation. Deterministic scripted runs. Teaches: task decomposition, agent roles, message passing, orchestration topologies, and the cost of coordination.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/multi-agent-orchestration.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-multi-agent-orchestration\">` containing: a `<div class=\"vt-title\">Multi-agent orchestration: supervisor and workers</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-multi-agent-orchestration`) and an inline `<script>` (an IIFE; all element ids prefixed `multi-agent-orchestration-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:multi-agent-orchestration}}` on its own line into content/08-agents-harness/07-multi-agent-systems.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/multi-agent-orchestration.html   (slug multi-agent-orchestration)\nChapter with the marker: content/08-agents-harness/07-multi-agent-systems.md\n\nRead the tool file (and the chapter section around the {{tool:multi-agent-orchestration}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/multi-agent-orchestration.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-multi-agent-orchestration / multi-agent-orchestration- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:multi-agent-orchestration}}` marker exists in content/08-agents-harness/07-multi-agent-systems.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "ddp-ring-allreduce", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Data-parallel training: ring all-reduce  (slug: ddp-ring-allreduce)\nIt teaches / does:\nAnimate the ring all-reduce that synchronizes gradients across data-parallel GPUs. Draw K GPUs in a ring, each starting with its own gradient vector split into K chunks. Phase 1 (reduce-scatter): over K-1 steps each GPU sends a chunk to its neighbor and adds the received chunk, so after the phase each GPU holds one fully-summed chunk (highlight the moving chunks and the accumulation). Phase 2 (all-gather): another K-1 steps circulate the summed chunks so every GPU ends with the full averaged gradient. Show the per-step communication and the total data moved = 2*(K-1)/K * gradient_size (independent of K per GPU \u2014 the key property that makes it scale). Contrast with a naive all-to-one gather (bottlenecked at the root). Slider: number of GPUs. Real chunk accounting; the bandwidth-optimality is the punchline.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/ddp-ring-allreduce.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-ddp-ring-allreduce\">` containing: a `<div class=\"vt-title\">Data-parallel training: ring all-reduce</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-ddp-ring-allreduce`) and an inline `<script>` (an IIFE; all element ids prefixed `ddp-ring-allreduce-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:ddp-ring-allreduce}}` on its own line into content/03-pretraining/05-distributed-data-parallel.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/ddp-ring-allreduce.html   (slug ddp-ring-allreduce)\nChapter with the marker: content/03-pretraining/05-distributed-data-parallel.md\n\nRead the tool file (and the chapter section around the {{tool:ddp-ring-allreduce}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/ddp-ring-allreduce.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-ddp-ring-allreduce / ddp-ring-allreduce- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:ddp-ring-allreduce}}` marker exists in content/03-pretraining/05-distributed-data-parallel.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "vit-patches", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Vision Transformer: an image becomes tokens  (slug: vit-patches)\nIt teaches / does:\nShow how a Vision Transformer turns an image into a sequence the transformer can process. Take a small grid image (editable / a couple of presets); animate: split into fixed patches (patch-size slider, e.g. 4x4), flatten each patch and linearly project it to a patch embedding (a token), prepend a [CLS] token, add positional embeddings, then feed the patch-token sequence to self-attention \u2014 show the attention from the [CLS] token (or a selected patch) back to all patches as an overlay on the image (which regions it attends to). Show the sequence length = (H/p)*(W/p) + 1 and how patch size trades sequence length (compute) vs spatial detail. Teaches: patchify -> linear projection -> tokens -> attention over image regions, the CLS token, and the patch-size/sequence-length tradeoff. Real patch-count math + a small seeded attention pattern.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/vit-patches.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-vit-patches\">` containing: a `<div class=\"vt-title\">Vision Transformer: an image becomes tokens</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-vit-patches`) and an inline `<script>` (an IIFE; all element ids prefixed `vit-patches-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:vit-patches}}` on its own line into content/10-multimodal-and-arch/01-vision-transformers.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/vit-patches.html   (slug vit-patches)\nChapter with the marker: content/10-multimodal-and-arch/01-vision-transformers.md\n\nRead the tool file (and the chapter section around the {{tool:vit-patches}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/vit-patches.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-vit-patches / vit-patches- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:vit-patches}}` marker exists in content/10-multimodal-and-arch/01-vision-transformers.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "dp-sgd-privacy", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Differential privacy: DP-SGD clip and noise  (slug: dp-sgd-privacy)\nIt teaches / does:\nShow how DP-SGD makes training differentially private. Draw a batch of per-example gradients as arrows of varying length; step 1 CLIP each to a max L2 norm C (long arrows get shortened, bounding any one example's influence); step 2 average and ADD Gaussian noise of scale sigma*C (show the noisy gradient jittering). Sliders: clip norm C, noise multiplier sigma, batch size. Show the resulting privacy budget epsilon (smaller with more noise / bigger batches / fewer steps \u2014 use a simple illustrative accountant) and the privacy-vs-utility tradeoff (more noise = smaller epsilon = more private but noisier updates = worse model). Include a 'memorization' demo: without DP a rare canary example is memorized (extractable); with DP it is not. Teaches: per-example clipping bounds sensitivity, noise gives the guarantee, and the epsilon/utility tradeoff. Real clip+noise math; illustrative epsilon.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/dp-sgd-privacy.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-dp-sgd-privacy\">` containing: a `<div class=\"vt-title\">Differential privacy: DP-SGD clip and noise</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-dp-sgd-privacy`) and an inline `<script>` (an IIFE; all element ids prefixed `dp-sgd-privacy-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:dp-sgd-privacy}}` on its own line into content/13-interp-safety-gov/03-privacy-memorization-dp.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/dp-sgd-privacy.html   (slug dp-sgd-privacy)\nChapter with the marker: content/13-interp-safety-gov/03-privacy-memorization-dp.md\n\nRead the tool file (and the chapter section around the {{tool:dp-sgd-privacy}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/dp-sgd-privacy.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-dp-sgd-privacy / dp-sgd-privacy- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:dp-sgd-privacy}}` marker exists in content/13-interp-safety-gov/03-privacy-memorization-dp.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "mixed-precision-scaling", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Mixed precision: fp16 underflow & loss scaling  (slug: mixed-precision-scaling)\nIt teaches / does:\nShow why fp16 training needs loss scaling and why bf16 mostly does not. Draw the distribution of gradient magnitudes on a log axis with the fp16 representable range overlaid (note the narrow low end where small gradients UNDERFLOW to zero \u2014 highlight them vanishing). Apply a loss-scale factor S (slider): multiply the loss/gradients by S to shift the distribution up into fp16's range, then unscale before the optimizer step; show underflow disappearing, and if S is too large, OVERFLOW/inf appearing (the dynamic-loss-scaling backoff halves S on inf). Compare a bf16 toggle: same exponent range as fp32 so the distribution fits without scaling (at the cost of fewer mantissa bits). Show the fp32 master-weight copy. Teaches: fp16 range problem, static vs dynamic loss scaling, and why bf16 simplified mixed precision. Real bucketed histogram + scale/underflow/overflow logic.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/mixed-precision-scaling.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-mixed-precision-scaling\">` containing: a `<div class=\"vt-title\">Mixed precision: fp16 underflow & loss scaling</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-mixed-precision-scaling`) and an inline `<script>` (an IIFE; all element ids prefixed `mixed-precision-scaling-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:mixed-precision-scaling}}` on its own line into content/03-pretraining/08-mixed-precision-fp8.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/mixed-precision-scaling.html   (slug mixed-precision-scaling)\nChapter with the marker: content/03-pretraining/08-mixed-precision-fp8.md\n\nRead the tool file (and the chapter section around the {{tool:mixed-precision-scaling}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/mixed-precision-scaling.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-mixed-precision-scaling / mixed-precision-scaling- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:mixed-precision-scaling}}` marker exists in content/03-pretraining/08-mixed-precision-fp8.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "gradient-checkpointing", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Gradient checkpointing: trade compute for memory  (slug: gradient-checkpointing)\nIt teaches / does:\nShow the activation-memory vs recompute tradeoff. Draw an N-layer forward pass as a chain; in normal training every layer's activation is STORED for the backward pass (a memory bar grows to O(N)). Toggle checkpointing: only a few 'checkpoint' activations are stored (segment-size slider); the rest are DISCARDED and RE-COMPUTED during backward from the nearest checkpoint (animate the recompute forward within a segment). Show activation memory drop to O(sqrt(N)) (with sqrt-spaced checkpoints) and the extra forward FLOPs (~1 extra forward = +33% compute). Sliders: layers, checkpoint segment size, showing the memory/compute Pareto curve (a dot moving along it). Teaches: why we recompute instead of store, the sqrt(N) memory optimum, and the ~33% compute cost that buys a much bigger model or batch. Real memory (segments stored) + FLOP (recomputed segments) accounting.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/gradient-checkpointing.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-gradient-checkpointing\">` containing: a `<div class=\"vt-title\">Gradient checkpointing: trade compute for memory</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-gradient-checkpointing`) and an inline `<script>` (an IIFE; all element ids prefixed `gradient-checkpointing-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:gradient-checkpointing}}` on its own line into content/04-kernels-efficiency/10-memory-efficient-training.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/gradient-checkpointing.html   (slug gradient-checkpointing)\nChapter with the marker: content/04-kernels-efficiency/10-memory-efficient-training.md\n\nRead the tool file (and the chapter section around the {{tool:gradient-checkpointing}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/gradient-checkpointing.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-gradient-checkpointing / gradient-checkpointing- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:gradient-checkpointing}}` marker exists in content/04-kernels-efficiency/10-memory-efficient-training.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "data-mixing-temperature", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Data mixing: domain weights & temperature sampling  (slug: data-mixing-temperature)\nIt teaches / does:\nShow how a pretraining data mixture is set. Several domains (web, code, math, books, wiki) with their raw corpus SIZES shown as bars. A sampling method: proportional (sample each domain by its size \u2014 big domains dominate), uniform, or temperature-sampled (p_i proportional to size_i^(1/T), T slider: T=1 is proportional, T->inf is uniform \u2014 upsampling small high-quality domains). Show the resulting sampling weights and the expected tokens-per-domain in a training batch, and an 'epochs' readout (small domains get repeated many times when upsampled -> overfitting risk). Optionally a curriculum toggle that shifts weights over training (more code/math late). Teaches: the size-vs-quality mixing problem, temperature sampling (the DoReMi/mixture-weights idea), and repetition/epoching of small domains. Real size^(1/T) normalization + per-domain token/epoch math.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/data-mixing-temperature.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-data-mixing-temperature\">` containing: a `<div class=\"vt-title\">Data mixing: domain weights & temperature sampling</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-data-mixing-temperature`) and an inline `<script>` (an IIFE; all element ids prefixed `data-mixing-temperature-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:data-mixing-temperature}}` on its own line into content/03-pretraining/14-data-mixing-curriculum.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/data-mixing-temperature.html   (slug data-mixing-temperature)\nChapter with the marker: content/03-pretraining/14-data-mixing-curriculum.md\n\nRead the tool file (and the chapter section around the {{tool:data-mixing-temperature}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/data-mixing-temperature.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-data-mixing-temperature / data-mixing-temperature- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:data-mixing-temperature}}` marker exists in content/03-pretraining/14-data-mixing-curriculum.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "distillation-temperature", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Knowledge distillation: soft targets & temperature  (slug: distillation-temperature)\nIt teaches / does:\nShow how a small student learns from a large teacher's SOFT probabilities. For one input, show the teacher's logits -> softmax at temperature T (a slider): at T=1 it's peaked (one class), at high T it's softened, revealing the 'dark knowledge' (relative probabilities of the wrong classes \u2014 e.g. a '7' image is a bit like a '1'). The student is trained to match the softened teacher distribution (KL loss) blended with the true-label cross-entropy (alpha slider). Show both distributions as bars and the KD loss shrinking as the student's bars approach the teacher's; the T^2 gradient-scaling note. Teaches: soft targets carry more information than hard labels, temperature reveals inter-class similarity, and the KD + CE blend. Real softmax-at-T + KL + weighted-loss math on seeded teacher logits.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/distillation-temperature.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-distillation-temperature\">` containing: a `<div class=\"vt-title\">Knowledge distillation: soft targets & temperature</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-distillation-temperature`) and an inline `<script>` (an IIFE; all element ids prefixed `distillation-temperature-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:distillation-temperature}}` on its own line into content/05-posttraining-alignment/12-distillation-compression.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/distillation-temperature.html   (slug distillation-temperature)\nChapter with the marker: content/05-posttraining-alignment/12-distillation-compression.md\n\nRead the tool file (and the chapter section around the {{tool:distillation-temperature}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/distillation-temperature.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-distillation-temperature / distillation-temperature- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:distillation-temperature}}` marker exists in content/05-posttraining-alignment/12-distillation-compression.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "knowledge-editing-rome", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Knowledge editing: a rank-one weight edit (ROME)  (slug: knowledge-editing-rome)\nIt teaches / does:\nShow how a single fact can be edited into a model by a targeted rank-one update to one MLP layer (ROME-style locate-then-edit). Part 1 LOCATE: causal tracing \u2014 corrupt the subject token and restore hidden states layer by layer to find WHICH layer's MLP most restores the fact (a heatmap of restoration effect over layers x positions, peaking at a mid layer / the subject's last token). Part 2 EDIT: treat the MLP as a key-value store; compute a rank-one update dW = (v* - W k) k^T / (k^T k) that maps the subject key k to a new value v* encoding the new fact, changing the model's output from the old object to the new one \u2014 without retraining. Show the old vs new fact completion. Teaches: facts localize to specific MLP layers, MLPs as associative memories, and a rank-one edit rewrites one association. Real rank-one update math on a tiny seeded key/value; illustrative causal-trace heatmap.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/knowledge-editing-rome.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-knowledge-editing-rome\">` containing: a `<div class=\"vt-title\">Knowledge editing: a rank-one weight edit (ROME)</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-knowledge-editing-rome`) and an inline `<script>` (an IIFE; all element ids prefixed `knowledge-editing-rome-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:knowledge-editing-rome}}` on its own line into content/13-interp-safety-gov/02-knowledge-editing-unlearning.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/knowledge-editing-rome.html   (slug knowledge-editing-rome)\nChapter with the marker: content/13-interp-safety-gov/02-knowledge-editing-unlearning.md\n\nRead the tool file (and the chapter section around the {{tool:knowledge-editing-rome}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/knowledge-editing-rome.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-knowledge-editing-rome / knowledge-editing-rome- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:knowledge-editing-rome}}` marker exists in content/13-interp-safety-gov/02-knowledge-editing-unlearning.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}];
const BSCHEMA = {"type": "object", "additionalProperties": false, "required": ["file", "what_it_teaches"], "properties": {"file": {"type": "string"}, "inserted_marker": {"type": "boolean"}, "controls": {"type": "array", "items": {"type": "string"}}, "what_it_teaches": {"type": "string"}}};
const VSCHEMA = {"type": "object", "additionalProperties": false, "required": ["file", "correct", "self_contained", "theme_safe", "notes"], "properties": {"file": {"type": "string"}, "correct": {"type": "boolean"}, "self_contained": {"type": "boolean"}, "theme_safe": {"type": "boolean"}, "issues_fixed": {"type": "integer"}, "notes": {"type": "string"}}};
log('Building ' + JOBS.length + ' interactive tools (build -> Opus verify)…');
const results = await pipeline(
  JOBS,
  function (j) {
    return agent(j.build, { label: 'build:' + j.slug, phase: 'Build', model: j.model, schema: BSCHEMA })
      .then(function (r) { return j; });
  },
  async function (j) {
    if (!j) return null;
    const v = await agent(j.verify, { label: 'verify:' + j.slug, phase: 'Verify', model: 'claude-opus-5', schema: VSCHEMA });
    return { slug: j.slug, verify: v };
  }
);
const done = results.filter(Boolean);
log('Interactive tools: ' + done.length + '/' + JOBS.length + ' built+verified.');
return { total: JOBS.length, done: done.length, results: results };
