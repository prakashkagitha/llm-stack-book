export const meta = {
  name: 'figdraw-b1',
  description: 'Draw + revise foundational figures for 5 chapters',
  phases: [{ title: 'Draw' }, { title: 'Revise' }],
}

const CH = [
  {
    "chapter": "02-transformer/12-diffusion-nonAR-lms",
    "n_new": 4,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/12-diffusion-nonAR-lms.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [animated]  name=dllm-serial-depth-ar-vs-diffusion\n       concept: Why diffusion is faster \u2014 serial depth L (AR, one token per pass) versus N \u226a L (diffusion, whole row per pass), at equal output length.\n       placement: after 'Why Non-Autoregressive? The Latency Argument'\n       brief: Two horizontal lanes sharing a token axis of 8 illustrative positions (small integer, no benchmark numbers). TOP lane 'Autoregressive \u2014 one token per forward pass': animate step-by-step, step 1 emits token 1 (a solid cell appears, fed by a small 'forward pass' box on the left), step 2 emits token 2 conditioned on the growing left context (draw a thin left-to-right context arrow and a small growing 'KV cache' chip), \u2026 8 sequential steps to fill 8 tokens; a 'serial depth' counter climbs 1\u21922\u2192\u20268. BOTTOM lane 'Masked diffusion \u2014 all positions per forward pass': start with all 8 cells as [MASK] (--muted); animate ~3 sweeps (N=3), each sweep is ONE forward pass over the whole row that resolves several masks at once (light up multiple cells per frame, --accent); a 'serial depth' counter reaches 3. Make the diffusion 'forward pass' box visibly fatter/heavier than the AR one and tag it 'compute-heavier per step' so the FLOPs-not-free caveat is pre-empted. Static end-frame must read clearly: AR lane 'serial depth = L = 8 passes', diffusion lane 'serial depth = N = 3 passes, N \u226a L', plus a caption strip 'same output length; fewer DEPENDENT steps \u2192 lower latency. Latency \u221d serial depth.' Counters in --good, AR tokens --ink, masks --muted, resolved diffusion tokens --accent. Everything inside the viewBox; clear top-to-bottom reading order; include the \u21bb replay button per repo convention.\n\n  NEW  [static]  name=dllm-masked-objective\n       concept: The absorbing forward (mask) process and the masked-only, mask-rate-weighted cross-entropy training objective.\n       placement: after 'The reverse (denoising) process and the objective'\n       brief: One short illustrative sentence of 6 tokens (e.g. 'the cat sat on the mat'). Draw the forward corruption as three rows top-to-bottom at increasing time t: row t=0 all 6 tokens clean; row t (middle) with 2 of 6 replaced by [MASK]; row t=1 all 6 are [MASK]. In the left margin annotate the schedule with a small monotone curve dropping from 1 to 0: '\u03b1_t = P(token survives unmasked), \u03b1_0=1, \u03b1_1=0'. From the MIDDLE (partially-masked) row, draw an upward arrow into a box 'bidirectional transformer \u2014 no causal mask' that reads the whole row and outputs predicted clean tokens; show predictions emerging ONLY at the masked positions. Below the middle row, tag each cell: masked cells get a small --accent 'loss here' tag, unmasked cells get a grey 'context \u2014 no loss' tag, making 'only masked positions contribute' unmistakable. Present the objective compactly near the transformer box: L = [1/(1-\u03b1_t)] \u00b7 \u03a3 over masked i of \u2212log p(x0^i | x_t), with the weight boxed and two annotations: 't\u21920: few masks \u2192 weight large, each rare mask counts heavily' and 't\u21921: many masks \u2192 reconstruct from almost nothing.' Add a small caption 'no left-to-right order: any subset recovered from any context.' Theme-safe: clean tokens --ink, masks --muted, loss tags --accent, weight/annotation text --ink-soft. Single static frame, all inside viewBox, clear reading order top(t=0)\u2192bottom(t=1) then up into the model.\n\n  NEW  [long-animation]  name=dllm-confidence-remasking-trajectory\n       concept: Iterative parallel denoising with confidence-based remasking, and why iterating beats filling everything in one step (the conditional-independence trap).\n       placement: after 'Iterative Parallel Denoising: A Tiny Sampler'\n       brief: One sequence of 8 cells, all starting as [MASK] (--muted). Animate ~4 denoising steps. EACH step: (1) the bidirectional pass predicts ALL currently-masked positions in parallel \u2014 render a faint tentative token in each masked cell, each with a small confidence bar or opacity shade; (2) COMMIT only the top-few most confident predictions \u2014 they become solid --accent with a small lock glyph; (3) the remaining low-confidence cells return to [MASK]. On the NEXT step, draw thin bidirectional context arrows from the committed cells into the still-masked cells and show the new tentative predictions arriving with visibly higher confidence (fuller bars) \u2014 this is the re-conditioning 'aha'. A step counter (1..4) and a shrinking masked-count gauge following a roughly linear schedule (8 \u2192 ~5 \u2192 ~2 \u2192 0). Include a compact side panel contrasting ONE-SHOT vs ITERATE: 'one-shot: fill all 8 at once \u2192 each token individually plausible but jointly incoherent' with a tiny crossed-out example of subject/verb disagreement, versus 'iterate: commit confident, re-condition \u2192 coherent.' Static end-frame must read clearly: fully resolved sentence + caption 'each committed token becomes context for the next step; N steps recover the correlations one step throws away \u2014 more steps trade compute for coherence.' Theme-safe: masks --muted, committed tokens --accent, confidence via opacity/height, arrows --ink-soft. All inside viewBox; include the \u21bb replay button.\n\n  NEW  [static]  name=dllm-block-diffusion-structure\n       concept: Semi-autoregressive block diffusion: autoregressive across blocks (block-causal, KV-cached) plus parallel diffusion within each block (bidirectional).\n       placement: after 'Semi-Autoregressive Block Diffusion'\n       brief: A left-to-right chain of 3 blocks, 4 tokens each (small illustrative integers). Blocks 1 and 2 are FINALIZED: solid tokens (--ink/--accent), each with a 'KV cache' chip beneath ('keys/values fixed \u2014 computed once, reused'). Block 3 is the CURRENT block, shown mid-denoise: 2 tokens committed (--accent) and 2 still [MASK] (--muted), with a small badge 'parallel denoise, N_B steps' and a curved bidirectional double-arrow INSIDE the block indicating every in-block position sees every other. Across blocks draw a causal arrow chain block1\u2192block2\u2192block3 labeled 'autoregressive across blocks (previous blocks finalized).' Include a small L\u00d7L attention-mask inset (12\u00d712 for 3 blocks of 4): dense filled squares on the diagonal blocks (bidirectional within block), filled rectangles below the diagonal (a block attends to all earlier blocks), and empty cells above (a block cannot see future blocks) \u2014 filled = --v-fill/--accent, blocked = empty with --v-grid outline; label it 'block-causal mask.' Bottom dial annotation: 'B = 1 \u2192 pure autoregressive \u2003\u2022\u2003 B = L \u2192 pure diffusion.' Two side tags: 'flexible length: keep emitting blocks until [EOS]' and 'cache reuse: only the current block is recomputed.' Theme-safe, single static frame, everything inside viewBox with clear left-to-right reading order and the mask inset clearly separated.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/12-diffusion-nonAR-lms.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/12-diffusion-nonAR-lms.md\nThe chapter's core intuitions: The latency win is a reduction in SERIAL DEPTH, not FLOPs: AR needs L sequential forward passes (one token per pass) while masked diffusion needs only N \u226a L parallel denoising steps that each process the whole sequence at once.; The forward process is absorbing masking (clean \u2192 progressively [MASK] \u2192 all [MASK]) with \u03b1_t = probability a token survives; the reverse-process training loss collapses to a mask-rate-weighted cross-entropy computed ONLY on masked positions \u2014 a continuous-time, order-agnostic generalization of BERT-style masked LM.; A single denoising step predicts all masked tokens CONDITIONALLY INDEPENDENTLY, which is wrong for language; iterative confidence-based remasking (commit the confident few, re-condition, repeat) recovers the inter-token correlations \u2014 more steps trade compute for coherence.; Block diffusion interpolates AR and diffusion: generate blocks left-to-right (block-causal mask, KV-cache reuse) but denoise the tokens WITHIN a block in parallel (bidirectional); block size B is the dial from pure AR (B=1) to pure diffusion (B=L), and enables flexible length.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  dllm-serial-depth-ar-vs-diffusion, dllm-masked-objective, dllm-confidence-remasking-trajectory, dllm-block-diffusion-structure\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "04-kernels-efficiency/05-cuda-essentials",
    "n_new": 3,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/05-cuda-essentials.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=cuda-memory-coalescing\n       concept: How a warp's 32 lanes hitting consecutive vs strided global addresses becomes 1 vs up to 32 memory transactions.\n       placement: after '### Global Memory Coalescing'\n       brief: Two stacked panels, same 32-lane warp in each. TOP panel titled 'Coalesced (stride 1)': draw a warp as a row of small labeled cells lane 0, lane 1, lane 2 ... (show ~6 then an ellipsis then lane 31). Below it a horizontal ribbon of contiguous 4-byte memory words; each lane sends a short vertical arrow to the word directly under it (lane i -> address A + i). Draw a single bracket spanning all 32 words labeled '1 transaction = one 128-byte cache line serves all 32 lanes'. Annotate with the code-level formula 'A[row*N + threadIdx.x]'. BOTTOM panel titled 'Strided (stride 64)': same warp row, but the memory ribbon is much wider; each lane's arrow reaches a word far apart (lane i -> address A + i*64), each landing in its own separate 128-byte cache-line box; show ~4 of these separated line-boxes then an ellipsis and the label '32 separate transactions -> up to 32x bandwidth wasted'. Annotate with 'A[threadIdx.x*N + col]'. Use the book's semantic positive accent for the coalesced panel and the caution/negative accent for the strided panel (do NOT hardcode raw green/red hex; use theme-safe semantic tokens). Only architectural constants appear (32 lanes, 128 bytes, stride 1 vs 64); invent no benchmark numbers. Reading order top (good) then bottom (bad), left to right. Everything must fit the viewBox with the wide strided ribbon scrolling only if unavoidable \u2014 prefer compressing with an ellipsis so it stays inside.\n\n  NEW  [static]  name=cuda-bank-conflict-padding\n       concept: Shared memory has 32 banks (bank = word-offset mod 32); a column of a [32][32] tile collides on one bank, and a +1 padding column staggers rows across all banks.\n       placement: after '### Shared Memory and Bank Conflicts'\n       brief: Two side-by-side grid panels representing a shared-memory tile, each cell tinted by its bank index (bank = address-in-words mod 32) using a theme-safe repeating categorical palette (a 32-hue cycle; reuse the book's categorical set, not hardcoded primaries). For legibility show only ~5 rows (i = 0..4) then a vertical ellipsis, and columns j = 0..31 (label ends, ellipsis in middle). LEFT panel titled 'float tile[32][32] \u2014 no padding': write the per-cell bank formula 'bank = (i*32 + j) mod 32 = j'. Highlight one column j (e.g. j=5) with a bold outline; every cell in that column shows the SAME bank number (5) and the SAME tint. Below, draw the 32 warp lanes all pointing at that column with the caption '32 lanes -> bank 5 -> 32-way conflict, serialized (throughput drops ~32x)'. RIGHT panel titled 'float tile[32][33] \u2014 +1 padding': same grid plus one extra hatched/greyed 'pad' cell at the end of each row labeled 'wasted 4 B/row'. Write 'bank = (i*33 + j) mod 32 = (i + j) mod 32'. Highlight the same column j; now the cells step through DISTINCT bank numbers down the rows (a diagonal rainbow: banks 5,6,7,8,9...), captioned 'lanes spread across all 32 banks -> zero conflict'. Keep all integers small and derived from the formulas shown; invent no benchmark numbers. Clear left(problem)->right(fix) reading order; both panels must fit fully inside the viewBox in both themes with legible bank labels.\n\n  NEW  [long-animation]  name=cuda-tiled-matmul-reuse\n       concept: A block cooperatively stages T x T tiles of A and B into shared memory and marches across the K dimension, reusing each loaded element T times and accumulating into a register \u2014 cutting HBM reads by ~a factor of T.\n       placement: after '### Tiled Matmul (SGEMM Quality)'\n       brief: Layout: matrix A (M x K) drawn as a wide horizontal strip on the upper left, matrix B (K x N) as a tall vertical strip on the upper right, output C (M x N) lower center with ONE highlighted T x T output tile (the tile this block computes) \u2014 use a small illustrative tile size T = 4 for legibility, label it 'tile size T'. Between them, two small T x T shared-memory buffers labeled 'As (shared)' and 'Bs (shared)'. Use theme-safe distinct accents for A, B, and C (semantic categorical tokens, not raw hex). ANIMATION marches over K-tiles t = 0, 1, 2, ...: STEP 1 (load) highlight tile t: the T x T sub-block of A's row-strip and B's col-strip that align with the output tile; draw arrows from these GLOBAL/HBM sub-blocks into As and Bs with the caption 'cooperative load: each thread loads 1 element from HBM (once)'. STEP 2 flash a '__syncthreads()' barrier bar. STEP 3 (compute + reuse) show one element of As fanning OUT across a row of threads and one element of Bs fanning DOWN a column, with the emphasis label 'each loaded value reused T times from shared memory (fast), not re-read from HBM'; a small register box for the output tile shows 'acc += As*Bs' ticking up. STEP 4 flash a second '__syncthreads()'. STEP 5 slide the tile highlight to t+1 along K and repeat, the C-tile shading in as the accumulator grows. FINAL frame: C tile fully shaded, caption 'naive: each element re-read ~N times from HBM  ->  tiled: loaded once per tile pass  ->  ~T x fewer global reads (compute-bound).' STATIC-FRAME REQUIREMENT: the first frame alone must be self-explanatory \u2014 show all three matrices, both shared buffers with the tile-t load arrows, the reuse fan-out, the accumulator, and a dashed 'next tile ->' arrow along K with small step dots to imply the march. Keep M, N, K, T symbolic/small; invent NO benchmark numbers (the ~T x and 'N times' stay structural/symbolic). Must fit the viewBox in both themes with no overlapping labels.\n\n  REFINE  existing figures/cuda-grid-block-thread-hierarchy.html\n       issue: Content and the logical->physical mapping are clear and correct in both themes, but the bottom-left annotation 'cross-block comm via global mem only' sits flush against the left viewBox edge (near-clipped in both light and dark). Otherwise strong.\n       change: Nudge the bottom-left annotation inward (add left padding inside the viewBox) so it is fully contained; optionally right-align it to start under the Grid box rather than the page edge. No other changes needed.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/04-kernels-efficiency/05-cuda-essentials.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/04-kernels-efficiency/05-cuda-essentials.md\nThe chapter's core intuitions: The logical grid -> block -> thread hierarchy maps onto physical SMs; blocks own an SM, warps of 32 threads run in lockstep (SIMT), and the SM hides memory latency by switching to a ready warp when one stalls.; The memory hierarchy is a bandwidth/latency pyramid: registers and shared memory are ~10-30x faster than HBM, so the whole game is maximizing reuse on-chip to avoid HBM round-trips.; Global memory coalescing: a warp reading consecutive addresses collapses into ONE 128-byte transaction, while a strided pattern explodes into up to 32 separate transactions (a ~32x bandwidth penalty).; Shared-memory bank conflicts: bank = address mod 32, so a whole column of a [32][32] tile lands in one bank -> 32-way serialized access; a +1 padding column staggers rows diagonally across all 32 banks and removes the conflict.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  cuda-memory-coalescing, cuda-bank-conflict-padding, cuda-tiled-matmul-reuse, cuda-grid-block-thread-hierarchy\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "09-rag-retrieval/01-embeddings-representation",
    "n_new": 3,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/09-rag-retrieval/01-embeddings-representation.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=emb-infonce-similarity-matrix\n       concept: InfoNCE as row-wise classification over a B x B batch similarity matrix: diagonal = positives, off-diagonal = in-batch negatives, softmax sharpened by temperature.\n       placement: after '### InfoNCE Derivation'\n       brief: Center the figure on a small square grid, e.g. 4x4 (B=4), labeled rows q1..q4 (queries) down the left and columns d1..d4 (documents) across the top. Each cell holds a similarity value sim = e_q . e_d; fill the grid with small illustrative decimals in [0,1] ONLY (invent no benchmark numbers) -- e.g. diagonal cells near 0.9 and off-diagonal near 0.1-0.3. Visually distinguish the diagonal cells (the true query-positive pairs) with an accent outline/fill and label them 'positive pair (target)'; render the off-diagonal cells in a muted tint labeled once as 'in-batch negatives (free)'. To the RIGHT of the matrix, expand ONE row (say q2) into a horizontal softmax strip: show the four cell values -> divide by tau (temperature) -> exp -> normalized probabilities, with the diagonal probability towering over the rest; annotate 'cross-entropy pushes P(correct doc) -> 1'. Add a compact temperature callout: a small paired sketch 'small tau = sharp / spiky softmax' vs 'large tau = soft / flat softmax' (two tiny bar clusters, no numbers needed beyond illustrative). Include one short caption tying it together: 'Row i is a classification over B documents; the answer is always the diagonal.' Optionally hint symmetry with a faint note that the loss is also applied column-wise (d->q). Everything must fit the viewBox with no overlaps; keep the matrix the dominant element and the softmax strip and temperature callout clearly secondary. Theme-safe: no hardcoded primary colors, use accent vs muted roles that work in light and dark.\n\n  NEW  [static]  name=emb-contrastive-space-geometry\n       concept: What the contrastive loss DOES to the embedding space: pull query->positive, push away negatives, turning an anisotropic cone into an isotropic sphere; random vs hard negatives.\n       placement: after '## Hard Negatives: Going Beyond In-Batch Sampling'\n       brief: Two side-by-side circular panels representing the unit hypersphere (draw as a 2D circle with points on/near it). LEFT panel 'Before contrastive fine-tuning (anisotropic)': all embedding dots crammed into a narrow wedge/cone of the circle, so even unrelated sentences have high cosine similarity -- annotate 'everything looks similar; cosine uninformative'. RIGHT panel 'After contrastive fine-tuning (isotropic)': the same dots spread roughly uniformly around the circle. In the RIGHT panel, single out one query point q with: (a) its positive d+ pulled TIGHT next to it (inward arrow labeled 'pull positive together'), (b) a HARD negative d- placed deceptively close to q with an outward arrow labeled 'push hard negative away -- large gradient', and (c) a RANDOM negative already far across the circle with a faint/short arrow labeled 'random negative -- already far, little signal'. Use an accent hue for q and its positive, a contrasting muted hue for negatives. Add a small legend distinguishing query / positive / hard negative / random negative by marker. Keep only a handful of dots per panel so it stays legible; no numbers required. The static frame must fully convey the transformation (a left->right arrow between panels labeled 'contrastive training' helps). Theme-safe roles only; ensure dots and arrows are legible on both light and dark backgrounds.\n\n  NEW  [static]  name=emb-matryoshka-nested-dims\n       concept: Matryoshka Representation Learning: one embedding vector whose growing prefixes (32, 64, 128, 256, full d) are each independently usable and re-normalizable, trained by summing InfoNCE losses over all prefixes.\n       placement: after '### How MRL Training Works'\n       brief: Show a single long horizontal embedding vector drawn as a row of cells, with vertical dividers marking prefix boundaries at illustrative granularities 32 | 64 | 128 | 256 | ... | d (small integers only). Above the vector, draw nested brackets or concentric 'Russian doll' bands that each span a prefix: the innermost band covers [0:32] labeled 'usable embedding', the next [0:64], then [0:128], [0:256], and the outermost the full d -- visually communicating that each larger prefix CONTAINS the smaller one (nesting). Annotate the leftmost region 'most discriminative signal packed early' and the rightmost 'later dims refine detail'. On the TRAINING side (left or below): show that InfoNCE is computed at EACH prefix (a small stack of loss terms L_32, L_64, ..., L_d) that sum into one total loss, with arrows/gradients flowing back into the SAME shared vector -- caption 'one model, many dimensions, trained jointly'. On the INFERENCE side (right or below): show a scissors/truncation glyph cutting at, say, dim 128, followed by 're-normalize (L2)' producing a shorter usable vector -- caption 'truncate at query time to trade quality for cost/latency/storage'. Keep the nested bands clearly distinguishable (graduated tints of one accent, legible in both themes) and avoid clutter -- 4-5 granularities max. Fit entirely in the viewBox; the nested-prefix vector is the hero element.\n\n  REFINE  existing figures/emb-biencoder-concept.html\n       issue: Correct and clear in both themes, but the single most important intuition of the whole architecture -- that document embeddings are precomputed offline and only the query is encoded at query time -- is relegated to a dim one-line caption at the bottom. The figure draws both towers identically, visually implying they run at the same time, which is exactly the misconception to dispel.\n       change: Encode the offline/online asymmetry visually: tag the document tower with an 'offline / precomputed, stored in ANN index' band and the query tower with an 'online / at query time' band (use theme-neutral tints, e.g. a muted vs an accent hue). Optionally show the document embedding flowing into a small stacked 'index' glyph rather than directly to the score, so the reader sees that d-side work happens once, ahead of time. Keep the shared-weights dashed link and the dot-product score. Drop the literal square brackets around 'Transformer' so it doesn't read as an unfilled placeholder.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/09-rag-retrieval/01-embeddings-representation.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/09-rag-retrieval/01-embeddings-representation.md\nThe chapter's core intuitions: Dense retrieval beats keyword matching because it maps meaning to geometry: 'car' and 'automobile' land close in vector space even with zero word overlap, whereas sparse/BM25 vectors are near-orthogonal.; The bi-encoder's power is INDEPENDENCE: query and document are encoded separately, so every document vector is precomputed offline and only the query is embedded at query time (cross-encoders can't do this).; InfoNCE turns a batch into a classification problem: a B x B similarity matrix whose diagonal are the true (query, positive) pairs and whose off-diagonal cells are free in-batch negatives; a row-wise softmax (sharpened by temperature) must pick the diagonal.; Contrastive training reshapes the space: it pulls each query toward its positive and pushes negatives away, converting an anisotropic cone of embeddings into a spread-out isotropic sphere; HARD negatives (superficially similar) provide far more signal than random ones.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  emb-infonce-similarity-matrix, emb-contrastive-space-geometry, emb-matryoshka-nested-dims, emb-biencoder-concept\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "01-foundations/01-linear-algebra",
    "n_new": 3,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/01-foundations/01-linear-algebra.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=matmul-three-views\n       concept: One product C = AB read three ways: dot products, column combinations, and a sum of rank-1 outer products.\n       placement: after 'Matrix Multiplication: The Workhorse Operation -> after the 'Three equivalent views' subsection'\n       brief: A wide static infographic (viewBox ~ 820x360) with three clearly separated panels sharing ONE tiny running example: A is 3x2, B is 2x4, so C = AB is 3x4. Use small integer-free grids (just cells, no invented numbers) rendered as v-fill rectangles with v-grid interior lines; label matrices with their letter (v-accent) and dims (e.g. '3x2', '2x4', '3x4') in muted mono. Panel titles in bold serif/sans. PANEL 1 'View 1 - Dot products': draw A, B, and C side by side; highlight ONE row of A (row i, accent stroke) and ONE column of B (col j, accent stroke), draw two faint guide lines converging onto a single highlighted cell C_ij; caption underneath: 'C_ij = row_i(A) . col_j(B)'. PANEL 2 'View 2 - Column combinations': draw B with its j-th column highlighted, an arrow 'A x' to C with its j-th column highlighted; small annotation showing C's j-th column as a weighted stack/blend of A's TWO columns (two faint tinted column swatches with '+' between them), caption: 'each output column = a blend of A\u2019s columns'. PANEL 3 'View 3 - Outer products (rank-1 sum)': show C = (col_1 of A)(row_1 of B) + (col_2 of A)(row_2 of B); render each term as a thin tall column swatch times a thin wide row swatch forming a full 3x4 tile, with a '+' between the two rank-1 tiles and '=' to C; label each term 'rank-1' in muted text. Add a short bridging annotation beneath panel 3 (accent, small): 'truncate this sum -> low-rank approximation (see SVD)'. Theme-safe: all fills/strokes via v-* classes or var(); no hardcoded primary colors; everything inside the viewBox with generous gutters so nothing clips; clear left-to-right, top reading order 1->2->3.\n\n  NEW  [static]  name=svd-geometry-and-lowrank\n       concept: SVD as rotate-scale-rotate, plus the singular-value spectrum cliff and Eckart-Young top-r truncation.\n       placement: after 'Eigendecomposition and the Singular Value Decomposition -> at the 'Best low-rank approximation: the Eckart-Young theorem' subsection'\n       brief: A static two-panel infographic (viewBox ~ 820x360), panels visually separated by a thin v-grid divider. LEFT PANEL 'SVD is rotate - scale - rotate': show the geometric action on a 2-D shape in four small stages left-to-right connected by arrows. Stage 0: a unit circle with two orthonormal axis arrows. Stage 1: apply V^T -> the SAME circle, axes rotated (label arrow 'V\u1d40 rotate'). Stage 2: apply Sigma -> the circle becomes an ELLIPSE stretched along the axes, long semi-axis labeled 'sigma_1', short labeled 'sigma_2' (label arrow 'Sigma scale'). Stage 3: apply U -> the ellipse rotated to its final orientation (label arrow 'U rotate'). Small caption under the panel: 'U, V preserve lengths & angles; ALL stretching lives in Sigma.' Keep the circle/ellipse small and clean; use v-accent for the ellipse outline and v-muted for axis arrows. RIGHT PANEL 'Best rank-r approximation (Eckart-Young)': (a) a compact singular-value BAR CHART: ~10 vertical bars, the first 4-5 tall and clearly decreasing, then a sharp CLIFF to a long flat 'noise floor' of tiny bars; a dashed vertical 'keep top-r' cut line after the tall bars, region left of the cut lightly tinted v-fill labeled 'signal', region right labeled 'noise'; axis label 'sigma_i' with bars unlabeled by number (illustrative only, invent no values). (b) beneath the chart, the truncated sum shown iconically: 'A_r = sigma_1 u_1 v_1\u1d40 + sigma_2 u_2 v_2\u1d40 + ...' as a few shrinking rank-1 tiles, with faded ghost tiles after the cut. (c) small accent annotation: 'error = sqrt( sum_{i>r} sigma_i^2 )' and a one-line bridge 'fast-decaying spectrum => LoRA works'. Theme-safe v-* classes / var() only; no invented numbers on axes; ensure the geometry stages and the bar chart do not overlap and all labels sit fully inside the viewBox.\n\n  NEW  [static]  name=linear-layer-backprop-transpose\n       concept: Forward Y = XW; backward reuses the transposes: dL/dW = X^T G and dL/dX = G W^T.\n       placement: after 'Matrix Calculus for Backpropagation -> at the 'Gradient of a linear layer' subsection'\n       brief: A static infographic (viewBox ~ 820x340) with two horizontal lanes and a mirror symmetry between them. TOP LANE 'Forward' (arrows flow LEFT -> RIGHT): three tiles X (labeled 'B x D_in'), an operator 'x' , W ('D_in x D_out'), then '=' , then Y ('B x D_out'). Tiles are v-fill rects with v-accent letters and muted dim labels; a bold arrow along the top labeled 'forward: Y = X W'. BOTTOM LANE 'Backward' (arrows flow RIGHT -> LEFT): the upstream gradient G ('B x D_out', same shape as Y, drawn directly beneath Y and tinted accent) ENTERS from the right and FORKS into two products drawn as small matmul groupings: (1) 'X\u1d40 . G' producing dL/dW ('D_in x D_out', drawn beneath W, same footprint as W) and (2) 'G . W\u1d40' producing dL/dX ('B x D_in', drawn beneath X, same footprint as X). Draw faint vertical alignment guides so the reader sees dL/dW sits under W and dL/dX sits under X (same shapes). Show the transpose visually: render X\u1d40 and W\u1d40 as the forward tiles flipped/mirrored with a small '\u1d40' badge, to make 'the backward pass reuses the transposed operand' literal. Central caption banner between the lanes (accent): 'backward = multiply by the transpose'. Small shape-check note: 'shapes must match the thing you\u2019re differentiating'. Theme-safe v-* classes only; ensure the two lanes are clearly separated and the fork arrows do not cross-clutter; everything inside the viewBox.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/01-foundations/01-linear-algebra.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/01-foundations/01-linear-algebra.md\nThe chapter's core intuitions: Matrix multiplication is one computation with three equivalent readings; the outer-product reading expresses C = AB as a sum of rank-1 matrices, which is the conceptual bridge from matmul to low-rank structure.; The SVD factors ANY matrix into rotate-scale-rotate (U, Sigma, V^T): the orthogonal factors U and V preserve lengths and angles, and ALL the stretching lives in Sigma.; A fast-decaying singular-value spectrum means keeping only the top-r rank-1 terms is the BEST rank-r approximation (Eckart-Young), and it captures almost all the signal while the tail is noise \u2014 this is the theoretical heart of the chapter and directly motivates LoRA.; Backprop through a linear map is the transpose rule: dL/dW = X^T G and dL/dX = G W^T. The forward matmul's transposes reappear in the backward pass; this is the single most important identity in deep learning.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  matmul-three-views, svd-geometry-and-lowrank, linear-layer-backprop-transpose\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "01-foundations/03-calculus-optimization",
    "n_new": 3,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/01-foundations/03-calculus-optimization.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=gradient-contour-steepest-descent\n       concept: Gradient geometry on a loss contour map: grad points uphill perpendicular to contours, -grad descends fastest, and the directional derivative is the cosine projection.\n       placement: after 'The Gradient: Steepest Ascent in Parameter Space'\n       brief: A 2D parameter plane, theta_1 horizontal and theta_2 vertical, filled with a few nested smooth elliptical contour lines of a loss (concentric, drawn thin in v-grid), with the minimum at the center marked theta* by a small dot. Label the contours qualitatively as increasing loss outward (e.g. an outer contour tagged 'higher loss', an inner one 'lower loss') -- no numeric loss values. Pick a point P sitting on one of the mid contours. From P draw a bold arrow in v-accent pointing radially OUTWARD, exactly perpendicular to the tangent of the contour at P, labeled 'grad f  (steepest ASCENT)'. From P draw the opposite arrow pointing INWARD toward theta*, labeled '-grad f  (gradient-descent step)'. Also from P draw a second, thinner arrow u at an angle phi away from grad f (say ~50 degrees), labeled 'arbitrary direction u'; drop a dashed perpendicular from the tip of a unit-length u onto the grad-f line to show the projection, annotated 'D_u f = ||grad f|| cos(phi)'. Add a small boxed callout near P: 'gradient is perpendicular to the contour lines'. A short caption strip: 'descent is fastest when you move straight down the gradient (phi = 180 degrees)'. Keep all four labels non-overlapping with clear leader lines. Theme-safe classes only (v-grid contours, v-accent arrows, v-stroke axes, v-muted secondary text); works in light and dark. Everything inside the viewBox.\n\n  NEW  [static]  name=optimizer-paths-conditioning\n       concept: GD vs Momentum vs Adam trajectories on an ill-conditioned (elongated) quadratic bowl, showing zigzag, damped oscillation, and preconditioned near-straight descent.\n       placement: after 'The Condition Number'\n       brief: Main panel: an anisotropic bowl drawn as a set of nested ELONGATED elliptical contours -- stretched wide along the horizontal axis and narrow (high curvature) along the vertical axis -- representing the chapter's own example L = theta_1^2 + 10*theta_2^2 (condition number kappa = 10). Mark the minimum at the center as theta*. From a single common start point near the upper edge (off to one side so the long axis is traversed), draw three trajectories as polylines of discrete steps ending at theta*: (1) GD in a neutral v-muted color -- a pronounced ZIGZAG that bounces back and forth across the narrow vertical axis while inching slowly along the horizontal axis; label 'GD: overshoots the steep axis, crawls the shallow axis (~kappa steps)'. (2) Momentum in a secondary accent -- a damped, smoother oscillation that overshoots once or twice then settles, clearly fewer bounces; label 'Momentum: velocity cancels the zigzag (~sqrt(kappa) steps)'. (3) Adam in v-accent -- a nearly STRAIGHT diagonal path to the minimum; label 'Adam: rescales each axis -> near-isotropic -> near-straight'. Add a small inset (bottom corner) with two little shapes side by side: an elongated ellipse labeled 'kappa = L/mu large' and an arrow to a near-circle labeled 'after preconditioning: kappa ~ 1', captioning why Adam's path is straight. Include a compact three-row legend keyed by color. Annotate a small note: 'step counts are illustrative; kappa here = 10 = ratio of the two curvatures'. No invented benchmark numbers. Theme-safe classes, legible in both themes, all trajectories and labels non-overlapping and inside the viewBox.\n\n  NEW  [static]  name=loss-landscape-convex-vs-nonconvex\n       concept: Side-by-side convex bowl vs non-convex neural terrain, marking global min, local min, a saddle with up/down curvature, and a flat plateau.\n       placement: after 'What Saves Us'\n       brief: Two panels side by side, both with a vertical 'Loss' axis and horizontal 'theta (parameter)' axis (single-parameter cross-section framing). LEFT panel titled 'Convex': one smooth U-shaped bowl, a single dot at the bottom labeled 'global min theta*', and a short note 'every local min IS the global min; GD with eta <= 1/L is guaranteed to converge'. RIGHT panel titled 'Non-convex (neural networks)': a wavy terrain curve with several features clearly marked -- a shallow basin dot labeled 'local min', a deeper basin dot labeled 'global min', and on a ridge between two basins a dot labeled 'saddle' with two tiny curvature arrows at it (one small arc curving UP across the ridge, one curving DOWN along it) and the note 'grad L = 0, curves up one way, down another'. Include one broad, gently-sloping SHADED stretch labeled 'plateau: grad L ~ 0 (dead ReLU / saturation)'. Below the right panel a one-line cause strip: 'permutation symmetry (many equal valleys)  .  saddles dominate in high dimension  .  flat plateaus'. Use v-accent for the marked min/saddle dots, v-grid for axes and gridlines, v-muted for secondary text, v-fill for the shaded plateau region. Keep it uncluttered; this must read cleanly as a static frame in both light and dark themes, everything within the viewBox.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/01-foundations/03-calculus-optimization.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/01-foundations/03-calculus-optimization.md\nThe chapter's core intuitions: The gradient is perpendicular to the loss contours and points uphill; -gradient is the steepest-descent direction, and the directional derivative along any other direction is just the projection ||grad|| cos(phi) onto it.; Curvature (the Hessian's eigenvalues) tells you the shape of the local landscape: all-positive = bowl/minimum, mixed sign = saddle, near-zero = flat plateau.; The condition number kappa = L/mu is the ratio of steepest-to-shallowest curvature; large kappa makes plain GD zigzag across the steep axis and crawl along the shallow one. Momentum damps the zigzag (~sqrt(kappa) steps) and Adam preconditions each axis so the elongated bowl looks nearly circular (near-straight path).; Neural loss surfaces are non-convex: permutation symmetry creates many equivalent valleys, and in high dimension almost every critical point is a saddle, not a local minimum.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  gradient-contour-steepest-descent, optimizer-paths-conditioning, loss-landscape-convex-vs-nonconvex\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  }
]
const DRAW_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "figures_done", "figures_skipped"], "properties": {"chapter": {"type": "string"}, "figures_done": {"type": "array", "items": {"type": "string"}}, "figures_skipped": {"type": "array", "items": {"type": "string"}, "description": "names you could NOT complete, with why appended"}, "notes": {"type": "string"}}}
const REVISE_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "figures", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "true iff every planned figure is present, correct, theme-safe, unclipped and builds intuition"}, "figures": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["name", "verdict"], "properties": {"name": {"type": "string"}, "verdict": {"type": "string", "enum": ["good", "fixed", "still-broken", "missing"]}, "issue": {"type": "string"}}}}, "notes": {"type": "string"}}}

// Draw on Sonnet 5, revise on Opus (user routing 2026-07-20).
const results = await pipeline(
  CH,
  c => agent(c.draw, { label: `draw:${c.chapter}`, phase: 'Draw', schema: DRAW_SCHEMA,
                       model: 'claude-sonnet-5' }).then(d => ({ c, d })),
  ({ c }) => agent(c.revise, { label: `revise:${c.chapter}`, phase: 'Revise', schema: REVISE_SCHEMA,
                                 model: 'opus', effort: 'high' })
                .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed)
                   .map(r => ({ chapter: r.chapter, figs: r.verdict?.figures?.filter(f => f.verdict === 'still-broken' || f.verdict === 'missing') }))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`figures: drawn+revised ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | died ${died.length}`)
log(`PASSED (append to figplan_done.txt): ${passed.join(' ')}`)
if (failed.length) log(`FAILED: ${JSON.stringify(failed)}`)
if (died.length) log(`DIED (re-run to resume): ${died.join(' ')}`)
return { passed, failed, died }
