export const meta = {
  name: 'interactive-tools-verify',
  description: 'Opus-5 verify 7 existing interactive tools (verify-only)',
  phases: [{ title: 'Verify' }],
}
const JOBS = [{"slug": "constitutional-ai-loop", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Constitutional AI / RLAIF: critique and revise  (slug: constitutional-ai-loop)\nIt teaches / does:\nAnimate the constitutional-AI self-improvement loop that replaces human feedback with AI feedback against a written 'constitution'. Step-through transcript on 'Next step': a red-team prompt -> the model's initial (possibly harmful) response -> a CRITIQUE step where the model checks the response against a sampled constitutional principle (shown) -> a REVISION producing a safer response -> repeat for a couple of rounds. Then the SL-CAI / RLAIF stage: an AI feedback model picks the better of two responses (a preference label generated WITHOUT humans) that trains the reward model / policy. Draw the two phases (supervised critique-revise, then RL from AI feedback) as a pipeline. Deterministic scripted example with a small set of principles the user can switch between. Teaches: principle-guided self-critique, AI feedback replacing human labels, and the two-stage CAI recipe.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/constitutional-ai-loop.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-constitutional-ai-loop\">` containing: a `<div class=\"vt-title\">Constitutional AI / RLAIF: critique and revise</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-constitutional-ai-loop`) and an inline `<script>` (an IIFE; all element ids prefixed `constitutional-ai-loop-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:constitutional-ai-loop}}` on its own line into content/05-posttraining-alignment/11-constitutional-rlaif.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/constitutional-ai-loop.html   (slug constitutional-ai-loop)\nChapter with the marker: content/05-posttraining-alignment/11-constitutional-rlaif.md\n\nRead the tool file (and the chapter section around the {{tool:constitutional-ai-loop}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/constitutional-ai-loop.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-constitutional-ai-loop / constitutional-ai-loop- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:constitutional-ai-loop}}` marker exists in content/05-posttraining-alignment/11-constitutional-rlaif.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "vit-patches", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Vision Transformer: an image becomes tokens  (slug: vit-patches)\nIt teaches / does:\nShow how a Vision Transformer turns an image into a sequence the transformer can process. Take a small grid image (editable / a couple of presets); animate: split into fixed patches (patch-size slider, e.g. 4x4), flatten each patch and linearly project it to a patch embedding (a token), prepend a [CLS] token, add positional embeddings, then feed the patch-token sequence to self-attention \u2014 show the attention from the [CLS] token (or a selected patch) back to all patches as an overlay on the image (which regions it attends to). Show the sequence length = (H/p)*(W/p) + 1 and how patch size trades sequence length (compute) vs spatial detail. Teaches: patchify -> linear projection -> tokens -> attention over image regions, the CLS token, and the patch-size/sequence-length tradeoff. Real patch-count math + a small seeded attention pattern.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/vit-patches.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-vit-patches\">` containing: a `<div class=\"vt-title\">Vision Transformer: an image becomes tokens</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-vit-patches`) and an inline `<script>` (an IIFE; all element ids prefixed `vit-patches-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:vit-patches}}` on its own line into content/10-multimodal-and-arch/01-vision-transformers.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/vit-patches.html   (slug vit-patches)\nChapter with the marker: content/10-multimodal-and-arch/01-vision-transformers.md\n\nRead the tool file (and the chapter section around the {{tool:vit-patches}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/vit-patches.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-vit-patches / vit-patches- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:vit-patches}}` marker exists in content/10-multimodal-and-arch/01-vision-transformers.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "dp-sgd-privacy", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Differential privacy: DP-SGD clip and noise  (slug: dp-sgd-privacy)\nIt teaches / does:\nShow how DP-SGD makes training differentially private. Draw a batch of per-example gradients as arrows of varying length; step 1 CLIP each to a max L2 norm C (long arrows get shortened, bounding any one example's influence); step 2 average and ADD Gaussian noise of scale sigma*C (show the noisy gradient jittering). Sliders: clip norm C, noise multiplier sigma, batch size. Show the resulting privacy budget epsilon (smaller with more noise / bigger batches / fewer steps \u2014 use a simple illustrative accountant) and the privacy-vs-utility tradeoff (more noise = smaller epsilon = more private but noisier updates = worse model). Include a 'memorization' demo: without DP a rare canary example is memorized (extractable); with DP it is not. Teaches: per-example clipping bounds sensitivity, noise gives the guarantee, and the epsilon/utility tradeoff. Real clip+noise math; illustrative epsilon.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/dp-sgd-privacy.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-dp-sgd-privacy\">` containing: a `<div class=\"vt-title\">Differential privacy: DP-SGD clip and noise</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-dp-sgd-privacy`) and an inline `<script>` (an IIFE; all element ids prefixed `dp-sgd-privacy-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:dp-sgd-privacy}}` on its own line into content/13-interp-safety-gov/03-privacy-memorization-dp.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/dp-sgd-privacy.html   (slug dp-sgd-privacy)\nChapter with the marker: content/13-interp-safety-gov/03-privacy-memorization-dp.md\n\nRead the tool file (and the chapter section around the {{tool:dp-sgd-privacy}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/dp-sgd-privacy.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-dp-sgd-privacy / dp-sgd-privacy- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:dp-sgd-privacy}}` marker exists in content/13-interp-safety-gov/03-privacy-memorization-dp.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "mixed-precision-scaling", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Mixed precision: fp16 underflow & loss scaling  (slug: mixed-precision-scaling)\nIt teaches / does:\nShow why fp16 training needs loss scaling and why bf16 mostly does not. Draw the distribution of gradient magnitudes on a log axis with the fp16 representable range overlaid (note the narrow low end where small gradients UNDERFLOW to zero \u2014 highlight them vanishing). Apply a loss-scale factor S (slider): multiply the loss/gradients by S to shift the distribution up into fp16's range, then unscale before the optimizer step; show underflow disappearing, and if S is too large, OVERFLOW/inf appearing (the dynamic-loss-scaling backoff halves S on inf). Compare a bf16 toggle: same exponent range as fp32 so the distribution fits without scaling (at the cost of fewer mantissa bits). Show the fp32 master-weight copy. Teaches: fp16 range problem, static vs dynamic loss scaling, and why bf16 simplified mixed precision. Real bucketed histogram + scale/underflow/overflow logic.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/mixed-precision-scaling.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-mixed-precision-scaling\">` containing: a `<div class=\"vt-title\">Mixed precision: fp16 underflow & loss scaling</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-mixed-precision-scaling`) and an inline `<script>` (an IIFE; all element ids prefixed `mixed-precision-scaling-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:mixed-precision-scaling}}` on its own line into content/03-pretraining/08-mixed-precision-fp8.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/mixed-precision-scaling.html   (slug mixed-precision-scaling)\nChapter with the marker: content/03-pretraining/08-mixed-precision-fp8.md\n\nRead the tool file (and the chapter section around the {{tool:mixed-precision-scaling}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/mixed-precision-scaling.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-mixed-precision-scaling / mixed-precision-scaling- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:mixed-precision-scaling}}` marker exists in content/03-pretraining/08-mixed-precision-fp8.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "gradient-checkpointing", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Gradient checkpointing: trade compute for memory  (slug: gradient-checkpointing)\nIt teaches / does:\nShow the activation-memory vs recompute tradeoff. Draw an N-layer forward pass as a chain; in normal training every layer's activation is STORED for the backward pass (a memory bar grows to O(N)). Toggle checkpointing: only a few 'checkpoint' activations are stored (segment-size slider); the rest are DISCARDED and RE-COMPUTED during backward from the nearest checkpoint (animate the recompute forward within a segment). Show activation memory drop to O(sqrt(N)) (with sqrt-spaced checkpoints) and the extra forward FLOPs (~1 extra forward = +33% compute). Sliders: layers, checkpoint segment size, showing the memory/compute Pareto curve (a dot moving along it). Teaches: why we recompute instead of store, the sqrt(N) memory optimum, and the ~33% compute cost that buys a much bigger model or batch. Real memory (segments stored) + FLOP (recomputed segments) accounting.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/gradient-checkpointing.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-gradient-checkpointing\">` containing: a `<div class=\"vt-title\">Gradient checkpointing: trade compute for memory</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-gradient-checkpointing`) and an inline `<script>` (an IIFE; all element ids prefixed `gradient-checkpointing-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:gradient-checkpointing}}` on its own line into content/04-kernels-efficiency/10-memory-efficient-training.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/gradient-checkpointing.html   (slug gradient-checkpointing)\nChapter with the marker: content/04-kernels-efficiency/10-memory-efficient-training.md\n\nRead the tool file (and the chapter section around the {{tool:gradient-checkpointing}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/gradient-checkpointing.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-gradient-checkpointing / gradient-checkpointing- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:gradient-checkpointing}}` marker exists in content/04-kernels-efficiency/10-memory-efficient-training.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "data-mixing-temperature", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Data mixing: domain weights & temperature sampling  (slug: data-mixing-temperature)\nIt teaches / does:\nShow how a pretraining data mixture is set. Several domains (web, code, math, books, wiki) with their raw corpus SIZES shown as bars. A sampling method: proportional (sample each domain by its size \u2014 big domains dominate), uniform, or temperature-sampled (p_i proportional to size_i^(1/T), T slider: T=1 is proportional, T->inf is uniform \u2014 upsampling small high-quality domains). Show the resulting sampling weights and the expected tokens-per-domain in a training batch, and an 'epochs' readout (small domains get repeated many times when upsampled -> overfitting risk). Optionally a curriculum toggle that shifts weights over training (more code/math late). Teaches: the size-vs-quality mixing problem, temperature sampling (the DoReMi/mixture-weights idea), and repetition/epoching of small domains. Real size^(1/T) normalization + per-domain token/epoch math.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/data-mixing-temperature.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-data-mixing-temperature\">` containing: a `<div class=\"vt-title\">Data mixing: domain weights & temperature sampling</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-data-mixing-temperature`) and an inline `<script>` (an IIFE; all element ids prefixed `data-mixing-temperature-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:data-mixing-temperature}}` on its own line into content/03-pretraining/14-data-mixing-curriculum.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/data-mixing-temperature.html   (slug data-mixing-temperature)\nChapter with the marker: content/03-pretraining/14-data-mixing-curriculum.md\n\nRead the tool file (and the chapter section around the {{tool:data-mixing-temperature}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/data-mixing-temperature.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-data-mixing-temperature / data-mixing-temperature- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:data-mixing-temperature}}` marker exists in content/03-pretraining/14-data-mixing-curriculum.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "knowledge-editing-rome", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Knowledge editing: a rank-one weight edit (ROME)  (slug: knowledge-editing-rome)\nIt teaches / does:\nShow how a single fact can be edited into a model by a targeted rank-one update to one MLP layer (ROME-style locate-then-edit). Part 1 LOCATE: causal tracing \u2014 corrupt the subject token and restore hidden states layer by layer to find WHICH layer's MLP most restores the fact (a heatmap of restoration effect over layers x positions, peaking at a mid layer / the subject's last token). Part 2 EDIT: treat the MLP as a key-value store; compute a rank-one update dW = (v* - W k) k^T / (k^T k) that maps the subject key k to a new value v* encoding the new fact, changing the model's output from the old object to the new one \u2014 without retraining. Show the old vs new fact completion. Teaches: facts localize to specific MLP layers, MLPs as associative memories, and a rank-one edit rewrites one association. Real rank-one update math on a tiny seeded key/value; illustrative causal-trace heatmap.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/knowledge-editing-rome.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-knowledge-editing-rome\">` containing: a `<div class=\"vt-title\">Knowledge editing: a rank-one weight edit (ROME)</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-knowledge-editing-rome`) and an inline `<script>` (an IIFE; all element ids prefixed `knowledge-editing-rome-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:knowledge-editing-rome}}` on its own line into content/13-interp-safety-gov/02-knowledge-editing-unlearning.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/knowledge-editing-rome.html   (slug knowledge-editing-rome)\nChapter with the marker: content/13-interp-safety-gov/02-knowledge-editing-unlearning.md\n\nRead the tool file (and the chapter section around the {{tool:knowledge-editing-rome}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/knowledge-editing-rome.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-knowledge-editing-rome / knowledge-editing-rome- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:knowledge-editing-rome}}` marker exists in content/13-interp-safety-gov/02-knowledge-editing-unlearning.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}];
const VSCHEMA = {"type": "object", "additionalProperties": false, "required": ["file", "correct", "self_contained", "theme_safe", "notes"], "properties": {"file": {"type": "string"}, "correct": {"type": "boolean"}, "self_contained": {"type": "boolean"}, "theme_safe": {"type": "boolean"}, "issues_fixed": {"type": "integer"}, "notes": {"type": "string"}}};
log('Verifying ' + JOBS.length + ' existing tools (Opus-5)…');
const results = await parallel(JOBS.map(function (j) {
  return function () {
    return agent(j.verify, { label: 'verify:' + j.slug, phase: 'Verify', model: 'claude-opus-5', schema: VSCHEMA })
      .then(function (v) { return { slug: j.slug, verify: v }; })
      .catch(function (e) { return { slug: j.slug, failed: String(e) }; });
  };
}));
const ok = results.filter(function (r) { return r && !r.failed; });
log('Verify: ' + ok.length + '/' + JOBS.length + ' verified.');
return { total: JOBS.length, done: ok.length, results: results };
