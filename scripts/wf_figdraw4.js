export const meta = {
  name: 'figdraw-b4',
  description: 'Draw + revise foundational figures for 8 chapters',
  phases: [{ title: 'Draw' }, { title: 'Revise' }],
}

const CH = [
  {
    "chapter": "01-foundations/08-gpu-architecture",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/01-foundations/08-gpu-architecture.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [animated]  name=roofline-intensity-decode-vs-matmul\n       concept: The roofline: attainable performance vs arithmetic intensity, with the ridge point, and the two canonical workloads (compute-bound matmul, memory-bound decode) plus the batching sweep that carries decode across the ridge.\n       placement: after '## Arithmetic Intensity: The Master Concept (anchor after the roofline dichotomy paragraph that ends '...You are FLOP-limited.', before the 'why decode is memory-bound and matmul is not' worked example)'\n       brief: A log-log roofline chart. X axis: arithmetic intensity I in FLOP/byte, spanning a wide range from ~1 up to a few thousand (label a few gridlines: 1, 10, 100, 1000). Y axis: attainable performance in FLOP/s (or TFLOP/s), also log. Draw the roofline as two joined line segments: a rising diagonal on the left (the memory-bandwidth roof, slope = peak bandwidth beta; annotate it 'memory-bound: P = I x beta, limited by HBM bandwidth') that bends at the RIDGE POINT into a horizontal ceiling on the right (the compute roof at peak FLOP/s pi; annotate 'compute-bound: P = pi, limited by Tensor Cores'). Mark the ridge point clearly with a dashed vertical drop line and label it 'ridge point = pi / beta ~ hundreds of FLOP/byte' (use a small illustrative integer like ~150-300, do NOT invent a precise benchmark number -- 'a few hundred' phrasing is fine). Place two workload markers ON or under the roof: (1) a dot far to the RIGHT under the flat ceiling labeled 'large matmul: I ~ thousands -> COMPUTE-bound, near peak', sitting right at the ceiling; (2) a dot far to the LEFT low on the diagonal labeled 'single-token decode: I ~ 1 -> MEMORY-bound, Tensor Cores ~idle', sitting on the rising diagonal near the far left, with a small caption 'Tensor Cores ~99% idle here'. Shade or tint the region left of the ridge as 'memory-bound' and right of the ridge as 'compute-bound' (use theme-safe muted fills, no hardcoded brand colors). ANIMATION (enhancement only; the single static frame must be fully legible without it): a highlighted decode marker starts at I~1 and steps rightward along the diagonal as a small 'batch = 1 -> 8 -> 32 -> 128 -> 256' counter increments, climbing the diagonal until it reaches the ridge and then rides along the flat ceiling -- visually showing that batching raises intensity (weight bytes constant, FLOPs scale with batch) until decode finally becomes compute-bound. Add a short caption tying it together: 'batching slides the same weight-load rightward until the Tensor Cores light up'. Must render correctly and read the same story in light and dark themes; keep all labels inside the viewBox, no overlapping text.\n\n  NEW  [static]  name=execution-model-warp-simt\n       concept: The software execution hierarchy (grid > block/CTA > warp of 32 > thread) mapped onto hardware (GPU > SM > warp scheduler > lane), with a zoom into one warp showing 32 lanes executing the SAME instruction in lockstep (SIMT), and the coalesced-vs-scattered memory access contrast.\n       placement: after '## The Execution Model: Threads, Warps, Blocks, and SMs (anchor after the '### The grid and the launch' paragraph and its ASCII, i.e. just before the saxpy python block)'\n       brief: A two-part static infographic. TOP: a left-to-right (or nested-boxes) mapping showing the software hierarchy stacked against the hardware it runs on. Software column: GRID (many blocks) contains BLOCK / CTA contains WARP (32 threads) contains THREAD. Hardware column, aligned row-for-row: GPU (many SMs) -> SM (one block lives here its whole life, owns the register file + shared memory) -> WARP SCHEDULER (picks a ready warp each cycle) -> LANE (one thread). Draw arrows or aligned brackets making the mapping explicit (Grid->GPU, Block->SM, Warp->scheduler, Thread->lane). Annotate the block box with 'threads in a block share fast SMEM + can __syncthreads()' and note 'different blocks cannot cheaply share/sync -> run independently -> scales'. BOTTOM (the payoff zoom): blow up ONE warp as a row of 32 small lane cells. Show all 32 lanes pointing at ONE shared instruction (a single 'fetch/decode + program counter' box feeding all lanes) with the caption 'SIMT: one instruction, 32 lanes, lockstep -- the warp is the real unit of execution.' Add two compact side-by-side mini-panels illustrating the two silent killers: (a) DIVERGENCE -- an if-split where ~half the lanes take path A and half path B, drawn as the warp running path A with the other lanes greyed/masked, THEN path B with the first half masked, labeled 'both sides run serially -> up to 2x slower'; (b) COALESCING -- two little rows of 32 lanes hitting memory: one where lane i reads address base+i (contiguous) collapsing into '1-2 transactions (coalesced, near peak BW)', and one where the 32 lanes read scattered addresses fanning out into 'up to 32 transactions (32x penalty)'. Use small illustrative integers only (32 lanes, 1-2 vs 32 transactions). Keep it uncluttered with clear top-to-bottom reading order; must be legible and theme-safe in both light and dark, all content inside the viewBox.\n\n  REFINE  existing figures/memory-hierarchy.html\n       issue: The core idea (three on-chip tiers all at tens of TB/s, then a ~10x bandwidth cliff to HBM while capacity balloons) reads clearly in both themes and the bar-length=bandwidth encoding is strong. Two concrete defects: (1) the left rotated label 'off-chip DRAM' sits at x=8 and is clipped by the left viewBox edge in both themes, and it is ALSO duplicated as an inline label inside the HBM bar -- redundant and messy. (2) The math glyphs proportional-to, approximately-equal and right-arrow render as empty tofu boxes in the raster/fallback font ('bar length [] speed', 'each tier [] 10x larger going down []', 'on-chip [] off-chip'), so the annotations partly break.\n       change: Nudge the whole drawing right (add ~16px left padding / move the rotated axis label to x>=18 so it clears the edge) and delete the duplicate inline 'off-chip DRAM' inside the HBM bar, keeping only one scope label. Replace the tofu-prone glyphs with font-robust equivalents: use the word 'bandwidth (bar length = speed)' or an SVG line+arrowhead instead of the proportional-to sign, write 'each tier ~10x larger going down' with a drawn arrow marker rather than the unicode arrow, and 'on-chip -> off-chip: ~10x' as a drawn arrow. This keeps the (already good) intuition intact while removing the clipping and the broken glyphs.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/01-foundations/08-gpu-architecture.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/01-foundations/08-gpu-architecture.md\nThe chapter's core intuitions: A GPU is a THROUGHPUT machine: thousands of arithmetic lanes fed by a comparatively thin memory pipe, hiding latency by instantly switching to another ready warp instead of caching/predicting like a CPU.; The execution model is a strict nesting mapped onto hardware: grid > block(CTA) > warp(32 lockstep SIMT lanes) > thread maps to GPU > SM > scheduler > lane; the 32-lane warp is the REAL unit of execution, so divergence and uncoalesced access are the silent throughput killers.; There are TWO arithmetic engines: CUDA cores do one scalar FMA per cycle, Tensor Cores consume whole tiles and emit a whole tile matmul-accumulate per instruction, which is why matmul throughput is ~16x scalar throughput -- but only for reduced-precision matmuls, everything else stays on CUDA cores and is memory-bound.; The memory hierarchy (registers > SMEM/L1 > L2 > HBM) drops ~10x in speed and rises ~10x in size per tier; keeping reused data in the fast tiers is the whole game (the FlashAttention insight).\n\nReview each of these figures (the ones this chapter just drew/edited):\n  roofline-intensity-decode-vs-matmul, execution-model-warp-simt, memory-hierarchy\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "01-foundations/10-accelerator-landscape",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/01-foundations/10-accelerator-landscape.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=accel-roofline-decode-vs-prefill\n       concept: One roofline with the ridge point I* separating memory-bound decode (rank by HBM TB/s) from compute-bound prefill/training (rank by peak TFLOPS).\n       placement: after 'Section 'Reading a Spec Sheet to Pick Hardware' -> subsection 'Step 2: Is it compute-bound or bandwidth-bound? (roofline)' (place the marker right after that subsection's paragraph)'\n       brief: A classic roofline plot on log-log-feel axes (do not over-label the axes with numbers; this is a schematic). X-axis: 'Arithmetic intensity I (FLOPs per byte of HBM traffic)'. Y-axis: 'Attainable throughput (FLOP/s)'. Draw the roofline as two joined segments: (1) a diagonal slanted line rising from the lower-left = the BANDWIDTH ROOF (slope = peak HBM bandwidth), (2) a horizontal line at the top = the COMPUTE ROOF (peak TFLOPS). They meet at the ridge point; drop a light dashed vertical line from the ridge to the x-axis and label it 'I* = peak FLOP/s / peak byte/s (the ridge point)'. Shade/annotate the LEFT region under the diagonal as 'MEMORY-BOUND' and the RIGHT region under the horizontal as 'COMPUTE-BOUND'. Place two labelled operating points sitting ON the roofline: (a) a 'DECODE (one token)' marker low-left on the diagonal, with a callout 'streams the whole weight matrix per token, tiny compute -> low I -> limited by HBM bandwidth -> rank chips by TB/s'; (b) a 'PREFILL / TRAINING (dense GEMM)' marker up-right on the flat plateau, with a callout 'big statically-shaped matmuls -> high I -> limited by peak matmul -> rank chips by FP8/bf16 TFLOPS'. Optionally show a faint second, higher bandwidth roof (steeper diagonal) labelled 'more HBM bandwidth lifts the memory-bound region' to hint why a memory-rich chip helps decode \u2014 keep it subtle and clearly secondary. Use only small illustrative integers or none; invent NO benchmark numbers (no real TFLOPS/TB/s values). Theme-safe: use the palette's ink/accent tokens, no hardcoded reds/greens for the two regions \u2014 distinguish them by fill opacity + labels, not by named colors. Everything must sit inside the viewBox with clear reading order: title top, roofline center, two operating-point callouts not overlapping the curve. The static frame must fully convey: which side each workload lives on, and therefore which single spec-sheet number picks the chip.\n\n  NEW  [static]  name=accel-capacity-decision-70b\n       concept: The chapter thesis in miniature: a 152 GB (70 GB weights + 82 GB KV) decode-serving footprint needs two 80 GB devices (tensor-parallel split + per-layer all-reduce) but fits on one 192 GB device with no collective at all.\n       placement: after 'Section 'Reading a Spec Sheet to Pick Hardware' -> admonition 'Worked example: serving a 70B model \u2014 capacity changes everything' (place the marker just before or right after that worked example)'\n       brief: A two-panel side-by-side comparison. TOP or LEFT of both panels: a single shared 'live footprint' stacked bar = 70 GB weights + 82 GB KV cache = ~152 GB total (label the two segments; use small illustrative integers exactly these, they come from the chapter's own worked example, not invented benchmarks). LEFT PANEL titled 'Two 80 GB devices (e.g. H100-class)': show the 152 GB footprint split across TWO device boxes each with an 80 GB capacity gauge \u2014 the model does not fit in one, so it is tensor-parallel sharded; draw a red-free but clearly-marked 'per-layer ALL-REDUCE over the interconnect' arrow looping between the two devices INSIDE the decode path, with a small caption 'decode pays a cross-device collective every layer'. RIGHT PANEL titled 'One 192 GB device (e.g. MI300X-class)': show the SAME 152 GB footprint sitting inside a single 192 GB capacity gauge with visible headroom, and a bold 'NO tensor-parallel collective in the decode path' badge; caption 'weights stream from one HBM stack; zero inter-device communication'. Under both panels a single takeaway strip: 'Decode is bandwidth-bound -> capacity that removes the all-reduce can beat higher aggregate FLOPS.' Keep device/vendor names generic-with-class-hint (write '80 GB device (H100-class)' / '192 GB device (MI300X-class)') so it reads as the method, not a datasheet. Show a small ceil-divide note near the left panel: 'ceil(152 / 80) = 2 devices' and near the right 'ceil(152 / 192) = 1 device'. Theme-safe: capacity gauges as fill-vs-outline (filled = used, outline = headroom), no hardcoded primary colors for 'good/bad'; distinguish the all-reduce arrow by weight/dash + label, not color alone. Everything inside the viewBox, two panels clearly separated, reading order: shared footprint bar -> left (2 devices, collective) -> right (1 device, none) -> takeaway strip. The static frame alone must convey the full causal chain.\n\n  REFINE  existing figures/accel-portability-spectrum.html\n       issue: The axis is literally 'how much of the NVIDIA programming model survives', yet NVIDIA \u2014 the origin/zero-difficulty reference the whole spectrum is measured against \u2014 is absent. The leftmost box is AMD Instinct labelled 'EASIEST PORT', which reads as if AMD were the baseline rather than the first step away from it. A reader misses that the true zero point (100% survives) is the NVIDIA stack itself.\n       change: Add an explicit NVIDIA CUDA anchor at the far-left origin of the axis (difficulty 0 / '100% of the model survives \u2014 you are already here'), visually distinct from the four porting-target boxes, so the spectrum reads as NVIDIA(origin) -> AMD(translation) -> Gaudi/Trainium/TPU(paradigm). Keep everything else; just seat the reference point that names the axis.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/01-foundations/10-accelerator-landscape.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/01-foundations/10-accelerator-landscape.md\nThe chapter's core intuitions: SIMT gives you a programmable machine and makes you manage the memory hierarchy; a systolic accelerator gives you a compiler and makes you accept its abstractions \u2014 this one contrast explains ~90% of the porting pain.; A systolic array does dense matmul only: weights sit stationary in a MAC grid, activations pump in from the left one column per clock, partial sums march downward and NEVER leave the array \u2014 almost no SRAM/register traffic, so huge FLOP/watt but rigid.; The roofline ridge point I* = peak FLOP/s / peak byte/s classifies every op: below I* it is memory-bound (rank chips by HBM bandwidth), above I* it is compute-bound (rank chips by peak TFLOPS).; Autoregressive decode is bandwidth-bound (stream the whole weight matrix from HBM per token, tiny per-token compute); training and prefill are compute-bound dense GEMMs \u2014 this single flip decides which chip wins.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  accel-roofline-decode-vs-prefill, accel-capacity-decision-70b, accel-portability-spectrum\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "02-transformer/05-positional-encoding",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/05-positional-encoding.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=rope-frequency-ladder\n       concept: Position as a spectrum of rotation frequencies (fast local pairs vs slow long-range pairs) and why the slow pairs cause extrapolation failure past the trained length.\n       placement: after 'Inside the RoPE section, right after the worked-example admonition 'Worked example: how fast does each RoPE dimension spin?' (which computes \u03b8_0=1 rad/tok and \u03b8_63\u22481e-4 rad/tok).'\n       brief: A wide two-part infographic. LEFT (~30% width): a vertical stack of 3 small clock dials labelled top-to-bottom 'fast pair k=0 (\u03b8\u22481 rad/tok, wavelength ~6 tokens)', 'mid pair', 'slow pair k=d/2\u22121 (\u03b8\u22481e-4 rad/tok, wavelength ~50k tokens)'. Each dial shows a single hand at the SAME token position: the fast dial's hand has swept most of the way around (many turns implied by a small spiral or '\u00d7many' tag), the mid dial's hand is at a moderate angle, the slow dial's hand is barely tilted off vertical. Caption under the stack: 'one position, d/2 hands spinning at geometrically-spaced rates.' RIGHT (~70%): a line plot, x-axis = token position (0 out to well past a marked L_train tick, e.g. label the tick '4K' as illustrative only), y-axis = rotation angle (radians, mark 0, \u03c0, 2\u03c0). Shade the vertical band [0, L_train] and label it 'trained window \u2014 angles the model has seen'; shade the region beyond and label it 'never seen in training'. Draw three curves colored to match the three dials: (1) fast pair \u2014 a steep line that wraps many times, drawn as a dense sawtooth (angle mod 2\u03c0) to make 'many full cycles even in short training' visible; (2) mid pair \u2014 a moderate line completing a couple of cycles; (3) slow pair \u2014 a nearly-flat line that inside the band rises to only a tiny fraction of one cycle (annotate '<8% of a turn at L_train'), then CONTINUES climbing into the 'never seen' region (annotate 'past L_train these angles are unfamiliar \u2192 RoPE degrades'). Add one summarizing callout arrow at the bottom: 'context extension = keep the slow-pair curve inside the trained band.' Theme-safe: use the theme's accent tokens for the three curves and a neutral hatch/desaturated emphasis token for the 'never seen' band (no hardcoded red). All integers illustrative; everything inside the viewBox with clear left-to-right, top-to-bottom reading order.\n\n  NEW  [static]  name=rope-scaling-methods\n       concept: PI vs NTK-aware vs YaRN as three distinct ways to reshape the RoPE frequency ladder so long positions stay within trained angles.\n       placement: after 'In the 'Context-Length Extension: Stretching RoPE to 128K' section, right after the paragraph introducing the 'three families of fixes ... instrument with many strings' metaphor and before the 'Position Interpolation (PI)' subsection.'\n       brief: Three stacked (or side-by-side, whichever fits legibly) panels that share one common motif: a horizontal 'frequency ladder' rendered as a row of ~8 rungs/dials running left=fast/local to right=slow/long-range, matching the color language of the rope-frequency-ladder figure. In each panel show, below or on each rung, an arrow or bar indicating how much that dimension is scaled. PANEL 1 'Position Interpolation (uniform squeeze)': every rung gets an identical inward-squeeze arrow of the same length; caption 'pos \u2192 pos/s (e.g. s=4): every dimension compressed equally \u2014 even the fast local dims that were never the problem lose resolution; usually needs a short fine-tune.' PANEL 2 'NTK-aware (stretch the base)': a graded response \u2014 leftmost (fast) rungs have near-zero arrows labelled 'untouched, local precision kept', arrows grow toward the right so the rightmost (slow) rungs have the largest stretch labelled 'stretched most'; caption 'base \u2192 base\u00b7s^(d/(d\u22122)): non-uniform; often training-free for 2\u20134\u00d7.' PANEL 3 'YaRN (per-dim ramp + temperature)': divide the ladder into three labelled regions \u2014 left segment 'short wavelength \u2192 untouched', right segment 'long wavelength \u2192 interpolated (PI-style)', middle segment drawn as a smooth ramp 'blend'; then off to the side a small companion icon of a softmax bar-distribution shown going from flat to peaked with the tag '\u00d7(0.1\u00b7ln s + 1) logit temperature \u2014 re-sharpens the flattened attention'; caption 'NTK-by-parts ramp + attention temperature: best reach, the production default.' BOTTOM: a single lineage arrow 'PI (uniform, needs fine-tune) \u2192 NTK-aware (non-uniform, often free) \u2192 YaRN (per-dim ramp + temperature)'. Theme-safe accent tokens only; s=4 and the 8 rungs are illustrative integers; keep all three panels inside the viewBox with no overlapping labels.\n\n  REFINE  existing figures/rope-rotation.html\n       issue: The core geometry (a pair rotating by m\u03b8, and the inset showing q\u00b7k depending on the angle difference) is correct and reads clearly in both themes \u2014 this is a genuinely good figure. Two small polish issues: in dark mode the phrase 'only offset matters' in the bottom-right box sits right against the box's right border (near-clip), and in the small 'Query m vs. key n' inset the 'relative position' caption crowds the horizontal axis line.\n       change: Add a few px of right padding inside the bottom-right result box so 'only offset matters' clears the border in both themes; nudge the inset's 'angle difference = relative position' caption down (or the inset axis up) so the text no longer overlaps the axis. No structural change \u2014 the figure conveys its idea well and should be kept.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/05-positional-encoding.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/05-positional-encoding.md\nThe chapter's core intuitions: Bare attention is permutation-equivariant: it sees an unordered SET of vectors, so 'dog bites man' and 'man bites dog' are identical to it. Word order carries no signal unless we inject position ourselves.; There are exactly two places to inject position: Approach A adds to the representation (x_i + p_i, before attention), Approach B adds a bias to the pre-softmax score S_ij. RoPE is the hybrid: it touches q,k (A's locus) but is engineered so q\u00b7k depends only on the relative offset (B's goal).; RoPE rotates each 2-D feature pair by an angle proportional to its position; when you dot a query rotated by m\u03b8 with a key rotated by n\u03b8, the absolute angles cancel and only the relative offset (m\u2212n)\u03b8 survives.; Position is encoded as a SPECTRUM of rotation frequencies: d/2 pairs on a geometric ladder from fast (local, wavelength ~6 tokens) to slow (long-range, wavelength ~50k tokens). This same ladder underlies sinusoidal encodings and RoPE.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  rope-frequency-ladder, rope-scaling-methods, rope-rotation\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/12-checkpointing-fault-tolerance",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/12-checkpointing-fault-tolerance.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=ckpt-topology-agnostic-reshard\n       concept: How a DCP sharded checkpoint saved on N ranks is remapped through a normalized named-chunk store and reloaded onto a DIFFERENT number of ranks M.\n       placement: after '3.12.2 The Sharding Problem \u2014 after the strategy table, anchored on the paragraph beginning 'PyTorch's torch.distributed.checkpoint (DCP)''\n       brief: A three-column left-to-right flow with a clear pivot in the middle. LEFT column titled 'Save on N=4 ranks': four small stacked GPU boxes (rank 0..3), each holding a disjoint SHARD of one logical parameter tensor W. Show W conceptually as a horizontal bar split into 8 illustrative chunks numbered 0..7; each rank owns 2 chunks (rank0: chunks 0,1; rank1: 2,3; rank2: 4,5; rank3: 6,7). Use one accent hue to tint chunks by which rank wrote them, but rely on chunk NUMBERS as the primary identity so it reads in grayscale. Arrows from all four ranks converge into the MIDDLE column: a single 'Normalized chunk store (on disk)' panel \u2014 draw it as an index table mapping logical name -> byte range, e.g. rows 'W[0:1k] -> shard_A @ off 0', 'W[1k:2k] -> shard_A @ off 4096', ... 'W[7k:8k] -> ...', plus a note 'chunks are addressed by LOGICAL tensor offset, NOT by device'. The key visual point: the store has NO notion of rank count. RIGHT column titled 'Load on M=2 ranks (elastic resume)': two GPU boxes (rank 0..1); rank0 pulls chunks 0,1,2,3 and rank1 pulls chunks 4,5,6,7 \u2014 i.e. the SAME 8 chunks, re-partitioned into 2 groups instead of 4. Draw fan-out arrows from the store to the two ranks, and label the reassembled bar under each rank so the reader sees the identical W reconstituted with a new split. Add a one-line takeaway strip at the bottom: 'Same bytes, any device count \u2014 no manual resharding.' Include a tiny caption noting N and M are illustrative. Keep everything inside the viewBox, generous spacing, no color hardcoding \u2014 use theme tokens for panels/text and a single accent for chunk tint. Small integers only (4, 2, 8 chunks); invent no benchmark numbers.\n\n  NEW  [static]  name=ckpt-failure-waste-and-daly\n       concept: Why fault tolerance is unavoidable at scale and how the checkpoint interval trades save cost against lost-work risk: a progress-vs-time sawtooth with a failure that rolls back to the last checkpoint, paired with the U-shaped waste-vs-interval curve whose minimum is Daly's T*.\n       placement: after '3.12.5 Hardware Failure Rates at Scale: The Math \u2014 after the '1024-node cluster' example box, anchored on the sentence introducing Daly's formula (T* ~ sqrt(2 * T_save * T_MTBF))'\n       brief: Two side-by-side panels sharing a clean, theme-aware style. LEFT panel 'What a failure costs': an x-axis = wall-clock time, y-axis = useful training progress (monotonic ideal line as a faint dashed diagonal 'ideal, no failures'). Overlay the REAL progress as a rising line punctuated by checkpoint markers (small solid dots labeled 'ckpt') at even intervals of width T_ckpt. At one point draw a jagged 'FAILURE' burst; from there drop the progress line vertically back DOWN to the height of the last checkpoint (the rollback), then resume rising. Shade the wedge between the failure point and the last checkpoint and label it 'lost work (avg ~ T_ckpt / 2)'. Also mark a short flat segment at each checkpoint labeled 'T_save (save cost)'. The reader should see: bigger interval => taller lost-work wedge on failure; every checkpoint also costs a little flat T_save. RIGHT panel 'Choosing the interval': a U-shaped curve, x-axis = checkpoint interval T_ckpt (short -> long), y-axis = expected wasted compute fraction. Decompose it visibly: a DECREASING dashed component labeled 'save overhead ~ T_save / T_ckpt' (frequent saves cost more) and an INCREASING dashed component labeled 'lost-work risk ~ T_ckpt / (2*T_MTBF)'; their sum is the solid U curve. Mark the minimum with a vertical drop line to the axis labeled 'T* ~ sqrt(2 * T_save * T_MTBF)' and annotate 'sweet spot: frequent + fast wins'. Include a small header strip tying the panels together: 'More nodes -> MTBF = 1/(N*lambda) shrinks -> failures every hour or less.' Use only illustrative axis ticks, no invented benchmark numbers; keep both panels fully legible as static frames, theme-safe tokens, single accent for the failure/optimum highlights, everything within the viewBox.\n\n  REFINE  existing figures/ckpt-gfs-retention-timeline.html\n       issue: In the dark theme the section headers (Recent / Daily / Weekly / Permanent) and their subtitles collide with each other and with the horizontal rule, reading as muddy overlapping text; the bottom caption is also very low-contrast in dark. Light theme is fine, so it is purely a dark-mode legibility/spacing problem, not a concept problem.\n       change: Add vertical breathing room so each section's subtitle sits clearly BELOW the rule/header rather than overprinting it, and use theme-aware muted-foreground tokens (not a dim gray) for subtitles and the caption so contrast holds in dark mode. Keep the metaphor and layout otherwise unchanged.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/12-checkpointing-fault-tolerance.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/12-checkpointing-fault-tolerance.md\nThe chapter's core intuitions: A complete checkpoint is FOUR things (weights, optimizer moments, per-rank RNG, training metadata); dropping optimizer state or the data cursor does not crash \u2014 it SILENTLY diverges.; Optimizer state dwarfs the weights: Adam keeps ~16P bytes vs 2P for bf16 weights, so most of a checkpoint's size and save time is optimizer moments, not parameters.; The topology-agnostic sharded format (DCP) saves parameters as named byte-range chunks in a normalized store, so a run saved on N ranks can be remapped and resumed on a DIFFERENT number of ranks M \u2014 this is what makes elastic resume possible.; Async checkpointing decouples the fast GPU->pinned-CPU snapshot (the only blocking step, ~seconds) from the slow CPU->disk write (minutes), which overlaps with subsequent training steps.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  ckpt-topology-agnostic-reshard, ckpt-failure-waste-and-daly, ckpt-gfs-retention-timeline\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "04-kernels-efficiency/01-roofline-performance",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/01-roofline-performance.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=intensity-reuse-ladder\n       concept: Why arithmetic intensity differs across ops: it is governed by how many times each byte read from HBM is reused before it is evicted.\n       placement: after '### Arithmetic intensity \u2014 anchor immediately after the paragraph ending '...swarm of tiny reuse-free element-wise and normalization ops.''\n       brief: A three-rung 'reuse ladder' read left-to-right (or top-to-bottom), intensity increasing along the reading direction, that turns the abstract I = FLOPs/bytes into a picture of data reuse. THREE panels, each with the same internal template: (1) a small schematic of the operation, (2) ONE highlighted loaded element (a single colored cell) with fan-out arrows showing how many multiply-add operations that one loaded byte feeds before it leaves on-chip memory, (3) a one-line intensity verdict.\n\nPanel A \u2014 Element-wise add (z = x + y): draw two short input strips x and y and an output strip z. Highlight one cell x[i]; a single arrow to one '+' to z[i]. Label: 'read x, read y, write z -> ~3 bytes moved per 1 FLOP' and 'I ~= 0.17 FLOP/B, each byte used ONCE.' Tag: 'no reuse -> memory-bound.'\n\nPanel B \u2014 Matrix x vector, GEMV (this is decode): draw a tall weight matrix W being streamed row by row into a small activation vector to make a small output vector. Highlight one weight element w; a single arrow to one MAC. Note the activation vector is reused across rows but the weights (the bulk of the bytes) are each touched once. Label: '2dk FLOPs, ~2dk bytes -> I ~= 1 FLOP/B' and 'each WEIGHT used once -> memory-bound. <-- single-stream decode lives here.' Tag: 'streaming, no weight reuse.'\n\nPanel C \u2014 Matrix x matrix, GEMM (FFN / prefill): draw a small tile of A loaded into an on-chip SRAM box, and fan-out arrows from that ONE tile to MANY columns of B producing many outputs \u2014 visibly more arrows than Panel B. Label: 'one loaded tile reused across n outputs -> I ~= m/3, grows with size' and 'high reuse -> compute-bound.' Tag: 'reuse amortizes the read.'\n\nAcross the bottom, a slim shared intensity axis (log) with the ridge I* marked, and the three panels' points (A ~0.17, B ~1, C large) dropped onto it \u2014 A and B left of ridge shaded as 'memory roof', C right of ridge as 'compute roof' \u2014 explicitly tying this figure back to {{fig:roofline}}. Theme-safe: use accent/accent2/good CSS vars for the memory-vs-compute coding, neutral ink for schematics. ONLY the small illustrative integers/symbols already in the chapter (0.17, ~1, m/3, 2dk); invent no hardware numbers. Everything inside the viewBox, generous whitespace, clear left-to-right reading order. Must be fully legible as a static frame.\n\n  NEW  [static]  name=decode-batching-amortization\n       concept: Single-stream decode streams all weights to make ONE token (I~1, on the memory roof); batching B requests reuses one weight read across B tokens (I~B), climbing the roof past the ridge into compute-bound \u2014 the entire economic argument for continuous batching.\n       placement: after '## Bytes, Intensity, and the Two Regimes of Inference \u2014 anchor after the paragraph ending '...serve many tokens at once \u2014 raising the effective intensity.''\n       brief: Two side-by-side panels sharing a small roofline inset, showing how batching converts a memory-bound GEMV into a compute-bound GEMM WITHOUT changing the bytes moved.\n\nLEFT panel 'single-stream decode (batch = 1)': draw the model as a vertical stack of a few weight-matrix blocks (label 'all weights, ~2N bytes'). A single wide arrow labeled 'stream every weight from HBM' feeds a tiny 1xd activation vector (one thin row) through the stack, producing exactly ONE output token (one small square popping out on the right). Annotate: 'bytes moved = all weights', 'FLOPs = 2N', 'I ~= 1 FLOP/B'. A small roofline inset in the corner shows a dot sitting ON the memory roof, left of the ridge.\n\nRIGHT panel 'batched decode (batch = B)': the SAME weight stack, the SAME single wide 'stream every weight once' arrow (emphasize identical byte cost \u2014 perhaps a faint 'same bytes' tie-line between the two panels' arrows), but now a B-row activation block (draw ~4-8 stacked rows for B) flows through, producing B output tokens (B small squares). Annotate: 'bytes moved = all weights (UNCHANGED)', 'FLOPs = B x 2N', 'I ~= B FLOP/B'. Its roofline inset dot has slid UP the slope, reaching/crossing the ridge into the compute-bound region \u2014 draw a small arrow on the inset from the left panel's position to this one.\n\nBottom caption strip: 'One weight read now serves B tokens -> intensity rises ~Bx -> decode crosses the ridge. (Until per-request KV-cache traffic takes over.)' Use a small illustrative B (e.g. B=8), no invented tok/s or bandwidth numbers. Theme-safe via CSS vars (memory-bound = accent2/blue coding, compute-bound = good/green, matching {{fig:roofline}}). Keep the two roofline insets tiny but unmistakably the same axes as the main roofline. Fully understandable as a static frame; if animated, the only motion is the activation rows streaming through and tokens popping out \u2014 never required for comprehension. Everything within the viewBox, no overlapping labels.\n\n  REFINE  existing figures/roofline.html\n       issue: The core diagram is correct and clear in both themes, but in dark mode the 'flat ceiling - peak FLOP/s' subtitle sits directly on the orange compute-roof line so the words partly overlap the stroke, and the compute-bound annotation box ('prefill, batched decode') crowds the right grid edge, slightly clipping visual breathing room.\n       change: Nudge the 'flat ceiling - peak FLOP/s' subtitle down a few px so it clears the compute-roof line (add ~6-8px vertical clearance and/or shift it left of the roof-label), and give the compute-bound annotation box ~10px more right margin so it doesn't touch the plot's right edge. No structural change \u2014 the figure otherwise conveys P(I)=min(pi, beta*I), the ridge, and the two example points excellently.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/04-kernels-efficiency/01-roofline-performance.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/04-kernels-efficiency/01-roofline-performance.md\nThe chapter's core intuitions: A GPU has two mismatched speed limits \u2014 peak FLOP/s (pi) and peak HBM bytes/s (beta) \u2014 and their ratio is the ridge point I* = pi/beta; you classify any kernel by comparing its arithmetic intensity to that single number.; Arithmetic intensity is set by REUSE: element-wise ops and matrix-vector (GEMV) products use each byte read from HBM only ~once (low I, memory-bound), while matrix-matrix (GEMM) products reuse each loaded tile across many outputs (high I, compute-bound).; Single-stream decode is a GEMV that streams the ENTIRE weight set from HBM to produce one token (I~1, bandwidth-bound); batching many requests lets one weight read serve many tokens, raising effective intensity above the ridge \u2014 this is the whole economic case for batching.; Transformer forward FLOPs are dominated by the d^2 terms (FFN 16d^2 + QKVO 8d^2); attention scores are ~4sd (proportional to s^2 over a sequence) and only dominate the FLOP budget at very long context \u2014 yet can dominate runtime because the score matrix is memory-bound (FLOPs are not time).\n\nReview each of these figures (the ones this chapter just drew/edited):\n  intensity-reuse-ladder, decode-batching-amortization, roofline\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "04-kernels-efficiency/07-quantization-ptq",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/07-quantization-ptq.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [animated]  name=gptq-error-compensation\n       concept: GPTQ quantizes one weight column at a time and compensates the not-yet-quantized columns for the rounding error it just committed, keeping the layer OUTPUT (wX) close rather than each weight.\n       placement: after '### The GPTQ algorithm'\n       brief: Visualize a SINGLE weight row as a horizontal strip of ~10 cells indexed by input feature (columns). Behind the strip draw a faint set of horizontal dashed 'grid levels' (the allowed quantized values). Each cell holds a small vertical bar = that weight's float value. A vertical SWEEP LINE moves left to right. State at the moment shown: cells to the LEFT of the sweep are 'quantized & locked' (filled/darker, a small lock glyph, their bars snapped exactly onto grid ticks); the CURRENT cell at the sweep is highlighted; cells to the RIGHT are 'not-yet-quantized' (outline only, floating between ticks). Animate the step: (1) the current cell's bar SNAPS to the nearest grid level, opening a small gap annotated 'rounding error d = w_i - q_i'; (2) a set of thin arrows fans from the current cell to every cell on its right, labeled 'compensate: -d/[H^-1]_ii * H^-1_:,i', and those right-hand bars visibly shift a little (some up, some down) to absorb the error; (3) the sweep advances one cell and repeats, and because the remaining bars were pre-nudged they land closer to their grid ticks. Include a small contrast callout: 'RTN: round each independently, errors accumulate' vs 'GPTQ: running compensation keeps output wX ~ w_hat X'. Show a tiny 'output = sum of w_i x_i' meter that stays near-flat as the sweep proceeds. Labels required: 'grid levels', 'quantized & locked', 'current column', 'not-yet-quantized (gets compensated)', 'rounding error d', 'compensation proportional to H^-1'. Only small illustrative bar heights and the symbol H^-1 \u2014 invent NO perplexity or benchmark numbers. The final frame must be self-explanatory: sweep at roughly the middle, left half locked-on-grid, current column showing its error gap, correction arrows fanning right. Theme-safe: no hardcoded primaries; distinguish locked/current/pending by shape+fill, not color alone.\n\n  NEW  [animated]  name=smoothquant-migration\n       concept: A per-channel diagonal scale diag(s) migrates quantization difficulty out of spiky activations into smooth weights, so BOTH tensors become cleanly INT8-quantizable, and the inverse factor folds into the upstream RMSNorm at zero cost.\n       placement: after '### SmoothQuant: migrate the difficulty'\n       brief: Two stacked bar-rows of per-channel magnitudes, aligned column-for-column: TOP = activation channels (mostly small bars with ONE or TWO towering outlier spikes, reusing the spike motif from the outlier figure), BOTTOM = weight channels (uniformly modest bars). BEFORE panel (left): over the activation row draw a COARSE INT8 grid (few widely-spaced levels forced by the spike) and show the small activation bars collapsing onto the zero level \u2014 annotate 'outlier forces coarse scale -> normal values crushed to 0'; the weight row already fits a fine grid ('smooth, easy'). CENTER: a diag(s) operator with a downward arrow on the activation ('divide channel by s_c') and an upward arrow on the matched weight channel ('multiply by s_c'), with the identity X W = (X diag(s)^-1)(diag(s) W) shown once. Animate the migration: the activation spike SHRINKS while the matching weight bar GROWS, difficulty visibly flowing top->bottom across the same column. AFTER panel (right): both rows now have MODERATE, comparable ranges, each fitting a fine INT8 grid -> a small 'INT8 x INT8 -> tensor cores' matmul badge. Include a compact alpha (migration strength) slider with three ticks: 'a=1 all to weights', 'a=0.5 balanced', 'a=0 none moved'. Add a free-lunch callout: '1/s folds into RMSNorm affine weights (zero runtime cost)'. Labels required: 'activation: spiky, hard', 'weight: smooth, easy', 'diag(s): migrate difficulty', 'both quantize cleanly', 'INT8 tensor cores', 'folds into RMSNorm'. Small illustrative bar heights only, no benchmark numbers. Static frame = before | migrate | after read left-to-right and fully legible on its own. Theme-safe: mark outlier vs normal by height/shape, not color alone; both light and dark styled.\n\n  REFINE  existing figures/quant-ptq-granularity.html\n       issue: In dark mode the dense multi-line caption under the per-group panel ('4 rows x 2 groups...', 'in LLMs ~in_features/128...', 'LLM INT4 default; +0.125 bit/weight') runs into the 'granularity cost vs quality tradeoff' axis arrow, and at the very top the 'Per-tensor' title crowds the 'in_features' axis label. Slight clutter that hurts legibility on dark backgrounds.\n       change: Add vertical breathing room: push the tradeoff axis down (or lift the panels up) so the per-group caption clears it in BOTH themes; trim the per-group caption to two lines max (e.g. keep '4 rows x 2 groups = 8 scales' and 'G=128 default; +0.125 bit/weight', drop the parenthetical). Nudge the top 'Per-tensor' label and the 'in_features' axis label apart so they don't touch.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/04-kernels-efficiency/07-quantization-ptq.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/04-kernels-efficiency/07-quantization-ptq.md\nThe chapter's core intuitions: Quantization maps floats to a small integer grid via a scale s (and optional zero-point z), and the rounding error is proportional to s, which is proportional to the tensor's dynamic RANGE \u2014 so one giant value poisons everyone.; Granularity (per-tensor -> per-channel -> per-group) buys fidelity by fitting each scale to a smaller, more homogeneous slice, at a tiny storage cost.; Activation outliers: past ~6B params a few feature channels run 10-100x the rest; they inflate a per-tensor scale and crush normal values to zero \u2014 and they live in activations, not weights, which is why weight-only INT4 is nearly free.; GPTQ minimizes the LAYER OUTPUT error, not the weight error: quantize columns left-to-right, and after committing each rounding error, nudge the not-yet-quantized columns via H^-1 to absorb it.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  gptq-error-compensation, smoothquant-migration, quant-ptq-granularity\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "05-posttraining-alignment/01-sft-instruction-tuning",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/05-posttraining-alignment/01-sft-instruction-tuning.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=superficial-alignment-quality-over-quantity\n       concept: Knowledge is stored in the pretraining weights; SFT is a thin format/access layer, which is why a small curated dataset can beat a large noisy one (LIMA).\n       placement: after 'LIMA: Less Is More for Alignment'\n       brief: Two-panel static infographic. LEFT panel titled 'Where the knowledge lives': draw an iceberg (or layered reservoir). The huge submerged mass is labeled 'Pretraining weights \u2014 facts, reasoning, syntax, style (hundreds of billions of tokens)'. Sitting on the waterline is a thin bright cap labeled 'SFT \u2014 format + how to access it'. A caption beneath reads 'SFT adds almost no knowledge; it teaches the model to surface what it already knows, in the right format.' Make the size contrast dramatic (thin cap vs vast body). RIGHT panel titled 'Quality > quantity': two identical base-model icons, each fed by a dataset stack. Top stack labeled '1,000 curated examples' (short, neat, a small check/star badge). Bottom stack labeled '100,000 noisy examples' (tall, with a few duplicate/error marks). Both arrows converge to a single 'human preference' comparison node where the curated-trained model carries a 'preferred' tag/ribbon and the noisy one a faded/neutral tag. Do NOT invent any win-rate percentage \u2014 use only the qualitative 'preferred' label and the two dataset sizes (1k, 100k) that appear in the text. Theme-safe: use currentColor for structure, one accent for the SFT cap and the 'preferred' badge, muted for secondary text. Everything inside the viewBox, clear left-to-right then panel reading order.\n\n  NEW  [static]  name=catastrophic-forgetting-loss-landscape\n       concept: Fine-tuning moves weights from the broad pretraining optimum toward a narrow SFT optimum; step size determines whether general skills survive.\n       placement: after 'Catastrophic Forgetting -> Why It Happens'\n       brief: Static loss-landscape cross-section (a smooth 1D curve of 'loss' vs 'parameter configuration'). Draw two valleys: a WIDE shallow basin on the left labeled 'General capabilities (pretraining optimum)' and a NARROWER basin on the right labeled 'SFT-task optimum'. Place a ball/marker at the bottom of the general basin as the starting point (the pretrained model). From it, draw TWO update trajectories: (1) a long arrow labeled 'large LR / many epochs' that leaps over the ridge and lands deep in the SFT basin, far from the general basin \u2014 annotate its endpoint 'general skills forgotten (MMLU drops)'; (2) a short arrow labeled 'small LR (1e-5..5e-5), 1-3 epochs' that nudges only to the near shoulder / overlap region between the basins \u2014 annotate 'still near general optimum -> both skills retained'. Add a compact side panel 'Mitigations' listing four levers as a ranked stack: low learning rate, short training (1-3 epochs), data mixing / replay (5-10% pretraining data), LoRA (freezes base -> structurally immune). Optionally show LoRA as a small padlock on the general basin meaning 'base frozen, only adapter moves'. Use only the LR values already in the text; invent no benchmark numbers. Theme-safe: curve and marker in currentColor, the 'safe' short-step arrow in the accent, the 'forgetting' long-step arrow in a warning/muted tone that works in both themes. Keep labels non-overlapping; the two arrows must be visually distinct.\n\n  REFINE  existing figures/sft-three-stage-posttraining-recipe.html\n       issue: Content is correct and the three-stage flow with reuse paths reads well, but in Stage 2 the 'rank pairs' arrow label overlaps the right edge of the 'preference data' box (renders as 'referencprank pairs'), and in dark mode the muted stage-title text ('Stage 2 \u2014 Reward Modeling', 'Stage 3 \u2014 RL Alignment') is very low-contrast against the panel.\n       change: Shift the 'rank pairs' arrow label right so it clears the preference-data box (or shorten to 'rank' and move it fully onto the arrow), and raise the stage-title color one step above --muted so titles stay legible in dark theme. Leave the layout and reuse-path arrows unchanged.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/05-posttraining-alignment/01-sft-instruction-tuning.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/05-posttraining-alignment/01-sft-instruction-tuning.md\nThe chapter's core intuitions: Response-only loss masking: the transformer sees one flat sequence, but gradients flow only from the assistant-response tokens, so SFT rewards reply quality rather than re-predicting the instruction.; The superficial alignment hypothesis (LIMA): knowledge already lives in the pretraining weights; SFT only teaches the model to access and present it in the right format, so a small curated dataset beats a large noisy one.; SFT is Stage 1 of a three-stage recipe (SFT -> Reward Modeling -> RL alignment), and it gives RL a strong behavioral prior to start from.; Catastrophic forgetting: fine-tuning moves the weights out of the broad pretraining optimum toward a narrow SFT optimum; a large LR / many epochs overshoots and erases general skills, while small LR, short training, replay, and LoRA keep the model near the shared basin.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  superficial-alignment-quality-over-quantity, catastrophic-forgetting-loss-landscape, sft-three-stage-posttraining-recipe\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "05-posttraining-alignment/10-reasoning-test-time-compute",
    "n_new": 2,
    "n_edit": 1,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/05-posttraining-alignment/10-reasoning-test-time-compute.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=cot-depth-unrolling\n       concept: CoT externalises intermediate state into the token stream, converting a fixed-depth forward pass into a variable-depth sequential computation.\n       placement: after 'Chain-of-Thought Prompting -> Why It Works (right after the 'bounded-depth circuit vs unbounded sequential program' paragraph)'\n       brief: A two-panel side-by-side contrast on a shared theme-aware canvas.\n\nLEFT panel, titled 'Single forward pass = FIXED depth'. Draw a vertical stack of ~4 identical transformer-layer boxes (label them L1..L4). Input tokens enter at the bottom, one answer token pops out the top. Annotate on the side: 'computation depth is capped at the number of layers L'. Below it a caption: 'a hard problem needing more sequential steps than L layers cannot be solved, and the model cannot look back to fix an early error.' Keep it visibly SHORT/shallow.\n\nRIGHT panel, titled 'Chain-of-thought = VARIABLE depth'. Show the model emitting intermediate reasoning tokens into a growing token stream: a horizontal row s1 -> s2 -> s3 -> answer. For each emitted step, draw a curved feedback arrow looping the emitted token back into the context that produces the NEXT step (externalised state). Critically, draw 2-3 thin 'attention' arrows from earlier emitted steps (s1, s2) forward into the computation of a later step (s3), labelled 'earlier steps become attention targets'. Annotate: 'each step is a shallow computation; chaining N steps reaches effective depth ~ N x L, governed by TRACE LENGTH, not layer count.'\n\nMake the visual punch the depth contrast: left is short, right is long/unrolled. Use small illustrative integers only (L1..L4, s1..s3). Theme-safe: layer boxes and arrows use CSS variables; one accent color for the feedback/attention arrows. Everything inside the viewBox, clear left-to-right / bottom-to-top reading order, no overlaps.\n\n  NEW  [static]  name=orm-vs-prm-scoring\n       concept: An Outcome Reward Model scores only the final answer; a Process Reward Model scores every step and surfaces the weakest link.\n       placement: after 'Process Reward Models vs. Outcome Reward Models (anchor after the PRM subsection intro, before PRM-Guided Best-of-N)'\n       brief: One reasoning trace drawn once as a horizontal chain of step boxes: Step 1 -> Step 2 -> Step 3 -> Step 4 -> [Final Answer]. Around this single shared chain, show the two reward models as two contrasting overlays.\n\nTOP overlay = ORM: a single wide bracket spanning the WHOLE trace, collapsing to one arrow into a scalar box on the right labelled 'r_ORM in R  (one number for the entire solution)'. Small caption: 'sees only the outcome \u2014 blind to WHERE the reasoning went wrong; a lucky-but-unsound answer can still score high.'\n\nBOTTOM overlay = PRM: a small score gauge/pill UNDER each step showing an illustrative per-step score (use 0-1 or 0-10, e.g. r1 high, r2 clearly LOW, r3 high, r4 high). Flag the low step 2 with a theme-aware warning accent and a small 'weakest link' tag. Then show the aggregation rule to the right: 's = min_t r(step t)' picking up the low r2, with caption: 'a flawed early step dooms the solution even if the final answer happens to be right \u2014 PRM catches it, ORM cannot.'\n\nUse only a few small illustrative scores (invent no benchmark numbers, these are schematic 0-1 values). Theme-safe: step boxes and text via CSS variables; one semantic 'warning' accent for the flagged weak step, one 'good' accent for high scores \u2014 both theme-aware, not hardcoded primaries. Clear vertical separation between the ORM band and PRM band so the two scoring philosophies read at a glance. Fits fully in viewBox.\n\n  REPLACE  existing figures/reasoning-search-tree.html\n       issue: Two problems. (1) Dark-mode bug: the central 'beam width / branching factor' caption is dark text on the dark background and is nearly illegible. (2) Weak metaphor: the figure is really three disconnected rows of boxes joined by one ambiguous brace \u2014 there are no parent-child edges, nothing shows that Step B nodes descend from a specific Step A node, and no single path is highlighted from Question down to CORRECT. It therefore fails to convey the chapter's opening intuition that 'the answer is the END of a PATH through a tree, not the top of a distribution.' The WRONG/CORRECT leaves float unattached to any branch.\n       change: Redraw as a genuine branching search tree. Root = 'Question' at top. Root branches to 3 candidate first steps (edges drawn explicitly). Each promising first step branches again to 2-3 second steps; let branching be uneven so it reads as real search, not a grid. Terminate several leaves as small 'x' dead-ends (dim) and exactly one leaf as CORRECT. HIGHLIGHT the single root->...->CORRECT path with a bold accent stroke and label it 'the solution is a path'. Dim the un-taken branches to show 'search space explored but pruned'. Put a small side caption: 'a single forward pass returns the top-of-distribution token; reasoning must find the path.' Fix theme-safety: every label must use a theme-aware text color (never a fixed dark hex), and verify the path/caption legibility in BOTH light and dark. Keep node text short (Step 1a, Step 2b, ...); this is a schematic tree, not real content.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/05-posttraining-alignment/10-reasoning-test-time-compute.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/05-posttraining-alignment/10-reasoning-test-time-compute.md\nThe chapter's core intuitions: Reasoning is a search problem: the correct answer lies at the END of a PATH through a reasoning tree, not at the top of the next-token distribution \u2014 more test-time compute buys a wider/deeper search of that tree.; Chain-of-thought works by externalising intermediate state into the context window: it turns a fixed-depth single forward pass into a variable-depth sequential computation, because each emitted token becomes an attention target for later tokens (chained shallow steps = deep computation).; Self-consistency (majority vote over N samples) amplifies a STRONG signal but cannot rescue a fundamentally weak model \u2014 the binomial makes this concrete.; A Process Reward Model scores EVERY step and exposes the weakest link, giving a richer search/training signal than an Outcome Reward Model, which sees only the final answer and can be fooled by a lucky-but-unsound solution.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  cot-depth-unrolling, orm-vs-prm-scoring, reasoning-search-tree\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  }
]
const DRAW_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "figures_done", "figures_skipped"], "properties": {"chapter": {"type": "string"}, "figures_done": {"type": "array", "items": {"type": "string"}}, "figures_skipped": {"type": "array", "items": {"type": "string"}, "description": "names you could NOT complete, with why appended"}, "notes": {"type": "string"}}}
const REVISE_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "figures", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "true iff every planned figure is present, correct, theme-safe, unclipped and builds intuition"}, "figures": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["name", "verdict"], "properties": {"name": {"type": "string"}, "verdict": {"type": "string", "enum": ["good", "fixed", "still-broken", "missing"]}, "issue": {"type": "string"}}}}, "notes": {"type": "string"}}}

// Draw on Sonnet 5, revise on Opus (user routing 2026-07-20).
const results = await pipeline(
  CH,
  c => agent(c.draw, { label: `draw:${c.chapter}`, phase: 'Draw', schema: DRAW_SCHEMA,
                       model: 'claude-sonnet-5' }).then(d => ({ c, d })),
  ({ c }) => agent(c.revise, { label: `revise:${c.chapter}`, phase: 'Revise', schema: REVISE_SCHEMA,
                                 model: 'opus', effort: 'high' })
                .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed)
                   .map(r => ({ chapter: r.chapter, figs: r.verdict?.figures?.filter(f => f.verdict === 'still-broken' || f.verdict === 'missing') }))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`figures: drawn+revised ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | died ${died.length}`)
log(`PASSED (append to figplan_done.txt): ${passed.join(' ')}`)
if (failed.length) log(`FAILED: ${JSON.stringify(failed)}`)
if (died.length) log(`DIED (re-run to resume): ${died.join(' ')}`)
return { passed, failed, died }
