export const meta = {
  name: 'interactive-tools',
  description: 'Build 15 interactive visualizer tools (Sonnet build -> Opus verify)',
  phases: [{ title: 'Build' }, { title: 'Verify' }],
}
const JOBS = [{"slug": "transformer-forward-pass", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: A full transformer forward pass, stage by stage  (slug: transformer-forward-pass)\nIt teaches / does:\nA GUIDED, animated walkthrough of one forward pass of a small GPT, with a 'Next stage' / 'Play' stepper. Stages, each highlighted in a data-flow diagram with the running TENSOR SHAPE annotated: (1) token ids [B,T]; (2) token + positional embeddings -> [B,T,d]; (3) inside one transformer block: RMSNorm -> QKV projection -> multi-head split -> scaled dot-product attention (show a small attention heatmap) -> output proj -> residual add -> RMSNorm -> SwiGLU MLP -> residual add; (4) repeat over N layers (a layer counter); (5) final norm -> lm_head [B,T,V] -> softmax -> next-token distribution. Use tiny concrete dims (B=1, T=4, d=8, heads=2, V=16) and compute REAL numbers with fixed seeded weights so the shown activations/shapes are correct at every stage. A side panel narrates what each stage does and why. This is the flagship 'see the whole model' visualizer \u2014 make the shape-tracking and the residual stream legible.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/transformer-forward-pass.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-transformer-forward-pass\">` containing: a `<div class=\"vt-title\">A full transformer forward pass, stage by stage</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-transformer-forward-pass`) and an inline `<script>` (an IIFE; all element ids prefixed `transformer-forward-pass-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:transformer-forward-pass}}` on its own line into content/02-transformer/07-build-gpt-from-scratch.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/transformer-forward-pass.html   (slug transformer-forward-pass)\nChapter with the marker: content/02-transformer/07-build-gpt-from-scratch.md\n\nRead the tool file (and the chapter section around the {{tool:transformer-forward-pass}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/transformer-forward-pass.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-transformer-forward-pass / transformer-forward-pass- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:transformer-forward-pass}}` marker exists in content/02-transformer/07-build-gpt-from-scratch.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "rlhf-ppo-pipeline", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: RLHF with PPO: the full loop  (slug: rlhf-ppo-pipeline)\nIt teaches / does:\nAnimate the complete PPO-RLHF loop as a cycle diagram with a 'step' button that advances one iteration: prompt -> POLICY generates a response (show a short token sequence) -> REWARD MODEL scores it (a scalar r) -> per-token advantages via GAE (show the value baseline) -> the PPO clipped surrogate objective (show the probability ratio pi/pi_old, the clip band [1-eps,1+eps], and which tokens are clipped) -> a KL penalty to the reference/SFT policy (a slider for the KL coefficient beta) -> policy update. Include a small live plot of reward vs KL over iterations showing the classic tradeoff (raise beta -> lower KL but lower reward; reward hacking when beta too small). Compute the real clipped-objective and KL-per-token math on toy seeded numbers. Label the FOUR models (policy, reference, reward, value/critic). Token-heavy and central to alignment.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/rlhf-ppo-pipeline.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-rlhf-ppo-pipeline\">` containing: a `<div class=\"vt-title\">RLHF with PPO: the full loop</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-rlhf-ppo-pipeline`) and an inline `<script>` (an IIFE; all element ids prefixed `rlhf-ppo-pipeline-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:rlhf-ppo-pipeline}}` on its own line into content/05-posttraining-alignment/06-ppo-for-llms.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/rlhf-ppo-pipeline.html   (slug rlhf-ppo-pipeline)\nChapter with the marker: content/05-posttraining-alignment/06-ppo-for-llms.md\n\nRead the tool file (and the chapter section around the {{tool:rlhf-ppo-pipeline}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/rlhf-ppo-pipeline.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-rlhf-ppo-pipeline / rlhf-ppo-pipeline- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:rlhf-ppo-pipeline}}` marker exists in content/05-posttraining-alignment/06-ppo-for-llms.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "rag-pipeline", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: RAG pipeline: retrieve, augment, generate  (slug: rag-pipeline)\nIt teaches / does:\nAn end-to-end RAG animation. A user query is embedded to a point in a 2D space that also holds ~12 document-chunk points (seeded); show the top-k nearest chunks by cosine similarity (draw the query point, the neighborhood circle, and lines to the retrieved chunks with their similarity scores). Then assemble the augmented prompt (system + retrieved context + query) shown as stacked blocks with a token-budget bar, and 'generate' a grounded answer that cites the retrieved chunks. Controls: k (top-k slider), a similarity threshold, and a toggle for a reranker step (re-order the retrieved set by a second score). Teaches the whole retrieve->rerank->augment->generate flow, why chunking + top-k + context-window budget interact, and grounding/citation. Real cosine-similarity + top-k math on the 2D points.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/rag-pipeline.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-rag-pipeline\">` containing: a `<div class=\"vt-title\">RAG pipeline: retrieve, augment, generate</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-rag-pipeline`) and an inline `<script>` (an IIFE; all element ids prefixed `rag-pipeline-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:rag-pipeline}}` on its own line into content/09-rag-retrieval/03-rag-architectures.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/rag-pipeline.html   (slug rag-pipeline)\nChapter with the marker: content/09-rag-retrieval/03-rag-architectures.md\n\nRead the tool file (and the chapter section around the {{tool:rag-pipeline}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/rag-pipeline.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-rag-pipeline / rag-pipeline- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:rag-pipeline}}` marker exists in content/09-rag-retrieval/03-rag-architectures.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "react-agent-loop", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: A ReAct agent loop: thought, action, observation  (slug: react-agent-loop)\nIt teaches / does:\nAnimate a ReAct agent solving a small toy task (e.g. a 2-step arithmetic-with-lookup question) as an interactive transcript that reveals one step at a time on 'Next step': Thought -> Action (a tool call, e.g. calculator(...) or search(...)) -> Observation (the tool result) -> ... -> Final Answer. Draw the loop as a cycle (LLM <-> tools <-> environment) with the growing context/scratchpad shown as an accumulating block (and a context-length bar). Include a 'the model can go wrong' toggle that injects a bad tool call and shows recovery on the next thought. Controls: pick one of 2-3 preset tasks; max-steps slider. Teaches the interleaved reason/act pattern, tools as the agent's hands, the growing context, and the stopping condition. Deterministic scripted trajectories (no LLM call); make the loop and the scratchpad growth the stars.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/react-agent-loop.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-react-agent-loop\">` containing: a `<div class=\"vt-title\">A ReAct agent loop: thought, action, observation</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-react-agent-loop`) and an inline `<script>` (an IIFE; all element ids prefixed `react-agent-loop-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:react-agent-loop}}` on its own line into content/08-agents-harness/02-agentic-loop.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/react-agent-loop.html   (slug react-agent-loop)\nChapter with the marker: content/08-agents-harness/02-agentic-loop.md\n\nRead the tool file (and the chapter section around the {{tool:react-agent-loop}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/react-agent-loop.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-react-agent-loop / react-agent-loop- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:react-agent-loop}}` marker exists in content/08-agents-harness/02-agentic-loop.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "parallelism-visualizer", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: How a model is split across GPUs: DP, TP, PP, FSDP  (slug: parallelism-visualizer)\nIt teaches / does:\nShow, on a grid of GPUs, how each parallelism strategy partitions the model+batch, with a per-GPU memory bar. A selector: Data Parallel (full model replicated, batch split; all-reduce gradients), Tensor Parallel (each layer's matrices sharded across GPUs; all-reduce activations within a layer), Pipeline Parallel (layers grouped into stages across GPUs; show the micro-batch pipeline bubble), FSDP/ZeRO (params+grads+optimizer-state sharded; all-gather in forward, reduce-scatter in backward). For the chosen strategy, animate ONE training step highlighting which GPU holds what and the communication (arrows labeled all-reduce / all-gather / reduce-scatter / P2P) and update each GPU's memory bar (params/grads/opt-state/activations). Controls: number of GPUs, model size, batch. Compute the real per-GPU memory split for each strategy (e.g. FSDP divides the 16-bytes/param optimizer+grad+master state by world size). The single best 'why do we need all these parallelisms' visual.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/parallelism-visualizer.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-parallelism-visualizer\">` containing: a `<div class=\"vt-title\">How a model is split across GPUs: DP, TP, PP, FSDP</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-parallelism-visualizer`) and an inline `<script>` (an IIFE; all element ids prefixed `parallelism-visualizer-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:parallelism-visualizer}}` on its own line into content/03-pretraining/06-distributed-model-parallel.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/parallelism-visualizer.html   (slug parallelism-visualizer)\nChapter with the marker: content/03-pretraining/06-distributed-model-parallel.md\n\nRead the tool file (and the chapter section around the {{tool:parallelism-visualizer}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/parallelism-visualizer.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-parallelism-visualizer / parallelism-visualizer- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:parallelism-visualizer}}` marker exists in content/03-pretraining/06-distributed-model-parallel.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "paged-attention-blocks", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: PagedAttention: KV cache as paged blocks  (slug: paged-attention-blocks)\nIt teaches / does:\nVisualize vLLM-style PagedAttention. Show the physical KV 'GPU memory' as a grid of fixed-size blocks (block_size slider, e.g. 16 tokens). Several sequences (rows) each grow token by token on a 'decode step'; each maps to a block table (logical->physical block ids) drawn as arrows into the physical grid, filling the last block partially. Contrast with a 'contiguous pre-allocation' toggle that reserves max_seq_len per sequence and shows the massive INTERNAL FRAGMENTATION / wasted memory the paged scheme avoids. Add prefix sharing: two sequences with a common prompt point their early block-table entries at the SAME physical blocks (copy-on-write). Show a utilization meter (used vs wasted). Teaches paging, the block table, near-zero fragmentation, and prefix/KV sharing. Real block accounting (ceil(seq/block_size) blocks, wasted = blocks*block_size - tokens).\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/paged-attention-blocks.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-paged-attention-blocks\">` containing: a `<div class=\"vt-title\">PagedAttention: KV cache as paged blocks</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-paged-attention-blocks`) and an inline `<script>` (an IIFE; all element ids prefixed `paged-attention-blocks-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:paged-attention-blocks}}` on its own line into content/04-kernels-efficiency/06-paged-attention-kv.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/paged-attention-blocks.html   (slug paged-attention-blocks)\nChapter with the marker: content/04-kernels-efficiency/06-paged-attention-kv.md\n\nRead the tool file (and the chapter section around the {{tool:paged-attention-blocks}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/paged-attention-blocks.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-paged-attention-blocks / paged-attention-blocks- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:paged-attention-blocks}}` marker exists in content/04-kernels-efficiency/06-paged-attention-kv.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "continuous-batching-scheduler", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Continuous (iteration-level) batching  (slug: continuous-batching-scheduler)\nIt teaches / does:\nAnimate iteration-level scheduling on a timeline. Requests arrive over time (each with its own prompt length + output length); a scheduler forms a running batch capped by max_num_seqs and a token budget. Step through iterations: at each decode iteration show the current running batch (a set of bars advancing one token), a finished request LEAVING mid-batch (its slot freed), and a waiting request ADMITTED into the freed slot immediately (that is the whole point vs static batching). Include a chunked-prefill toggle (a long prompt's prefill is split across iterations so it doesn't stall decodes) and a preemption event when KV memory is full (a request is swapped/recomputed). Show live GPU-utilization and throughput vs a 'static batching' comparison (which idles until the whole batch finishes). Controls: arrival rate, max batch, token budget. Real slot/token accounting; this is the core serving-throughput idea.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/continuous-batching-scheduler.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-continuous-batching-scheduler\">` containing: a `<div class=\"vt-title\">Continuous (iteration-level) batching</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-continuous-batching-scheduler`) and an inline `<script>` (an IIFE; all element ids prefixed `continuous-batching-scheduler-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:continuous-batching-scheduler}}` on its own line into content/07-inference-serving/02-continuous-batching.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/continuous-batching-scheduler.html   (slug continuous-batching-scheduler)\nChapter with the marker: content/07-inference-serving/02-continuous-batching.md\n\nRead the tool file (and the chapter section around the {{tool:continuous-batching-scheduler}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/continuous-batching-scheduler.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-continuous-batching-scheduler / continuous-batching-scheduler- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:continuous-batching-scheduler}}` marker exists in content/07-inference-serving/02-continuous-batching.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "claude-opus-5"}, {"slug": "induction-head", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: An induction head, mechanistically  (slug: induction-head)\nIt teaches / does:\nIllustrate the canonical induction-head circuit that implements in-context copying ([A][B]...[A]->[B]). Show a token sequence with a repeated pattern (e.g. '... D E ... D ?'). Two-part animation: (1) the PREVIOUS-TOKEN head writes, at each position, information about the token before it; (2) the INDUCTION head at the final [D] attends BACK to the position right after the earlier [D] (draw the attention arrow) and copies that token [E] to the output, predicting [E] next. Show the two heads' attention patterns as small heatmaps and the Q-composition between them (the induction head's query reads what the prev-token head wrote). Let the user edit the sequence / move the repeated token and watch the induction attention follow. Teaches attention-head composition, why induction heads underlie in-context learning, and how mech-interp reads a circuit. Deterministic pattern-matching model of the two heads.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/induction-head.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-induction-head\">` containing: a `<div class=\"vt-title\">An induction head, mechanistically</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-induction-head`) and an inline `<script>` (an IIFE; all element ids prefixed `induction-head-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:induction-head}}` on its own line into content/13-interp-safety-gov/01-mechanistic-interpretability.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/induction-head.html   (slug induction-head)\nChapter with the marker: content/13-interp-safety-gov/01-mechanistic-interpretability.md\n\nRead the tool file (and the chapter section around the {{tool:induction-head}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/induction-head.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-induction-head / induction-head- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:induction-head}}` marker exists in content/13-interp-safety-gov/01-mechanistic-interpretability.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "dpo-explorer", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: DPO: preferences into a policy, no reward model  (slug: dpo-explorer)\nIt teaches / does:\nShow how Direct Preference Optimization turns a (prompt, chosen, rejected) triple into a gradient without a separate reward model. For a preference pair, show the four log-probs (policy and reference, on chosen and rejected), the implicit reward r = beta*log(pi/pi_ref), the DPO logistic loss -log sigmoid(beta*(r_chosen - r_rejected)), and how a gradient step raises the chosen margin and lowers the rejected one (animate the two log-prob bars moving apart, with the reference anchoring them). A beta slider shows the KL-vs-sharpness tradeoff. Plot the loss as the margin grows. Teaches DPO as classification on preference pairs, the implicit reward, and beta's role. Real sigmoid/log-prob math on seeded numbers; contrast one line with PPO-RLHF.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/dpo-explorer.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-dpo-explorer\">` containing: a `<div class=\"vt-title\">DPO: preferences into a policy, no reward model</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-dpo-explorer`) and an inline `<script>` (an IIFE; all element ids prefixed `dpo-explorer-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:dpo-explorer}}` on its own line into content/05-posttraining-alignment/07-dpo-and-variants.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/dpo-explorer.html   (slug dpo-explorer)\nChapter with the marker: content/05-posttraining-alignment/07-dpo-and-variants.md\n\nRead the tool file (and the chapter section around the {{tool:dpo-explorer}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/dpo-explorer.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-dpo-explorer / dpo-explorer- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:dpo-explorer}}` marker exists in content/05-posttraining-alignment/07-dpo-and-variants.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "grpo-advantage", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: GRPO: group-relative advantages  (slug: grpo-advantage)\nIt teaches / does:\nVisualize GRPO's critic-free advantage. For one prompt, sample a GROUP of G responses (G slider); each gets a (verifiable) reward shown as a bar. Compute the group-relative advantage A_i = (r_i - mean(r)) / std(r) and draw each response's advantage as a signed bar (above/below the group mean line), so above-average responses are pushed up and below-average down \u2014 no value network needed. Show a degenerate all-correct or all-wrong group collapsing to zero advantage (motivating dynamic sampling / DAPO). A 'reward spread' control changes the variance. Teaches why the group mean is the baseline, the normalization, and the zero-variance failure. Real mean/std/advantage math; contrast with PPO's learned value baseline.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/grpo-advantage.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-grpo-advantage\">` containing: a `<div class=\"vt-title\">GRPO: group-relative advantages</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-grpo-advantage`) and an inline `<script>` (an IIFE; all element ids prefixed `grpo-advantage-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:grpo-advantage}}` on its own line into content/05-posttraining-alignment/08-grpo-rloo.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/grpo-advantage.html   (slug grpo-advantage)\nChapter with the marker: content/05-posttraining-alignment/08-grpo-rloo.md\n\nRead the tool file (and the chapter section around the {{tool:grpo-advantage}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/grpo-advantage.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-grpo-advantage / grpo-advantage- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:grpo-advantage}}` marker exists in content/05-posttraining-alignment/08-grpo-rloo.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "lora-lowrank", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: LoRA: adapting a weight with a low-rank update  (slug: lora-lowrank)\nIt teaches / does:\nShow W' = W + (alpha/r) B A. Draw the frozen d x d weight W as a grid, and the trainable A (r x d) and B (d x r) as thin strips; a rank slider r changes the strip width and updates the trainable-parameter count (2*r*d) and the compression vs full fine-tuning (d^2 / 2rd). Visualize the low-rank update BA as an outline heatmap over W and the scaling alpha/r. Include a QLoRA note (W stored in NF4, adapters in bf16). Teaches why a tiny r captures most of the useful update, the parameter savings, and the alpha/r scaling. Real param-count + rank math (this complements the ag-lora-params self-check with a geometric view).\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/lora-lowrank.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-lora-lowrank\">` containing: a `<div class=\"vt-title\">LoRA: adapting a weight with a low-rank update</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-lora-lowrank`) and an inline `<script>` (an IIFE; all element ids prefixed `lora-lowrank-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:lora-lowrank}}` on its own line into content/05-posttraining-alignment/03-peft-lora-qlora.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/lora-lowrank.html   (slug lora-lowrank)\nChapter with the marker: content/05-posttraining-alignment/03-peft-lora-qlora.md\n\nRead the tool file (and the chapter section around the {{tool:lora-lowrank}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/lora-lowrank.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-lora-lowrank / lora-lowrank- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:lora-lowrank}}` marker exists in content/05-posttraining-alignment/03-peft-lora-qlora.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "scaling-law-fit", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Fit your own scaling law and extrapolate  (slug: scaling-law-fit)\nIt teaches / does:\nInteractive scaling-law fitting. Show a handful of (N, D, loss) data points from small 'ladder' runs on a loss-vs-compute (log-log) plot; fit L = E + A/N^alpha + B/D^beta (or the compute form L = E + (C0/C)^a) via least squares and draw the fitted curve, then EXTRAPOLATE a dashed line to a target compute the user picks with a slider, showing the predicted loss. Let the user add noise to the points or drag one to see the fit + extrapolation update (and how a bad point misleads the extrapolation). Also mark the compute-optimal N*,D* for a chosen budget (the Chinchilla ~20 tokens/param point) and the over-trained regime. Teaches fit-then-extrapolate, why you run a ladder before the big run, and extrapolation risk. Real log-log least-squares fit (closed form).\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/scaling-law-fit.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-scaling-law-fit\">` containing: a `<div class=\"vt-title\">Fit your own scaling law and extrapolate</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-scaling-law-fit`) and an inline `<script>` (an IIFE; all element ids prefixed `scaling-law-fit-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:scaling-law-fit}}` on its own line into content/03-pretraining/04-scaling-laws.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/scaling-law-fit.html   (slug scaling-law-fit)\nChapter with the marker: content/03-pretraining/04-scaling-laws.md\n\nRead the tool file (and the chapter section around the {{tool:scaling-law-fit}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/scaling-law-fit.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-scaling-law-fit / scaling-law-fit- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:scaling-law-fit}}` marker exists in content/03-pretraining/04-scaling-laws.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "reward-model-preference", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Reward modeling from human preferences  (slug: reward-model-preference)\nIt teaches / does:\nShow how pairwise human preferences train a scalar reward model via the Bradley-Terry model. Present several (response A vs response B) comparisons with a human 'chosen' label; the model assigns each response a scalar reward, and P(A preferred) = sigmoid(r_A - r_B). Animate a training step raising the chosen reward and lowering the rejected one to match the labels (loss = -log sigmoid(r_chosen - r_rejected)); show the accuracy on the pairs climbing. Add a slider for label noise / disagreement showing the reward model's ceiling, and a note on reward over-optimization (Goodhart) when a policy exploits it. Teaches Bradley-Terry, why rewards are only meaningful up to differences (relative not absolute), and preference-data quality. Real sigmoid/BT math on seeded pairs.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/reward-model-preference.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-reward-model-preference\">` containing: a `<div class=\"vt-title\">Reward modeling from human preferences</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-reward-model-preference`) and an inline `<script>` (an IIFE; all element ids prefixed `reward-model-preference-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:reward-model-preference}}` on its own line into content/05-posttraining-alignment/05-rlhf-reward-modeling.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/reward-model-preference.html   (slug reward-model-preference)\nChapter with the marker: content/05-posttraining-alignment/05-rlhf-reward-modeling.md\n\nRead the tool file (and the chapter section around the {{tool:reward-model-preference}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/reward-model-preference.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-reward-model-preference / reward-model-preference- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:reward-model-preference}}` marker exists in content/05-posttraining-alignment/05-rlhf-reward-modeling.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "watermark-greenlist", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: LLM watermarking: the green-list rule  (slug: watermark-greenlist)\nIt teaches / does:\nIllustrate the Kirchenbauer-style green-list watermark. At each generation step, a hash of the previous token seeds a pseudo-random split of the vocabulary into a GREEN list (fraction gamma) and a RED list; a bias delta is added to green logits before sampling, so generated text contains more green tokens than chance. Visualize: the vocabulary colored green/red (re-seeded each step by the prior token), the sampled token, and a running green-token counter with a z-score detector (z = (observed_green - gamma*T)/sqrt(T*gamma*(1-gamma))) that crosses a detection threshold as text grows \u2014 while HUMAN text stays near chance. Sliders: gamma, delta (strength), sequence length. Show the quality-vs-detectability tradeoff (higher delta = easier detection but more distortion). Real hashing-split + z-score math; deterministic.\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/watermark-greenlist.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-watermark-greenlist\">` containing: a `<div class=\"vt-title\">LLM watermarking: the green-list rule</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-watermark-greenlist`) and an inline `<script>` (an IIFE; all element ids prefixed `watermark-greenlist-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:watermark-greenlist}}` on its own line into content/13-interp-safety-gov/04-watermarking-provenance.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/watermark-greenlist.html   (slug watermark-greenlist)\nChapter with the marker: content/13-interp-safety-gov/04-watermarking-provenance.md\n\nRead the tool file (and the chapter section around the {{tool:watermark-greenlist}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/watermark-greenlist.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-watermark-greenlist / watermark-greenlist- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:watermark-greenlist}}` marker exists in content/13-interp-safety-gov/04-watermarking-provenance.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}, {"slug": "long-context-rope-scaling", "build": "You are building ONE interactive, self-contained visualizer widget for a definitive LLM-systems web textbook. It must teach a concept through direct manipulation and be CORRECT, beautiful, and dependency-free.\n\nTool: Extending context: RoPE frequency scaling & YaRN  (slug: long-context-rope-scaling)\nIt teaches / does:\nShow how a model trained at length L_train is extended to a longer L_target by rescaling RoPE frequencies. Plot the rotation angle vs position for each RoPE dimension pair; a method selector: none (angles run off the trained range past L_train -> extrapolation failure), position-interpolation (compress positions by L_train/L_target so max angle stays in-range), NTK-aware / base scaling (increase theta so low frequencies stretch while high frequencies stay), YaRN (per-frequency: interpolate low freqs, keep high freqs, with a temperature). Highlight the 'in-distribution vs out-of-distribution angle' band the model saw in training, and show which method keeps positions inside it. Sliders: L_train, L_target, theta. Teaches why naive extrapolation breaks and how each scaling keeps the effective angles trained. Real RoPE angle math (m/theta^(2i/d) with the scaling applied).\n\nWRITE the widget to EXACTLY: /local-ssd/pk669/programming/llm-stack-textbook/tools/long-context-rope-scaling.html\nStructure (match the book's other tools):\n  - A single root `<div class=\"viz-tool\" id=\"tool-long-context-rope-scaling\">` containing: a `<div class=\"vt-title\">Extending context: RoPE frequency scaling & YaRN</div>`, the controls (labels + inputs/sliders/selects, grouped in a `<div class=\"vt-grid\">`), the visualization area (inline SVG and/or DOM/canvas), a live readout, and a short `<div class=\"vt-note\">` explaining the takeaway.\n  - A scoped `<style>` (EVERY selector prefixed with `#tool-long-context-rope-scaling`) and an inline `<script>` (an IIFE; all element ids prefixed `long-context-rope-scaling-` to avoid collisions; no globals leaking).\nHARD CONSTRAINTS:\n  - COMPLETELY SELF-CONTAINED: no external scripts, stylesheets, fonts, images, CDNs, network/fetch/XHR. Pure vanilla JS + inline SVG/canvas. It must work offline pasted into a page.\n  - CORRECTNESS IS THE POINT: implement the REAL math (softmax with max-subtraction, exact BPE merges, exact optimizer updates, exact KV-cache bytes, exact quantization scale/round/dequant, exact online-softmax recurrence, etc.). Deterministic where randomness is used (seed a small PRNG). No fabricated formulas.\n  - THEME-SAFE (light + dark): NEVER hardcode a primary color. Use CSS vars with hex fallbacks: var(--accent,#4f46e5), var(--ink,#1a1a2e), var(--ink-soft,#556), var(--muted,#889), var(--surface,#fff), var(--surface-2,#f6f7f9), var(--surface-3,#eceef3), var(--border,#e5e7eb), var(--border-2,#e5e7eb), var(--good,#2f9e6e), var(--warn,#e0a106), var(--mono,monospace), var(--sans,sans-serif). Text must be readable in both themes.\n  - RESPONSIVE: fluid width, SVG uses viewBox, wide content scrolls inside its own container; never overflow the page horizontally. Inputs are keyboard-accessible with `<label>`s; add aria-labels.\n  - ASCII-only in code/markup (use ->, <=, x, theta written out, HTML entities like &times; &rarr; &sigma; are fine). No console errors; guard against empty/invalid input.\nINTERACTIVITY: it must update live on input (attach event listeners; render once on load). Keep it snappy.\nThen INSERT the marker `{{tool:long-context-rope-scaling}}` on its own line into content/03-pretraining/13-long-context-pretraining.md at the most pedagogically relevant spot (right after the section that explains this concept; a blank line before and after). Do NOT remove or alter any existing content, code fence, figure marker, or exercise.\nReturn a short JSON summary (file, inserted_marker, controls, what_it_teaches).", "verify": "You are the Opus verifier for ONE interactive textbook visualizer. Make it correct, dependency-free, theme-safe, and genuinely instructive, then fix it in place.\n\nTool file: /local-ssd/pk669/programming/llm-stack-textbook/tools/long-context-rope-scaling.html   (slug long-context-rope-scaling)\nChapter with the marker: content/03-pretraining/13-long-context-pretraining.md\n\nRead the tool file (and the chapter section around the {{tool:long-context-rope-scaling}} marker). Verify HARD and FIX in place (rewrite /local-ssd/pk669/programming/llm-stack-textbook/tools/long-context-rope-scaling.html):\n  1. CORRECTNESS: re-derive the math the widget computes and check the code implements it exactly (softmax/BPE/optimizer/KV-cache/quantization/online-softmax/etc.). Fix any wrong formula, off-by-one, or misleading visualization. Verify determinism (seeded PRNG).\n  2. SELF-CONTAINED: NO external scripts/styles/fonts/images/CDN/network. If any exist, inline or remove them. Pure vanilla JS + inline SVG/canvas only.\n  3. JS VALIDITY: the inline <script> must be syntactically valid and error-free on load and on every control change; guard invalid/empty inputs. (Mentally execute the event handlers.)\n  4. THEME-SAFETY: no hardcoded primary colors; CSS vars with hex fallbacks; readable in light AND dark. All selectors and element ids scoped/prefixed to #tool-long-context-rope-scaling / long-context-rope-scaling- (no collisions with other tools on the /tools hub page, which concatenates all of them).\n  5. UX: labeled, keyboard-accessible controls; responsive (viewBox SVG, no horizontal page overflow); a clear vt-title and a vt-note stating the takeaway; live updates.\n  6. MARKER: confirm exactly one `{{tool:long-context-rope-scaling}}` marker exists in content/03-pretraining/13-long-context-pretraining.md at a sensible spot and that no other chapter content/code/figure/exercise was damaged. Fix if needed.\nReturn the verdict object (file, correct, self_contained, theme_safe, issues_fixed, notes).", "model": "sonnet"}];
const BSCHEMA = {"type": "object", "additionalProperties": false, "required": ["file", "what_it_teaches"], "properties": {"file": {"type": "string"}, "inserted_marker": {"type": "boolean"}, "controls": {"type": "array", "items": {"type": "string"}}, "what_it_teaches": {"type": "string"}}};
const VSCHEMA = {"type": "object", "additionalProperties": false, "required": ["file", "correct", "self_contained", "theme_safe", "notes"], "properties": {"file": {"type": "string"}, "correct": {"type": "boolean"}, "self_contained": {"type": "boolean"}, "theme_safe": {"type": "boolean"}, "issues_fixed": {"type": "integer"}, "notes": {"type": "string"}}};
log('Building ' + JOBS.length + ' interactive tools (build -> Opus verify)…');
const results = await pipeline(
  JOBS,
  function (j) {
    return agent(j.build, { label: 'build:' + j.slug, phase: 'Build', model: j.model, schema: BSCHEMA })
      .then(function (r) { return j; });
  },
  async function (j) {
    if (!j) return null;
    const v = await agent(j.verify, { label: 'verify:' + j.slug, phase: 'Verify', model: 'claude-opus-5', schema: VSCHEMA });
    return { slug: j.slug, verify: v };
  }
);
const done = results.filter(Boolean);
log('Interactive tools: ' + done.length + '/' + JOBS.length + ' built+verified.');
return { total: JOBS.length, done: done.length, results: results };
