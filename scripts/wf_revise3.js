export const meta = {
  name: 'revise-b3',
  description: 'Deepen/fix 8 chapters to CS336 buildability standard',
  phases: [{ title: 'Spec' }, { title: 'Apply' }, { title: 'Verify' }],
}

const WORK = [
  {
    "target": "03-pretraining/11-training-stability",
    "kind": "REVISE",
    "n_critical": 1,
    "n_total": 1,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/03-pretraining/11-training-stability.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 1 audited deficits (1 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [critical/correctness-bug] (from C2-debug-and-verify)\n     MISSING: The Adam spike-magnitude worked example is internally inconsistent: the spike update uses (1-beta1)=0.1 (beta1=0.9) giving 3e-3, but the 'normal' update applies an extra (1-beta1) factor and reports 3e-6, which requires beta1=0.99. With beta1=0.9 throughout, the normal update is 3e-5 and the spike is ~100x normal, not the claimed '1000x'. The follow-on claim that clipping limits damage 'to ~10x normal' (0.03 vs 3e-6 is 10,000x; vs 3e-5 is 1000x) is also inconsistent.\n     SUGGESTED FIX: Redo the worked example with one consistent beta1=0.9: normal steady-state update ~alpha (m-hat converges to g), spike update ~alpha*(1-beta1)*g/sqrt(v-hat-old); state the ratio correctly (~100x under the fresh-moment assumption) and recompute the with-clipping ratio to match.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/11-training-stability.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/11-training-stability.md\n\nThe chapter was just edited to resolve these deficits:\n  - [critical] The Adam spike-magnitude worked example is internally inconsistent: the spike update uses (1-beta1)=0.1 (beta1=0.9) giving 3e-3, but the 'normal' update applies an extra (1-beta1) \n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "04-kernels-efficiency/08-quantization-formats-qat",
    "kind": "REVISE",
    "n_critical": 1,
    "n_total": 1,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/04-kernels-efficiency/08-quantization-formats-qat.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 1 audited deficits (1 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [critical/correctness-bug] (from C3-open-source-stack)\n     MISSING: The Q4_K_M internal structure inverts the precision hierarchy: the book says the super-block stores '2 high-precision (6-bit) scales and mins' and 'each sub-block has a 4-bit scale'. In llama.cpp's actual block_q4_K, the super-block scale d and min dmin are fp16, and the 8 sub-block scales/mins are 6-bit (packed in scales[12]); weights are 4-bit.\n     SUGGESTED FIX: Correct to: fp16 super-block scale/min, 6-bit sub-block scales and mins for the 8 sub-blocks of 32 weights, 4-bit quantized weights; cite ggml's block_q4_K struct as the source of truth.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/08-quantization-formats-qat.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/08-quantization-formats-qat.md\n\nThe chapter was just edited to resolve these deficits:\n  - [critical] The Q4_K_M internal structure inverts the precision hierarchy: the book says the super-block stores '2 high-precision (6-bit) scales and mins' and 'each sub-block has a 4-bit scale\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "02-transformer/05-positional-encoding",
    "kind": "REVISE",
    "n_critical": 1,
    "n_total": 1,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/02-transformer/05-positional-encoding.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 1 audited deficits (1 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [critical/correctness-bug] (from L03-architectures-hyperparameters)\n     MISSING: YaRN attention-temperature formula is stated as 'multiplies the attention logits by a constant 1/t = 0.1 ln(s) + 1'. The YaRN paper defines sqrt(1/t) = 0.1 ln(s) + 1; the scalar mscale = 0.1 ln(s) + 1 is folded into the cos/sin cache (multiplying both q and k), so the logits are effectively scaled by (0.1 ln(s) + 1)^2, not by 0.1 ln(s) + 1. As written, a reader implementing YaRN from this text would use the wrong temperature.\n     SUGGESTED FIX: Correct to: sqrt(1/t) = 0.1 ln(s) + 1, i.e. logits scaled by (0.1 ln s + 1)^2; note that implementations fold mscale = 0.1 ln(s)+1 into the cos/sin tables so the square arises automatically from applying it to both q and k.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/05-positional-encoding.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/05-positional-encoding.md\n\nThe chapter was just edited to resolve these deficits:\n  - [critical] YaRN attention-temperature formula is stated as 'multiplies the attention logits by a constant 1/t = 0.1 ln(s) + 1'. The YaRN paper defines sqrt(1/t) = 0.1 ln(s) + 1; the scalar ms\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "04-kernels-efficiency/06-paged-attention-kv",
    "kind": "REVISE",
    "n_critical": 1,
    "n_total": 1,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/04-kernels-efficiency/06-paged-attention-kv.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 1 audited deficits (1 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [critical/correctness-bug] (from L10-inference)\n     MISSING: Off-by-one in the 'Minimal Paged Serving Loop': serve_step writes the new token's KV at slot_index(seq_id, cur_len) BEFORE growing the block table, but append_token(seq_id, s['len']) only allocates a new block after the write. When prompt_len (or any current length) is an exact multiple of block_size, slot_index indexes table[cur_len // block_size], one past the last allocated block, and raises IndexError. E.g. prompt_len=32, block_size=16: allocate() gives 2 blocks (positions 0-31); the first decode write at position 32 reads table[2], which does not exist.\n     SUGGESTED FIX: Either call block_mgr.append_token (i.e., grow the table when cur_len % block_size == 0) BEFORE computing slot_index for the write, or make allocate() reserve ceil((num_tokens+1)/block_size) blocks. Add a boundary-case test (prompt length exactly divisible by block_size) to the code listing.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/06-paged-attention-kv.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/04-kernels-efficiency/06-paged-attention-kv.md\n\nThe chapter was just edited to resolve these deficits:\n  - [critical] Off-by-one in the 'Minimal Paged Serving Loop': serve_step writes the new token's KV at slot_index(seq_id, cur_len) BEFORE growing the block table, but append_token(seq_id, s['len'\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "10-multimodal-and-arch/05-unified-any-to-any",
    "kind": "REVISE",
    "n_critical": 1,
    "n_total": 1,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/10-multimodal-and-arch/05-unified-any-to-any.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 1 audited deficits (1 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [critical/correctness-bug] (from L17-alignment-multimodality)\n     MISSING: transfusion_attention_mask() never grants attention for j > i, even within the same image block ('if j > i: pass'), so the produced mask is purely causal. This directly contradicts its own docstring ('all image positions in the same block (bidirectional within-image)') and Transfusion's defining design of bidirectional attention inside image blocks. A reader copying this code trains with the wrong mask.\n     SUGGESTED FIX: Fix the loop so image position i also attends to j > i when image_block[j] == image_block[i], and make the 'Quick test' assert the bidirectional property (e.g., assert mask[3,6] and mask[6,3] for a 4-token image block) instead of just printing the mask.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/10-multimodal-and-arch/05-unified-any-to-any.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/10-multimodal-and-arch/05-unified-any-to-any.md\n\nThe chapter was just edited to resolve these deficits:\n  - [critical] transfusion_attention_mask() never grants attention for j > i, even within the same image block ('if j > i: pass'), so the produced mask is purely causal. This directly contradicts\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "03-pretraining/04-scaling-laws",
    "kind": "REVISE",
    "n_critical": 0,
    "n_total": 13,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/03-pretraining/04-scaling-laws.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 13 audited deficits (0 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [major/code] (from A3-scaling)\n     MISSING: The IsoFLOP method (Chinchilla approach 2) \u2014 the method the deliverable is named after \u2014 gets exactly one prose sentence ('fits a parabola in log N to find the valley... then fits a power law through those valleys') with no code, no worked example, and no discussion of how many points per slice, quadratic-fit robustness, or extracting D_opt from each valley. Only the parametric fit (approach 3) is fully implemented.\n     SUGGESTED FIX: Add a runnable IsoFLOP section: group synthetic runs into 4-6 fixed-C slices, fit a quadratic in log N per slice (np.polyfit), extract N_opt(C) and D_opt(C)=C/(6*N_opt), fit log N_opt vs log C to recover exponent a, and cross-check a against the parametric fit's beta/(alpha+beta). Include the expected recovered exponent as a printed check.\n  [major/explanation] (from A3-scaling)\n     MISSING: No concrete procedure for DESIGNING a sweep under a constrained experimentation budget. The chapter says 'run a grid of small models at varying N and D' and lists pitfalls (unconverged runs, LR schedule matched to token count), but never answers: given a sweep budget of, say, 1-2% of the final run's FLOPs, how many compute slices, how many models per slice, how to space N around the expected optimum, how to set per-run LR/batch (linking the ch. 3.10 scaling rules), and how the run-table FLOPs sum to the budget.\n     SUGGESTED FIX: Add a worked sweep-design example: 'You have 2e18 FLOPs for experiments before a 1e21 run.' Produce an explicit run table (C slice, N values, D values, LR, batch, per-run FLOPs, total) using log-spaced slices and ~6 models per slice spanning ~1 order of magnitude in N, with LR chosen via muP or the batch-scaling rules, and total cost verified against the budget. This is the exact skill the assignment demands.\n  [major/correctness-bug] (from L09-scaling-laws-1)\n     MISSING: The text claims 'The tokens_per_param column comes out roughly constant across the four budgets (it drifts only because alpha != beta exactly)'. Running the chapter's own chinchilla_optimal code with the given constants (A=406.4, B=410.7, alpha=0.34, beta=0.28) prints tok/param = 32.1, 50.1, 78.2, 122.1 for C = 1e19..1e25 \u2014 a 4x drift that is never near 20. D/N scales as C^{(alpha-beta)/(alpha+beta)} = C^0.097, i.e. x1.25 per decade of compute, which is not 'roughly constant' over 6 decades.\n     SUGGESTED FIX: Either (a) demonstrate constancy with alpha = beta (e.g. both 0.34), noting that Chinchilla's Approaches 1-2 fitted a ~ b ~ 0.5, and separately show the Approach-3 constants produce a slow C^0.1 drift; or (b) print the actual expected output in the text and rewrite the sentence to say the ratio drifts by ~25% per decade of compute and exceeds 20 tok/param at frontier budgets under the Approach-3 fit (a known property of that fit). Show the expected numeric output of the loop either way.\n  [major/verification] (from L11-scaling-laws-2)\n     MISSING: The 'Fitting a Scaling Law From Scratch' demo has no stated expected values or tolerance, and when actually run it recovers A=878 vs ground-truth 406 and E=1.83 vs 1.69 (from just 2% noise on 42 points), overpredicting extrapolated loss at C=1e25 by ~0.1 nats. The text never mentions this identifiability problem (E/A/alpha are strongly correlated), so a reader who runs the code sees a 2x parameter mismatch with no way to know whether it is a bug or expected behavior. There is also no quantitative uncertainty treatment (bootstrap/Hessian confidence intervals on alpha, beta, E; prediction intervals on extrapolated loss), despite 'fit diagnostics and confidence' being a core objective.\n     SUGGESTED FIX: Add a paragraph after the fit acknowledging the parameter-correlation/identifiability issue with expected recovery ranges, then add ~20 lines of bootstrap resampling (refit on resampled grids, report percentile CIs on exponents and a fan of extrapolated-loss predictions). Note the compensating-error phenomenon the demo exhibits: the allocation N*(C) extrapolates far better than the loss L*(C) because the loss surface is flat near the optimum. Cite the Epoch AI Chinchilla replication ('Chinchilla Scaling: A replication attempt', Besiroglu et al. 2024) as the real-world case where the original fit's parametric estimates were contested.\n  [minor/correctness-bug] (from A3-scaling)\n     MISSING: The emergent-abilities demo defines smooth_per_token_accuracy(N) = 1 - 0.9*(N/1e9)**-0.12, which is NEGATIVE for N < ~4.2e8; since Ns = np.logspace(8, 12, 9), the first two printed rows show per-token 'accuracy' of about -0.19 and -0.03 (only exact_match clips). Verified numerically.\n     SUGGESTED FIX: Clip inside smooth_per_token_accuracy (np.clip(..., 0, 1)) or start Ns at 1e9 so the printed table shows valid probabilities.\n  [minor/code] (from A3-scaling)\n     MISSING: predict_log_loss's comment claims 'log-sum-exp form keeps it numerically stable', but the code exponentiates each term and sums directly \u2014 no logsumexp is used. Harmless at these magnitudes but the comment misdescribes the code and misattributes Chinchilla's actual LSE trick.\n     SUGGESTED FIX: Either use scipy.special.logsumexp over the stacked terms (matching Hoffmann et al.'s actual objective) or correct the comment to say the parameterization via logs keeps the fitted constants positive.\n  [minor/exercise] (from A3-scaling)\n     MISSING: No end-of-chapter exercises rehearsing the full assignment loop (design sweep -> fit both methods -> project N*, D* at 10x budget), and component-to-LOSS attribution (e.g., how loss responds to aspect ratio, head count, batch/LR misconfiguration at fixed FLOPs) is only qualitative, spread across 02-transformer/10 and 03-pretraining/10.\n     SUGGESTED FIX: Add 2-3 exercises: (a) hold out the largest synthetic run, fit on the rest, verify extrapolated loss within 2%; (b) refit with under-converged runs deliberately included and observe the Kaplan-style bias in the exponents; (c) given a fixed C slice, predict the loss penalty of training at 2x and 0.5x the optimal N.\n  [minor/verification] (from C1-end-to-end-pretrain)\n     MISSING: Expected loss/perplexity-vs-tokens milestones for a real BPE run are absent. build-gpt states the toy expectation (first loss ~= ln(65)=4.17, <1.5 within a couple thousand steps) and the objective chapter gives the init sanity check (loss ~= ln V), but there is no 'a compute-optimal 125M model on ~2.5B tokens should reach ~X nats/token / BPB ~Y' checkpoint a reader can validate a real run against.\n     SUGGESTED FIX: Add 2-3 concrete (N, D, expected held-out nats/token and BPB) reference points for small models (e.g. 125M and 1B at Chinchilla-optimal tokens) so a reader can confirm their real run is on-curve, not just their toy run.\n  [minor/code] (from L09-scaling-laws-1)\n     MISSING: IsoFLOP profiles are an explicit learning objective but receive only two sentences of prose ('runs many models at each of several fixed compute budgets, then fits a parabola in log N to find the valley... finally fits a power law through those valleys'). There is no implementation of Chinchilla Approach 2, no worked example of the parabola fit in log N, and no guidance on grid design (how many N values per budget, how wide a range). Only Approach 3 (parametric fit) is implemented.\n     SUGGESTED FIX: Add a ~30-line code block implementing the IsoFLOP method on the same synthetic grid: for each of 4-5 fixed C budgets, sweep N (with D = C/6N), fit a quadratic in log N to the losses, extract the valley N*(C), then fit log N* vs log C to recover the exponent a; show it agrees with the parametric fit's beta/(alpha+beta). Populate the {{fig:scaling-kaplan-vs-chinchilla}} placeholder with an isoFLOP-valley illustration.\n  [minor/verification] (from L09-scaling-laws-1)\n     MISSING: The fitting code's expected recovery quality is not stated. Running it as written recovers exponents well (alpha 0.390 vs true 0.340, beta 0.290 vs 0.280) but E and A poorly (E 1.826 vs 1.69, A 878.5 vs 406.0) due to ill-conditioning of the additive power-law fit at 2% noise. A reader who runs the code and sees A off by 2x has no way to know whether their run succeeded.\n     SUGGESTED FIX: Print/state the expected recovered values for the fixed seed, and add a sentence explaining that E/A/B are weakly identified while the exponents (which determine the allocation) recover much more tightly \u2014 and that the right accuracy check is the implied allocation exponent beta/(alpha+beta) and the extrapolated loss, not the raw constants.\n  [minor/code] (from L11-scaling-laws-2)\n     MISSING: Chinchilla's IsoFLOP-profile method (method 2) is described in one prose bullet but never implemented, even though it is the most practical method for a small sweep and the most robust to misspecification of the parametric form.\n     SUGGESTED FIX: Add ~30 lines: at each of several fixed compute budgets, fit a parabola to loss vs log(N), extract the valley N_opt(C), then fit log N_opt vs log C by least squares to get the exponent a directly. Show it agrees with the parametric fit's beta/(alpha+beta).\n  [minor/correctness-bug] (from L11-scaling-laws-2)\n     MISSING: Comment in predict_log_loss claims 'log-sum-exp form keeps it numerically stable', but the code simply exponentiates each term and sums (np.exp(terms[0]) + np.exp(terms[1]) + np.exp(terms[2])) \u2014 no max-subtraction, no scipy.special.logsumexp. It is not an LSE implementation and provides no stability benefit over computing the loss directly.\n     SUGGESTED FIX: Either use scipy.special.logsumexp(terms, axis=0) as Chinchilla's Appendix actually does, or delete the misleading comment.\n  [minor/explanation] (from L11-scaling-laws-2)\n     MISSING: Guidance on designing the real small-model sweep is thin: how many runs, what N/D spread (orders of magnitude needed), that each run needs its cosine decay matched to its own token endpoint (stated as Kaplan's bug but not turned into a sweep-design checklist), and how to hold out the largest run for extrapolation validation in code rather than prose.\n     SUGGESTED FIX: Add a short 'designing your sweep' checklist (>=2 orders of magnitude in N, >=5 points per axis, per-run LR decay to endpoint, fixed data distribution and tokenizer across runs, several seeds at the smallest scale to estimate noise) and a 5-line code cell that refits with the largest-N runs held out and reports prediction error on them.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/04-scaling-laws.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/04-scaling-laws.md\n\nThe chapter was just edited to resolve these deficits:\n  - [major] The IsoFLOP method (Chinchilla approach 2) \u2014 the method the deliverable is named after \u2014 gets exactly one prose sentence ('fits a parabola in log N to find the valley... then fits \n  - [major] No concrete procedure for DESIGNING a sweep under a constrained experimentation budget. The chapter says 'run a grid of small models at varying N and D' and lists pitfalls (unconve\n  - [major] The text claims 'The tokens_per_param column comes out roughly constant across the four budgets (it drifts only because alpha != beta exactly)'. Running the chapter's own chinchill\n  - [major] The 'Fitting a Scaling Law From Scratch' demo has no stated expected values or tolerance, and when actually run it recovers A=878 vs ground-truth 406 and E=1.83 vs 1.69 (from just \n  - [minor] The emergent-abilities demo defines smooth_per_token_accuracy(N) = 1 - 0.9*(N/1e9)**-0.12, which is NEGATIVE for N < ~4.2e8; since Ns = np.logspace(8, 12, 9), the first two printed\n  - [minor] predict_log_loss's comment claims 'log-sum-exp form keeps it numerically stable', but the code exponentiates each term and sums directly \u2014 no logsumexp is used. Harmless at these m\n  - [minor] No end-of-chapter exercises rehearsing the full assignment loop (design sweep -> fit both methods -> project N*, D* at 10x budget), and component-to-LOSS attribution (e.g., how los\n  - [minor] Expected loss/perplexity-vs-tokens milestones for a real BPE run are absent. build-gpt states the toy expectation (first loss ~= ln(65)=4.17, <1.5 within a couple thousand steps) a\n  - [minor] IsoFLOP profiles are an explicit learning objective but receive only two sentences of prose ('runs many models at each of several fixed compute budgets, then fits a parabola in log\n  - [minor] The fitting code's expected recovery quality is not stated. Running it as written recovers exponents well (alpha 0.390 vs true 0.340, beta 0.290 vs 0.280) but E and A poorly (E 1.8\n  - [minor] Chinchilla's IsoFLOP-profile method (method 2) is described in one prose bullet but never implemented, even though it is the most practical method for a small sweep and the most ro\n  - [minor] Comment in predict_log_loss claims 'log-sum-exp form keeps it numerically stable', but the code simply exponentiates each term and sums (np.exp(terms[0]) + np.exp(terms[1]) + np.ex\n  - [minor] Guidance on designing the real small-model sweep is thin: how many runs, what N/D spread (orders of magnitude needed), that each run needs its cosine decay matched to its own token\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "02-transformer/01-tokenization",
    "kind": "REVISE",
    "n_critical": 0,
    "n_total": 8,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/02-transformer/01-tokenization.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 8 audited deficits (0 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [major/code] (from A1-basics)\n     MISSING: No complete end-to-end byte-level BPE tokenizer. The trainer (train_bpe) is character-level with a '</w>' end-of-word marker; the bytes_to_unicode map is given separately, but they are never assembled: there is no vocab-to-integer-ID table, no encode(str)->List[int], no decode(List[int])->str implementation (decode is prose only), no special-token handling in encode/decode, and the GPT-2 pre-tokenization regex is described but never shown. The '</w>' convention conflicts with the byte-level '\u0120' convention and the reader is not told to drop it when going byte-level.\n     SUGGESTED FIX: Add a complete `ByteLevelBPETokenizer` class: train on raw text via regex pre-tokenization (print the actual GPT-2/cl100k regex) over byte-mapped symbols without '</w>', build merges + vocab->ID table, implement encode()->List[int] and decode()->str (inverting bytes_to_unicode, bytes.decode(errors='replace')), handle special tokens, and verify with a round-trip test on a unicode string plus a comparison against tiktoken('gpt2') or minbpe output on a fixed input.\n  [major/verification] (from C2-debug-and-verify)\n     MISSING: The named 'tokenizer round-trip' test is absent as code. The chapter asserts the GPT-2 bytes-to-unicode map (b2u[32]=='G-dot', 256 distinct chars) and states in prose that byte-level BPE decodes losslessly, but there is no decode() implementation for the from-scratch BPE, no explicit `assert decode(encode(s)) == s` test over adversarial inputs (multi-byte unicode, emoji, invalid UTF-8, leading spaces, special tokens), and no cross-validation of the from-scratch encoder against a reference (tiktoken/HF) on identical text.\n     SUGGESTED FIX: Add a decode() for the byte-level BPE (ids -> merged symbols -> b2u-inverse -> bytes -> utf-8 with errors='replace'), a round-trip test loop over an adversarial string list, and a short harness that encodes the same corpus with the from-scratch tokenizer and tiktoken gpt2 and asserts identical token counts/ids when using GPT-2's merges.\n  [major/code] (from L01-tokenization)\n     MISSING: No end-to-end byte-level BPE tokenizer. The runnable trainer (train_bpe/encode_word) is character-level with a '</w>' end-of-word marker, while the byte-level section provides only the bytes_to_unicode map. The glue -- pre-tokenize into chunks, map UTF-8 bytes through the table, train WITHOUT '</w>' (GPT-2 does not use it), assign integer IDs, and decode ids -> printable symbols -> bytes -> UTF-8 text -- is described in one prose sentence but never shown as code. The reader must reconcile two incompatible schemes ('</w>' marker vs. byte-level leading-space chunks) on their own.\n     SUGGESTED FIX: Add a ~60-line ByteLevelBPETokenizer class combining the existing pieces: train(corpus) using pre-tokenized byte-mapped chunks (no '</w>'), encode(text) -> list[int], decode(ids) -> str (invert b2u, bytes(...).decode('utf-8', errors='replace')), with an explicit ID convention (256 base byte tokens + one ID per merge in learned order) and a round-trip assert: decode(encode(s)) == s on a string containing emoji, CJK, and leading spaces.\n  [major/code] (from L01-tokenization)\n     MISSING: The GPT-2/GPT-4 pre-tokenization regex is repeatedly invoked ('BPE merges only within a chunk', 'why cl100k keeps digits in groups of at most three') but the actual pattern is never given, so a reader cannot reproduce the merge-boundary behavior of a real byte-level tokenizer, nor handle whitespace attachment (the ' hello' single-token behavior the chapter itself highlights).\n     SUGGESTED FIX: Print the actual GPT-2 pattern ('s|'t|'re|'ve|'m|'ll|'d| ?\\p{L}+| ?\\p{N}+| ?[^\\s\\p{L}\\p{N}]+|\\s+(?!\\S)|\\s+) and the cl100k digit-group change, explain each alternation in one line, and show it feeding chunks into the BPE trainer.\n  [minor/correctness-bug] (from L01-tokenization)\n     MISSING: Line ~248 claims 'GPT-2, GPT-3, GPT-4, Llama, Mistral ... use byte-level BPE', but line ~366 (and the comparison table) says Llama and early Mistral use SentencePiece. In fact Llama-1/2 and early Mistral use SentencePiece BPE with byte-fallback (not GPT-2-style byte-level BPE); only Llama-3 switched to a tiktoken-style byte-level tokenizer. The two passages contradict each other.\n     SUGGESTED FIX: State the lineage precisely: Llama-1/2 and Mistral-v0.x = SentencePiece BPE (32k, byte-fallback, '\u2581' marker); Llama-3 = tiktoken-style byte-level BPE (128k). Remove Llama/Mistral from the blanket byte-level claim.\n  [minor/correctness-bug] (from L01-tokenization)\n     MISSING: Claim that 'Llama models split numbers into individual digits' is only true of Llama-1/2; Llama-3 tokenizes digits in groups of up to three (like cl100k). Stated unqualified, a reader inspecting Llama-3 tokens would find the book wrong.\n     SUGGESTED FIX: Qualify: Llama-1/2 and PaLM split per digit; Llama-3/GPT-4 group digits 1-3 at a time via the pre-tokenizer regex.\n  [minor/verification] (from L01-tokenization)\n     MISSING: No verification against a reference implementation for the byte-level deliverable: the chapter never shows checking the from-scratch encoder's output token-by-token against tiktoken/minbpe on the same merge table, and gives no expected vocab/merge counts for a small real corpus (e.g. train 500 merges on a few MB and report bytes-per-token).\n     SUGGESTED FIX: Add a short 'verify your tokenizer' block: load GPT-2's vocab.json/merges.txt into the from-scratch encoder and assert equality with tiktoken's gpt2 encoding on a test paragraph; report expected bytes/token (~4.0 for English at 50k vocab).\n  [minor/resource-accounting] (from L01-tokenization)\n     MISSING: Training-at-scale cost is not treated: the shown trainer recounts every pair from scratch each merge (O(corpus) per merge). Real trainers (HF tokenizers, minbpe on large corpora, CS336 assignment 1) update pair counts incrementally and parallelize pre-tokenization; nothing tells the reader what to expect when training 32k merges on gigabytes.\n     SUGGESTED FIX: Add a paragraph on incremental pair-count updates (only pairs adjacent to a merge site change), unique-word compression of the corpus, and a rough wall-clock estimate (naive vs incremental) for a 1 GB corpus at 32k merges.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/01-tokenization.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/01-tokenization.md\n\nThe chapter was just edited to resolve these deficits:\n  - [major] No complete end-to-end byte-level BPE tokenizer. The trainer (train_bpe) is character-level with a '</w>' end-of-word marker; the bytes_to_unicode map is given separately, but they\n  - [major] The named 'tokenizer round-trip' test is absent as code. The chapter asserts the GPT-2 bytes-to-unicode map (b2u[32]=='G-dot', 256 distinct chars) and states in prose that byte-lev\n  - [major] No end-to-end byte-level BPE tokenizer. The runnable trainer (train_bpe/encode_word) is character-level with a '</w>' end-of-word marker, while the byte-level section provides only\n  - [major] The GPT-2/GPT-4 pre-tokenization regex is repeatedly invoked ('BPE merges only within a chunk', 'why cl100k keeps digits in groups of at most three') but the actual pattern is neve\n  - [minor] Line ~248 claims 'GPT-2, GPT-3, GPT-4, Llama, Mistral ... use byte-level BPE', but line ~366 (and the comparison table) says Llama and early Mistral use SentencePiece. In fact Llam\n  - [minor] Claim that 'Llama models split numbers into individual digits' is only true of Llama-1/2; Llama-3 tokenizes digits in groups of up to three (like cl100k). Stated unqualified, a rea\n  - [minor] No verification against a reference implementation for the byte-level deliverable: the chapter never shows checking the from-scratch encoder's output token-by-token against tiktoke\n  - [minor] Training-at-scale cost is not treated: the shown trainer recounts every pair from scratch each merge (O(corpus) per merge). Real trainers (HF tokenizers, minbpe on large corpora, C\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  },
  {
    "target": "03-pretraining/06-distributed-model-parallel",
    "kind": "REVISE",
    "n_critical": 0,
    "n_total": 5,
    "spec": "You are the SPEC author (Opus) for a per-chapter revision of the textbook \"The LLM Stack\".\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter file: content/03-pretraining/06-distributed-model-parallel.md\n\nRead the ENTIRE chapter file first. Then read any chapter it cross-references that matters.\nThis chapter has 5 audited deficits (0 critical). Your job is to turn\neach into a precise, correct edit instruction that a drafting agent can apply mechanically.\n\nDEFICITS TO RESOLVE:\n  [major/code] (from L08-parallelism-2)\n     MISSING: TP coverage omits the vocab-parallel embedding and the parallel (vocab-sharded) cross-entropy loss \u2014 the embedding and LM head are the largest single matrices in the model, and an end-to-end TP transformer cannot be assembled without sharding them (Megatron's VocabParallelEmbedding masking + all-reduce trick, and the max-subtraction + two-all-reduce parallel softmax cross-entropy).\n     SUGGESTED FIX: Add a short section with code for VocabParallelEmbedding (mask out-of-range token ids, zero their rows, all-reduce) and vocab-parallel cross-entropy (local max, all-reduce max, local sum-exp, all-reduce sum, gather target logit), with exact shapes.\n  [minor/code] (from L04-attention-alternatives-moe)\n     MISSING: The expert-parallel MoE forward sketch calls undefined helpers (bucket_tokens_by_rank, run_local_experts, weighted_scatter_add) and uses a global ep_group; the all-to-all recv buffer sized with torch.empty_like(send_buf) silently assumes perfectly equal per-rank counts, which is exactly the imbalanced case the surrounding prose warns about.\n     SUGGESTED FIX: Either label the count-exchange step explicitly (a preliminary all_to_all of per-rank token counts to size recv buffers, as real implementations do) or provide the ~15-line bucket_tokens_by_rank helper, so the sketch does not paper over the hardest part of EP.\n  [minor/correctness-bug] (from L08-parallelism-2)\n     MISSING: Prose swaps FlashAttention's standard notation: 'the running max \u2113 and running sum m' \u2014 \u2113 is the running sum and m the running max, and the chapter's own code uses m_run as the max and l_run as the sum, contradicting the sentence.\n     SUGGESTED FIX: Change to 'the running max m and running sum \u2113' to match both the FlashAttention papers and the code below it.\n  [minor/code] (from L08-parallelism-2)\n     MISSING: The expert-parallel MoE sketch relies on undefined helpers (bucket_tokens_by_rank, weighted_scatter_add, run_local_experts) and uses torch.empty_like(send_buf)/empty_like(local_out) for all_to_all_single, which silently assumes equal per-rank token counts \u2014 the variable-split-size handling (input_split_sizes/output_split_sizes from an exchanged counts tensor) is the actual hard part of EP.\n     SUGGESTED FIX: Show the counts exchange (all_to_all on a per-rank count tensor) and pass explicit input/output split sizes to all_to_all_single, or explicitly defer to the MoE chapter if a complete implementation lives there.\n  [minor/verification] (from L08-parallelism-2)\n     MISSING: No correctness-verification recipe for any of the parallelism schemes: nothing tells the reader to check that a TP=2 (or PP=2, CP=2) run reproduces the TP=1 loss/logits bit-for-bit (up to reduction order) on 2 GPUs before scaling up \u2014 the standard sanity harness for distributed implementations.\n     SUGGESTED FIX: Add a short 'verify before you scale' box: fixed seed, tiny model, compare single-GPU logits/loss vs TP=2/PP=2/CP=2 with torch.allclose tolerances, plus the KV-head divisibility and stage-balance checks as assertions.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nCRITICAL RULE FOR BUGS: for every correctness-bug, actually WORK OUT the correct result yourself\n(do the arithmetic, fix the code, derive the formula) and put the fully-correct value/code in\n`corrected_value`. Do not defer this to the drafting agent \u2014 it will copy your `corrected_value` verbatim.\nVerify your own math before emitting it.\n\nKEEP STRUCTURED FIELDS COMPACT (avoids truncating your JSON output): put only the specific\ncorrected value/formula/short snippet in `corrected_value` (aim < 30 lines). For a LARGE code\naddition or rewrite, describe precisely what to write in `instruction` (signature, shapes, the\nalgorithm, edge cases, the exact expected test value) and let the drafting agent produce the full\ncode \u2014 do NOT paste long multi-hundred-line code into structured fields.\n\nFor each edit give a `locator` = a short, UNIQUE substring currently in the file so the drafting\nagent can find the exact site. Prefer surgical `replace`/`insert-after` over `rewrite-section`.\nKeep the chapter's voice and existing correct content; change only what the deficits require\n(plus any adjacent text that must stay consistent with a bug fix).\n\nWrite nothing to disk. Return the structured spec object.",
    "apply": "You are the DRAFTING agent (Sonnet 5) applying an edit spec to a textbook chapter.\nFile to edit: /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/06-distributed-model-parallel.md\n\nYou will receive an edit spec (JSON) computed by a reasoning model. Apply EVERY edit to the file\nusing the Edit tool (surgical string replacements \u2014 do NOT rewrite the whole file). For each edit:\n- Find the site using its `locator`.\n- Apply `action` with `instruction`; if `corrected_value` is present, use it VERBATIM (it is the\n  verified-correct number/formula/code \u2014 do not recompute or alter it).\n- Write real, runnable, copy-paste-complete code with exact shapes and edge cases where asked.\n\nMUST PRESERVE (do not break these \u2014 the site build depends on them):\n- {{fig:NAME}} figure markers \u2014 never delete or rename; the figure files stay valid.\n- Admonitions (!!! interview, !!! key, !!! note, SOTA/resources boxes) \u2014 keep their exact syntax.\n- The single H1, chapter front-matter, cross-links [text](../part/chap.html), and code fences.\n- Only math-alphanumeric Unicode and emoji are true tofu; ASCII/Greek/arrows are fine in prose.\n  In NEW code use ASCII (no fancy Unicode operators). Keep prose within existing style (STYLE.md).\n\nAfter applying all edits, re-read the changed regions to confirm they landed and the file still\nhas balanced code fences and intact {fig:} markers. Return a short summary listing, per edit,\nthe deficit it addressed and confirmation it was applied. Do not report success for edits you\ncould not locate \u2014 list those explicitly as SKIPPED so the verifier catches them.",
    "verify": "You are the VERIFIER (Opus) for a revised textbook chapter.\nRe-read the CURRENT state of: /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/06-distributed-model-parallel.md\n\nThe chapter was just edited to resolve these deficits:\n  - [major] TP coverage omits the vocab-parallel embedding and the parallel (vocab-sharded) cross-entropy loss \u2014 the embedding and LM head are the largest single matrices in the model, and an \n  - [minor] The expert-parallel MoE forward sketch calls undefined helpers (bucket_tokens_by_rank, run_local_experts, weighted_scatter_add) and uses a global ep_group; the all-to-all recv buff\n  - [minor] Prose swaps FlashAttention's standard notation: 'the running max \u2113 and running sum m' \u2014 \u2113 is the running sum and m the running max, and the chapter's own code uses m_run as the max\n  - [minor] The expert-parallel MoE sketch relies on undefined helpers (bucket_tokens_by_rank, weighted_scatter_add, run_local_experts) and uses torch.empty_like(send_buf)/empty_like(local_out\n  - [minor] No correctness-verification recipe for any of the parallelism schemes: nothing tells the reader to check that a TP=2 (or PP=2, CP=2) run reproduces the TP=1 loss/logits bit-for-bit\n\nFor EACH deficit, check the current file and decide if it is genuinely resolved to the buildability\nstandard below. Also independently re-check any code/arithmetic that was changed \u2014 recompute it; if a\n\"fix\" introduced a new error, that is a new_problem. Confirm build-safety: every {fig:} marker and\nadmonition intact, cross-links unbroken, code fences balanced, no tofu Unicode in new code.\n\nBUILDABILITY STANDARD (the bar every fix must hit):\n- FULL SPECTRUM hardware: where relevant, give configs/expectations for laptop/CPU toy,\n  single GPU, 8-GPU node, and multi-node. State expected loss curves / timings / failure signatures.\n- INLINE, COPY-PASTE-COMPLETE code: runnable, self-contained blocks with exact tensor shapes,\n  dtypes, edge cases (causal masking, padding, numerical stability, the backward pass),\n  and concrete hyperparameters. Pseudo-code is NOT acceptable where real code is needed.\n- VERIFICATION: give a way to check correctness (round-trip test, reference comparison,\n  finite-difference grad check, or an expected numeric value the reader can reproduce).\n- LIBRARY MAPPING: point to how the real library (PyTorch/Triton/HF/Megatron/DeepSpeed/\n  FSDP/TRL/veRL/vLLM/SGLang/FlashAttention/lm-eval-harness) implements this, and where to read it.\n- CS336 reference quality: a Stanford CS336 student should be able to complete the matching\n  assignment/lecture using only this chapter.\n\nSet `passed=true` ONLY if all deficits are resolved AND there are no new problems AND build_safe.\nList any unresolved deficits and any new problems specifically. Return the structured object."
  }
]
const SPEC_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["target", "edits", "overall_plan"], "properties": {"target": {"type": "string"}, "overall_plan": {"type": "string"}, "edits": {"type": "array", "maxItems": 40, "items": {"type": "object", "additionalProperties": false, "required": ["addresses", "locator", "action", "instruction"], "properties": {"addresses": {"type": "string", "description": "which deficit(s) this edit resolves"}, "locator": {"type": "string", "description": "unique anchor text in the file to find the edit site"}, "action": {"type": "string", "enum": ["replace", "insert-after", "insert-before", "rewrite-section"]}, "instruction": {"type": "string", "description": "exactly what the drafting agent must write, including corrected values/code verbatim where known"}, "corrected_value": {"type": "string", "description": "for correctness-bugs: the right number/formula/code, fully worked out"}, "is_bug_fix": {"type": "boolean"}}}}}}
const VERIFY_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["target", "passed", "deficits_resolved", "deficits_unresolved", "new_problems", "notes"], "properties": {"target": {"type": "string"}, "passed": {"type": "boolean", "description": "true only if all deficits resolved AND no new problems AND build-safe"}, "deficits_resolved": {"type": "integer"}, "deficits_unresolved": {"type": "array", "items": {"type": "string"}}, "new_problems": {"type": "array", "items": {"type": "string"}}, "build_safe": {"type": "boolean", "description": "figure markers/admonitions/links intact, code fences balanced"}, "notes": {"type": "string"}}}

// Reasoning on Opus 4.8; drafting on Sonnet 5 (user routing 2026-07-20).
const results = await pipeline(
  WORK,
  // Stage 1: SPEC (Opus)
  w => agent(w.spec, { label: `spec:${w.target}`, phase: 'Spec', schema: SPEC_SCHEMA,
                       model: 'opus', effort: 'high' })
        .then(spec => ({ w, spec })),
  // Stage 2: APPLY (Sonnet 5) — gets the spec inline
  ({ w, spec }) => agent(
     w.apply + "\n\n## EDIT SPEC (apply every edit)\n" + JSON.stringify(spec, null, 2),
     { label: `apply:${w.target}`, phase: 'Apply', model: 'claude-sonnet-5' })
     .then(applied => ({ w, applied })),
  // Stage 3: VERIFY (Opus)
  ({ w }) => agent(w.verify, { label: `verify:${w.target}`, phase: 'Verify',
                                  schema: VERIFY_SCHEMA, model: 'opus', effort: 'high' })
                .then(v => ({ target: w.target, n_critical: w.n_critical, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.target)
const failed = done.filter(r => !r.verdict || !r.verdict.passed)
                   .map(r => ({ target: r.target, unresolved: r.verdict?.deficits_unresolved,
                                new_problems: r.verdict?.new_problems }))
const died = WORK.filter((w, i) => !results[i]).map(w => w.target)

log(`revised ${done.length}/${WORK.length} | passed ${passed.length} | failed ${failed.length} | died ${died.length}`)
log(`PASSED (append to revise_done.txt): ${passed.join(' ')}`)
if (failed.length) log(`FAILED (needs another pass): ${failed.map(f => f.target).join(' ')}`)
if (died.length) log(`DIED (re-run to resume): ${died.join(' ')}`)

return { passed, failed, died }
