export const meta = {
  name: 'figplan-p1',
  description: 'Strategize foundational infographics for 45 chapters',
  phases: [{ title: 'Strategize', detail: '45 chapters, Opus' }],
}

const CH = [
  {
    "id": "03-pretraining/09-optimizers",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/09-optimizers.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__09-optimizers.json and also return the object."
  },
  {
    "id": "11-evaluation/02-llm-as-judge",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/11-evaluation/02-llm-as-judge.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/11-evaluation__02-llm-as-judge.json and also return the object."
  },
  {
    "id": "11-evaluation/01-eval-landscape",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/11-evaluation/01-eval-landscape.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/11-evaluation__01-eval-landscape.json and also return the object."
  },
  {
    "id": "01-foundations/01-linear-algebra",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/01-linear-algebra.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__01-linear-algebra.json and also return the object."
  },
  {
    "id": "03-pretraining/01-pretraining-data",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/01-pretraining-data.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__01-pretraining-data.json and also return the object."
  },
  {
    "id": "03-pretraining/11-training-stability",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/11-training-stability.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__11-training-stability.json and also return the object."
  },
  {
    "id": "02-transformer/12-diffusion-nonAR-lms",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/12-diffusion-nonAR-lms.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__12-diffusion-nonAR-lms.json and also return the object."
  },
  {
    "id": "01-foundations/03-calculus-optimization",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/03-calculus-optimization.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__03-calculus-optimization.json and also return the object."
  },
  {
    "id": "03-pretraining/03-pretraining-objective",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/03-pretraining-objective.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__03-pretraining-objective.json and also return the object."
  },
  {
    "id": "11-evaluation/06-statistical-rigor-eval",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/11-evaluation/06-statistical-rigor-eval.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/11-evaluation__06-statistical-rigor-eval.json and also return the object."
  },
  {
    "id": "08-agents-harness/04-context-engineering",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/08-agents-harness/04-context-engineering.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/08-agents-harness__04-context-engineering.json and also return the object."
  },
  {
    "id": "01-foundations/06-neural-nets-from-scratch",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/06-neural-nets-from-scratch.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__06-neural-nets-from-scratch.json and also return the object."
  },
  {
    "id": "02-transformer/10-modern-arch-improvements",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/10-modern-arch-improvements.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__10-modern-arch-improvements.json and also return the object."
  },
  {
    "id": "03-pretraining/13-long-context-pretraining",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/13-long-context-pretraining.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__13-long-context-pretraining.json and also return the object."
  },
  {
    "id": "07-inference-serving/12-inference-economics",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/07-inference-serving/12-inference-economics.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/07-inference-serving__12-inference-economics.json and also return the object."
  },
  {
    "id": "03-pretraining/17-end-to-end-pretrain-recipe",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/17-end-to-end-pretrain-recipe.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__17-end-to-end-pretrain-recipe.json and also return the object."
  },
  {
    "id": "04-kernels-efficiency/03-flash-attention-2-3",
    "n_existing": 0,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/04-kernels-efficiency/03-flash-attention-2-3.md\n\nExisting figures in this chapter:\n    (none \u2014 this chapter has NO figures yet)\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/04-kernels-efficiency__03-flash-attention-2-3.json and also return the object."
  },
  {
    "id": "02-transformer/01-tokenization",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/01-tokenization.md\n\nExisting figures in this chapter:\n    - figures/bpe-merges.html  (marker {{fig:bpe-merges}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__01-tokenization.json and also return the object."
  },
  {
    "id": "99-appendix/05-from-scratch-index",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/99-appendix/05-from-scratch-index.md\n\nExisting figures in this chapter:\n    - figures/fsindex-stack-dataflow.html  (marker {{fig:fsindex-stack-dataflow}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/99-appendix__05-from-scratch-index.json and also return the object."
  },
  {
    "id": "01-foundations/08-gpu-architecture",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/08-gpu-architecture.md\n\nExisting figures in this chapter:\n    - figures/memory-hierarchy.html  (marker {{fig:memory-hierarchy}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__08-gpu-architecture.json and also return the object."
  },
  {
    "id": "06-rl-infra/10-agentic-multiturn-rl",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/06-rl-infra/10-agentic-multiturn-rl.md\n\nExisting figures in this chapter:\n    - figures/agentic-trajectory-loss-mask.html  (marker {{fig:agentic-trajectory-loss-mask}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/06-rl-infra__10-agentic-multiturn-rl.json and also return the object."
  },
  {
    "id": "01-foundations/04-numerics-precision",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/04-numerics-precision.md\n\nExisting figures in this chapter:\n    - figures/float-formats.html  (marker {{fig:float-formats}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__04-numerics-precision.json and also return the object."
  },
  {
    "id": "02-transformer/05-positional-encoding",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/05-positional-encoding.md\n\nExisting figures in this chapter:\n    - figures/rope-rotation.html  (marker {{fig:rope-rotation}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__05-positional-encoding.json and also return the object."
  },
  {
    "id": "03-pretraining/02-data-cleaning-dedup",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/02-data-cleaning-dedup.md\n\nExisting figures in this chapter:\n    - figures/dedup-paragraph-minhash-pipeline.html  (marker {{fig:dedup-paragraph-minhash-pipeline}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__02-data-cleaning-dedup.json and also return the object."
  },
  {
    "id": "08-agents-harness/08-agent-evaluation",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/08-agents-harness/08-agent-evaluation.md\n\nExisting figures in this chapter:\n    - figures/ageval-swebench-protocol.html  (marker {{fig:ageval-swebench-protocol}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/08-agents-harness__08-agent-evaluation.json and also return the object."
  },
  {
    "id": "02-transformer/11-ssm-and-alternatives",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/11-ssm-and-alternatives.md\n\nExisting figures in this chapter:\n    - figures/ssm-scan.html  (marker {{fig:ssm-scan}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__11-ssm-and-alternatives.json and also return the object."
  },
  {
    "id": "03-pretraining/16-continual-pretraining",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/16-continual-pretraining.md\n\nExisting figures in this chapter:\n    - figures/cpt-stability-plasticity-tradeoff.html  (marker {{fig:cpt-stability-plasticity-tradeoff}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__16-continual-pretraining.json and also return the object."
  },
  {
    "id": "04-kernels-efficiency/04-triton-kernels",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/04-kernels-efficiency/04-triton-kernels.md\n\nExisting figures in this chapter:\n    - figures/triton-cuda-vs-triton-mental-model.html  (marker {{fig:triton-cuda-vs-triton-mental-model}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/04-kernels-efficiency__04-triton-kernels.json and also return the object."
  },
  {
    "id": "02-transformer/07-build-gpt-from-scratch",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/07-build-gpt-from-scratch.md\n\nExisting figures in this chapter:\n    - figures/buildgpt-data-flow-stages.html  (marker {{fig:buildgpt-data-flow-stages}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__07-build-gpt-from-scratch.json and also return the object."
  },
  {
    "id": "09-rag-retrieval/02-vector-databases-ann",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/09-rag-retrieval/02-vector-databases-ann.md\n\nExisting figures in this chapter:\n    - figures/hnsw.html  (marker {{fig:hnsw}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/09-rag-retrieval__02-vector-databases-ann.json and also return the object."
  },
  {
    "id": "01-foundations/02-probability-information",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/02-probability-information.md\n\nExisting figures in this chapter:\n    - figures/prob-info-unified-picture.html  (marker {{fig:prob-info-unified-picture}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__02-probability-information.json and also return the object."
  },
  {
    "id": "07-inference-serving/11-multi-gpu-inference",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/07-inference-serving/11-multi-gpu-inference.md\n\nExisting figures in this chapter:\n    - figures/mgpu-decision-tree.html  (marker {{fig:mgpu-decision-tree}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/07-inference-serving__11-multi-gpu-inference.json and also return the object."
  },
  {
    "id": "04-kernels-efficiency/01-roofline-performance",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/04-kernels-efficiency/01-roofline-performance.md\n\nExisting figures in this chapter:\n    - figures/roofline.html  (marker {{fig:roofline}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/04-kernels-efficiency__01-roofline-performance.json and also return the object."
  },
  {
    "id": "07-inference-serving/04-sglang-radixattention",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/07-inference-serving/04-sglang-radixattention.md\n\nExisting figures in this chapter:\n    - figures/radix-tree.html  (marker {{fig:radix-tree}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/07-inference-serving__04-sglang-radixattention.json and also return the object."
  },
  {
    "id": "08-agents-harness/01-tool-use-function-calling",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/08-agents-harness/01-tool-use-function-calling.md\n\nExisting figures in this chapter:\n    - figures/toolcall-loop-architecture.html  (marker {{fig:toolcall-loop-architecture}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/08-agents-harness__01-tool-use-function-calling.json and also return the object."
  },
  {
    "id": "05-posttraining-alignment/04-peft-prompt-merging",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/05-posttraining-alignment/04-peft-prompt-merging.md\n\nExisting figures in this chapter:\n    - figures/softprompt-frozen-backbone-gradient-flow.html  (marker {{fig:softprompt-frozen-backbone-gradient-flow}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/05-posttraining-alignment__04-peft-prompt-merging.json and also return the object."
  },
  {
    "id": "05-posttraining-alignment/02-chat-templates-packing",
    "n_existing": 1,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/05-posttraining-alignment/02-chat-templates-packing.md\n\nExisting figures in this chapter:\n    - figures/chattmpl-multiturn-loss-mask.html  (marker {{fig:chattmpl-multiturn-loss-mask}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/05-posttraining-alignment__02-chat-templates-packing.json and also return the object."
  },
  {
    "id": "11-evaluation/03-eval-harnesses",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/11-evaluation/03-eval-harnesses.md\n\nExisting figures in this chapter:\n    - figures/evalharness-lm-eval-architecture.html  (marker {{fig:evalharness-lm-eval-architecture}})\n    - figures/evalharness-helm-three-layer.html  (marker {{fig:evalharness-helm-three-layer}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/11-evaluation__03-eval-harnesses.json and also return the object."
  },
  {
    "id": "03-pretraining/15-synthetic-data",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/03-pretraining/15-synthetic-data.md\n\nExisting figures in this chapter:\n    - figures/syndata-grounding-spectrum.html  (marker {{fig:syndata-grounding-spectrum}})\n    - figures/syndata-verification-ladder.html  (marker {{fig:syndata-verification-ladder}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/03-pretraining__15-synthetic-data.json and also return the object."
  },
  {
    "id": "06-rl-infra/05-openrlhf-nemo-ray",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/06-rl-infra/05-openrlhf-nemo-ray.md\n\nExisting figures in this chapter:\n    - figures/openrlhf-ray-role-decomposition.html  (marker {{fig:openrlhf-ray-role-decomposition}})\n    - figures/nemo-aligner-megatron-3d-layout.html  (marker {{fig:nemo-aligner-megatron-3d-layout}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/06-rl-infra__05-openrlhf-nemo-ray.json and also return the object."
  },
  {
    "id": "01-foundations/05-ml-fundamentals",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/01-foundations/05-ml-fundamentals.md\n\nExisting figures in this chapter:\n    - figures/mlfund-paradigm-spectrum.html  (marker {{fig:mlfund-paradigm-spectrum}})\n    - figures/mlfund-confusion-matrix.html  (marker {{fig:mlfund-confusion-matrix}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/01-foundations__05-ml-fundamentals.json and also return the object."
  },
  {
    "id": "02-transformer/02-embeddings-input",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/02-transformer/02-embeddings-input.md\n\nExisting figures in this chapter:\n    - figures/embed-text-to-tensors-pipeline.html  (marker {{fig:embed-text-to-tensors-pipeline}})\n    - figures/embed-full-stack-architecture.html  (marker {{fig:embed-full-stack-architecture}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/02-transformer__02-embeddings-input.json and also return the object."
  },
  {
    "id": "06-rl-infra/09-advantage-kl-tricks",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/06-rl-infra/09-advantage-kl-tricks.md\n\nExisting figures in this chapter:\n    - figures/advkl-terminal-reward-rollout.html  (marker {{fig:advkl-terminal-reward-rollout}})\n    - figures/advkl-ppo-clip-quadrant.html  (marker {{fig:advkl-ppo-clip-quadrant}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/06-rl-infra__09-advantage-kl-tricks.json and also return the object."
  },
  {
    "id": "07-inference-serving/13-serving-moe",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/07-inference-serving/13-serving-moe.md\n\nExisting figures in this chapter:\n    - figures/moe-serving-dense-tp-vs-moe-ep.html  (marker {{fig:moe-serving-dense-tp-vs-moe-ep}})\n    - figures/moe-serving-attn-dp-expert-ep-layout.html  (marker {{fig:moe-serving-attn-dp-expert-ep-layout}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/07-inference-serving__13-serving-moe.json and also return the object."
  },
  {
    "id": "09-rag-retrieval/03-rag-architectures",
    "n_existing": 2,
    "prompt": "You are the FIGURE STRATEGIST (Opus) for a public web textbook, \"The LLM Stack\".\nYour job for ONE chapter: decide the infographics that are FOUNDATIONAL to understanding it \u2014\nthe visuals that build INTUITION, not decoration. Quality over quantity: a chapter needs the\n1-3 figures that make its core mechanism click, not a figure per section.\n\nBook root: /local-ssd/pk669/programming/llm-stack-textbook\nChapter (READ IT IN FULL FIRST): content/09-rag-retrieval/03-rag-architectures.md\n\nExisting figures in this chapter:\n    - figures/rag-pipeline.html  (marker {{fig:rag-pipeline}})\n    - figures/rag-arch-pipeline-five-stages.html  (marker {{fig:rag-arch-pipeline-five-stages}})\n\n## Do this\n1. Read the chapter fully. Identify its `core_intuitions`: the handful of ideas a reader MUST\n   grasp intuitively (e.g. \"why decode is memory-bound\", \"how the online-softmax rescale works\",\n   \"what tensor-parallel all-reduce actually moves\"). These drive everything else.\n2. AUDIT each existing figure: RENDER it to actually SEE it \u2014\n   `python3 scripts/preview_fig.py <name> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then view\n   /tmp/figpreview/<name>.light.png and .dark.png. Judge: does it correctly and clearly convey\n   its idea in BOTH themes? Verdict keep / refine (state the concrete improvement) / replace.\n   Do NOT rubber-stamp \u2014 look for clipping, clutter, weak metaphors, or a missed chance at intuition.\n3. Specify NEW figures for the core_intuitions not already well-served. For each: a unique kebab-case\n   `name`, the `concept`, the `rationale` (why it's foundational for intuition HERE), `kind`\n   (static / animated / long-animation), `placement` (section to anchor it), and a rich `brief` a\n   drawer can execute from. Use `long-animation` ONLY where motion is genuinely essential to the\n   intuition (a multi-step process that a static frame cannot convey \u2014 e.g. an online-softmax pass,\n   a pipeline schedule filling and draining, a diffusion denoising trajectory, autoregressive decode\n   with a growing KV cache). Prefer a crisp STATIC infographic when a single frame suffices.\n4. Be disciplined: if the chapter is already well-served, return few or zero new figures and mostly\n   `keep` verdicts. If it is figure-poor but concept-rich, propose the foundational 1-3.\n\n## Constraints on what you propose (the drawer must obey these; keep briefs within them)\n- Theme-safe: no hardcoded primary colors; small illustrative integers only, invent NO benchmark numbers.\n- Everything must fit inside the viewBox; no illegible overlaps; clear reading order.\n- The figure must be fully understandable as a STATIC frame; animation is enhancement only.\n\nWrite your plan as JSON to /local-ssd/pk669/programming/llm-stack-textbook/figplan/09-rag-retrieval__03-rag-architectures.json and also return the object."
  }
]
const SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "core_intuitions", "existing", "new_figures", "summary"], "properties": {"chapter": {"type": "string"}, "core_intuitions": {"type": "array", "maxItems": 6, "items": {"type": "string"}, "description": "the handful of ideas a reader MUST grok intuitively to understand this chapter"}, "existing": {"type": "array", "maxItems": 15, "items": {"type": "object", "additionalProperties": false, "required": ["name", "verdict"], "properties": {"name": {"type": "string"}, "verdict": {"type": "string", "enum": ["keep", "refine", "replace"]}, "issue": {"type": "string", "description": "what's weak (for refine/replace)"}, "improvement": {"type": "string", "description": "concrete change to make"}}}}, "new_figures": {"type": "array", "maxItems": 5, "items": {"type": "object", "additionalProperties": false, "required": ["name", "concept", "rationale", "kind", "placement", "brief"], "properties": {"name": {"type": "string", "description": "kebab-case file stem, unique, descriptive"}, "concept": {"type": "string", "description": "one line: what it visualizes"}, "rationale": {"type": "string", "description": "why THIS is foundational for intuition here"}, "kind": {"type": "string", "enum": ["static", "animated", "long-animation"]}, "placement": {"type": "string", "description": "section heading / anchor text to place the marker after"}, "brief": {"type": "string", "description": "rich, specific drawing brief: what to show, the visual metaphor, labels, stages, what must be legible; enough for a drawer to execute without re-reading the chapter"}, "priority": {"type": "integer", "minimum": 1, "maximum": 3, "description": "1=must-have foundational, 3=nice-to-have"}}}}, "summary": {"type": "string"}}}

phase('Strategize')
const results = await parallel(CH.map(c => () =>
  agent(c.prompt, { label: `figplan:${c.id}`, phase: 'Strategize', schema: SCHEMA,
                    model: 'opus', effort: 'high' })))

const ok = results.filter(Boolean)
const died = CH.filter((c, i) => !results[i]).map(c => c.id)
let nNew = 0, nRefine = 0, nReplace = 0, nLong = 0
for (const r of ok) {
  nNew += (r.new_figures || []).length
  nLong += (r.new_figures || []).filter(f => f.kind === 'long-animation').length
  for (const e of (r.existing || [])) { if (e.verdict === 'refine') nRefine++; if (e.verdict === 'replace') nReplace++ }
}
log(`strategized ${ok.length}/${CH.length} | new=${nNew} (long-anim=${nLong}) | refine=${nRefine} | replace=${nReplace}`)
if (died.length) log(`DIED (re-run to resume): ${died.join(', ')}`)
return { strategized: ok.length, died, nNew, nLong, nRefine, nReplace }
