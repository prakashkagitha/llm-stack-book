export const meta = {
  name: 'codetest-b4',
  description: 'Assemble + run + verify CI-tested code for 5 chapters',
  phases: [{ title: 'Assemble+Run' }, { title: 'Verify' }],
}

const CH = [
  {
    "chapter": "12-production-mlops/03-caching-routing-cost",
    "slug": "12-production-mlops__03-caching-routing-cost",
    "n_ci": 7,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/12-production-mlops/03-caching-routing-cost.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/12-production-mlops__03-caching-routing-cost.py\n\nThis chapter has 7 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~50, 27 lines)\n    - block #1 (line ~109, 34 lines)\n    - block #3 (line ~235, 46 lines)\n    - block #4 (line ~287, 29 lines)\n    - block #5 (line ~345, 25 lines)\n    - block #6 (line ~405, 23 lines)\n    - block #10 (line ~585, 46 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #2=needs-net, #7=needs-net, #8=needs-net, #9=needs-net\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/12-production-mlops__03-caching-routing-cost.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/12-production-mlops__03-caching-routing-cost.py` (or `python3 -m pytest tests/12-production-mlops__03-caching-routing-cost.py -q` if you\n   write pytest-style). Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/12-production-mlops/03-caching-routing-cost.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/12-production-mlops/03-caching-routing-cost.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/12-production-mlops__03-caching-routing-cost.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/12-production-mlops__03-caching-routing-cost.py` (or pytest). It must pass.\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/12-production-mlops/03-caching-routing-cost.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "02-transformer/03-attention-from-scratch",
    "slug": "02-transformer__03-attention-from-scratch",
    "n_ci": 6,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/02-transformer/03-attention-from-scratch.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/02-transformer__03-attention-from-scratch.py\n\nThis chapter has 6 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~21, 3 lines)\n    - block #1 (line ~151, 50 lines)\n    - block #2 (line ~205, 22 lines)\n    - block #4 (line ~281, 44 lines)\n    - block #5 (line ~331, 47 lines)\n    - block #6 (line ~386, 11 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #3=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/02-transformer__03-attention-from-scratch.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/02-transformer__03-attention-from-scratch.py` (or `python3 -m pytest tests/02-transformer__03-attention-from-scratch.py -q` if you\n   write pytest-style). Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/02-transformer/03-attention-from-scratch.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/02-transformer/03-attention-from-scratch.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/02-transformer__03-attention-from-scratch.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/02-transformer__03-attention-from-scratch.py` (or pytest). It must pass.\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/02-transformer/03-attention-from-scratch.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "02-transformer/10-modern-arch-improvements",
    "slug": "02-transformer__10-modern-arch-improvements",
    "n_ci": 6,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/02-transformer/10-modern-arch-improvements.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/02-transformer__10-modern-arch-improvements.py\n\nThis chapter has 6 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~33, 45 lines)\n    - block #2 (line ~121, 41 lines)\n    - block #3 (line ~188, 56 lines)\n    - block #5 (line ~290, 65 lines)\n    - block #7 (line ~465, 29 lines)\n    - block #9 (line ~642, 144 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #1=non-python, #4=non-python, #6=fragment, #8=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/02-transformer__10-modern-arch-improvements.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/02-transformer__10-modern-arch-improvements.py` (or `python3 -m pytest tests/02-transformer__10-modern-arch-improvements.py -q` if you\n   write pytest-style). Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/02-transformer/10-modern-arch-improvements.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/02-transformer/10-modern-arch-improvements.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/02-transformer__10-modern-arch-improvements.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/02-transformer__10-modern-arch-improvements.py` (or pytest). It must pass.\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/02-transformer/10-modern-arch-improvements.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "03-pretraining/16-continual-pretraining",
    "slug": "03-pretraining__16-continual-pretraining",
    "n_ci": 6,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/03-pretraining/16-continual-pretraining.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/03-pretraining__16-continual-pretraining.py\n\nThis chapter has 6 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~51, 55 lines)\n    - block #1 (line ~135, 38 lines)\n    - block #2 (line ~241, 30 lines)\n    - block #4 (line ~308, 32 lines)\n    - block #5 (line ~351, 30 lines)\n    - block #6 (line ~409, 33 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #3=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/03-pretraining__16-continual-pretraining.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/03-pretraining__16-continual-pretraining.py` (or `python3 -m pytest tests/03-pretraining__16-continual-pretraining.py -q` if you\n   write pytest-style). Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/03-pretraining/16-continual-pretraining.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/03-pretraining/16-continual-pretraining.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/03-pretraining__16-continual-pretraining.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/03-pretraining__16-continual-pretraining.py` (or pytest). It must pass.\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/03-pretraining/16-continual-pretraining.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "04-kernels-efficiency/09-compilers-fusion",
    "slug": "04-kernels-efficiency__09-compilers-fusion",
    "n_ci": 6,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/04-kernels-efficiency/09-compilers-fusion.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/04-kernels-efficiency__09-compilers-fusion.py\n\nThis chapter has 6 heuristically CPU-runnable Python blocks to test:\n    - block #1 (line ~122, 14 lines)\n    - block #2 (line ~146, 14 lines)\n    - block #6 (line ~222, 9 lines)\n    - block #10 (line ~487, 12 lines)\n    - block #11 (line ~503, 9 lines)\n    - block #12 (line ~518, 11 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=needs-gpu, #3=needs-gpu, #4=needs-gpu, #5=shell, #7=needs-gpu, #8=non-python, #9=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/04-kernels-efficiency__09-compilers-fusion.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/04-kernels-efficiency__09-compilers-fusion.py` (or `python3 -m pytest tests/04-kernels-efficiency__09-compilers-fusion.py -q` if you\n   write pytest-style). Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/04-kernels-efficiency/09-compilers-fusion.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/04-kernels-efficiency/09-compilers-fusion.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/04-kernels-efficiency__09-compilers-fusion.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 tests/04-kernels-efficiency__09-compilers-fusion.py` (or pytest). It must pass.\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/04-kernels-efficiency/09-compilers-fusion.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  }
]
const VERIFY_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "blocks_tested", "blocks_skipped", "real_bugs_fixed", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "test runs green AND is faithful (no cheating) AND skips are legit"}, "blocks_tested": {"type": "integer"}, "blocks_skipped": {"type": "integer"}, "real_bugs_fixed": {"type": "array", "items": {"type": "string"}, "description": "real bugs in the book's code found and fixed"}, "cheating_found": {"type": "array", "items": {"type": "string"}, "description": "any blanket try/except, fake stub, or removed assert you had to fix"}, "notes": {"type": "string"}}}

const results = await pipeline(
  CH,
  c => agent(c.assemble, { label: `run:${c.chapter}`, phase: 'Assemble+Run', model: 'claude-sonnet-5' })
        .then(() => c),
  c => agent(c.verify, { label: `verify:${c.chapter}`, phase: 'Verify', schema: VERIFY_SCHEMA,
                         model: 'opus', effort: 'high' })
        .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed).map(r => r.chapter)
const bugs = done.flatMap(r => (r.verdict?.real_bugs_fixed || []).map(b => `${r.chapter}: ${b}`))
const cheats = done.flatMap(r => (r.verdict?.cheating_found || []).map(b => `${r.chapter}: ${b}`))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`codetest: ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | real-bugs ${bugs.length}`)
log(`PASSED (append to code_done.txt): ${passed.join(' ')}`)
if (bugs.length) log(`REAL BUGS FIXED: ${JSON.stringify(bugs, null, 1)}`)
if (cheats.length) log(`CHEATING CORRECTED: ${JSON.stringify(cheats)}`)
if (failed.length) log(`FAILED: ${failed.join(' ')}`)
if (died.length) log(`DIED (re-run): ${died.join(' ')}`)
return { passed, failed, died, bugs, cheats }
