export const meta = {
  name: 'figdraw-b2',
  description: 'Draw + revise foundational figures for 8 chapters',
  phases: [{ title: 'Draw' }, { title: 'Revise' }],
}

const CH = [
  {
    "chapter": "01-foundations/06-neural-nets-from-scratch",
    "n_new": 3,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/01-foundations/06-neural-nets-from-scratch.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [long-animation]  name=backprop-two-sweep-dag\n       concept: A small computation DAG showing the forward sweep caching tensors left-to-right and the backward sweep pushing adjoints right-to-left, each node applying its local VJP.\n       placement: after 'Forward graph, backward graph'\n       brief: Draw one horizontal computation graph shared by two lanes. Nodes left-to-right: x  ->  [*W1 +b1]  ->  z1  ->  [ReLU]  ->  a1  ->  [*W2 +b2]  ->  z2  ->  [softmax+CE]  ->  L. Render operation nodes as rounded rectangles (use v-fill with a v-stroke border) and tensor values as small labelled circles/pills between them. TOP LANE = FORWARD: a left-to-right arrow flow (accent-colored, e.g. v-accent) with a small annotation under each op node reading 'caches its inputs' or the cached quantity (e.g. z1 caches x,W1; ReLU caches sign(z1)). BOTTOM LANE = BACKWARD: the identical node chain mirrored below, with arrows flowing right-to-left in a muted/neutral stroke; over each edge write the adjoint being carried, using an overbar notation typeset as 'z2-bar', 'a1-bar', 'z1-bar', 'x-bar'. Seed the backward lane at the far right with a highlighted pill 'L-bar = dL/dL = 1'. Inside each backward op node write its one-line VJP rule: softmax+CE node -> 'z2-bar = P - Y'; the [*W2+b2] node -> 'W2-bar = a1^T z2-bar ; a1-bar = z2-bar W2^T'; ReLU node -> 'z1-bar = a1-bar (dot) 1[z1>0]  (gate)'; [*W1+b1] node -> 'W1-bar = x^T z1-bar'. Label the two lanes clearly on the left: 'FORWARD (cache)' and 'BACKWARD (apply VJP)'. Keep integers/notation symbolic only -- NO invented numbers. ANIMATION (enhancement only): stage 1 lights up forward arrows and cache annotations left-to-right; stage 2 the seed pill L-bar=1 flashes; stage 3 backward arrows illuminate right-to-left one node at a time, each op node's VJP rule fading in as its adjoint arrives. The still frame (all elements visible) must already tell the complete story. Theme-safe: use class-based colors (v-accent, v-fill, v-stroke, v-muted), no hardcoded primaries. Everything inside the viewBox with clear vertical separation between the two lanes.\n\n  NEW  [static]  name=dense-layer-gradient-transpose-symmetry\n       concept: The two golden identities as a shape-and-transpose diagram: forward Z=XW, and the two backward matmuls dW=X^T dZ, dX=dZ W^T, showing each operand reappears transposed in the other's gradient.\n       placement: after 'Forward graph, backward graph'\n       brief: A three-panel static infographic of labelled rectangles-as-matrices, each rectangle proportioned to its shape and annotated with its dimensions. PANEL 1 (FORWARD): X (N x d_in) times W (d_in x d_out) = Z (N x d_out); draw as adjacent blocks with an '=' and label 'Z = X W'. PANEL 2 (WEIGHT GRADIENT): X^T (d_in x N) times dZ (N x d_out) = dW (d_in x d_out); label 'dW = X^T dZ'. PANEL 3 (INPUT GRADIENT): dZ (N x d_out) times W^T (d_out x d_in) = dX (N x d_in); label 'dX = dZ W^T'. Use an overbar or a small delta/bar glyph consistently for gradients (dZ shown as 'Z-bar' etc.), matching the chapter's adjoint notation. The KEY VISUAL PAYOFF: draw a subtle connective cue (matching accent tint or a thin linking arc) between the SAME operand across panels -- highlight that X appears upright in the forward and transposed in dW, and that W appears in the forward and transposed in dX. Add a one-line caption strip: 'each operand reappears transposed in the other operand's gradient'. Optionally include the bias identity as a small footnote block: 'b-bar = 1^T dZ  (sum over the N batch rows)'. Shapes and letters only -- NO numeric entries, only symbolic dimensions (N, d_in, d_out). Theme-safe class colors; matrices as v-fill blocks with v-stroke borders, transposed operands tinted with v-accent to draw the eye. All within viewBox, generous spacing, clear reading order left-to-right / top-to-bottom.\n\n  NEW  [static]  name=variance-propagation-three-regimes\n       concept: Signal standard deviation versus layer depth on a log axis, showing three trajectories: vanish (bad small init), stable (Kaiming ~1), explode (too-large init).\n       placement: after 'Worked example: signal scale across 50 layers'\n       brief: A single line chart. X axis = layer depth (0 to ~50, labelled 'layer index / depth'). Y axis = activation standard deviation on a LOG scale (label 'signal std (log scale)'), with gridlines at powers of ten spanning roughly 10^-60 up to 10^+... enough to show both collapse and blow-up; mark the reference line std = 1 prominently (dashed, neutral). Three curves, each a straight line on the log axis (because the factor is constant per layer): (1) VANISH -- a steep downward line labelled 'Var(W) too small -> factor < 1', annotate its endpoint near the floor '~10^-69: total collapse'; (2) STABLE -- a flat horizontal line pinned at std = 1, labelled 'Kaiming Var(W)=2/d_in  ->  factor = 1', drawn in the accent color as the hero curve; (3) EXPLODE -- a steep upward line labelled 'Var(W) too large -> factor > 1', annotate 'gradients blow up -> NaN'. Add a compact side note: 'each layer multiplies std by sqrt(d * Var(W) * 1/2); anything not exactly 1, raised to the depth, runs away.' Use only illustrative round exponents drawn from the chapter's own worked example (10^-69, 1.0) -- invent NO new benchmark numbers. Theme-safe: hero/stable curve in v-accent, the other two in v-stroke/v-muted with differing dash patterns so they are distinguishable in BOTH light and dark and without relying on color alone; label each line directly (no legend-only encoding). Axes and all annotations inside the viewBox.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/01-foundations/06-neural-nets-from-scratch.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/01-foundations/06-neural-nets-from-scratch.md\nThe chapter's core intuitions: Backprop is the chain rule run right-to-left: each node turns its output-adjoint into input-adjoints via one local vector-Jacobian product, and a single backward sweep computes every gradient at once.; The forward pass caches intermediates and the backward pass reuses them, which is why backprop costs about 2x a forward pass rather than scaling with the number of parameters.; Every dense-layer gradient is one of two transposed matmuls -- dW = X^T dZ and dX = dZ W^T -- so each operand of the forward matmul reappears, transposed, in the other's gradient.; Signal variance multiplies by a per-layer factor as it propagates; unless that factor is kept near 1 (Kaiming 2/d_in for ReLU), activations and gradients vanish or explode geometrically with depth.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  backprop-two-sweep-dag, dense-layer-gradient-transpose-symmetry, variance-propagation-three-regimes\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "11-evaluation/02-llm-as-judge",
    "n_new": 3,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/11-evaluation/02-llm-as-judge.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=judge-pointwise-vs-pairwise\n       concept: Side-by-side of the two evaluation paradigms: absolute rubric score for one response vs a winner picked between two responses, with their opposing tradeoffs.\n       placement: after 'The Two Evaluation Paradigms: Pointwise vs Pairwise'\n       brief: A two-panel infographic, clearly divided down the middle with a thin vertical rule. LEFT panel titled 'Pointwise (absolute)': show a single response card (a small document glyph labeled 'Response') flowing by an arrow into a 'Judge' box, which flows into a vertical 1-to-5 rubric scale drawn as five stacked ticks with a filled marker at, say, 4 -- label the emitted value as a score badge reading 'score = 4'. Under it a compact caption line: 'one response -> a number'. RIGHT panel titled 'Pairwise (comparative)': show TWO response cards ('Response A', 'Response B') both feeding into one 'Judge' box, whose output is a winner ribbon reading 'A wins' (with a small 'or tie' note). Caption: 'two responses -> a winner'. Beneath both panels, a shared horizontal tradeoff strip with three labeled rows aligned under each panel using small paired indicators (use a filled/good dot vs a hollow/warn dot, NOT color words): Cost row -> pointwise 'O(N)' (good), pairwise 'O(N^2)' (warn); Calibration vs humans -> pointwise 'moderate' (warn), pairwise 'high' (good); Position bias -> pointwise 'low' (good), pairwise 'high' (warn). Use var(--accent) only for the emphasis on the emitted score badge and winner ribbon; use var(--good)/var(--warn) semantics for the tradeoff dots; keep boxes in var(--v-fill) with var(--v-stroke) borders and var(--ink-soft) text. Small illustrative integers only (scale 1-5, the marker at 4). Everything must fit; reading order left-to-right within each panel, top-to-bottom overall. No invented benchmark numbers.\n\n  NEW  [static]  name=judge-bias-menagerie\n       concept: The five systematic judge biases as a gallery of labeled cards, each pairing a one-glyph visual metaphor of the bias with its specific mitigation.\n       placement: after 'Biases in LLM Judges: The Menagerie'\n       brief: A grid of five equal cards (e.g. a 3-over-2 or single-row-scrollable layout, whichever fits the viewBox without overlap), each with a bold title, a tiny schematic metaphor at top, a one-line 'symptom', and a one-line 'fix' in a distinct footer band. Card 1 POSITION BIAS: metaphor = two slots [A][B] with a thumb/weight tipping toward the first slot; symptom 'favors whichever response is shown first'; fix 'score both orderings, aggregate'. Card 2 VERBOSITY (LENGTH) BIAS: metaphor = a short bar vs a long bar with a crown auto-landing on the long one; symptom 'prefers longer answers regardless of quality'; fix 'anti-length instruction + conciseness criterion'. Card 3 SELF-PREFERENCE: metaphor = a judge glyph looking into a mirror that reflects a response of the same shape/family; symptom 'overrates its own model family'; fix 'judge with a different model family'. Card 4 SYCOPHANCY: metaphor = a response card with a confident/authoritative badge and the judge nodding along; symptom 'defers to confident tone or leaked labels'; fix 'neutral framing, never leak the human label'. Card 5 INSTRUCTION-FOLLOWING BIAS: metaphor = a checklist with all format boxes ticked but a small warning mark on a factual-error line; symptom 'rewards format compliance over accuracy'; fix 'separate, explicitly weighted rubric criteria'. Keep each card body in var(--v-fill) with var(--v-stroke) border; titles in var(--ink) / var(--accent); the 'fix' footer band tinted with a subtle var(--good) accent to read as the remedy; use var(--warn) sparingly on the symptom metaphors. No numbers needed beyond the small A/B slot labels. All five metaphors must be legible as simple line schematics, not detailed illustrations; ensure no text clipping and consistent card heights in both themes.\n\n  NEW  [long-animation]  name=elo-battles-to-ranking\n       concept: How a stream of noisy pairwise battles converges to a stable global Elo ranking: logistic expected-score from the rating gap, a surprise-scaled update per battle, and a leaderboard that reorders and stabilizes over many battles.\n       placement: after 'Chatbot Arena and the Elo Rating System'\n       brief: Three coordinated regions; must be fully legible as a STATIC first frame, with animation layering the convergence story on top. REGION 1 (left) 'Expected score': the logistic curve P(A beats B) = 1 / (1 + 10^((theta_B - theta_A)/400)) drawn on axes x = rating gap (theta_A - theta_B) from about -400 to +400, y = win probability 0 to 1, with the midpoint marked at (0, 0.5) and a guide showing that a +400 gap ~ 0.9 (the '10:1' convention). Mark the current battle's gap on the curve with a dot reading off its expected probability. REGION 2 (center) 'One battle -> update': two model chips A (rating 1050) and B (rating 980); show expected P(A) ~ 0.6 pulled from the curve; show the actual outcome (A wins) and the update delta = K*(actual - expected) with K=32, so A: +12.8 -> 1062.8, B: -12.8 -> 967.2. Include a second, contrasting mini-callout: if the UNDERDOG B had won, the update is larger (~+19 for B) -- annotate 'upsets move ratings more'. REGION 3 (right) 'Leaderboard converges': a vertical bar/row ranking of ~4 illustrative models (label them Model 1..4, ratings near 1000 baseline, NO real model names, NO real leaderboard numbers) with small error whiskers. ANIMATION: battles stream in one at a time (a small pill '3 vs 1: 3 wins' flashing), each triggering (a) the dot sliding along the logistic curve to the new gap, (b) the two chips nudging by the surprise, (c) rows in the leaderboard swapping order early then settling, and (d) the error whiskers visibly SHRINKING as battle count climbs (show a 'battles: N' counter). End state: a stable order with tight whiskers; a final caption 'noisy pairwise battles -> stable global ranking'. Respect prefers-reduced-motion: the static frame must already show the logistic curve, the single worked update (1050/980, K=32, P~0.6, +/-12.8), and a settled leaderboard with whiskers. Use var(--accent) for the logistic curve and the active battle, var(--good)/var(--warn) or neutral fills for win/loss deltas, var(--muted) for whiskers/grid, var(--v-stroke)/var(--ink-soft) for axes and labels. Only the illustrative integers already in the chapter's worked example (1050, 980, 400, K=32, ~0.6, 12.8, 19); invent no benchmark ratings.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/11-evaluation/02-llm-as-judge.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/11-evaluation/02-llm-as-judge.md\nThe chapter's core intuitions: The judge trades human-level fidelity for inference speed: it is an approximate, reproducible human proxy with systematic, correctable biases, and must never be treated as ground truth.; Pointwise scoring produces an absolute score per response (O(N) queries, weak cross-prompt anchor); pairwise scoring produces a relative winner (O(N^2) queries, better calibrated but position-biased). The relative judgment is cognitively easier and more reliable.; LLM judges deviate from humans in a small menagerie of predictable ways -- position, verbosity, self-preference, sycophancy, instruction-following -- and each has a specific mitigation. Knowing them is the central skill.; Position bias is cancelled by scoring BOTH orderings (A,B) and (B,A) and only crowning a winner when the two passes agree, otherwise declaring a tie.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  judge-pointwise-vs-pairwise, judge-bias-menagerie, elo-battles-to-ranking\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "11-evaluation/06-statistical-rigor-eval",
    "n_new": 3,
    "n_edit": 0,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/11-evaluation/06-statistical-rigor-eval.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=statrig-ci-overlap-verdict\n       concept: A leaderboard read with error bars: overlapping intervals mean 'no demonstrated gap', separated intervals mean a real difference.\n       placement: after 'Reading Leaderboards Like a Statistician'\n       brief: A horizontal leaderboard-style panel. Left: a vertical list of 4-5 model rows (label them Model A, B, C, D generically, never real names). Each row is a horizontal error-bar: a dot for the point estimate with a symmetric whisker for the 95% CI, plotted against a shared horizontal accuracy axis (show a generic axis, e.g. tick marks without inventing benchmark numbers \u2014 use relative positions only). Arrange so the TOP TWO models (A and B) have heavily OVERLAPPING intervals even though A's dot sits slightly to the right \u2014 annotate this pair with a bracket and the verdict 'CIs overlap -> NO demonstrated gap; rank between A and B is noise'. Arrange the THIRD model (C) so its interval sits clearly to the LEFT with NO overlap with A/B \u2014 annotate 'separated -> real difference'. Add a small caption strip reminding: 'width ~ 1/sqrt(n); to halve it, 4x the items'. Optionally show one row (a 'new entrant', Model D) with a much WIDER interval labelled 'few evaluations -> rank not yet meaningful'. Theme-safe: use the theme's neutral/foreground for bars and a single accent hue only for the verdict brackets; no hardcoded red/green. Everything must fit; keep whisker labels from colliding. The static frame alone must make the overlap-vs-separation contrast legible at a glance.\n\n  NEW  [static]  name=statrig-pairing-discordant-cells\n       concept: A 2x2 contingency of A-correct vs B-correct: only the two off-diagonal (discordant) cells carry signal about which model is better; the diagonal agreements are discarded.\n       placement: after 'Three paired tests and when to use each'\n       brief: A 2x2 grid. Columns = 'B correct' / 'B wrong'; rows = 'A correct' / 'A wrong'. Fill the two DIAGONAL cells (both-right top-left, both-wrong bottom-right) with a muted/greyed treatment and label them 'concordant \u2014 carry NO comparative signal, discarded'. Use small illustrative integers for cell counts consistent with the chapter's worked example (both-right 372, both-wrong 38, A-right-only b=51, B-right-only c=33) so it ties directly to the text \u2014 these are the text's own numbers, not invented. Highlight the two OFF-diagonal cells: top-right 'A right, B wrong = b (=51)' and bottom-left 'A wrong, B right = c (=33)' with the accent hue, labelled 'discordant \u2014 the ONLY items that vote'. Off to the side, show the McNemar intuition as a tiny coin: 'under H0, each of the b+c=84 discordant items is a fair coin; is 51 vs 33 more lopsided than chance?'. Include a one-line takeaway strip: 'pairing cancels the difficulty variance living in the diagonal \u2014 worth a 4-10x larger unpaired test set.' Keep the grid square and uncluttered; ensure the greyed diagonal still reads clearly in dark theme (don't let it vanish). Theme-safe: single accent hue for discordant cells, neutral foreground elsewhere.\n\n  NEW  [animated]  name=statrig-bootstrap-resample\n       concept: The bootstrap: resample the scored items with replacement, recompute the statistic, repeat thousands of times, and read the 2.5/97.5 percentiles of the resulting cloud as the CI.\n       placement: after 'The bootstrap in one paragraph'\n       brief: Three-stage left-to-right pipeline that also works as a single static frame. STAGE 1 (left): the original sample \u2014 a small row of n scored items drawn as tiles (some marked pass, some fail, small n like 12 for legibility). STAGE 2 (middle): a 'resample with replacement' operation \u2014 show a new row drawn from the original where SOME tiles are duplicated and some are MISSING (make duplication visually obvious, e.g. two copies of the same tile), with a small die/arrow icon labelled 'draw n indices with replacement'. Compute one statistic value (the resample mean) shown as a single dot dropping onto a histogram axis on the right. STAGE 3 (right): a histogram of accumulated resample statistics building up; mark the central point estimate and shade the middle 95%, with the 2.5th and 97.5th percentile edges labelled 'lo' and 'hi' and a bracket 'percentile bootstrap 95% CI'. ANIMATION: loop the resample->recompute->drop-a-dot cycle so the histogram visibly grows from a few dots to a full bell, then the percentile bracket snaps in; a small counter 'replicate b = 1 ... B'. The FINAL/static frame must show the completed histogram with the CI bracket so a still reader gets the whole idea. Small caption: 'no distributional assumptions \u2014 works for accuracy, Elo, F1, p90 latency.' Theme-safe: neutral tiles with pass/fail distinguished by shape or fill pattern (not red/green), single accent hue for the shaded 95% band; ensure duplicated tiles are distinguishable in both themes. Keep n small and labels non-overlapping.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/11-evaluation/06-statistical-rigor-eval.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/11-evaluation/06-statistical-rigor-eval.md\nThe chapter's core intuitions: Every benchmark number is a random variable with sampling error; the 95% margin near p=0.5 is about 1/sqrt(n), so it shrinks only as 1/sqrt(n) and small gaps demand huge test sets.; Overlapping confidence intervals mean 'no demonstrated gap' \u2014 rank order without interval separation is noise, and this is the single leaderboard-reading skill the chapter exists to instill.; Pairing crushes variance: evaluate A and B on the SAME items and only the discordant items (one right, one wrong) carry comparative signal \u2014 concordant agreements are thrown away, which is exactly what McNemar formalizes.; The bootstrap builds a sampling distribution empirically by resampling your own data with replacement and recomputing the statistic many times; the 2.5/97.5 percentiles of that cloud are the CI, with no distributional assumptions.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  statrig-ci-overlap-verdict, statrig-pairing-discordant-cells, statrig-bootstrap-resample\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "02-transformer/04-mha-gqa-mla",
    "n_new": 2,
    "n_edit": 4,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/04-mha-gqa-mla.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=kv-cache-hbm-wall\n       concept: GPU HBM is a fixed budget: model weights take a fixed slab, the rest holds one growing KV cache per concurrent sequence \u2014 and the cache, not the weights, is what hits the ceiling first. Shrinking it (GQA) multiplies how many users / how much context fit.\n       placement: after 'The KV Cache: Why Inference Memory, Not FLOPs, Becomes the Bottleneck (beside the 70B worked example)'\n       brief: Two side-by-side panels sharing one identical fixed-height 'GPU memory (HBM)' budget bar, with a bold horizontal ceiling line at the top labeled 'memory wall'. In BOTH panels the bottom slab is an identical neutral block labeled 'model weights (fixed)'. Above it is the region labeled 'free for KV cache'. LEFT panel titled 'MHA': the free region is packed with a few WIDE accent-colored blocks stacked/tiled, each labeled '1 sequence's KV cache'; only a small illustrative number fit before the ceiling (e.g. 'fits ~4 sequences'). Add one small callout arrow on a block: 'grows with context length S' (show a faint taller ghost block hitting the ceiling to convey 'longer context = fewer users'). Annotate the block size as proportional to '2 * L_layers * S * (KV-head count) * d_h * P'. RIGHT panel titled 'GQA (g = h/8)': same budget, same weights slab, but each sequence's KV block is drawn ~8x narrower, so many more tile under the same ceiling (e.g. 'fits ~32 sequences'). Bottom caption spanning both: 'Shrinking the KV cache doesn't just save memory \u2014 it multiplies how many users (or how much context) fit under the same ceiling. Cache per sequence is proportional to the KV-head count.' STRICT: keep it schematic \u2014 use only small illustrative integer counts of blocks (like 4 vs 32); invent NO GB/byte benchmark numbers. Theme-safe: neutral fill for weights, one consistent accent hue for cache blocks, legible ceiling line and labels in both light and dark; everything inside the viewBox, no overlaps.\n\n  NEW  [static]  name=gqa-projection-and-repeat\n       concept: The two load-bearing facts of the GQA code: W_k/W_v are NARROW projections (output g*d_h, not d_model) so you store only g KV heads, and repeat_kv is a cheap no-copy 'expand' that broadcasts those g heads back up to h to align with the query heads before scaled_dot_product_attention.\n       placement: after 'Implementing the MHA -> GQA conversion (the subsection with the GroupedQueryAttention code, replacing the retired mhagqamla-headsharing-spectrum marker)'\n       brief: A left-to-right two-stage flow with h=8 query heads and g=2 KV groups (n_rep = h/g = 4). START: a single hidden vector token labeled 'h_t  (B, L, d_model)'. STAGE 1 'Projection widths differ': three projection blocks fanning out \u2014 W_q drawn WIDE, outputting eight query-head slots q0 q1 ... q7 (label '(B, h, L, d_h), h = 8'); W_k and W_v drawn visibly NARROW (about 1/4 the width of W_q), each outputting only two KV-head slots (k0 k1 for W_k, v0 v1 for W_v; label '(B, g, L, d_h), g = 2'). Annotate under W_k/W_v: 'output g*d_h, not d_model -> smaller matrix = the parameter AND cache saving; only k0,k1 / v0,v1 are cached'. STAGE 2 'repeat_kv broadcast (cheap expand, no data copied)': show k0 duplicating into [k0 k0 k0 k0] and k1 into [k1 k1 k1 k1], producing eight aligned KV slots; a small note 'query head j uses KV group j // n_rep'. Draw the alignment: the eight broadcast KV heads line up under the eight query heads. FINAL: an arrow into a box 'scaled_dot_product_attention -> (B, h, L, d_h)'. Reinforce the contrast with a compact legend: 'stored / cached = g heads (small)   \u00b7   fed to kernel = h heads (via broadcast)'. Use consistent role coloring for Q vs K vs V that works in both themes; keep d_h a small integer conceptually (no benchmark numbers); everything inside the viewBox, clear reading order left-to-right, no overlaps in either theme.\n\n  REFINE  existing figures/mha-forward-dataflow.html\n       issue: Top-to-bottom dataflow (x -> W_Q/W_K/W_V -> reshape -> per-head SDPA -> concat -> W_O) is correct and well-sequenced, and W_Q/W_K/W_V read well. But in dark theme the inner 'reshape -> (B,h,L,d_h)' boxes and the four per-head 'softmax(QK^T/sqrt(d_h)).V' boxes render with dark fill and near-dark text, so those labels have weak contrast against the dark page.\n       change: Make the inner reshape and per-head SDPA box fills and their text theme-adaptive so every label is as legible in dark as in light; keep the excellent overall reading order and the two callouts ('project once per role' and 'W_O mixes information across heads').\n\n  REFINE  existing figures/gqa-kv-sharing.html\n       issue: The three-panel MHA/GQA/MQA query->KV mapping with cache=8x/2x/1x is clear and correct in both themes. Two defects: (1) the bottom caption uses an arrow glyph that renders as a missing-glyph tofu box in both themes ('fewer KV heads [] smaller KV cache', 'MHA 8 [] GQA 2 [] MQA 1'); (2) the MQA panel's query heads are drawn as empty outlined boxes while the MHA and GQA panels label them q0..q7, an inconsistency that reads like missing labels.\n       change: Replace the tofu arrow glyph with a rendering-safe arrow (ASCII '->' or a verified-in-font glyph). Label the MQA query heads q0..q7 to match the other two panels. Leave the layout, colors, and cache multipliers as-is.\n\n  REPLACE  existing figures/mhagqamla-headsharing-spectrum.html\n       issue: This stacked MHA/GQA/MQA head-sharing diagram conveys the same core intuition (query heads sharing few KV heads, with cache = 8x/2x/1x) that gqa-kv-sharing already conveys more compactly ~30 lines earlier in the same section. Two near-identical figures for one idea is redundant, and this one also hardcodes blue/purple fills. The chapter's GQA implementation mechanism (why W_k/W_v are narrow, what repeat_kv actually does) is meanwhile unvisualized despite being a stated core takeaway.\n       change: Retire this figure and use its slot (the 'Implementing the MHA -> GQA conversion' subsection) for a new figure that visualizes the GQA mechanism instead of re-stating the sharing pattern: narrow W_k/W_v projections plus the repeat_kv broadcast. See new figure 'gqa-projection-and-repeat'.\n\n  REFINE  existing figures/mhagqamla-cache-layout-compare.html\n       issue: Strong figure: the proportional-width cache bars (MHA large, GQA smaller, MLA tiny latent) plus the MLA reconstruction diagram (c^KV up-projected to per-head K/V, 'absorbed into W^Q/W^O, no materialization') nail the MLA intuition in both themes. Minor: the right-edge annotations ('(large) 2*h*d_h', '(g/h x MHA) 2*g*d_h') sit right against the bar edges and slightly crowd them.\n       change: Nudge the right-side size annotations off the bar edges so nothing touches or overlaps; keep the K=blue / V=green role coloring (consistent and legible in both themes) and the two-part layout. No structural change.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/04-mha-gqa-mla.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/04-mha-gqa-mla.md\nThe chapter's core intuitions: MHA = split d_model into h subspaces, run independent scaled-dot-product heads in parallel, then concatenate and mix with W_O \u2014 many kinds of 'relevance' at once for roughly one wide head's FLOPs.; Heads are an implementation reshape: one big d_model->d_model GEMM per role, then reshape the last axis into (h, d_h); mathematically identical to h small matmuls but one large kernel.; The KV cache is the real inference bottleneck: size = 2*B*L_layers*S*(KV-head count)*d_h*P, it must be streamed from HBM every decode step (memory-bandwidth bound), and it competes with the weights for HBM so it caps concurrency and context length.; Query-head count and KV-head count can be decoupled: many query heads can share few key/value heads \u2014 the single observation that generates MQA, GQA, and MLA.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  kv-cache-hbm-wall, gqa-projection-and-repeat, mha-forward-dataflow, gqa-kv-sharing, mhagqamla-headsharing-spectrum, mhagqamla-cache-layout-compare\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "05-posttraining-alignment/05-rlhf-reward-modeling",
    "n_new": 2,
    "n_edit": 4,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/05-posttraining-alignment/05-rlhf-reward-modeling.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=bradley-terry-loss-shape\n       concept: How the Bradley-Terry pairwise loss maps the RM's score gap Delta = r_w - r_l to a preference probability sigma(Delta) and a self-limiting loss -log sigma(Delta), and why only differences matter.\n       placement: after 'Section 'The Bradley-Terry model: from comparisons to a scalar', immediately after the '### Deriving the pairwise loss' subsection (after the gradient equation dL/dDelta = -(1 - sigma(Delta))).'\n       brief: A single static infographic built around one horizontal axis: the margin Delta = r_w - r_l (the score gap the RM assigns the chosen minus rejected response). Two stacked, x-aligned curves share this axis. TOP curve: the preference probability sigma(Delta), an S-curve rising from 0 to 1; mark and label three points -- sigma(0) = 0.5 ('coin flip: equal scores'), sigma(+2) approx 0.88 ('chosen preferred ~88% of the time'), and note in a caption 'the score IS a log-odds of preference'. BOTTOM curve (x-aligned beneath): the per-example loss L(Delta) = -log sigma(Delta) = softplus(-Delta), a smooth decreasing curve. Annotate its THREE regimes with callouts: far right (large +Delta) 'confidently correct: loss -> 0, gradient -> 0 (almost no push)'; center at Delta=0 'L = log 2 approx 0.69'; far left (large -Delta) 'confidently WRONG: loss grows ~linearly, gradient saturates at magnitude 1 (strong push to flip)'. On the loss curve, draw two small tangent/arrow glyphs of DIFFERENT lengths -- a tiny arrow on the right (small push) and a long arrow on the left (big push) -- labeled 'push proportional to (1 - sigma(Delta)): self-limiting'. Include a small self-contained INSET panel titled 'Only differences matter': a mini number line with two dots r_w and r_l and a bracket showing Delta between them; below it, the same two dots shifted right by a constant c, with the bracket showing the SAME Delta; caption 'shift both scores by any c -> Delta, sigma(Delta), loss, and gradient all unchanged. Reward is identified only up to an additive constant.' Use small illustrative integers only (Delta values 0, +2, -2; treat r_w, r_l as unlabeled positions or tiny integers). Theme-safe: one accent color for the sigma curve, a second muted accent for the loss curve; no hardcoded page-background-dependent fills; everything inside the viewBox with clear top-to-bottom reading order (axis label, sigma curve, loss curve, inset). Invent NO benchmark numbers; sigma(2) approx 0.88 and log 2 approx 0.69 are exact math, fine to state.\n\n  NEW  [static]  name=rm-siamese-comparator\n       concept: The reward model trains as a Siamese (weight-tied) shared-encoder comparator: chosen and rejected pass through the SAME network, only their scalar difference enters the loss, and all C(K,2) pairs from one prompt are formed from K encodings done once.\n       placement: after 'Section 'Reward model architecture and training', at the end of the '### The from-scratch pairwise loss' subsection (right after the paragraph noting the shared-encoder/Siamese structure and that only the difference enters the loss).'\n       brief: A two-panel static figure. LEFT PANEL ('the pairwise comparator'): two parallel horizontal tracks. Top track = prompt x ++ chosen response y_w; bottom track = prompt x ++ rejected response y_l. Each track flows into a box labeled 'Reward model r_phi (scalar head at last token)'. Draw the two boxes as visibly the SAME network -- link them with a lock/equals glyph and a label 'WEIGHTS TIED (one network, two forward passes)'. Each box emits a scalar: r_w (top) and r_l (bottom). The two scalars converge into a small subtractor node computing Delta = r_w - r_l, feeding a box '-log sigma(Delta)  (Bradley-Terry loss)'. Emphasize with a highlighted label 'only the DIFFERENCE enters the loss'. Add dashed gradient-flow arrows running BACKWARD from the loss through BOTH copies to the shared weights, labeled 'both forward passes update the same weights'. RIGHT PANEL ('all K pairs from one prompt, encoded once'): one prompt x at top, fanning out to K completions (use K=4: y_1..y_4, note 'ranked by the human, y_1 best'). Each completion is encoded ONCE by the same shared r_phi (draw four small tied encoder glyphs or one encoder with four passes) -> four scalars r_1, r_2, r_3, r_4. From those four scalars draw a compact triangular grid of the C(4,2)=6 pairwise differences (r_i - r_j for i<j), labeled 'C(K,2)=6 comparisons = cheap arithmetic on K=4 scalars'. Caption the panel: 'Encode K completions once, form all C(K,2) pairs in ONE forward/backward pass -- InstructGPT's efficiency win AND anti-overfitting regularizer.' Use small illustrative integers only (K=4, 6 pairs). Theme-safe: use surface/accent tokens, no hardcoded white or primary fills; ensure both panels fit the viewBox with a clear left-to-right, top-to-bottom reading order and no overlapping labels. Invent no benchmark numbers.\n\n  REPLACE  existing figures/rlhf-three-stage-pipeline.html\n       issue: It is a near-duplicate of rlhf-pipeline placed immediately after it (markers on consecutive lines 13 and 15) \u2014 two three-stage overviews back to back is redundant. It also has theme-safety defects: hardcoded primary-purple stage headers and light lavender/white box fills that do not adapt to dark mode (the panels stay bright while the page is dark, and the low-contrast caption becomes hard to read).\n       change: Replace at the same marker with a NEW conceptual figure that serves the 'key conceptual move' the very next paragraph makes and that no figure currently visualizes: WHY RLHF EXCEEDS SFT via decoupling the source from the form of supervision. See new figure 'why-rlhf-beats-sft' below \u2014 it belongs exactly at this marker (immediately before the decoupling paragraph).\n\n  REFINE  existing figures/rm-scalar-head-architecture.html\n       issue: Light mode is clear. Dark mode has two real defects: (1) the transformer-backbone box and especially the 'Linear(d_model to 1)' box use hardcoded near-white fills that do not adapt \u2014 in dark mode the Linear box renders as white outline with near-white text and is close to illegible; (2) the h_{T-1} hidden-state node overlaps its own label, clipping the 'h_' portion behind the discarded-node circle.\n       change: Make the backbone and Linear-head box fills theme-aware (use surface tokens, not hardcoded white) so text keeps contrast in dark mode; ensure the Linear box text is legible on its fill. Nudge the h_{T-1} node or its label so they no longer overlap. Keep the strong 'last-token hidden state has attended over the whole sequence' idea and the 'smaller RM is fine' note.\n\n  REFINE  existing figures/rm-overoptimization-curve.html\n       issue: Light mode is excellent and conveys the rise-then-fall of true quality vs monotonically-rising proxy reward with the early-stop peak. Dark-mode issues: the 'over-optimization zone' shaded rectangle is nearly invisible (too-dark fill on a dark page), the dashed 'proxy RM reward' legend swatch is hard to see, and a few labels sit on top of the curves ('early stop here' near the peak dot, 'proxy reward keeps rising' crossing the gray line).\n       change: Strengthen the dark-mode zone shading so the over-optimization region reads clearly; make the dashed legend swatch visible in dark; nudge the 'early stop here' and 'proxy reward keeps rising' labels off the curves. Keep the two-curve design and the small-beta/large-beta axis annotations.\n\n  REFINE  existing figures/rlhf-four-model-setup.html\n       issue: Strong, well-organized figure (roles, trained-vs-frozen badges, per-model 'why it exists' captions). Dark-mode defects: panels 3 (REWARD) and 4 (REFERENCE) use hardcoded light/white fills that do not adapt, so in dark mode they render bright white while panels 1 and 2 render dark \u2014 an inconsistent, jarring treatment; and the bottom dashed caption row ('baseline gives advantage...', 'terminal sparse reward...', '-beta*KL per token...') is low-contrast gray on dark and hard to read.\n       change: Make ALL four panel fills theme-aware so they share one consistent treatment in dark mode (differentiate trained vs frozen with the badge and a subtle accent, not with a non-adapting white fill). Bump the bottom caption-row text contrast in dark. Keep the layout, the TRAINED/FROZEN badges, and the per-model rationale text intact.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/05-posttraining-alignment/05-rlhf-reward-modeling.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/05-posttraining-alignment/05-rlhf-reward-modeling.md\nThe chapter's core intuitions: Decoupling the SOURCE of supervision (cheap human comparisons) from its FORM (a dense, differentiable scalar reward defined over ALL responses) is why RLHF can exceed SFT: the policy generates and grades its own training data, exploring outputs better than any demonstration, and learns from negative contrast, not just imitation.; Bradley-Terry converts pairwise comparisons into a latent scalar strength: P(y_w > y_l) = sigma(r_w - r_l), so the reward IS a log-odds of preference and only score DIFFERENCES are identified (adding a constant to every score changes nothing).; The RM loss -log sigma(Delta) = softplus(-Delta) is self-limiting: near-zero loss and push when the model is confidently correct (large +Delta), log 2 at Delta=0, and a saturating push of magnitude (1 - sigma(Delta)) when confidently wrong.; The reward model is a Siamese shared-encoder comparator: the SFT network with the LM head replaced by a scalar head read at the last token, run on chosen and rejected through the SAME weights, with only their difference entering the loss; all C(K,2) pairs from one prompt are encoded once (efficiency + anti-overfit).\n\nReview each of these figures (the ones this chapter just drew/edited):\n  bradley-terry-loss-shape, rm-siamese-comparator, rlhf-three-stage-pipeline, rm-scalar-head-architecture, rm-overoptimization-curve, rlhf-four-model-setup\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "03-pretraining/05-distributed-data-parallel",
    "n_new": 2,
    "n_edit": 3,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/03-pretraining/05-distributed-data-parallel.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=hybrid-shard-device-mesh\n       concept: How HYBRID_SHARD lays sharding on the fast intra-node axis and replication on the slow inter-node axis of a 2-D device mesh.\n       placement: after 'A Decision Guide (place immediately after the decision-guide table, before 'Composing With Model Parallelism')'\n       brief: Draw a DeviceMesh as a 2-row by 4-column grid of GPU tiles: two rows labeled 'Node 0' and 'Node 1', four columns GPU0..GPU3 within each node (small illustrative integers only: 2 nodes x 4 GPUs). Horizontal (within-node) axis = the SHARD axis, connected by THICK links labeled 'intra-node NVLink (fast, high beta)'; vertical (across-node) axis = the REPLICATE axis, connected by a THIN/DASHED link labeled 'inter-node InfiniBand (slow, low beta)'. Distinguish fast vs slow links by line weight and dash pattern, NOT color alone (theme-safe). Inside each node's row, show one FSDP unit's FlatParameter split into 4 contiguous shards, one small hatched slice per GPU (GPU0=slice0 ... GPU3=slice3); make Node 1's row an identical mirror of the same 4-way sharding to signal it is a replica. Two callout arrows/notes: (1) along the fast horizontal axis, 'all-gather reconstructs a unit's full params over NVLink \u2014 cheap, hidden by compute'; (2) along the slow vertical axis, 'once-per-step all-reduce of gradients only \u2014 the bandwidth-heavy param all-gather never crosses IB'. Add a small contrast inset or ghost note: 'FULL_SHARD would all-gather across BOTH axes -> param gather crosses slow IB -> exposed'. Bottom caption: 'Shard on the fast axis, replicate on the slow axis; the expensive all-gathers never touch the bottleneck. Requires the model to fit in one node's aggregate memory.' Everything inside the viewBox, top-to-bottom reading order, no illegible overlaps. Use real arrowheads (avoid Unicode arrow glyphs that may render as boxes).\n\n  NEW  [long-animation]  name=zero3-jit-param-sweep\n       concept: The just-in-time parameter sweep of ZeRO-3/FSDP: one unit is all-gathered, computed, and freed as a window slides across the layers, holding peak memory flat regardless of model depth.\n       placement: after 'ZeRO-3: Also Shard Parameters (place after the forward/backward bullet list, near {{fig:zero-stages-sharding-ladder}})'\n       brief: One rank's-eye view. Top: a horizontal row of ~6 unit blocks L0..L5, each drawn at REST as a thin hatched sliver (this rank permanently holds only its 1/N slice). A left-to-right 'gather window' highlights one unit at a time. FORWARD sweep (L0->L5): as the window enters a unit, animate an all-gather \u2014 thin slivers converging from abstract peers \u2014 inflating THAT one block to FULL height labeled 'materialized full params'; a short compute tick runs on it; then it deflates back to a sliver labeled 'freed' as the window advances. BACKWARD sweep (L5->L0): each unit is all-gathered again (it had been freed), computed, then a reduce-scatter (arrows fanning OUT to peers) leaves only this rank's grad shard. Right side: a MEMORY GAUGE (vertical bar) that stays visibly FLAT the entire time at roughly '(1/N of whole model) + (one full unit)', with a faint dashed ghost line far above it marking where full-replication (DDP) memory would sit, off the useful scale \u2014 this contrast is the whole point. Every frame must stand alone as a static: always label which unit is currently materialized, which collective is in flight (all-gather in fwd, reduce-scatter in bwd), and show the flat gauge with its ghost reference. Small integers only (6 units; peers abstract, do not invent an N). Theme-safe (no hardcoded primary colors; distinguish sliver vs full by height/hatch, not color alone). Caption: 'Only one unit is ever unsharded; the window slides, peak memory stays flat \u2014 how a model far larger than one GPU trains.' Keep all elements inside the viewBox at every keyframe; use real arrowheads.\n\n  REFINE  existing figures/ddp-dp-allreduce-mean.html\n       issue: The averaging formula renders the summation symbol as an empty box ('g\u0304 = (1/N) \u25a1 g_i') and the arrows in the header ('B = 256 \u25a1 local batch') and 'g\u0304 -> ...' also render as \u25a1 boxes in both themes \u2014 a genuine legibility bug on the load-bearing equation. It also overlaps heavily with data-parallel (same replicate/shard/all-reduce flow within ~30 lines of text).\n       change: Replace the Unicode Sigma and arrow glyphs with rendered forms that survive the font (spell out 'sum of g_i' or draw a proper summation, and use real arrowheads or the word 'gives'). Lean into what this figure uniquely owns vs data-parallel \u2014 the global-batch box literally splitting into per-GPU shards and the 'optimizer.step() with SAME g\u0304 -> lockstep' framing \u2014 and de-emphasize the parts data-parallel already shows, so the two figures feel complementary rather than duplicative.\n\n  REFINE  existing figures/zero-stages-sharding-ladder.html\n       issue: The strongest figure in the chapter \u2014 the stacked memory bars (2B|2B|12B) shrinking DDP -> ZeRO-1 -> 2 -> 3 alongside the comm column is exactly the payoff. But several captions render arrows as \u25a1 boxes: '120 GB \u25a1 31 \u25a1 17 \u25a1 1.9 GB' and 'ZeRO-3: 16/N \u25a1 0 as N \u25a1 inf' lose their meaning as boxes. The top legend swatches for params/grads/optim are small and a bit hard to distinguish, especially in dark.\n       change: Fix the arrow glyphs (use real arrowheads or the word 'to') so the progression captions read correctly, and 'as N -> infinity'. Slightly enlarge or add outline contrast to the three legend swatches so params vs grads vs optim state are distinguishable in dark theme. Content and layout otherwise perfect \u2014 do not restructure.\n\n  REFINE  existing figures/fsdp-unit-lifecycle.html\n       issue: Conveys the forward/backward/optimizer lifecycle well, but has real layout defects: the right-edge 'free gathered params + full grads' box is clipped (shows 'free gathered param' in dark, running off the viewBox), and in the OPTIMIZER row the 'step()' label overlaps the 'updated param shard' box while 'no cross-rank comm' text crowds the right edge. Arrows again render as \u25a1 boxes ('entering block \u25a1', 'params freed \u25a1 gather again', '\u25a1 full grads').\n       change: Reflow the right column so the 'free gathered params + full grads' box sits fully inside the viewBox in both themes; separate the 'step()' arrow label from the 'updated param shard' box so they don't overlap; give 'no cross-rank comm' its own clear space. Replace the box-rendered arrows with real arrowheads. Keep the three-band FORWARD/BACKWARD/OPTIMIZER structure \u2014 it is good.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/03-pretraining/05-distributed-data-parallel.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/03-pretraining/05-distributed-data-parallel.md\nThe chapter's core intuitions: The mean of the per-worker local gradients equals the true full-batch gradient, so DP's synchronization is an all-reduce-with-mean and every replica applies the identical update, staying bit-for-bit in lockstep.; Backprop produces gradients last-layer-first; DDP buckets them and fires an async all-reduce the instant a bucket fills, so communication overlaps the still-running backward and only the final bucket's all-reduce is exposed.; Mixed-precision Adam costs ~16 bytes/param (2 bf16 params + 2 bf16 grads + 4 fp32 master + 4+4 Adam moments); 12 of those bytes are optimizer state touched only at optimizer.step(), so replicating them on every GPU is pure redundancy.; ZeRO shards that redundancy in cumulative stages (optimizer -> +grads -> +params), shrinking per-GPU state 16 -> 4+12/N -> 2+14/N -> 16/N; stages 1-2 keep DDP's ~2Psi comm, stage 3 adds a param all-gather (~1.5x comm).\n\nReview each of these figures (the ones this chapter just drew/edited):\n  hybrid-shard-device-mesh, zero3-jit-param-sweep, ddp-dp-allreduce-mean, zero-stages-sharding-ladder, fsdp-unit-lifecycle\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "10-multimodal-and-arch/05-unified-any-to-any",
    "n_new": 2,
    "n_edit": 3,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/10-multimodal-and-arch/05-unified-any-to-any.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=vqvae-image-to-tokens-pipeline\n       concept: How a continuous image becomes a 1-D sequence of discrete integer tokens via VQ-VAE nearest-codebook snapping.\n       placement: after 'Section 'Discrete Image Tokens' (### Discrete Image Tokens), after the VQ-VAE equations'\n       brief: A left-to-right pipeline in five stages. (1) An input image tile (abstract: a small square with a few illustrative shapes/colour blocks, no photo). Label 'image x, H x W x 3'. (2) An arrow through a box 'Encoder E' producing a small spatial feature grid, e.g. a 4x4 grid of cells, each cell a short continuous vector shown as a tiny bar-stack or a coloured dot. Label 'feature map z_e, h x w x d  (h = H/f)'. (3) The KEY step: to the side draw a small 'Codebook C' as a short vertical list of ~5 labelled entries (C_0, C_1, C_2, ...), each a coloured swatch. Draw one representative feature-grid cell with a dashed 'nearest neighbour' arrow snapping to its closest codebook entry, annotated 'argmin_k || z_e - C_k ||'. Emphasise this snap visually \u2014 it is the quantisation. (4) The grid now redrawn as a grid of integer indices, e.g. cells reading 12, 7, 7, 40, ... Label 'z_q: each cell = one codebook index'. (5) An arrow 'flatten (row-major)' turning the 4x4 index grid into a 1-D strip of tokens: [12,7,7,40,...] with a length annotation 'h x w tokens (e.g. 32x32 = 1024 for a 256px image at f=8) \u2014 the length of a paragraph'. Below the flatten step, a faint return path: a box 'Decoder D' with an arrow back to a reconstructed image tile labelled 'x_hat = D(z_q)', to signal it is an autoencoder. Keep colours theme-safe (use the book's accent for the snap arrow and codebook), small illustrative integers only, everything inside the viewBox with clear left-to-right reading order. The one idea that must be legible: continuous cell -> nearest codebook entry -> integer -> flat sequence.\n\n  NEW  [static]  name=transfusion-block-causal-mask\n       concept: Transfusion's block-causal attention mask: text causal, image bidirectional within its block, causal across blocks, never future.\n       placement: after 'Section 'Attention Masking in Transfusion' (### Attention Masking in Transfusion), near the transfusion_attention_mask code'\n       brief: Center a square attention-mask grid for the exact example in the code: a 9-token sequence 'text text text | image image image image | text text' (positions 0-2 text, 3-6 one image block, 7-8 text). Rows = query position i (top to bottom, 0..8), columns = key position j (left to right, 0..8). Each cell is filled (attend = solid accent) or empty (masked = faint outline). Fill cells exactly per the rule: text query i attends all j <= i (lower-triangular within text); image query (rows 3-6) attends columns 0-2 (earlier text, causal) AND all of columns 3-6 (the whole image block, including future j>i \u2014 this is the bidirectional square); later text (rows 7-8) attends all j <= i including the image block. Draw a bright outline box around the 4x4 image-block sub-square (rows/cols 3-6) and label it 'bidirectional within image block \u2014 attends both directions'. Label the strict lower-triangular text region 'causal'. Annotate one 'future text' cell that is empty for an image query (e.g. row 3, col 7) with 'image never attends to future text'. Put a small legend: filled = attend, empty = masked. Along the top, bracket and colour the three spans (text / image / text) so the reader maps columns to modality. Optionally add one contrast line: 'Chameleon's mask would be the full lower triangle everywhere \u2014 Transfusion opens the image block into a square.' Theme-safe colours; grid, labels, and legend all inside the viewBox; the filled/empty distinction must survive both light and dark themes (use fill for attend, thin outline for masked).\n\n  REFINE  existing figures/unified-ar-image-seq-layout.html\n       issue: Content and reading order are good in light theme, but in dark theme the top phase labels ('PREFIX \u2014 fixed at inference', 'GENERATED autoregressively') and the two-line bottom caption render as near-black text on the near-black background and are barely legible. The two caption lines also visibly overlap vertically.\n       change: Switch every text label and caption to a theme-safe fill (currentColor / CSS variable) so headers and captions read in both themes; increase the line spacing between the two caption sentences so they no longer collide. Keep the prefix/generated split and the orange image-token span as-is.\n\n  REFINE  existing figures/unified-chameleon-interleaved-seq.html\n       issue: Strong content (single embedding table, uniform causal mask, joint 65K vocab). But in dark theme the top 'text / image / text' region labels, the '1024 image tokens' bracket label, the right-hand 'no separate vision encoder' note, and the bottom caption are dark-on-dark and hard to read; the 'I' glyphs inside the orange image boxes are also low-contrast.\n       change: Move all annotation text to theme-safe fills so labels and the caption survive the dark background; raise the contrast of the token-box glyphs. No structural change \u2014 the layout and single-table message are correct.\n\n  REFINE  existing figures/unified-transfusion-arch.html\n       issue: The core two-headed architecture (shared trunk splitting into a text/LM head and an image/diffusion head, block-causal mask noted in the trunk) reads clearly in both themes. Only the small connector annotations ('embed', 'linear patch proj', 'hidden state') are plain dark text that washes out on the dark background.\n       change: Recolour just the small connector/annotation labels to a theme-safe fill so they stay legible in dark mode; leave the boxes, arrows, and loss equation untouched.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/10-multimodal-and-arch/05-unified-any-to-any.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/10-multimodal-and-arch/05-unified-any-to-any.md\nThe chapter's core intuitions: Tokenize-everything: if every modality can be expressed as a sequence of integers from a codebook, the ordinary autoregressive next-token objective already knows how to model it \u2014 no task-specific heads required.; VQ-VAE turns a continuous image into discrete tokens by snapping each encoder feature-map cell to its nearest codebook vector, then flattening the grid into a 1-D integer sequence the same length as a paragraph.; The quantisation gap: a codebook index carries ~13 bits per cell while a raw pixel patch carries ~24 bits, so discrete tokenisation throws information away \u2014 this is the ceiling Transfusion tries to escape.; Chameleon = pure early fusion: one shared embedding table over a joint 65K vocab, a uniform causal mask over interleaved text+image tokens, and a single cross-entropy loss.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  vqvae-image-to-tokens-pipeline, transfusion-block-causal-mask, unified-ar-image-seq-layout, unified-chameleon-interleaved-seq, unified-transfusion-arch\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  },
  {
    "chapter": "02-transformer/09-mixture-of-experts",
    "n_new": 2,
    "n_edit": 2,
    "draw": "You are the FIGURE DRAWER (Sonnet 5) for the web textbook \"The LLM Stack\".\nChapter (read the relevant sections for exact framing/terminology): /local-ssd/pk669/programming/llm-stack-textbook/content/02-transformer/09-mixture-of-experts.md\n\nAuthor these figures. Do them ONE AT A TIME; finish and verify each before the next. Do NOT stop\nearly \u2014 complete every task or list what you couldn't in figures_skipped.\n\n  NEW  [static]  name=moe-total-vs-active-resource-split\n       concept: The sparsity dial made visual: E expert blocks = TOTAL params (capacity), only k highlighted = ACTIVE params (FLOPs), plus three resource meters showing quality~total, latency~active, memory~total.\n       placement: after 'From One FFN to a Bank of Experts (after the 'Total parameters / Active parameters' paragraph ending '...so you want that ratio large.')'\n       brief: A single infographic in two coupled parts. LEFT (the model): draw a shared trunk box labeled 'shared: attention + embeddings + norms + head (counted once)', then a bank of E small FFN blocks (use E=8 illustrative) stacked/gridded, all the same size, bracketed as 'TOTAL params = shared + all E experts (capacity / knowledge)'. Highlight exactly k=2 of the E experts (glow/accent) and bracket 'shared + these k experts = ACTIVE params (what ONE token uses -> FLOPs/latency)'. Between them show the dial: 'total-to-active RATIO = E/k = 4x here (the sparsity dial)'. RIGHT (three resource meters, stacked): three horizontal bars/gauges each with a pointer, labeled: (1) 'QUALITY / capacity -> tracks TOTAL' with its fill reaching the full E-expert width; (2) 'LATENCY / FLOPs per token -> tracks ACTIVE' with its fill reaching only the k-expert width (annotate 'decodes like a small dense model'); (3) 'MEMORY / HBM -> tracks TOTAL' filled to full width (annotate 'all experts must stay resident: any token can route anywhere'). The punchline caption: 'you get TOTAL capacity at ACTIVE compute, but pay TOTAL memory - so compare MoE to dense by ACTIVE params or by FLOPs, never by total.' Use ONLY small illustrative integers (E=8, k=2, ratio 4x); do NOT print benchmark param counts (no 47B/13B) - keep it schematic so it generalizes. Theme-safe: shared trunk and inactive experts in a neutral fill, active experts in a single theme-safe accent, meter fills as accent-on-track; all text in theme foreground; strict left-to-right reading order, everything inside the viewBox.\n\n  NEW  [static]  name=moe-router-collapse-and-aux-loss\n       concept: The rich-get-richer collapse feedback loop that kills naive MoE, and how the auxiliary loss breaks it by multiplying a hard (non-diff) dispatch count as DIRECTION by a soft (differentiable) probability as GRADIENT.\n       placement: after 'Load Balancing: The Make-or-Break Problem (after the opening rich-get-richer paragraph, before 'The auxiliary load-balancing loss')'\n       brief: Two stacked panels. TOP PANEL - THE COLLAPSE (the problem): draw a closed feedback CYCLE of 4 nodes with arrows forming a loop: 'one expert is slightly better (random init)' -> 'router sends it MORE tokens' -> 'it gets MORE gradient' -> 'it improves FASTER' -> (back to start) with a bold '+' / 'reinforces' marker on the loop to signal a runaway positive-feedback cycle. To the right of the cycle, a small before/after pair of expert-load HISTOGRAMS over E=8 experts: 'early: roughly uniform' (flat bars) --arrow labeled 'left unchecked'--> 'collapsed: one tall bar, the rest starved (no gradient, atrophy)'. Caption: 'you paid for E experts and trained one; in EP the hot expert's GPU saturates while others idle.' BOTTOM PANEL - THE FIX (aux loss): show the loss L_aux = alpha * E * sum_e (f_e * P_e) with the product visually decomposed: over the SAME collapsed histogram, draw f_e as HARD dispatch counts tagged 'hard count -> DIRECTION (which experts are overloaded); non-differentiable, treated as constant (detached)' and P_e as a soft probability curve tagged 'soft mean router prob -> GRADIENT (nudges router down on overloaded, up on starved); differentiable'. Draw a small 'x' between them and an arrow to a corrective force pushing the tall bar DOWN and the starved bars UP toward the uniform 1/E line (draw a dashed 'uniform 1/E target' line across the histogram). One-line moral: 'the loss READS imbalance from the hard counts and WRITES the correction through the soft probabilities.' Optionally a tiny footnote chip: 'expert-choice routing avoids this by construction (each expert picks its top-C tokens).' Use small illustrative integers (E=8); invent no numbers. Theme-safe: neutral bars with a single accent for the overloaded expert and the corrective arrows; the uniform target as a dashed neutral line; all labels in theme foreground with leader lines so nothing overprints the bars; clear top-then-bottom reading order, everything within the viewBox.\n\n  REFINE  existing figures/moe-routing.html\n       issue: Strong single-token walkthrough (tiny gate -> per-expert scores -> top-2 active -> weighted sum -> residual), but two things blur the softmax-then/topk-then distinction the chapter is careful about: (1) the per-expert 'routing score' numbers (0.05, 0.62, 0.07, 0.11, 0.38, 0.04) sum to ~1.27, yet 0.62 and 0.38 are ALSO shown as the renormalized top-2 weights that sum to 1 - the same two numbers doing double duty reads as if the raw scores already summed to 1; (2) the 'weighted sum' label overlaps the x0.38 combine curve, and the bottom caption grazes the FFN6 box.\n       change: Make the raw scores clearly a slice of a full softmax (e.g. show them as small gray probabilities that visibly do NOT sum to 1) and then show the RENORMALIZE step producing distinct top-2 weights (relabel them so the reader sees 0.62/0.38 are computed FROM the scores, not equal to them) - this is the topk-then-softmax point. Nudge the 'weighted sum' label off the combine curve and lift the bottom caption clear of FFN6. Keep the overall layout.\n\n  REFINE  existing figures/moe-router-dispatch-pipeline.html\n       issue: Comprehensive but cluttered, and it overlaps heavily with moe-routing (same route->select->softmax->run->sum story) so it must earn its place by being the crisp end-to-end pipeline. Several labels overprint their own boxes/arrows in BOTH themes: 'non-differentiable step' sits on top of the top-k hexagon, 'rows sum to 1 (stable magnitude)' overlaps the softmax box, and 'run only experts {e1,...,ek}' collides with the container's top edge and the dashed weights arrow. The tiny 'indices {e1..ek}' text inside its pill is near-illegible. The dashed weights-routing arrows on the right add visual noise.\n       change: De-clutter: move the annotations ('non-differentiable step', 'rows sum to 1', 'no gradient this step') to the OUTSIDE margin with short leader lines so they never sit on the shapes. Enlarge the 'indices'/'gate weights' pill text. This figure's unique value over moe-routing is (a) the optional noise/bias stage and (b) making the two outputs of top-k explicit: the INDICES (stop-gradient, used only to dispatch) vs the WEIGHTS (carry the gradient) - lean into that split and let moe-routing own the single-token intuition.\n\nFIGURE AUTHORING SPEC (follow exactly \u2014 correct, beautiful, theme-safe):\nOUTPUT FILE: a self-contained figures/<NAME>.html: a `<figure class=\"viz\" id=\"fig-<NAME>\">` with\n  optionally `<button class=\"viz-replay\" type=\"button\">/replay</button>`, exactly one inline\n  `<svg viewBox=\"0 0 W H\" role=\"img\" aria-label=\"...\">` (W ~ 660-820; H to fit), and a\n  `<figcaption>` with a <b>bold lead sentence</b> + 1-2 sentences on the takeaway.\n  No <script>, no external images. A scoped `<style>` (selectors prefixed #fig-<NAME>) is fine.\nLAYOUT: EVERYTHING inside the viewBox with ~10px padding \u2014 text must NOT run past edges (it clips).\n  No illegible overlaps (>= ~16px gaps between groups). Clear left-to-right or top-to-bottom order.\nCOLOR (must work in BOTH themes \u2014 NEVER hardcode a primary color): prefer classes v-fill, v-accent,\n  v-accent-s, v-stroke, v-grid, v-muted, v-label, v-mono; or inline `var(--accent)` /\n  `stroke=\"var(--good,#2f9e6e)\"`. Vars: --accent,--accent-2,--ink-soft,--muted,--good(#2f9e6e),\n  --warn(#e0a106),--accent2(#3b82f6),--surface,--surface-3,--border-2. Raw hex is FORBIDDEN as a\n  primary color (breaks one theme); hex allowed ONLY as the fallback inside var(...).\nFONTS: `font:600 13px var(--sans,sans-serif)`; code/labels `var(--mono,monospace)`.\nANIMATION (enhancement only \u2014 the figure MUST be fully clear as a STATIC image; never rely on motion):\n  stage reveals via `data-anim=\"1\"..\"5\"` on top-level groups; `class=\"viz-draw\" style=\"--len:NNN\"`\n  draws a line; `class=\"viz-sweep\"` sweeps; `class=\"viz-pulse\"` pulses. For a `long-animation`, use a\n  longer ordered sequence of data-anim stages that walks through the process step by step, but every\n  stage's end-state must still read correctly as a static frame.\nTEXT SAFETY: ASCII only inside the SVG (no fancy Unicode arrows/operators/emoji/math-alphanumerics \u2014\n  they tofu). Use ->, <=, x, pi, sqrt written out, etc. Small illustrative integers only; invent NO\n  benchmark numbers. role=\"img\" + a one-sentence aria-label.\n\nFor EACH figure:\n1. Write (NEW) or Edit (refine/replace) /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html per the brief and SPEC.\n2. VISUALLY VERIFY: `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook, then READ\n   /tmp/figpreview/<NAME>.light.png and .dark.png. Fix anything clipped, overlapping, low-contrast in\n   either theme, or that misrepresents the idea. Iterate up to 3 passes until both look clean.\n3. For a NEW figure, insert the marker `{{fig:<NAME>}}` on its own line (blank line before/after)\n   into content/02-transformer/09-mixture-of-experts.md at the planned placement, using Edit. Change NOTHING else in the chapter.\n   (refine/replace keep their existing marker \u2014 do not add a duplicate.)\n\nReturn the structured result: figures_done (names finished + verified) and figures_skipped (with why).",
    "revise": "You are the FIGURE REVISER (Opus) for \"The LLM Stack\". Chapter: content/02-transformer/09-mixture-of-experts.md\nThe chapter's core intuitions: MoE decouples the number of parameters (capacity/knowledge, = TOTAL) from the FLOPs per token (cost/latency, = ACTIVE): a bank of E experts + a router that runs only k<<E of them, so the model 'contains' E FFNs but 'pays' for k.; The three-way resource split is the whole bet: quality tracks TOTAL params, latency/FLOPs track ACTIVE params, and memory tracks TOTAL (every expert must stay resident because any token can route anywhere) - which is why you reason with the total-to-active RATIO, never a single number.; The router is a tiny linear gate -> per-expert logits -> top-k select (the discrete, non-differentiable step) -> softmax gate weights, and the order matters: softmax-then-topk (weights don't sum to 1) vs topk-then-softmax (renormalized, stable magnitude).; Load balancing is make-or-break: a sparse router collapses via a rich-get-richer feedback loop (favored expert -> more tokens -> more gradient -> improves faster -> even more tokens); the aux loss breaks it with a hard dispatch-count (direction, non-diff) times a soft probability (gradient), and expert-choice routing balances by construction.\n\nReview each of these figures (the ones this chapter just drew/edited):\n  moe-total-vs-active-resource-split, moe-router-collapse-and-aux-loss, moe-routing, moe-router-dispatch-pipeline\n\nFor EACH: render and SEE it \u2014 `python3 scripts/preview_fig.py <NAME> --theme both` from /local-ssd/pk669/programming/llm-stack-textbook,\nthen view /tmp/figpreview/<NAME>.light.png and .dark.png. Judge strictly:\n  - Does it actually BUILD INTUITION for its concept (not just decorate)? Is it correct vs the chapter?\n  - Theme-safe (no hardcoded primary color; readable in BOTH light and dark)?\n  - Nothing clipped by the viewBox; no illegible overlaps; no tofu glyphs (ASCII only in the SVG)?\n  - Marker present in the chapter exactly once; figcaption has a bold lead + takeaway?\nIf a figure has ANY problem, FIX it: Edit /local-ssd/pk669/programming/llm-stack-textbook/figures/<NAME>.html (and re-render to confirm), or\nfix a missing/duplicate marker in the chapter. Re-verify after fixing.\n\nSet verdict per figure: good / fixed / still-broken / missing. `passed` = every figure good-or-fixed\nAND present. Return the structured object."
  }
]
const DRAW_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "figures_done", "figures_skipped"], "properties": {"chapter": {"type": "string"}, "figures_done": {"type": "array", "items": {"type": "string"}}, "figures_skipped": {"type": "array", "items": {"type": "string"}, "description": "names you could NOT complete, with why appended"}, "notes": {"type": "string"}}}
const REVISE_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "figures", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "true iff every planned figure is present, correct, theme-safe, unclipped and builds intuition"}, "figures": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["name", "verdict"], "properties": {"name": {"type": "string"}, "verdict": {"type": "string", "enum": ["good", "fixed", "still-broken", "missing"]}, "issue": {"type": "string"}}}}, "notes": {"type": "string"}}}

// Draw on Sonnet 5, revise on Opus (user routing 2026-07-20).
const results = await pipeline(
  CH,
  c => agent(c.draw, { label: `draw:${c.chapter}`, phase: 'Draw', schema: DRAW_SCHEMA,
                       model: 'claude-sonnet-5' }).then(d => ({ c, d })),
  ({ c }) => agent(c.revise, { label: `revise:${c.chapter}`, phase: 'Revise', schema: REVISE_SCHEMA,
                                 model: 'opus', effort: 'high' })
                .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed)
                   .map(r => ({ chapter: r.chapter, figs: r.verdict?.figures?.filter(f => f.verdict === 'still-broken' || f.verdict === 'missing') }))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`figures: drawn+revised ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | died ${died.length}`)
log(`PASSED (append to figplan_done.txt): ${passed.join(' ')}`)
if (failed.length) log(`FAILED: ${JSON.stringify(failed)}`)
if (died.length) log(`DIED (re-run to resume): ${died.join(' ')}`)
return { passed, failed, died }
