export const meta = {
  name: 'codetest-capcode',
  description: 'Assemble + run + verify CI-tested code for 11 chapters',
  phases: [{ title: 'Assemble+Run' }, { title: 'Verify' }],
}

const CH = [
  {
    "chapter": "14-capstone/11-evaluation-and-serving",
    "slug": "14-capstone__11-evaluation-and-serving",
    "n_ci": 15,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/11-evaluation-and-serving.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__11-evaluation-and-serving.py\n\nThis chapter has 15 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~36, 52 lines)\n    - block #3 (line ~229, 21 lines)\n    - block #4 (line ~256, 46 lines)\n    - block #5 (line ~308, 77 lines)\n    - block #7 (line ~446, 88 lines)\n    - block #8 (line ~625, 33 lines)\n    - block #10 (line ~725, 56 lines)\n    - block #12 (line ~908, 87 lines)\n    - block #13 (line ~999, 11 lines)\n    - block #14 (line ~1016, 44 lines)\n    - block #15 (line ~1064, 45 lines)\n    - block #16 (line ~1133, 63 lines)\n    - block #17 (line ~1221, 63 lines)\n    - block #18 (line ~1315, 13 lines)\n    - block #19 (line ~1334, 9 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #1=fragment, #2=needs-gpu, #6=fragment, #9=fragment, #11=fragment, #20=shell, #21=shell, #22=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__11-evaluation-and-serving.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__11-evaluation-and-serving.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/11-evaluation-and-serving.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/11-evaluation-and-serving.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__11-evaluation-and-serving.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__11-evaluation-and-serving.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/11-evaluation-and-serving.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/07-pretraining-run",
    "slug": "14-capstone__07-pretraining-run",
    "n_ci": 12,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/07-pretraining-run.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__07-pretraining-run.py\n\nThis chapter has 12 heuristically CPU-runnable Python blocks to test:\n    - block #5 (line ~387, 8 lines)\n    - block #6 (line ~404, 38 lines)\n    - block #7 (line ~446, 5 lines)\n    - block #8 (line ~554, 26 lines)\n    - block #9 (line ~610, 15 lines)\n    - block #10 (line ~651, 8 lines)\n    - block #11 (line ~682, 17 lines)\n    - block #13 (line ~734, 27 lines)\n    - block #15 (line ~916, 59 lines)\n    - block #16 (line ~979, 4 lines)\n    - block #17 (line ~1017, 19 lines)\n    - block #18 (line ~1104, 21 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #1=fragment, #2=needs-gpu, #3=needs-gpu, #4=needs-gpu, #12=needs-gpu, #14=needs-gpu, #19=needs-gpu, #20=needs-gpu, #21=needs-gpu, #22=needs-gpu, #23=fragment, #24=needs-gpu, #25=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__07-pretraining-run.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__07-pretraining-run.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/07-pretraining-run.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/07-pretraining-run.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__07-pretraining-run.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__07-pretraining-run.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/07-pretraining-run.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/03-tokenizer",
    "slug": "14-capstone__03-tokenizer",
    "n_ci": 11,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/03-tokenizer.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__03-tokenizer.py\n\nThis chapter has 11 heuristically CPU-runnable Python blocks to test:\n    - block #2 (line ~97, 10 lines)\n    - block #5 (line ~157, 179 lines)\n    - block #7 (line ~573, 12 lines)\n    - block #8 (line ~615, 37 lines)\n    - block #10 (line ~787, 8 lines)\n    - block #11 (line ~799, 3 lines)\n    - block #13 (line ~876, 3 lines)\n    - block #14 (line ~896, 64 lines)\n    - block #15 (line ~964, 41 lines)\n    - block #16 (line ~1143, 26 lines)\n    - block #20 (line ~1367, 13 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #1=non-python, #3=fragment, #4=fragment, #6=fragment, #9=needs-net, #12=needs-net, #17=fragment, #18=fragment, #19=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__03-tokenizer.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__03-tokenizer.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/03-tokenizer.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/03-tokenizer.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__03-tokenizer.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__03-tokenizer.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/03-tokenizer.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/05-mini-scaling-laws",
    "slug": "14-capstone__05-mini-scaling-laws",
    "n_ci": 11,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/05-mini-scaling-laws.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__05-mini-scaling-laws.py\n\nThis chapter has 11 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~53, 66 lines)\n    - block #1 (line ~154, 7 lines)\n    - block #2 (line ~201, 30 lines)\n    - block #3 (line ~250, 65 lines)\n    - block #6 (line ~436, 60 lines)\n    - block #8 (line ~529, 9 lines)\n    - block #9 (line ~549, 12 lines)\n    - block #10 (line ~575, 49 lines)\n    - block #11 (line ~685, 32 lines)\n    - block #13 (line ~859, 32 lines)\n    - block #14 (line ~1034, 23 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #4=non-python, #5=needs-gpu, #7=fragment, #12=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__05-mini-scaling-laws.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__05-mini-scaling-laws.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/05-mini-scaling-laws.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/05-mini-scaling-laws.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__05-mini-scaling-laws.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__05-mini-scaling-laws.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/05-mini-scaling-laws.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/10-agentic-narrow",
    "slug": "14-capstone__10-agentic-narrow",
    "n_ci": 11,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/10-agentic-narrow.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__10-agentic-narrow.py\n\nThis chapter has 11 heuristically CPU-runnable Python blocks to test:\n    - block #2 (line ~300, 143 lines)\n    - block #3 (line ~462, 29 lines)\n    - block #5 (line ~521, 74 lines)\n    - block #7 (line ~621, 13 lines)\n    - block #8 (line ~638, 109 lines)\n    - block #12 (line ~822, 82 lines)\n    - block #13 (line ~910, 83 lines)\n    - block #14 (line ~999, 74 lines)\n    - block #15 (line ~1105, 81 lines)\n    - block #16 (line ~1201, 142 lines)\n    - block #17 (line ~1368, 23 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #1=needs-gpu, #4=non-python, #6=non-python, #9=non-python, #10=fragment, #11=needs-net, #18=needs-gpu, #19=fragment, #20=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__10-agentic-narrow.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__10-agentic-narrow.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/10-agentic-narrow.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/10-agentic-narrow.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__10-agentic-narrow.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__10-agentic-narrow.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/10-agentic-narrow.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/06-optimizer-and-schedule",
    "slug": "14-capstone__06-optimizer-and-schedule",
    "n_ci": 8,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/06-optimizer-and-schedule.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__06-optimizer-and-schedule.py\n\nThis chapter has 8 heuristically CPU-runnable Python blocks to test:\n    - block #3 (line ~133, 56 lines)\n    - block #4 (line ~209, 46 lines)\n    - block #6 (line ~324, 34 lines)\n    - block #9 (line ~427, 51 lines)\n    - block #10 (line ~490, 29 lines)\n    - block #11 (line ~561, 35 lines)\n    - block #14 (line ~917, 25 lines)\n    - block #15 (line ~951, 28 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #1=non-python, #2=needs-gpu, #5=needs-gpu, #7=non-python, #8=needs-gpu, #12=needs-gpu, #13=needs-gpu, #16=non-python, #17=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__06-optimizer-and-schedule.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__06-optimizer-and-schedule.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/06-optimizer-and-schedule.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/06-optimizer-and-schedule.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__06-optimizer-and-schedule.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__06-optimizer-and-schedule.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/06-optimizer-and-schedule.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/08-mid-training",
    "slug": "14-capstone__08-mid-training",
    "n_ci": 8,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/08-mid-training.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__08-mid-training.py\n\nThis chapter has 8 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~65, 77 lines)\n    - block #1 (line ~160, 36 lines)\n    - block #3 (line ~257, 20 lines)\n    - block #4 (line ~279, 26 lines)\n    - block #5 (line ~331, 105 lines)\n    - block #6 (line ~465, 13 lines)\n    - block #7 (line ~486, 31 lines)\n    - block #11 (line ~1040, 21 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #2=fragment, #8=needs-gpu, #9=needs-gpu, #10=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__08-mid-training.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__08-mid-training.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/08-mid-training.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/08-mid-training.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__08-mid-training.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__08-mid-training.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/08-mid-training.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/09-post-training",
    "slug": "14-capstone__09-post-training",
    "n_ci": 7,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/09-post-training.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__09-post-training.py\n\nThis chapter has 7 heuristically CPU-runnable Python blocks to test:\n    - block #1 (line ~57, 59 lines)\n    - block #3 (line ~167, 39 lines)\n    - block #7 (line ~418, 64 lines)\n    - block #10 (line ~619, 28 lines)\n    - block #12 (line ~836, 2 lines)\n    - block #14 (line ~888, 11 lines)\n    - block #17 (line ~1244, 19 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #2=needs-net, #4=needs-gpu, #5=needs-gpu, #6=needs-net, #8=needs-gpu, #9=needs-gpu, #11=needs-gpu, #13=needs-gpu, #15=needs-net, #16=needs-gpu\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__09-post-training.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__09-post-training.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/09-post-training.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/09-post-training.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__09-post-training.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__09-post-training.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/09-post-training.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/12-retrospective-and-scaleup",
    "slug": "14-capstone__12-retrospective-and-scaleup",
    "n_ci": 7,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/12-retrospective-and-scaleup.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__12-retrospective-and-scaleup.py\n\nThis chapter has 7 heuristically CPU-runnable Python blocks to test:\n    - block #1 (line ~36, 32 lines)\n    - block #2 (line ~143, 72 lines)\n    - block #4 (line ~366, 17 lines)\n    - block #5 (line ~389, 22 lines)\n    - block #15 (line ~906, 58 lines)\n    - block #18 (line ~1252, 21 lines)\n    - block #19 (line ~1279, 35 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #0=non-python, #3=needs-gpu, #6=fragment, #7=needs-gpu, #8=needs-gpu, #9=shell, #10=non-python, #11=needs-gpu, #12=fragment, #13=needs-gpu, #14=needs-gpu, #16=fragment, #17=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__12-retrospective-and-scaleup.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__12-retrospective-and-scaleup.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/12-retrospective-and-scaleup.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/12-retrospective-and-scaleup.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__12-retrospective-and-scaleup.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__12-retrospective-and-scaleup.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/12-retrospective-and-scaleup.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/04-architecture",
    "slug": "14-capstone__04-architecture",
    "n_ci": 5,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/04-architecture.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__04-architecture.py\n\nThis chapter has 5 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~31, 33 lines)\n    - block #1 (line ~176, 18 lines)\n    - block #6 (line ~475, 24 lines)\n    - block #10 (line ~577, 31 lines)\n    - block #15 (line ~908, 36 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #2=fragment, #3=fragment, #4=needs-gpu, #5=fragment, #7=fragment, #8=non-python, #9=fragment, #11=fragment, #12=fragment, #13=fragment, #14=fragment, #16=fragment, #17=fragment, #18=fragment, #19=fragment, #20=fragment, #21=fragment, #22=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__04-architecture.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__04-architecture.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/04-architecture.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/04-architecture.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__04-architecture.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__04-architecture.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/04-architecture.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  },
  {
    "chapter": "14-capstone/01-overview-and-landscape",
    "slug": "14-capstone__01-overview-and-landscape",
    "n_ci": 2,
    "assemble": "You are proving that the runnable code in ONE textbook chapter actually RUNS.\nBook root: /local-ssd/pk669/programming/llm-stack-textbook. Chapter: content/14-capstone/01-overview-and-landscape.md\nWrite the test to: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__01-overview-and-landscape.py\n\nThis chapter has 2 heuristically CPU-runnable Python blocks to test:\n    - block #0 (line ~191, 129 lines)\n    - block #2 (line ~586, 4 lines)\nOther blocks (default SKIP unless trivially CPU-safe): #1=non-python, #3=fragment\n\nDo this:\n1. Read the chapter. Extract each CPU-runnable Python block's code faithfully.\n2. Write /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__01-overview-and-landscape.py: assemble the blocks in order into a runnable module (later blocks\n   may depend on names defined by earlier blocks in the same chapter \u2014 that's expected; concatenate\n   them so the chapter's code runs as a whole). Add minimal honest glue and tiny fixtures so it\n   executes on CPU. Every tested block should actually EXECUTE (a function defined by a block must be\n   CALLED with a tiny input; a class must be instantiated and used).\n3. RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__01-overview-and-landscape.py`\n   (ci_sim_run.py blocks every package CI lacks -- transformers, sentence_transformers, trl, wandb,\n   matplotlib, requests, ... -- so it catches non-hermetic code exactly as CI will). It MUST pass there. Iterate until it runs clean OR every remaining failure is an honestly-SKIPPED\n   block. If a failure is a REAL bug in the book's code, fix content/14-capstone/01-overview-and-landscape.md and mirror it in the test.\n\nHARD RULES (these make the test HONEST \u2014 violating them defeats the whole point):\n- The test must run the BOOK'S ACTUAL CODE, verbatim where possible. Copy each tested block's\n  code faithfully; do not rewrite its logic to make it pass.\n- NO cheating: no blanket `try/except: pass` around a block to swallow its error; no trivial stub\n  that bypasses the very logic being demonstrated; no deleting asserts the book states.\n- If a block ERRORS because the book's code is genuinely WRONG (bug), FIX it in\n  content/<chapter>.md (and mirror the fix in the test) and record it as a real bug found.\n- If a block is a DELIBERATELY-BROKEN demo (the chapter says \"this crashes / this is the bug\"),\n  wrap ONLY that block in a narrow assert-it-raises check \u2014 do not \"fix\" it.\n- NETWORK/API CALLS ARE FORBIDDEN in the test (CI has no network/keys \u2014 a real call hangs or 401/403).\n  For a block that calls an external API or network (OpenAI/Anthropic/Cohere/HF hub/requests/httpx/\n  boto3/socket/model or dataset download): EITHER (a) MOCK the boundary with `unittest.mock` so the\n  block's OWN logic still executes offline against a canned response (preferred when the block's point\n  IS the surrounding logic, e.g. retry/parse/prompt-assembly), OR (b) `# SKIP(network): ...` it.\n  NEVER leave a real network/API call in the test.\n- SKIP (don't test) blocks that truly need a GPU, or that are non-standalone fragments \u2014\n  leave an explicit `# SKIP(<reason>): ...` comment naming which block.\n- Minimal honest glue is allowed: small fixtures/toy tensors, tiny shapes, fixed seeds, and\n  CPU dtype (float32) substitutions where a block only differs by device. Keep runtime under\n  ~60s and memory small.\n- INSTANTIATING A REAL MODEL IS NETWORK, even if a local cache makes it \"work\" here: SentenceTransformer(...),\n  CrossEncoder(...), AutoModel/AutoTokenizer.from_pretrained(...), transformers.pipeline(...), mteb.get_model(...),\n  trl configs (GRPOConfig/SFTConfig \u2014 also version-fragile), tensorrt_llm/vllm engines. CI has neither the\n  package nor the weights. For such a block: replace the model object with a tiny SHAPE-CORRECT offline STUB\n  (e.g. a fake with .encode returning a deterministic (N, dim) float32 array) so the block's surrounding\n  logic still runs, OR `# SKIP(network): ...` it. Do NOT call a real model.\n- ASSERTIONS MUST BE DETERMINISTIC: seed all RNG (np.random.seed / torch.manual_seed) before any random data;\n  never assert a fitted/optimized constant to a tight tolerance that varies by scipy/numpy version \u2014 assert the\n  robust, order-of-magnitude or exponent quantity the chapter actually claims is stable.\n- ALLOWED IMPORTS (guaranteed in CI): numpy, torch (CPU), einops, scikit-learn (sklearn), and the\n  Python standard library. ANY OTHER third-party import (openai, anthropic, transformers,\n  sentence_transformers, qdrant_client, faiss, datasets, dspy, tiktoken, cohere, ...) MUST be wrapped\n  in `try: import X ... except Exception: X = None` at module scope so the test file still LOADS in CI\n  without that package \u2014 the block using it must then be defined-not-called or skipped. Never let an\n  un-guarded optional import sit at module scope; it will ImportError in CI and fail the whole file.\n\nReturn a short summary: blocks tested, blocks skipped (with reasons), and any real bugs you fixed.",
    "verify": "You are VERIFYING a code test for honesty and correctness. Chapter: content/14-capstone/01-overview-and-landscape.md\nTest file: /local-ssd/pk669/programming/llm-stack-textbook/tests/14-capstone__01-overview-and-landscape.py\n\nRead the test AND the chapter. Then:\n1. RE-RUN it UNDER THE SIMULATED CI ENV: `cd /local-ssd/pk669/programming/llm-stack-textbook && python3 scripts/ci_sim_run.py tests/14-capstone__01-overview-and-landscape.py`.\n   It MUST pass there (this is what CI runs -- packages like sentence_transformers/trl are absent).\n2. Check FAITHFULNESS (this is the point): the test runs the book's ACTUAL code; there is NO blanket\n   try/except swallowing errors, NO trivial stub bypassing the demonstrated logic, NO removed asserts.\n   Each \"tested\" block is actually executed (functions called, classes used), not just imported.\n3. Check SKIPS are legitimate (genuinely GPU/network/fragment, each with a `# SKIP(...)` reason).\n3.5. Check HERMETICITY: no real network/API call remains \u2014 any OpenAI/Anthropic/HF/requests/httpx\n   call must be MOCKED (unittest.mock) or SKIPped. A test that imports `openai` and calls it for real\n   is a FAIL; fix it by mocking the client boundary or skipping the block.\n4. Check any book fixes (edits to content/14-capstone/01-overview-and-landscape.md) are correct and improve the book.\nIf anything is wrong, FIX it (edit the test or the chapter) and re-run. Set passed only when the test\nruns green AND is faithful AND skips are legit. Report blocks_tested, blocks_skipped, real_bugs_fixed,\nand any cheating you had to correct."
  }
]
const VERIFY_SCHEMA = {"type": "object", "additionalProperties": false, "required": ["chapter", "passed", "blocks_tested", "blocks_skipped", "real_bugs_fixed", "notes"], "properties": {"chapter": {"type": "string"}, "passed": {"type": "boolean", "description": "test runs green AND is faithful (no cheating) AND skips are legit"}, "blocks_tested": {"type": "integer"}, "blocks_skipped": {"type": "integer"}, "real_bugs_fixed": {"type": "array", "items": {"type": "string"}, "description": "real bugs in the book's code found and fixed"}, "cheating_found": {"type": "array", "items": {"type": "string"}, "description": "any blanket try/except, fake stub, or removed assert you had to fix"}, "notes": {"type": "string"}}}

const results = await pipeline(
  CH,
  c => agent(c.assemble, { label: `run:${c.chapter}`, phase: 'Assemble+Run', model: 'claude-sonnet-5' })
        .then(() => c),
  c => agent(c.verify, { label: `verify:${c.chapter}`, phase: 'Verify', schema: VERIFY_SCHEMA,
                         model: 'opus', effort: 'high' })
        .then(v => ({ chapter: c.chapter, verdict: v })),
)

const done = results.filter(Boolean)
const passed = done.filter(r => r.verdict && r.verdict.passed).map(r => r.chapter)
const failed = done.filter(r => !r.verdict || !r.verdict.passed).map(r => r.chapter)
const bugs = done.flatMap(r => (r.verdict?.real_bugs_fixed || []).map(b => `${r.chapter}: ${b}`))
const cheats = done.flatMap(r => (r.verdict?.cheating_found || []).map(b => `${r.chapter}: ${b}`))
const died = CH.filter((c, i) => !results[i]).map(c => c.chapter)

log(`codetest: ${done.length}/${CH.length} | passed ${passed.length} | failed ${failed.length} | real-bugs ${bugs.length}`)
log(`PASSED (append to code_done.txt): ${passed.join(' ')}`)
if (bugs.length) log(`REAL BUGS FIXED: ${JSON.stringify(bugs, null, 1)}`)
if (cheats.length) log(`CHEATING CORRECTED: ${JSON.stringify(cheats)}`)
if (failed.length) log(`FAILED: ${failed.join(' ')}`)
if (died.length) log(`DIED (re-run): ${died.join(' ')}`)
return { passed, failed, died, bugs, cheats }
